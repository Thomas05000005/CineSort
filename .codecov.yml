# Configuration Codecov pour CineSort
# https://docs.codecov.com/docs/codecov-yaml

coverage:
  # Seuil codecov : 80%. C'est un reglage DISTINCT de la porte CI, qui
  # vaut --fail-under=75 (ci.yml:211). Le commentaire disait « aligne avec
  # --fail-under=80 dans ci.yml » : cette valeur n'a jamais existe la-bas.
  # Garder 80 ici est un choix (viser plus haut que la porte) ; ce qui
  # etait faux, c'est de le presenter comme un alignement.
  range: "70...100"
  round: nearest
  precision: 1

  status:
    project:
      default:
        target: 80%
        # Tolere une baisse de 1% sans bloquer la PR (sinon trop bruyant
        # sur les refactors qui touchent du code peu testable).
        threshold: 1%
        # Branches a verifier
        branches:
          - main
    patch:
      default:
        # Nouveau code (patch) : on veut idealement 70% de coverage
        # sur les lignes ajoutees, mais pas bloquant pour la beta.
        target: 70%
        threshold: 5%
        if_no_uploads: success

# Ne pas commenter sur les PRs Dependabot (bruit inutile)
comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
  require_base: false
  require_head: false
  branches:
    - main

# Ignorer les fichiers non-applicatifs pour le calcul
ignore:
  - "tests/**"
  - "scripts/**"
  - "build/**"
  - "dist/**"
  - "docs/**"
  - "packages/**"
  - "**/__init__.py"
