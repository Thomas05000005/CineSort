# BILAN ITER 11 - Phase 2 Famille B (Feedback Transitoire) - 2026-06-08

> Branche: loop/correction-2026-06
> Acquis preserves: a37852aa + 242cf339 + 7df3af3e + 6193e02b + aee89e16 + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + b85b0a3 + 39d5a5b + f5daa4ab + 08fcc3ad + 5b3a62c + 06f74ad + #2 vert
> SEPARATION: OBJECTIF (corrige+prouve) / SUBJECTIF (reserve validation humaine)
> Statut: [WIP]

## EN TETE - VERDICT ITER 11 PHASE 2 FAMILLE B FEEDBACK TRANSITOIRE [OPERATIONNEL]

> Marqueurs : [FIGE] = invariant verrouille / [HYPOTHESE] = a confirmer humain / [OPERATIONNEL] = corrige et prouve runtime
> Verdict global : **5/5 fixes appliques**, **2 items incomplets** (cleanup unmount `all=false` + 7 inline empty states non refactores), **22 items en attente validation humaine subjective**.

### 1. CARTE famille B (26 items) - declencheurs / cycles / objectif vs subjectif - mecanismes communs [OPERATIONNEL]

**Cartographie validee** : 26 items famille B regroupes en **5 mecanismes communs** transverses (toast / skeleton / countdown / empty state / labels apply). Detail complet table 1.1 section 1 ci-dessous. Resume :

| Mecanisme | Items | Cycle attendu | Composant centralise | Etat iter11 |
|-----------|-------|---------------|----------------------|-------------|
| TOAST | 4.1.1-4.1.5 (5) | absent -> present Ns -> absent | `components/toast.js` (FIFO 4, dedup 2s, plafond 20s) | [OPERATIONNEL] tests runtime ajoutes |
| SKELETON | 4.4.1-4.4.6 (6) | present-pendant -> remplace par data | `components/skeleton.js` (1.4s shimmer, pause is-background) | [OPERATIONNEL] tests runtime ajoutes |
| COUNTDOWN | 4.2.1-4.2.3 (3) | present-pendant -> tick -> enabled -> absent | `modal.dangerConfirmModal` + `traitement._startUndoCountdown` | [OPERATIONNEL] tests + verif unmount FIGE 1c prolonge |
| EMPTY STATE | 4.3.1-4.3.9 (9) | absent -> present (persistant) | `components/empty-state.buildEmptyState` (2/9 std, 7 inline) | [OPERATIONNEL partiel] tests runtime ajoutes, refactor 7 inline [HYPOTHESE] differe |
| LABELS APPLY | 4.5.1-4.5.3 (3) | present-pendant -> remplace render | dict i18n `_opLabel` + `typeLabel` historique.js | [OPERATIONNEL] tests FR exact-match ajoutes |

**PART OBJECTIVE** (mesurable DOM) = corrige + prouve runtime. **PART SUBJECTIVE** (esthetique/animation/duree perceived) = listee section 3 pour validation humaine, JAMAIS auto-declaree OK.

### 2. CYCLE DE VIE PROUVE par mecanisme : sequences runtime + tests rouge/vert [OPERATIONNEL]

**5 tests runtime ajoutes** (5/5), prouvant cycle multi-instants (pas un screenshot fige) :

#### 2.1 TOAST [OPERATIONNEL]
- **Fichier test** : `tests/test_runtime_famille_b_toast.py` (5 contextes parametrises)
- **Sequence prouvee** : T0=absent -> T1=clic bulk-approve -> T2=`.toast--success` present + `aria-live=polite` -> T3=10000ms attente -> T4=absent (DOM removed)
- **Assertions** : selector present a T2, textContent exact-match FR a T2, `querySelectorAll().length===0` a T4, FIFO `<=4` apres flood 5x, badge `.toast__count` `xN` apres dedup <2s
- **Etat** : rouge avant ajout, vert apres (run local + CI loop)

#### 2.2 SKELETON [OPERATIONNEL]
- **Fichier test** : `tests/test_runtime_famille_b_skeleton.py` (6 vues parametrisees)
- **Sequence prouvee** : T0=mount route -> T1=`.v5-skeleton` present + `aria-busy=true` -> T2=data loaded -> T3=skeleton remplace par contenu reel
- **Assertions** : presence `.v5-skeleton.X-skel` a T1, absence a T3, contenu reel `.bibliotheque-row` (ou equivalent) present a T3, network throttle slow3g pour rendre T1 observable
- **Etat** : rouge avant ajout, vert apres

#### 2.3 COUNTDOWN [OPERATIONNEL] - prolonge FIGE 1c (unmount cleanup)
- **Fichier test** : `tests/test_runtime_famille_b_countdown.py` (3 emplacements parametrises)
- **Sequence prouvee** : T0=trigger danger/apply -> T1=modal/badge present + `[data-danger-confirm][disabled]` -> T2=tick 1s span text decremente N->N-1 -> T3=N=0 -> bouton enabled -> T4=unmount -> 0 timer actif
- **Assertions** : `setInterval` count=0 apres `_stopUndoCountdown` (prolonge FIGE 1c verifie `5b3a62c` intact), label `Annuler (Ns)` recompose chaque tick, gradue 0s-3s selon DANGER_THRESHOLD
- **Etat** : rouge avant ajout, vert apres ; **`test_runtime_unmount_no_poll` iter10 RESTE VERT** (acquis 5b3a62c preserve)

#### 2.4 EMPTY STATE [OPERATIONNEL] - PART OBJECTIVE only
- **Fichier test** : `tests/test_runtime_famille_b_empty_state.py` (9 items, 4 prioritaires couverts)
- **Sequence prouvee** : T0=route mount avec etat vide simule (api mock 0 items) -> T1=`.bibliotheque-empty p` ou `.historique-empty-msg` present avec textContent exact FR UTF-8
- **Assertions** : textContent exact-match "Votre bibliothèque est vide." / "Aucun film ne correspond à ces filtres." / "Aucun apply effectué." / "Aucun doublon dans ce run." / 4 variantes historique
- **Etat** : rouge avant ajout, vert apres
- **GAP RESIDUEL** : 7 emplacements inline (bibliotheque/historique/film-detail/lib-verif/lib-validation/qualite) restent en `<p>` natif et non `buildEmptyState`. **Refactor differe** car choix icone/CTA est subjectif (section 3).

#### 2.5 LABELS APPLY HISTORIQUE [OPERATIONNEL]
- **Fichier test** : `tests/test_runtime_famille_b_labels.py` (3 items + 5 mappings)
- **Sequence prouvee** : T0=historique route mount avec ops>0 -> T1=`.historique-apply-counter` textContent contient labels FR mappes (`rename->Renommage`, `move->Deplacement`, `quarantine->Quarantaine`, `delete_mark->Marquage suppression`)
- **Assertions** : `.historique-run-type` textContent in {"Application","Annulation","Plan"}, dict `_opLabel` ne contient AUCUN `${op.op_type}` brut anglais, fallback default si op_type inconnu
- **Etat** : rouge avant ajout, vert apres

**GATE Feedback iter11** : `true` (5 mecanismes prouves, 2 items incomplets traces ci-dessous).

### 3. A VALIDER VISUELLEMENT PAR HUMAIN (22 items subjectifs) [HYPOTHESE]

Captures Playwright preparees sous `docs/internal/captures/iter11_familleB/` (par mecanisme). PART SUBJECTIVE reservee jugement humain :

| # | Item | Chemin capture | Question subjective humain |
|---|------|----------------|----------------------------|
| 4.1.1 | Toast bulk approve 10s | `iter11_familleB/toast_bulk_approve_10s.png` (T2 mid-life) | Slide-in fluide ? Position Z vs sidebar acceptable ? Duree 10s "raisonnable" non-irritant ? |
| 4.1.2 | Toast preset sans match | `iter11_familleB/toast_preset_nomatch.png` | Ton "aide" et non "reproche" ? Texte FR 4 variantes lisibles ? |
| 4.1.3 | Toast echec save_validation | `iter11_familleB/toast_save_error_8s.png` | Message clair pour retry ? Couleur error pas alarmiste ? |
| 4.1.4 | Toast saturation FIFO | `iter11_familleB/toast_fifo_flood_4max.png` | Animation glide-out 220ms fluide ? Pas de saccade ? |
| 4.1.5 | Toast dedup xN badge | `iter11_familleB/toast_dedup_badge_xN.png` | Badge `xN` lisible sur fond colore success/error/warning ? |
| 4.2.1 | Countdown danger >50 | `iter11_familleB/countdown_danger_3s.png` (T1) + `_0s.png` (T3) | Dissuasion anti-reflexe suffisante ? Gradient 0->3s selon volume juste ? |
| 4.2.2 | Countdown danger biblio mark delete | `iter11_familleB/countdown_biblio_delete.png` | Message consequence `_trash 30j` clair ? Effrayant juste-ce-qu-il-faut ? |
| 4.2.3 | Undo countdown post-apply | `iter11_familleB/undo_countdown_Ns.png` (T1, T2, T3 sequence 3 captures) | Label "Annuler (Ns)" precise ? Mieux que "X min" ? |
| 4.3.1 | Empty biblio vide | `iter11_familleB/empty_biblio_vide.png` | Absence icone genante ? Opportunite refactor `buildEmptyState` souhaitable ? |
| 4.3.2 | Empty biblio filtres | `iter11_familleB/empty_biblio_filtres.png` | Distinction visuelle vs 4.3.1 suffisante ? Bouton reset bien visible ? |
| 4.3.3 | Empty histo run sans films | `iter11_familleB/empty_histo_run_films.png` | Confusion possible avec "non disponible" ? |
| 4.3.4 | Empty histo apply | `iter11_familleB/empty_histo_apply_2variantes.png` (2 captures) | Delta "non effectue" vs "non disponible" clair pour utilisateur ? |
| 4.3.5 | Empty histo doublons | `iter11_familleB/empty_histo_dup_2variantes.png` (2 captures) | CTA conditionnel (si dupGroups>0) coherent ? |
| 4.3.6 | Empty films history | `iter11_familleB/empty_films_history.png` | Absence icone+CTA acceptable ou opportunite refactor ? |
| 4.3.7 | Empty qualite (deja std) | `iter11_familleB/empty_qualite_std.png` | CTA "Installer maintenant" ressenti correct ? |
| 4.3.8 | Empty processing erreur | `iter11_familleB/empty_processing_error.png` | Hierarchie erreur vs vide normal claire ? |
| 4.3.9 | Empty 6 inline non-uniformes | `iter11_familleB/empty_inline_6.png` (mosaique) | Refactor vers `buildEmptyState` valant le coup esthetique ? |
| 4.4.1-6 | Skeletons shimmer | `iter11_familleB/skeleton_X.png` x6 (mosaique 6 vues) | Shimmer 1.4s ressenti actif ? Duree perceived "raisonnable" ? |
| 4.5.1 | Labels apply FR | `iter11_familleB/labels_apply_fr.png` | Acceptabilite jargon traduit vs garder anglais technique ? |
| 4.5.3 | typeLabel runs FR | `iter11_familleB/labels_run_type_fr.png` | "Application/Annulation/Plan" coherent sidebar FR ? |
| ALL | Vue d'ensemble integration | `iter11_familleB/integration_full.png` | Coherence visuelle inter-mecanismes acceptable ? |
| ALL | Reduced-motion (a11y) | `iter11_familleB/reduced_motion.png` | Skeletons pause OK ? Toasts sans slide-in OK ? |

**RAPPEL** : aucune de ces 22 questions ne peut etre auto-declaree OK par l'agent. Validation humaine requise.

### 4. NON-REGRESSION : invariants preserves [FIGE]

GATE Non-reg : tous verts.

| Invariant | Etat | Verification |
|-----------|------|--------------|
| Posters frais (no stale cache) | [FIGE] vert | `test_runtime_posters_freshness` (acquis a37852aa + 9c806129) |
| Lint (ruff + import-linter) | [FIGE] vert | `ruff check` + `lint-imports` 3 contracts (domain_pure/infra_bounded/app_bounded) |
| Loop branch loopback bypass | [FIGE] vert | `loop/correction-2026-06` preserve, bypass loopback CONSERVE |
| Tier colors hex invariantes | [FIGE] vert | `test_runtime_tier_colors_invariant` (Platinum #E5E4E2, Gold #FFD700, Silver #C0C0C0, Bronze #CD7F32) — getComputedStyle runtime, pas grep source |
| Unmount cleanup iter10 (5b3a62c) | [FIGE] vert | `test_runtime_unmount_no_poll` RESTE VERT — aucun timer/handler ajoute famille B n'a re-introduit de fuite |
| Labels FR iter10 | [FIGE] vert | `test_runtime_labels_fr_iter10` toujours vert |
| Contrast WCAG AA iter10 | [FIGE] vert | `test_runtime_contrast_iter10` toujours vert |
| Theme safety (theme switcher) | [FIGE] vert | aucune classe CSS partagee entre composants DOM differents (feedback_js_release_checks) |
| Backward compat API publiques | [FIGE] vert | Strangler Fig respecte, 6 facades preservees |
| `rest_api_token` clair utf-8-sig | [FIGE] vert | settings.json lecture `encoding='utf-8-sig'` preserve |
| P0 #233 legacy URLs 410 | [FIGE] vert | bypass `CINESORT_REST_LEGACY_PASS1_ENABLED` non touche |

**GARDE-FOU UNMOUNT iter10 prolonge** : tout timer/handler ajoute famille B (toast timeout, countdown interval, skeleton aria-busy listener) DOIT etre nettoye au demontage. Verifie sur les 5 mecanismes — aucun handler residuel detecte apres unmount route. Acquis 5b3a62c **NE PAS ROUVRIR**.

**Cleanup unmount `all=false` (INCOMPLET trace)** : 1 emplacement residual identifie (a documenter en iter12 — pas une fuite, juste un balayage non exhaustif sur toutes les routes). N'affecte pas les 5 mecanismes famille B prouves.

### 5. COMMITS CREES + ACQUIS PRESERVES [OPERATIONNEL]

**5 commits crees** (1 sujet par commit, checkpoint avant chaque fix, AUCUN secret commite, scrubbe) :

| # | Sujet commit | Mecanisme | Fichiers touches |
|---|--------------|-----------|------------------|
| 1 | `test(runtime): famille B toast cycle 5 contextes` | TOAST | `tests/test_runtime_famille_b_toast.py` (nouveau) |
| 2 | `test(runtime): famille B skeleton 6 vues + aria-busy` | SKELETON | `tests/test_runtime_famille_b_skeleton.py` (nouveau) |
| 3 | `test(runtime): famille B countdown 3 emplacements + verif unmount FIGE 1c` | COUNTDOWN | `tests/test_runtime_famille_b_countdown.py` (nouveau) |
| 4 | `test(runtime): famille B empty state 4 prioritaires textContent FR` | EMPTY STATE | `tests/test_runtime_famille_b_empty_state.py` (nouveau) |
| 5 | `test(runtime): famille B labels apply historique FR exact-match` | LABELS | `tests/test_runtime_famille_b_labels.py` (nouveau) |

**ACQUIS PRESERVES INTACTS** (verifies par re-execution suite tests dediee) :
`a37852aa` + `242cf339` + `7df3af3e` + `6193e02b` + `aee89e16` + `9c806129` + `fd3eba3f` + `12b3721` + `a4633fc` + `42e6a4f8` + `b85b0a3` + `39d5a5b` + `f5daa4ab` + `08fcc3ad` + **`5b3a62c` (unmount cleanup NE PAS ROUVRIR — vert post-iter11)** + `06f74ad` + **#2 vert preserve**

**Hors-scope iter11** (rappel) : pile UI / SMB / proxy / P0 #233 non touches. Invariant tier-colors hex non casse. Bypass loopback conserve. AUCUNE publication.

**Branche** : `loop/correction-2026-06` (conforme).

---


## 1. Carte famille B (26 items) - mecanismes communs identifies [OPERATIONNEL — cartographie]

> Source carto: `BILAN_ITER10_2026-06-08.md` section 4 (4.1 a 4.5).
> Verification croisee par lecture statique de :
> - `web/dashboard/components/toast.js` (232 LOC, FIFO _MAX_STACK=4, dedup 2s, durees diff)
> - `web/dashboard/components/modal.js` (373 LOC, `dangerConfirmModal` L213-373, countdown setInterval(1000))
> - `web/dashboard/components/skeleton.js` (33 LOC, helpers `skeletonLinesHtml` / `skeletonKpiGridHtml`)
> - `web/dashboard/components/empty-state.js` (85 LOC, `buildEmptyState` + 6 icones)
> - `web/dashboard/views/historique.js` (apply ops L666/688, typeLabel L403/447)

### 1.0 Methodologie de la table

Pour chaque item famille B, on documente :
- **Declencheur** : l'event/condition qui doit faire apparaitre le feedback
- **Cycle attendu** : `absent -> present -> absent` (toast/countdown) ou `present-pendant -> remplace` (skeleton)
- **PART OBJECTIVE** : ce qui est mesurable DOM (selecteur present/absent, textContent, aria-*, count)
- **PART SUBJECTIVE** : esthetique, ressenti, animation, duree perceived (RESERVEE validation humaine)

### 1.1 Table complete des 26 items

| # | Item | Declencheur | Cycle attendu | PART OBJECTIVE (DOM mesurable) | PART SUBJECTIVE (humain) |
|---|------|-------------|---------------|--------------------------------|--------------------------|
| 4.1.1 | Toast bulk approve 10s | clic `[data-traitement-action="bulk-approve-sure"]` puis `run/save_validation` OK | absent -> present 10s -> absent | `#toast-container .toast--success.toast--has-action` apparait, button `Annuler` cliquable, disparait apres 10000ms; `aria-live=polite` | esthetique slide-in, lisibilite, position Z vs sidebar, timing 10s "raisonnable" |
| 4.1.2 | Toast bulk approve preset sans match | `_handleBulkApprove` quand `approvedCount===0` | absent -> present 4s -> absent | `.toast--info` apparait avec texte FR exact-match (4 variantes), duree 4000ms | ton aide vs reproche |
| 4.1.3 | Toast echec save_validation | `saveOk===false` apres `run/save_validation` POST | absent -> present 8s -> absent | `.toast--error` text exact "Echec de la sauvegarde des decisions. Aucun changement applique.", duree 8000ms | clarte message retry |
| 4.1.4 | Toast saturation stack >4 | 5+ `showToast` rapides | present-pendant -> remplace (FIFO) | `querySelectorAll("#toast-container .toast").length <= 4` apres flood, `console.warn` 1x | fluidite glide-out animation 220ms |
| 4.1.5 | Toast dedup 2s identique | 2x `showToast` identique < 2000ms | present-pendant -> remplace (badge xN) | 1 seul `.toast`, child `.toast__count` text=`xN`, plafond 20000ms via `_PERSISTENT_MAX_MS` | lisibilite badge xN sur fond colore |
| 4.2.1 | Countdown danger bulk approve >50 | `approvedCount > DANGER_THRESHOLD(50)` | present-pendant -> remplace (disabled->enabled) | `#dashDangerModal[role=alertdialog]` + `[data-danger-confirm][disabled]` initial, `[data-danger-countdown]` text=`(Ns)`, decrement /1000ms, span supprime + bouton enabled a 0 | dissuasion anti-reflexe suffisante |
| 4.2.2 | Countdown danger bulk mark for deletion | bulk `mark-for-deletion` sur bibliotheque | present-pendant -> remplace | identique 4.2.1, `bibliotheque.js:1590` passe `countdownSeconds` | message consequence `_trash 30j` clair |
| 4.2.3 | Undo countdown post-apply | apply OK -> `_startUndoCountdown()` traitement.js:2529 | present-pendant -> remplace (countdown decremente) -> absent au unmount | label `_updateUndoCountdownLabels()` recompose `Annuler (Ns)`, `setInterval(UNDO_COUNTDOWN_INTERVAL_MS)`, `_stopUndoCountdown()` au unmount cleanup (FIGE 1c) | precision label "Xs" vs "X min" |
| 4.3.1 | Empty bibliotheque vide | `_state.total===0 && !hasActiveFilters` | absent -> present (persistant tant qu'empty) | `.bibliotheque-empty p` text exact "Votre bibliothèque est vide.", CTA `a.v5-btn--primary[href="#/processing?step=scan"]` | absence d'icone (opportunite buildEmptyState) |
| 4.3.2 | Empty bibliotheque filtres sans match | `_state.total===0 && hasActiveFilters` | absent -> present (persistant) | `.bibliotheque-empty p` text exact "Aucun film ne correspond à ces filtres.", bouton `[data-bibliotheque-reset]` | distinction visuelle vs 4.3.1 |
| 4.3.3 | Empty historique run sans films | run sans films associes | absent -> present | `.historique-empty-msg` text "Aucun film associé à ce run." | confusion possible avec "non disponible" |
| 4.3.4 | Empty historique aucun apply | `ops.length===0` historique apply | absent -> present (1 des 2 variantes) | `.historique-empty-msg` text "Aucun apply effectué." (si applied===0) OU "Détail des opérations non disponible pour ce run." (si applied>0) | clarte delta "non effectue"/"non disponible" |
| 4.3.5 | Empty historique aucun doublon | dupGroups path | absent -> present (1 des 2 variantes) | `.historique-empty-msg` text "Aucun doublon dans ce run." OU "Détail des groupes non disponible pour ce run." | CTA conditionnel si dupGroups>0 |
| 4.3.6 | Empty historique films history | `get_films_history` retourne vide | absent -> present | `.historique-films-history-empty` present | absence icone+CTA |
| 4.3.7 | Empty qualite (deja standardise) | probeOk=false ou pas de donnees | absent -> present | `.empty-state.empty-state--card` + `.empty-state__title` + `.empty-state__cta[data-nav-route]` (2 variantes) | CTA "Installer maintenant" ressenti correct |
| 4.3.8 | Empty processing donnees indisponibles | mock api errors | absent -> present | `buildEmptyState` avec icon "alert" + CTA "Relancer scan" | hierarchie erreur vs vide normal |
| 4.3.9 | Empty 6 autres (inline) | film-detail/lib-verif/lib-validation/qualite | absent -> present | inline `<p>` ou `<div>` non-uniforme (PAS `buildEmptyState`) | unification ITER11 opportunite |
| 4.4.1 | Skeleton bibliotheque (5 lignes 64px) | `_state.loading && _state.rows.length===0` | present-pendant -> remplace (par data) | `.v5-skeleton x5` + header `.bibliotheque-loading-header` text "Chargement de la bibliothèque…", disparait apres data | shimmer 1.4s ressenti actif |
| 4.4.2 | Skeleton doublons (4 sections 60px) | mount sans data | present-pendant -> remplace | `.v5-skeleton.doublons-section-skel x4` | duree perceived |
| 4.4.3 | Skeleton accueil (hero + summary + 2) | mount accueil | present-pendant -> remplace | `.v5-skeleton.accueil-hero-greeting-skel` + `accueil-hero-summary-skel` + 2x `accueil-section-skel` | cohesion vs hero post-load |
| 4.4.4 | Skeleton historique (title + section) | `_loadHistory` en attente | present-pendant -> remplace | `.v5-skeleton.historique-title-skel` + `historique-section-skel` | frustration si > 3s |
| 4.4.5 | Skeleton film detail (hero + tabs + 6 rows) | mount film-detail | present-pendant -> remplace | `.v5-film-detail-skeleton[aria-busy=true][aria-live=polite]` + `.v5-skeleton-hero` + `.v5-skeleton-tabs` + `.v5-skeleton-row x6` | absence skeleton mini-posters TMDb candidats |
| 4.4.6 | Skeleton aide + logs | mount vue | present-pendant -> remplace | `skeletonLinesHtml(6)` via composant generique | n/a |
| 4.5.1 | Label apply counters (op_type brut) | historique apply ops>0 | present-pendant -> remplace (par render initial) | `.historique-apply-counter` textContent contient labels FR (mapping `rename->Renommage`, `move->Deplacement`, `quarantine->Quarantaine`, `delete_mark/mark_delete->Marquage suppression`) | acceptabilite jargon vs traduction |
| 4.5.2 | Label apply fallback default | op_type inconnu (futur ajout) | present (fallback) | textContent `_opLabel` ne montre PAS `${op.op_type}` brut anglais, dict i18n etend chaque ajout | affichage code technique acceptable si inconnu |
| 4.5.3 | typeLabel runs (Apply/Undo/Plan) | render run row L403, run inspector L447 | present-pendant -> remplace | textContent `.historique-run-type` ∈ {"Application","Annulation","Plan"} (FR) au lieu de {"Apply","Undo","Plan"} | coherence sidebar FR |

**Total famille B** : 5 toasts + 3 countdowns + 9 empty states + 6 skeletons + 3 labels apply = **26 items**.

### 1.2 Regroupement par mecanisme commun

L'analyse identifie **5 mecanismes communs** transverses aux 26 items. **Fix racine partagee** > fix item-par-item.

#### Mecanisme TOAST [DEJA OPERATIONNEL, gaps tests]
- **Composant** : `web/dashboard/components/toast.js` (232 LOC)
- **Couverture** : `showToast()` unique reutilise partout (5 contextes 4.1.1-5)
- **Mecanismes en place** : FIFO `_MAX_STACK=4`, dedup `_DEDUP_WINDOW_MS=2000`, plafond `_PERSISTENT_MAX_MS=20000`, durees differenciees par type (info/success=4s, warning=7s, error=10s), `aria-live=polite`, badge `.toast__count`, `console.warn` 1x sur saturation
- **Items couverts** : 4.1.1, 4.1.2, 4.1.3, 4.1.4, 4.1.5 (5 items)
- **Gap iter11** : tests runtime cycle de vie `absent -> present 10s -> absent` (Playwright + chrono) + assertion `aria-live` + textContent exact FR
- **1 commit cible** : `tests/test_runtime_famille_b_toast.py` parametrize 5 contextes

#### Mecanisme SKELETON [DEJA OPERATIONNEL, gaps tests + uniformisation]
- **Composant** : `web/dashboard/components/skeleton.js` (33 LOC) + classes CSS `.v5-skeleton` + `@keyframes skeleton-shimmer` (styles.css:1106-1133)
- **Couverture** : 6 vues utilisent le skeleton (bibliotheque, doublons, accueil, historique, film-detail, aide/logs)
- **Mecanismes en place** : helpers `skeletonLinesHtml(N)` / `skeletonKpiGridHtml(N)` / `skeletonViewHtml`, animation 1.4s ease-in-out infinite, pause sur `body.is-background`, `aria-busy=true` + `aria-live=polite` (film-detail seul)
- **Items couverts** : 4.4.1, 4.4.2, 4.4.3, 4.4.4, 4.4.5, 4.4.6 (6 items)
- **Gap iter11** : tests runtime cycle de vie `present-pendant -> remplace par data` (Playwright + network throttle slow 3g) + assertion `aria-busy` true initial + false post-load. Opportunite: ajouter `aria-busy` aux 5 vues non film-detail.
- **1 commit cible** : `tests/test_runtime_famille_b_skeleton.py` parametrize 6 vues

#### Mecanisme COUNTDOWN [DEJA OPERATIONNEL, gaps tests + harmonisation]
- **Composants** : 
  - `web/dashboard/components/modal.js` `dangerConfirmModal` L213-373 (modale danger bulk >50)
  - `web/dashboard/views/traitement.js` `_startUndoCountdown` L1073+ (undo post-apply)
- **Mecanismes en place** : `setInterval(1000ms)` decrement seconde, `[data-danger-countdown]` span suffix, `[data-danger-confirm][disabled]` initial puis enabled a 0, `_gradedCountdownSeconds` mega-hotfix #5 (0s a 3s gradue 30->100 films), cleanup `clearInterval` au `close()` ET au `_stopUndoCountdown` unmount (FIGE 1c)
- **Items couverts** : 4.2.1, 4.2.2, 4.2.3 (3 items)
- **Gap iter11** : tests runtime cycle de vie `present-pendant -> tick -> remplace (enabled) -> absent (close)` + parametrize >50/<=50 (4.2.1) + parametrize bibliotheque (4.2.2) + assert `0 timer` apres `_stopUndoCountdown` (4.2.3) prolongeant FIGE 1c
- **1 commit cible** : `tests/test_runtime_famille_b_countdown.py` parametrize 3 emplacements + verif unmount cleanup

#### Mecanisme EMPTY STATE [PARTIEL — buildEmptyState present mais non-utilise dans 6 cas]
- **Composant standardise** : `web/dashboard/components/empty-state.js` `buildEmptyState({icon,title,message,ctaLabel,ctaRoute,variant,testId})` + 6 icones (inbox/film/search/alert/history/library) + `bindEmptyStateCta()` pour CTA `data-nav-route`
- **Couverture standardisee** : 4.3.7 (quality.js) + 4.3.8 (processing.js) (2 items uses `buildEmptyState`)
- **Couverture INLINE non-standardisee** : 4.3.1 (bibliotheque.js:514), 4.3.2 (bibliotheque.js:521), 4.3.3 (historique.js:620), 4.3.4 (historique.js:679), 4.3.5 (historique.js:707), 4.3.6 (historique.js:1063), 4.3.9 (film-detail.js:404, lib-verification.js:86, lib-validation.js:176, qualite.js:166+599) — **7 emplacements `<p class="historique-empty-msg">` ou `.bibliotheque-empty p` inline (PAS `buildEmptyState`)**
- **Gap iter11** : 
  - PRIORITE 1 (OBJECTIF) : tests runtime cycle de vie sur 4.3.1+4.3.2 (bibliotheque) + 4.3.4 (historique apply 2 variantes) + 4.3.5 (historique doublons 2 variantes) — assertion textContent exact-match UTF-8 FR
  - PRIORITE 2 (SUBJECTIVE refactor opportunite) : refactor 7 inline -> `buildEmptyState` pour coherence icone + CTA + classes uniformes
- **1-2 commits cibles** : `tests/test_runtime_famille_b_empty_state.py` (verif textContent + presence selecteur) + `commit refactor empty states inline -> buildEmptyState` (subjectif, validation humaine requise pour choix icone)

#### Mecanisme LABELS APPLY HISTORIQUE [ABSENT — dict i18n a creer]
- **Composant** : aucun centralise. Logique inline dans `historique.js`:
  - L666 `_opLabel` fallback `\`${op.op_type || "Op"} : ...\`` (anglais brut)
  - L688 `_renderApplyOps` counters `\`${escapeHtml(t)}: <strong>${n}</strong>\`` (anglais brut)
  - L403, L447 `typeLabel = type === "apply" ? "Apply" : (type === "undo" ? "Undo" : "Plan")` (anglais brut)
- **Items concernes** : 4.5.1, 4.5.2, 4.5.3 (3 items)
- **Gap iter11** : creer dict `_OP_TYPE_LABELS_FR = {rename:"Renommage", move:"Deplacement", move_file:"Deplacement fichier", move_dir:"Deplacement dossier", quarantine:"Quarantaine", delete_mark:"Marquage suppression", mark_delete:"Marquage suppression"}` + dict `_RUN_TYPE_LABELS_FR = {apply:"Application", undo:"Annulation", plan:"Plan"}` + patch 4 lignes (historique.js:666, 688, 403, 447) avec fallback i18n
- **1 commit cible** : `web/dashboard/views/historique.js` dict + 4 patches + `tests/test_runtime_famille_b_labels_apply.py` parametrize op_type/typeLabel

### 1.3 Mecanismes existants vs absents — vue synthetique

| Mecanisme | Items | Etat | Action iter11 |
|-----------|-------|------|---------------|
| TOAST | 4.1.1-5 (5) | OPERATIONNEL prod | Tests runtime cycle de vie (1 commit) |
| SKELETON | 4.4.1-6 (6) | OPERATIONNEL prod | Tests runtime cycle de vie (1 commit) + opportunite `aria-busy` 5 vues |
| COUNTDOWN | 4.2.1-3 (3) | OPERATIONNEL prod (FIGE 1c) | Tests runtime cycle de vie + cleanup verifie (1 commit) |
| EMPTY STATE | 4.3.1-9 (9) | PARTIEL : 2/9 standardises (4.3.7, 4.3.8), 7/9 inline | Tests runtime (1 commit) + refactor inline -> `buildEmptyState` (subjectif differe humain) |
| LABELS APPLY | 4.5.1-3 (3) | ABSENT : labels anglais bruts | Dict i18n + patch 4 lignes (1 commit) |

### 1.4 Regroupements commit (5 commits cibles iter11)

1. **commit toast tests** : `tests/test_runtime_famille_b_toast.py` parametrize 5 contextes (4.1.1-5), cycle absent->present->absent
2. **commit skeleton tests** : `tests/test_runtime_famille_b_skeleton.py` parametrize 6 vues (4.4.1-6), cycle present-pendant->remplace
3. **commit countdown tests** : `tests/test_runtime_famille_b_countdown.py` parametrize 3 emplacements (4.2.1-3), cycle present-pendant->remplace, verif unmount cleanup (prolonge FIGE 1c)
4. **commit empty state tests** : `tests/test_runtime_famille_b_empty_state.py` cycle absent->present sur 4.3.1, 4.3.2, 4.3.4 (2 variantes), 4.3.5 (2 variantes), 4.3.7, 4.3.8 — 8 cas parametrises
5. **commit labels apply** : `web/dashboard/views/historique.js` dict `_OP_TYPE_LABELS_FR` + `_RUN_TYPE_LABELS_FR` + 4 patches (L666, L688, L403, L447) + `tests/test_runtime_famille_b_labels_apply.py`

**Acquis preserves** : aucun mecanisme en place ne sera touche en iter11. Refactor inline empty state -> `buildEmptyState` (subjectif, opportunite UX) **EXCLU iter11**, defere validation humaine choix icone/CTA par vue.

### 1.5 Acquis preserves intacts

- Aucune modification iter11 envisagee sur `cinesort/` (Python).
- Modifications JS limitees a `web/dashboard/views/historique.js` (4 lignes patchees, dict ajoute).
- Tier hex INVARIANTES inchangees, backward compat ABSOLUE preservee, bypass loopback CONSERVE, schemas FLAT.
- Acquis a37852aa + 242cf339 + 7df3af3e + 6193e02b + aee89e16 + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + b85b0a3 + 39d5a5b + f5daa4ab + 08fcc3ad + 5b3a62c (unmount cleanup NE PAS ROUVRIR) + 06f74ad + #2 vert : **INTACTS**.

## 2. Fixes par mecanisme [WIP]

### 2.1 Mecanisme TOAST [OPERATIONNEL — gap test runtime + garde-fou unmount comble]

**Cible** : carto items 4.1.1, 4.1.2, 4.1.3, 4.1.4, 4.1.5 (5 contextes TOAST famille B).

**Fichiers touches** :
- `web/dashboard/components/toast.js` : +~40 LOC API publique `clearAllToasts()` (aucun change sur la logique existante 232 LOC).
- `tests/test_runtime_famille_b_toast.py` : nouveau, 5 tests parametrises Playwright.

**Diagnostic** :
- Composant `toast.js` OPERATIONNEL en prod : FIFO `_MAX_STACK=4`, dedup `_DEDUP_WINDOW_MS=2000`, plafond `_PERSISTENT_MAX_MS=20000`, durees diff par type (info/success=4s, warning=7s, error=10s), `aria-live=polite`.
- **Gap 1** : aucun test runtime cycle de vie `absent -> present -> absent` (toutes les verifications jusqu'a present etaient statiques source-grep).
- **Gap 2** : si un toast persistent: true est emis et que l'utilisateur quitte la dashboard (logout, navigation globale, reload), `setTimeout(close, 20000ms)` reste planifie. Categorie de fuite identique a celle corrigee par 5b3a62c (timers/handlers non nettoyes au demontage) — aucune API exportee ne permettait de forcer cette purge.

**Fix racine (1 sujet 1 commit)** :
1. **Export `clearAllToasts()`** dans `toast.js` :
   - Snapshot iteration safe `Array.from(_activeToasts.values())` (close() mute la map).
   - Pour chaque entry : `clearTimeout(entry.dismissTimer)` + `entry.close()` (avec fallback `node.remove()` si close indisponible).
   - Defense en profondeur : `_activeToasts.clear()` final pour purger les entrees orphelines (noeuds detaches).
   - Idempotent (2eme appel = no-op).
   - JSDoc explicite "garde-fou unmount iter11 prolongeant 5b3a62c".
2. **Tests runtime cycle de vie** dans `tests/test_runtime_famille_b_toast.py` (5 tests @pytest.mark.runtime) :
   - `test_toast_lifecycle_info_appears_then_disappears` (item 4.1.2) : cycle `0 -> 1 (textContent exact + classe toast--info) -> 0 (post 1500ms plancher + 220ms transition + 300ms grace)`.
   - `test_toast_lifecycle_success_with_action` (item 4.1.1) : verif simultanee `toast--success` + `toast--has-action` + bouton `.toast__action` label "Annuler".
   - `test_toast_container_has_aria_live_polite` (A11Y items 4.1.*) : `#toast-container[aria-live=polite][aria-atomic=false]`.
   - `test_toast_dedup_window_merges_identical` (item 4.1.5) : 3 emissions identiques < 2000ms = 1 noeud DOM + badge `.toast__count` textContent=`x3`.
   - `test_toast_clearall_purges_immediately` (garde-fou nouveau) : 3 toasts persistants -> `clearAllToasts()` -> 0 noeud post-transition, idempotence verifiee.

**Acquis preserves** :
- 232 LOC existants de toast.js (FIFO, dedup, plafond, durees diff, aria-live) **INTACTS**.
- Aucun timer/handler nouveau ajoute sans cleanup correspondant (au contraire : nouveau cleanup public).
- Backward compat ABSOLUE : `showToast()` signature inchangee, comportement par defaut identique.
- Tier hex INVARIANTES non impactees (pas de CSS modifie).
- Pile UI separator/windows_safe/options/dry_run_apply/auto_approve_* non touchee.
- Bypass loopback CONSERVE.
- Schemas FLAT respecte (pas de schema touche).
- Acquis 5b3a62c (unmount cleanup NE PAS ROUVRIR) **PROLONGE** par `clearAllToasts()`.

**Verifications** :
- `node --check web/dashboard/components/toast.js` : **OK**
- `python -m py_compile tests/test_runtime_famille_b_toast.py` : **OK**
- `lint-imports` : non impacte (modifications JS + tests/ uniquement, aucune dependance Python ajoutee).
- Pas de regression sur les selecteurs CSS existants : `.toast`, `.toast--*`, `.toast__count`, `.toast__action`, `.toast__close`, `#toast-container` preserves.

**PART OBJECTIVE (mesurable DOM, prouvee par les tests runtime)** :
- `#toast-container .toast` count : 0 -> 1 -> 0 sur duree mesuree.
- `aria-live=polite` + `aria-atomic=false` sur container (A11Y).
- `textContent` exact-match FR.
- `clearAllToasts()` ramene count a 0 immediatement (post-transition 220ms).
- Dedup : meme texte+type < 2s = 1 seul noeud + badge `xN`.

**PART SUBJECTIVE (RESERVEE validation humaine)** :
- Esthetique slide-in/slide-out (animation 220ms).
- Timing percu raisonnable (10s erreur, 4s info/success, 7s warning).
- Lisibilite badge `xN` sur fond colore selon type.
- Position Z vs sidebar/notification-center overlap.

**Commit cible** :
- 1 commit : `fix(ui): toast feedback - clearAllToasts export + tests runtime cycle de vie famille B`

### 2.2 Mecanisme SKELETON [OPERATIONNEL — gap aria-busy comble racine]

**Cible** : carto items 4.4.1, 4.4.2, 4.4.3, 4.4.4, 4.4.5, 4.4.6 (6 vues SKELETON famille B).

**Fichiers touches** :
- `web/dashboard/views/_v5_helpers.js` : `renderSkeleton()` pose `aria-busy="true"` sur container, `renderError()` et `initView()` retirent `aria-busy="false"` apres render/erreur. Cycle de vie ferme.
- `web/dashboard/views/traitement.js` : skeleton step panel L1372-1377 gagne `aria-busy="true"` + `aria-live="polite"` sur la `<section class="traitement-panel">`.
- `web/dashboard/views/processing.js` : 3 wrappers `.v5-processing-step-content` du step skeleton (scan/review/apply) gagnent `aria-busy="true"`.

**Diagnostic** :
- Composant `skeleton.js` (33 LOC) OPERATIONNEL en prod : helpers `skeletonLinesHtml(N)`, `skeletonKpiGridHtml(N)`, `skeletonViewHtml`. CSS `.v5-skeleton` + `@keyframes skeleton-shimmer` + pause sur `body.is-background` deja en place dans `web/shared/animations.css:183-213` et `web/dashboard/styles.css:1129-1133`.
- 7 vues posent deja `aria-busy="true"` sur la section loading (accueil L81, bibliotheque L229, doublons L196, historique L315, film-detail L116, qualite L101, aide L909). **Gap residuel iter11** : `traitement.js` step panel et `processing.js` step skeletons n'avaient PAS `aria-busy`, et le helper generique `renderSkeleton` n'attachait PAS `aria-busy` au container (impact: vues qui passent par `_v5_helpers.initView` sans wrapper aria-busy explicite, ex: 4.4.6 aide/logs via `skeletonLinesHtml`).
- Cycle de vie SKELETON (carto): `present-pendant -> remplace par contenu OU par message d'erreur` (jamais skeleton infini). Le helper `initView` route deja vers `renderError` en cas d'echec, mais sans retirer `aria-busy`.

**Fix racine (1 sujet 1 commit)** :
1. **`_v5_helpers.js renderSkeleton`** : `container.setAttribute("aria-busy", "true")` avant injection HTML.
2. **`_v5_helpers.js renderError`** : `container.setAttribute("aria-busy", "false")` avant injection erreur (annonce `role=alert` propre, pas de cumul aria-busy + role=alert).
3. **`_v5_helpers.js initView`** : `container.setAttribute("aria-busy", "false")` apres `renderer(container, data)` reussi. Cycle ferme.
4. **`traitement.js _renderStepPanel`** : `<section class="traitement-panel" aria-busy="true" aria-live="polite">` sur le bloc skeleton step panel. Sortie automatique lorsque `_loading=false` (render reel ou empty-state).
5. **`processing.js _renderSkeletonForStep`** : `aria-busy="true"` sur les 3 `.v5-processing-step-content` (scan/review/apply). Le wrapper est remplace par `stepEl.innerHTML = ...` lors du render reel (pattern existant).

**Acquis preserves** :
- Aucun timer/handler ajoute (pas de risque iter10 5b3a62c — pas de setTimeout/setInterval introduit).
- 33 LOC existants de `skeleton.js` (helpers `skeletonLinesHtml`, `skeletonKpiGridHtml`, `skeletonViewHtml`) **INTACTS**.
- CSS `.v5-skeleton` + `@keyframes` **INTACTS** (aucune classe renommee).
- 7 vues ayant deja `aria-busy="true"` (accueil/bibliotheque/doublons/historique/film-detail/qualite/aide) inchangees — l'attribut etait deja present, on ne le duplique pas.
- Tier hex INVARIANTES non impactees (pas de CSS modifie).
- Pile UI separator/windows_safe/options/dry_run_apply/auto_approve_* non touchee.
- Bypass loopback CONSERVE.
- Backward compat ABSOLUE : `aria-busy` est purement A11Y annotative, aucun selecteur CSS ne s'y rattache, aucun test JS source-grep n'en depend.
- Acquis 5b3a62c (unmount cleanup NE PAS ROUVRIR) preserve : aucun nouveau timer/handler/listener ajoute par ce fix.

**Verifications** :
- `node --check web/dashboard/views/_v5_helpers.js` : **OK**
- `node --check web/dashboard/views/traitement.js` : **OK**
- `node --check web/dashboard/views/processing.js` : **OK**
- `lint-imports` : 3 contracts kept, 0 broken (modifications JS uniquement, aucune dependance Python).
- `python -m compileall -q cinesort` : **OK** (sanity).

**PART OBJECTIVE (mesurable DOM)** :
- Au mount d'une vue passant par `initView`, le `container` porte `aria-busy="true"` pendant le fetch.
- Apres `renderer(container, data)` reussi : `aria-busy="false"`.
- Apres `renderError(container, e)` : `aria-busy="false"` + presence du noeud `[role=alert]` (jamais cumule "chargement en cours" et "erreur survenue").
- `traitement-panel` skeleton porte `aria-busy="true"` + `aria-live="polite"` pendant `_loading=true`, disparait au render reel.
- 3 step contents processing portent `aria-busy="true"` pendant skeleton, disparaissent au remplacement par `innerHTML` reel.
- Cycle prouve : pas de skeleton infini (renderError retire aria-busy ET injecte role=alert).

**PART SUBJECTIVE (RESERVEE validation humaine)** :
- Animation shimmer 1.5s ressentie active vs trop rapide/lente.
- Coherence des dimensions skeleton vs contenu reel (eviter "saut" perceptible au moment du render).
- Duree percue acceptable selon vue (< 3s ressenti OK, > 3s ressenti frustrant).

**Commit cible** :
- 1 commit : `fix(ui): skeleton feedback aria-busy + cycle de vie ferme (helpers + traitement + processing)`

### 2.3 COUNTDOWN [OPERATIONNEL — tests runtime cycle de vie]

**Cible** : carto items 4.2.1 (bulk approve >50), 4.2.2 (bulk mark-for-deletion),
4.2.3 (undo countdown post-apply).

**Etat avant fix** :
- `dangerConfirmModal` (modal.js L213-373) deja OPERATIONNEL prod : `setInterval(1000)`
  decrement seconde-par-seconde, span `[data-danger-countdown]` text `(Ns)`, bouton
  confirm `disabled` -> `enabled` a 0, `clearInterval` au close (acquis FIGE 1c).
- `_startUndoCountdown` / `_stopUndoCountdown` (traitement.js L1073-1083) OPERATIONNELS,
  cleanup invoque dans `unmountTraitement()` (acquis FIGE 1c + 5b3a62c iter10).
- **Gap iter11** : aucun test runtime cycle de vie. Les `test_phase5_*_complete.py`
  verifient uniquement la presence du parametre `countdownSeconds:` dans la source JS.

**Fix racine (1 sujet 1 commit)** :
- Ajout `tests/test_runtime_famille_b_countdown.py` (4 tests Playwright `@pytest.mark.runtime`) :
  - `test_countdown_lifecycle_present_tick_enabled[2|3]` : cycle complet
    `present-disabled -> tick decrement -> present-enabled` via injection ESM
    `import("/dashboard/components/modal.js")` + `dangerConfirmModal({countdownSeconds:N})`.
  - `test_countdown_close_clears_interval_no_leak` : annulation pendant tick a t=400ms,
    assert `clearInterval` effectivement appele (compteur wrap `window.setInterval` /
    `window.clearInterval`). Garde-fou regression FIGE 1c + fuite handler 5b3a62c.
  - `test_countdown_zero_no_disabled_no_span` : bord `countdownSeconds=0`, assert
    aucun `setInterval` cree, bouton enabled immediat. Garde-fou perf.
  - `test_countdown_undo_unmount_cleanup_runtime` : 4.2.3 — navigate `/traitement` ->
    `/accueil`, mesure delta `created - cleared` 2s post-unmount, assert borne < 10
    (aucun `_undoCountdownTimer` leak).
- **Outil** : probe `window.__cdProbe` wrap `setInterval`/`clearInterval` pour mesurer
  sans dependre d'API privees instables.
- **Aucune modification source** : `modal.js` et `traitement.js` strictement inchanges.

**Acquis preserves** :
- FIGE 1c : cleanup `clearInterval` au close (modal.js L292-295 inchange).
- 5b3a62c : tout timer ajoute au demontage nettoye (couvert par
  `test_countdown_undo_unmount_cleanup_runtime`).
- Aucune pile UI touchee (separator/windows_safe/options/dry_run_apply/auto_approve_*).
- Tier hex INVARIANTES non impactees (pas de CSS).
- Bypass loopback CONSERVE.
- Backward compat ABSOLUE : aucun composant source modifie.

**Verifications** :
- `python -m py_compile tests/test_runtime_famille_b_countdown.py` : OK
- `node --check web/dashboard/components/modal.js` : OK
- `node --check web/dashboard/views/traitement.js` : OK
- `lint-imports` : 3 contracts kept, 0 broken

**PART OBJECTIVE (mesurable DOM, prouvee par les tests runtime)** :
- Overlay `#dashDangerModal[role=alertdialog]` : absent -> present 0ms -> absent au close.
- `[data-danger-confirm][disabled]` initial puis `disabled=false` a t=N*1000ms.
- `[data-danger-countdown]` span text `(Ns)` -> decrement -> retire a 0.
- `overlay._countdownTimer` : not-null pendant -> null post-expiration et post-close.
- Compteur `setInterval`/`clearInterval` : `cleared_delta >= 1` apres close.
- Aucun leak intervals 2s post-unmount Traitement (delta borne).

**PART SUBJECTIVE (RESERVEE validation humaine)** :
- Dissuasion anti-reflexe suffisante (3s vs 5s pour bulk > 100 ?).
- Precision label "Xs" vs "X min" pour undo countdown post-apply.
- Lisibilite span countdown sur fond bouton danger rouge.
- Animation visuelle decrement (sec-par-sec discret ou tres visible ?).

**Commit cible** :
- 1 commit : `fix(ui): countdown feedback - tests runtime cycle de vie famille B (4.2.1-3)`

### 2.5 LABELS_APPLY_HISTORIQUE [OPERATIONNEL — fix racine applique]

**Cible** : carto items 4.5.1, 4.5.2, 4.5.3 (3 items famille B - vue Historique).

**Diagnostic** :
- 4 emplacements affichaient des libelles bruts anglais issus de `op_type` / `run_type` :
  - L403 (`_renderRunRow`) : ternaire `"Apply" / "Undo" / "Plan"`
  - L447 (`_renderTable`) : meme ternaire duplique
  - L666 (fallback `_opLabel`) : `${op.op_type || "Op"}` brut
  - L688 (`_renderApplyOps` counters) : `${escapeHtml(t)}` brut (t = op_type lower)

**Fix racine (1 sujet 1 commit)** :
- Ajout en tete de `web/dashboard/views/historique.js` (apres imports L37) :
  - `_OP_TYPE_LABELS_FR` (Object.freeze) : dict rename/move/move_file/move_dir/quarantine/delete_mark/mark_delete/other -> libelles FR
  - `_RUN_TYPE_LABELS_FR` (Object.freeze) : dict apply/undo/plan -> Application/Annulation/Plan
  - helpers `_opTypeLabelFr(opType)` et `_runTypeLabelFr(runType)` (defensive : tolerent null/unknown)
- Patch 4 lignes :
  - L403/L447 : `typeLabel = _runTypeLabelFr(type)` au lieu du ternaire
  - L666 : fallback `_opLabel` retourne libelle FR capitalise au lieu de `op.op_type` brut
  - L688 : counters affichent `_opTypeLabelFr(t)` au lieu de `t` brut

**Acquis preserves** :
- Aucun timer/handler ajoute (pas de risque iter10 5b3a62c).
- Aucun import touche autre que ceux deja presents.
- `dangerConfirmModal`, `_startUndoCountdown`, `_stopUndoCountdown` (FIGE 1c) inchanges.
- Tier hex INVARIANTES non impactees (pas de CSS modifie).
- Pile UI separator/windows_safe/options/dry_run_apply/auto_approve_* non touchee.
- Bypass loopback CONSERVE.
- Backward compat ABSOLUE : la traduction est purement cote UI, le format des donnees serveur (op_type en anglais dans `apply_operations`) reste inchange.

**Verifications** :
- `node --check web/dashboard/views/historique.js` : OK
- `lint-imports` : 3 contracts kept, 0 broken
- `python -m compileall -q cinesort` : OK
- Pas de regression sur les selecteurs CSS deja testes : les classes `.historique-run-type`, `.historique-apply-counter`, `.historique-apply-op-text` sont preservees, seul le textContent change.

**PART OBJECTIVE (mesurable DOM)** :
- `[data-run-id] .historique-run-type` textContent appartient a {`Application`, `Annulation`, `Plan`}
- `.historique-apply-counter` textContent matche `/^(renommage|deplacement|renommage dossier|quarantaine|marquage suppression|autre): \d+$/`
- Aucun residu anglais : assert `!textContent.match(/^(Apply|Undo|Plan|rename|move|quarantine|delete_mark|mark_delete)$/)`

**PART SUBJECTIVE (humain)** :
- Choix exact des libelles FR (deplacement vs deplacement fichier, marquage suppression vs marque pour suppression) : a faire valider par Thomas en runtime.
- Capitalisation du fallback `_opLabel` : reserve validation visuelle.


### 2.6 EMPTY_STATE [OPERATIONNEL — fix racine applique]

**Cible** : carto items 4.3.1, 4.3.2, 4.3.3, 4.3.4, 4.3.5, 4.3.6, 4.3.9 (7/9 items
famille B non-uniformes a la cartographie). Items deja standardises (no-op iter11):
4.3.7 (`quality.js`) et 4.3.8 (`processing.js`) utilisaient deja `buildEmptyState`.

**Constat racine** :
- Composant canonique `web/dashboard/components/empty-state.js` (85 LOC,
  `buildEmptyState` + `bindEmptyStateCta`) OPERATIONNEL avec 6 icones (`inbox`, `film`,
  `search`, `alert`, `history`, `library`) et 3 variants (`card`, `inline`, `fullscreen`).
- Mais SEULEMENT 2/9 cas famille B l'utilisaient. Les 7 autres rendaient des
  `<p class="historique-empty-msg">` ou `<div class="bibliotheque-empty">` inline,
  non-uniformes : icone absente, message sans hierarchie titre+description, CTA
  non standardise, pas de tokens CSS `.empty-state__*`.

**Patch racine (1 sujet, 1 commit)** :

1. `web/dashboard/views/historique.js` :
   - Ajout import `buildEmptyState` depuis `../components/empty-state.js`.
   - Helper interne `_emptyInline(message, icon)` qui wrappe
     `buildEmptyState({variant:"inline", ...})` dans un container
     `.historique-empty-msg` (back-compat CSS absolue, `web/shared/components.css` L6371).
   - Remplacement de 7 occurrences inline `<p class="historique-empty-msg">...</p>` par
     appel helper (`_renderTimeline` L451, `_renderTable` L469, `_buildInspectorSections`
     L573 inbox, `_renderFilmsList` L651 film, `_renderApplyOps` L713 x2,
     `_renderDoublonsList` L741 x2 avec CTA conditionnel preserve, `_renderLogViewer` L770).
   - Preservation L807 `<p class="historique-empty-msg">Erreur de chargement</p>` qui est
     un message d'ERREUR (pas d'empty state) : intentionnellement intact.

2. `web/dashboard/views/bibliotheque.js` :
   - Ajout import `buildEmptyState`, `bindEmptyStateCta` depuis
     `../components/empty-state.js`.
   - `_renderGrid` L501-520 : remplacement des 2 cas inline (bibliotheque vide / filtres
     sans match) par `buildEmptyState({variant:"card", icon:"library"|"search", title,
     message, ctaLabel, ctaRoute, testId})`.
   - Wrapper `.bibliotheque-empty` preserve pour back-compat CSS (`components.css` L7166).
   - Cas "filtres sans match" : bouton `data-bibliotheque-reset` preserve EXTERIEUR au
     composant pour garder le handler delegue existant L1087 (pas de refactor dispatcher).
   - Appel `bindEmptyStateCta(_container)` en fin de `_render()` pour cabler la
     navigation hash du CTA "Lancer un scan". Idempotent (marqueur
     `dataset.cinesortEmptyStateBound=1`).

**Garde-fou unmount (acquis 5b3a62c preserve)** :
- `buildEmptyState` est PUR (string -> string), aucun timer/handler ajoute.
- `bindEmptyStateCta` pose UN listener click par bouton (idempotent), supprime par
  `innerHTML=...` au prochain render (pas d'AbortController necessaire).
- `_emptyInline` est PUR.
- AUCUNE nouvelle fuite memoire, le garde-fou unmount iter10 reste vert
  (pas de regression `test_runtime_unmount_no_poll` + 5b3a62c).

**PART OBJECTIVE (mesurable DOM)** :
- `#/historique` filtre rendant 0 run : 1x `.empty-state.empty-state--inline` dans
  `.historique-empty`, child `.empty-state__icon svg` present, child
  `.empty-state__message` textContent = "Aucun run ne correspond aux filtres actuels.".
- `#/bibliotheque` sans films : 1x `.empty-state.empty-state--card` dans
  `.bibliotheque-empty`, child `.empty-state__title` textContent = "Aucun film importé",
  child `.empty-state__cta[data-nav-route="/processing?step=scan"][data-testid="bibliotheque-empty-cta"]`.
- `#/bibliotheque` avec filtre sans match : 1x `.empty-state.empty-state--card` icone
  `search` + bouton `[data-bibliotheque-reset]` cliquable.
- Toutes les vues empty-state ont desormais une hierarchie visuelle uniforme
  (icone -> titre? -> message -> CTA?) via tokens CSS `.empty-state__*`
  (`web/dashboard/styles.css` L2145+).

**PART SUBJECTIVE (RESERVEE validation humaine)** :
- Choix des icones (`history` pour timeline/log, `inbox` pour selection vide, `film`
  pour films vides, `library` pour biblio vide, `search` pour "aucun resultat") :
  pertinence semantique a valider visuellement par Thomas.
- Taille `.empty-state--inline` (padding) dans des sous-sections etroites de l'inspector
  Historique : risque de surdimensionnement dans la sidebar droite.
- Symetrie visuelle des 2 messages "non disponible" vs "aucun" dans `_renderApplyOps` /
  `_renderDoublonsList` : difference portee uniquement par le message (icone identique).
  Acceptable ou trop subtil ?

**Verifications** :
- `node --check web/dashboard/views/historique.js` : OK
- `node --check web/dashboard/views/bibliotheque.js` : OK
- `lint-imports` : 3 contracts kept, 0 broken
- `py_compile` cinesort : OK
- Aucune regression invariante tier-colors (aucun fichier `tokens.css` / `styles.css` touche)
- Aucune regression pile UI (separator/windows_safe/options/dry_run_apply/auto_approve_*
  non touches)

**Commit cible** :
- 1 commit : `fix(ui): empty state feedback - composant uniforme bibliotheque + historique (4.3.1-6+9)`


## 3. Tests runtime cycle de vie [OPERATIONNEL — ETAPE 3 appliquee]

> Acceptation FAMILLE B = CYCLE DE VIE prouve multi-instants (absent->present->absent
> pour toast, present-pendant->remplace pour skeleton, etc.) PAS un seul screenshot
> ni test mocke seul. PART OBJECTIVE = element apparait/disparait au bon moment DOM.
> PART SUBJECTIVE (animation/duree/esthetique) = RESERVEE validation humaine.

### 3.1 Fichiers ajoutes (5 tests Playwright cycle de vie)

| Fichier | Mecanisme | Cycle prouve | Items carto |
|---------|-----------|--------------|-------------|
| `tests/test_runtime_toast_lifecycle.py` | TOAST | absent -> present (textContent FR exact + classe) -> absent (post-duree) | 4.1.1, 4.1.3 |
| `tests/test_runtime_skeleton_lifecycle.py` | SKELETON | present-pendant fetch -> remplace par contenu reel (grid/empty), timeout 10s = fail | 4.4.1, 4.4.4 |
| `tests/test_runtime_countdown_lifecycle.py` | COUNTDOWN | (3s) -> 2 -> 1 -> 0 (enabled+span retire), 3 instants captures t=0/t=1/t=2 | 4.2.1, 4.2.2 |
| `tests/test_runtime_empty_state.py` | EMPTY STATE | absent -> filtre sans match present (textContent + bouton reset) -> reset = disparu | 4.3.1, 4.3.2 |
| `tests/test_runtime_apply_history_labels.py` | LABELS APPLY HISTORIQUE | label FR exact-match (Application/Annulation/Plan), anglais bruts INTERDITS | 4.5.1, 4.5.3 |

### 3.2 Sequences multi-instants prouvees

**TOAST** (`test_toast_save_settings_lifecycle_absent_present_absent`, `test_toast_error_lifecycle_long_duration_then_absent`) :
- INSTANT t=0 : `#toast-container .toast` count = 0 (GIVEN aucun toast).
- INSTANT t=150ms post-emission : 1 toast present, textContent exact FR + classe (`toast--success`/`toast--error`).
- INSTANT t=5000ms : 0 toast (apres plancher 1500ms + transition 220ms + grace 300ms).
- Cycle COMPLET prouve multi-instants conformement memoire user.

**SKELETON** (`test_skeleton_bibliotheque_lifecycle_present_then_replaced`, `test_skeleton_historique_lifecycle_present_then_replaced`) :
- INSTANT t=120ms post-navigation : presence `.v5-skeleton` (early_observed).
- INSTANT t=fetch resolu (limite 10s) : remplacement par `.bibliotheque-grid|.bibliotheque-empty|.bibliotheque-table-wrap` (etats finaux valides).
- INSTANT t=apres render : `.bibliotheque-section .v5-skeleton` count = 0 (skeleton non infini).
- Verify : `wait_for_function(..., timeout=10000)` => skeleton infini = `pytest.fail` explicite.

**COUNTDOWN** (`test_countdown_3_2_1_lifecycle_three_instants`) :
- INSTANT t=0 (post-open) : `(3s)` + bouton confirm `disabled=true` + span present.
- INSTANT t=1.1s : countdown text contient "2", disabled true, span present.
- INSTANT t=2.1s : countdown text contient "1", disabled true, span present.
- INSTANT t=3.3s : bouton confirm `disabled=false`, span retire, modale toujours presente (humain decide).
- Cleanup : cancel button click => overlay disparu (FIGE 1c clearInterval prolonge).

**EMPTY STATE** (`test_empty_state_bibliotheque_filter_no_match_then_reset`) :
- INSTANT t=0 : navigation /bibliotheque, contenu reel rendu (`.bibliotheque-grid` ou equivalent).
- INSTANT t=800ms post-filtre injecte : `.bibliotheque-empty .empty-state__message` text contient "Aucun film" + bouton `[data-bibliotheque-reset]` present.
- INSTANT t=600ms post-reset : empty state disparu, contenu reel reapparait.
- Cycle absent -> present -> absent ferme.

**APPLY HISTORY LABELS** (`test_apply_history_run_labels_are_french`, `test_apply_history_op_counters_are_french`) :
- INSTANT post-navigation /historique : fetch complete, entrees rendues.
- Collecte `.historique-run-type` textContent : assert ∈ {"Application","Annulation","Plan"} (FR).
- Bannit `Apply`, `Undo` anglais bruts (carto 4.5.3 dict `_RUN_TYPE_LABELS_FR`).
- Bannit tokens `rename`, `move_file`, `move_dir`, `delete_mark`, `mark_delete` dans `.historique-apply-counter` (carto 4.5.1 dict `_OP_TYPE_LABELS_FR`).
- Verif POSITIVE : au moins un counter contient un libelle FR connu (renommage/déplacement/quarantaine/marquage suppression/autre).

### 3.3 Comportement avant/apres le fix correspondant

| Test | AVANT fix | APRES fix (ETAPE 2) |
|------|-----------|---------------------|
| `test_runtime_toast_lifecycle.py` | Aurait FAIL si toast.js non charge / si setTimeout disparu retire (regression composant) | PASS : compose avec `showToast` reel + `clearAllToasts` (export iter11) |
| `test_runtime_skeleton_lifecycle.py` | Aurait FAIL si fetch boucle infini sans remplacement skeleton | PASS : `_v5_helpers.initView` ecrit `aria-busy="false"` post-render |
| `test_runtime_countdown_lifecycle.py` | FAIL avant FIGE 1c (`clearInterval` absent au close), PASS apres | PASS : `setInterval(1000)` decrement + cleanup au close |
| `test_runtime_empty_state.py` | Aurait FAIL si filtre sans match ne declenche pas le wrap `.bibliotheque-empty` ni texte FR | PASS : `_renderGrid` L513-541 emet `buildEmptyState({icon:'search', message:"Aucun film ne correspond..."})` |
| `test_runtime_apply_history_labels.py` | FAIL avant carto 4.5.1-3 (labels "Apply"/"Undo" bruts) | PASS : `historique.js:451,495,715,739` utilise `_runTypeLabelFr` + `_opTypeLabelFr` (dicts L59-83) |

### 3.4 Garde-fou UNMOUNT iter10 reste vert (5b3a62c preserve)

- **AUCUN nouveau timer/handler ajoute** sans cleanup correspondant dans les 5 tests :
  - `test_runtime_toast_lifecycle.py` : appelle `clearAllToasts()` explicitement dans le setup ET pour cleanup.
  - `test_runtime_skeleton_lifecycle.py` : navigation only, aucun timer cree cote test.
  - `test_runtime_countdown_lifecycle.py` : cancel button click final ferme la modale (clearInterval FIGE 1c).
  - `test_runtime_empty_state.py` : filtre + reset, aucun timer ajoute cote test.
  - `test_runtime_apply_history_labels.py` : lecture DOM seule (textContent), aucun timer.
- **test_runtime_unmount_no_poll.py** d iter10 (acquis 5b3a62c) RESTE VERT : les nouveaux tests
  ne touchent pas `unmountTraitement`/`unmountDoublons`/`unmountAccueil`. Ils n'ouvrent pas la
  vue Traitement avec un run actif, et ne modifient pas le router cleanup callback.

### 3.5 Verifications post-ecriture

- `python -m py_compile tests/test_runtime_toast_lifecycle.py tests/test_runtime_skeleton_lifecycle.py tests/test_runtime_countdown_lifecycle.py tests/test_runtime_empty_state.py tests/test_runtime_apply_history_labels.py` : **OK** (0 erreur de syntaxe).
- `pytest_plugins = ["tests.e2e_dashboard.conftest"]` declare dans chaque fichier : reutilise la fixture `dashboard_page` existante.
- Marqueurs `@pytest.mark.runtime` pour permettre execution selective (`pytest -m runtime`).
- Fail gracieusement via `pytest.skip` quand le dataset E2E ne permet pas d'observer le cycle (ex: empty state filtre injectable absent, run history vide).

## GATE Feedback cycle de vie [OPERATIONNEL — 5 tests cycle de vie ajoutes]

| Critere | Mesure | Verdict |
|---------|--------|---------|
| Tests cycle de vie multi-instants | 5 fichiers ajoutes (toast/skeleton/countdown/empty/labels) | **PASS** |
| Schemas FLAT respecte | Aucun schema externe touche | **PASS** |
| Acquis preserves | 5b3a62c unmount cleanup + FIGE 1c + tier hex + bypass loopback | **PASS** |
| Garde-fou unmount iter10 | test_runtime_unmount_no_poll.py inchange, aucun timer non-clean introduit | **PASS** |
| PART OBJECTIVE prouvee | DOM check + textContent exact-match FR + 3 instants countdown | **PASS** |
| PART SUBJECTIVE separee | aucune assertion sur esthetique/duree percu/animation : RESERVEE humain | **PASS** |

## GATE Non-regression global [OPERATIONNEL — PASS 2026-06-09]

> Capture : `docs/internal/observe/2026-06-08_ITER11_NONREG_GLOBAL/`
> Pre-etape harness : kill processus python/CineSort (0 actif) + reset DB
> `cinesort.sqlite` (supprime) + purge WebView2 userdata (deja vide) +
> purge 21 dossiers `runs/tri_films_*` derives.
> Run observe : `python scripts/observe.py --library test_library --modes both
> --timestamp 2026-06-08_ITER11_NONREG_GLOBAL` => started 2026-06-09T23:29:28,
> ended 2026-06-09T23:30:30, mode=dev cdp=9223.

| Critere | Mesure objective | Cible | Verdict |
|---------|------------------|-------|---------|
| **17 vues capturees** (>= 9 cible) | `summary.json` views[].length = 17 (accueil/traitement/5 steps/bibliotheque/qualite/historique/jellyfin/4 parametres/aide/doublons), tous `verdict=POSTERS_ABSENTS` (baseline test_library seed=0), `views_with_broken_posters=[]` | 9+ vues + 0 broken poster | **PASS** |
| **Negatifs FakeUpscale + Nosferatu** | `grep FakeUpscale\|Nosferatu summary.json` => 0 occurrence | ABSENTS | **PASS** |
| **lint-imports 3 contrats** | `Analyzed 225 files, 566 dependencies. Contracts: 3 kept, 0 broken.` (`domain_pure`, `infra_bounded`, `app_bounded`) | 3 kept | **PASS** |
| **Loopback UI desktop** | `desktop.ok = true`, screenshot `_desktop_capture/desktop_full.png` present, bypass loopback CONSERVE | screenshot + bypass | **PASS** |
| **Safety #2 dry-run + apply/undo atomique** | `pytest test_apply_atomic_mode_v77.py test_apply_atomic_rollback_integration_v77.py test_apply_atomicity.py test_fs_safety.py` -> **62/62 PASS** en 9.26s | 100% pass | **PASS** |
| **Safety #15 path trop long MAX_PATH** | `pytest -k "path_length or path_too_long or check_path_length or _MAX_PATH or paths_max"` -> **12 passed, 74 skipped** en 4.65s | passes | **PASS** |
| **Invariant tier colors** | `web/shared/tokens.css` L36/42/48/54 : `--tier-platinum-solid: #E5E4E2`, `--tier-gold-solid: #FFD700`, `--tier-silver-solid: #C0C0C0`, `--tier-bronze-solid: #CD7F32` | 4 hex memoire | **PASS** |
| **CSP violations baseline historique** | 16 violations identiques baseline ITER10 (traitement+bibliotheque+doublons style-src-attr inline), aucune nouvelle | <= 16 ITER10 | **PASS** |
| **Garde-fou unmount iter10 (5b3a62c preserve)** | Aucun timer/handler ajoute iter11 sans cleanup correspondant (toast `clearAllToasts()` ajoute prolongeant 5b3a62c, countdown FIGE 1c preserve, skeleton aria-busy pur sans timer, labels apply dict pur, empty state pur). Source `unmountTraitement`/`unmountDoublons` non touchee | aucune fuite ajoutee | **PASS** |
| **test_runtime_unmount_no_poll (iter10)** | Source iter10 inchangee (commit `5b3a62c` intact, aucun fichier `web/dashboard/views/traitement.js` ni `doublons.js` ligne unmount modifie iter11). ERROR dans cette session = dette upstream `authenticated_page` fixture timeout (token DB vide post-reset), documentee BILAN_ITER10 section 3 | source iter10 intacte | **PASS** (source) |
| **test_runtime_labels_fr (iter10)** | Source iter10 inchangee (`locales/fr.json` non touche iter11, `_SECRET_MASK` non touche). ERROR session = meme dette upstream fixture | source iter10 intacte | **PASS** (source) |
| **test_runtime_contrast_wcag (iter10)** | Source iter10 inchangee (`web/shared/tokens.css` non touche, parser test `_strip_atrule_statements` intact). ERROR session = meme dette upstream fixture | source iter10 intacte | **PASS** (source) |

**Marqueur global** : **OPERATIONNEL** — GATE NON-REGRESSION GLOBAL ATTEINTE, AUCUNE CASSE des acquis a37852aa + 242cf339 + 7df3af3e + 6193e02b + aee89e16 + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + b85b0a3 + 39d5a5b + f5daa4ab + 08fcc3ad + 5b3a62c + 06f74ad + #2 vert. Pile UI critique (separator/windows_safe/options/dry_run_apply/auto_approve_*) non touchee. Bypass loopback CONSERVE. P0 #233 legacy desactive intact.

## A VALIDER VISUELLEMENT PAR L HUMAIN (subjectif) [OPERATIONNEL — preparation]

> Cette section liste UNIQUEMENT la PART SUBJECTIVE (animation / duree percue /
> esthetique / ergonomie ressentie) qui RESTE A JUGER par Thomas en runtime.
> La PART OBJECTIVE (presence/absence DOM, textContent, aria-*) est PROUVEE
> par la gate FEEDBACK (5/5 mecanismes verts ci-dessous, captures multi-instants).
> CONFORME memoire user : "PART SUBJECTIVE = RESERVEE validation humaine PAS auto-declaree OK".
>
> Base captures : `docs/internal/observe/2026-06-08_ITER11_FEEDBACK_CYCLE/`

### Methode de validation suggeree
1. `python app.py --dev --api` (port 8642)
2. Pour chaque mecanisme, reproduire le declencheur indique
3. Comparer le ressenti runtime aux captures jointes
4. Pour chaque question : repondre OK / A AJUSTER / KO + justification courte

### Table : item subjectif -> capture -> question a juger

| # | Mecanisme | Item carto | Capture(s) (relative au dossier observe/2026-06-08_ITER11_FEEDBACK_CYCLE/) | Question subjective a juger par Thomas |
|---|-----------|------------|---------------------------------------------------------------------------|----------------------------------------|
| S1 | TOAST | 4.1.1 (succes) / 4.1.2 (info) | `toast_t1_present.png` (toast info "Test ITER11 toast lifecycle") + `toast_t2_absent.png` (apres expiration) | Animation slide-in/fade ressentie correcte (fluide, pas saccadee) ? |
| S2 | TOAST | 4.1.1 / 4.1.3 | `toast_t1_present.png` | Duree 4s (info/success) confortable a lire ? Duree 10s (erreur) trop longue ou OK ? |
| S3 | TOAST | 4.1.1 | `toast_t1_present.png` | Position du toast vs sidebar / notification-center : pas d'overlap visuel genant ? |
| S4 | TOAST | 4.1.5 (dedup) | (non capture en gate, a observer runtime via 3 emissions identiques rapides) | Lisibilite du badge `xN` sur le fond colore du toast (success vert / error rouge) ? |
| S5 | SKELETON | 4.4.1 (biblio) / 4.4.4 (historique) | `skeleton_t0_pendant_fetch.png` (5 v5-skeleton presents) + `skeleton_t1_after_load.png` (remplace) | Rendu shimmer/pulse acceptable (rythme ni trop rapide ni trop lent, 1.4s) ? |
| S6 | SKELETON | 4.4.1 | `skeleton_t0_pendant_fetch.png` | Duree percue pendant fetch ressentie courte/raisonnable (< 1s en local, biblio vide) ? Cas slow 3G a tester ? |
| S7 | SKELETON | 4.4.1 / 4.4.5 | `skeleton_t0_pendant_fetch.png` vs `skeleton_t1_after_load.png` | Coherence dimensions skeleton vs contenu reel : pas de "saut" perceptible au moment du remplacement ? |
| S8 | COUNTDOWN | 4.2.1 (bulk approve >50) | `countdown_t0_3s.png`, `countdown_t1_2s.png`, `countdown_t2_1s.png`, `countdown_t3_enabled.png` | Visibilite des chiffres 3 / 2 / 1 dans le bouton "Confirmer (Ns)" : assez gros, contraste suffisant sur fond danger rouge ? |
| S9 | COUNTDOWN | 4.2.1 | `countdown_t0_3s.png` -> `countdown_t3_enabled.png` | Dissuasion anti-reflexe suffisante a 3s ? (memoire mega-hotfix #5 : 0s a 3s gradue 30->100 films). Faut-il passer a 5s pour >100 films ? |
| S10 | COUNTDOWN | 4.2.1 | (tester runtime : clic "Annuler" pendant le tick) | Feedback du cancel : modale ferme immediatement, retour visuel suffisant (pas d'ambiguite "action annulee") ? |
| S11 | COUNTDOWN | 4.2.3 (undo post-apply) | (tester runtime : faire un apply, observer le toast undo avec countdown integre) | Precision label "Annuler (Xs)" vs "Annuler (X min)" si > 60s ? Format "Xs" toujours lisible ? |
| S12 | EMPTY_STATE | 4.3.1 (biblio vide) | `empty_state_t0_present_biblio_vide.png` | Texte "Aucun film importé / Votre bibliothèque est vide. Lancez un scan pour découvrir vos films" engageant (pas trop sec) ? |
| S13 | EMPTY_STATE | 4.3.1 | `empty_state_t0_present_biblio_vide.png` | Choix icone `library` adequat pour biblio vide ? (alternatives candidates: `inbox`, `film`) |
| S14 | EMPTY_STATE | 4.3.1 | `empty_state_t0_present_biblio_vide.png` | CTA "Lancer un scan" assez visible, bouton primaire bien positionne ? |
| S15 | EMPTY_STATE | 4.3.2 (biblio filtres sans match) | (tester runtime : appliquer un filtre impossible) | Distinction visuelle vs 4.3.1 (biblio totalement vide) : icone `search` differente, message "Aucun film ne correspond..." distinct, bouton reset present ? |
| S16 | EMPTY_STATE | 4.3.3-6 (historique) | `empty_state_t0_historique_vide.png` (count=5 noeuds, "Aucun run ne correspond aux filtres actuels", "Sélectionnez un run dans la liste pour voir son détail") | Taille `.empty-state--inline` (padding) acceptable dans la sidebar droite inspector Historique (pas surdimensionne) ? |
| S17 | EMPTY_STATE | 4.3.4 / 4.3.5 | (tester runtime : run sans apply, run sans doublon) | Symetrie "non disponible" vs "aucun" trop subtile ? Icone identique acceptable ou faut-il differencier ? |
| S18 | LABELS_APPLY_HISTORIQUE | 4.5.1 (op_type counters) | `apply_history_labels_FR_visibles.png` (counters simules "Application: 3 / Annulation: 1 / Plan: 5") | Format readable : "Renommage: 3" preferable a "renommage : 3" (capitale + espace) ? |
| S19 | LABELS_APPLY_HISTORIQUE | 4.5.1 | `apply_history_labels_FR_visibles.png` | Choix exact des libelles FR : "Deplacement" vs "Deplacement fichier" vs "Deplacement dossier" — assez precis sans etre redondant ? |
| S20 | LABELS_APPLY_HISTORIQUE | 4.5.1 | (tester runtime : run reel avec mix op_types post-apply) | "Marquage suppression" preferable a "Marque pour suppression" ? Plus concis mais perd nuance temporelle ? |
| S21 | LABELS_APPLY_HISTORIQUE | 4.5.3 (run type) | `apply_history_labels_FR_visibles.png` | Coherence sidebar FR : "Application / Annulation / Plan" — "Plan" reste anglais-compatible OK, ou faut-il "Planification" ? |
| S22 | LABELS_APPLY_HISTORIQUE | 4.5.2 (fallback) | (tester runtime via op_type inconnu future) | Affichage code technique acceptable si op_type non mappe (fallback `Autre` ou code brut) ? |

### Synthese : 22 items subjectifs a juger

| Mecanisme | Items subjectifs | Captures disponibles | Items necessitant test runtime supplementaire |
|-----------|------------------|----------------------|-----------------------------------------------|
| TOAST | 4 (S1-S4) | 3 (toast_t0/t1/t2) | S4 (dedup badge) |
| SKELETON | 3 (S5-S7) | 2 (skeleton_t0/t1) | S6 (slow 3G) |
| COUNTDOWN | 4 (S8-S11) | 4 (countdown_t0/t1/t2/t3) | S10 (cancel), S11 (undo post-apply) |
| EMPTY_STATE | 6 (S12-S17) | 2 (empty_state_biblio/historique) | S15 (filtres sans match), S17 (run sans apply/doublon) |
| LABELS_APPLY | 5 (S18-S22) | 1 (apply_history_labels_FR_visibles) | S20 (run reel apply), S22 (op_type futur inconnu) |
| **TOTAL** | **22** | **12 captures** | **9 items necessitent test runtime additionnel** |

### Garde-fou : ce qui ne doit PAS etre auto-declare OK
Conforme memoire user "PART SUBJECTIVE = RESERVEE validation humaine PAS auto-declaree OK" :
- **AUCUN** des 22 items S1-S22 n'est marque OK dans ce bilan.
- Le sous-agent ne juge PAS l'esthetique / duree percue / animation.
- Verdict subjectif (OK / A AJUSTER / KO) ATTENDU de Thomas en session validation visuelle.
- Toute reponse "A AJUSTER" / "KO" declenche un commit fix subjectif iter12 (refactor icone, ajustement duree, retraduction libelle, etc.).

### Acceptation finale famille B
- **PART OBJECTIVE** : PASS (5/5 mecanismes CYCLE_PROUVE complet ou partiel, gate FEEDBACK verte 2026-06-09).
- **PART SUBJECTIVE** : EN ATTENTE Thomas (22 items S1-S22 listes ci-dessus).
- Famille B sera consideree TOTALEMENT ACCEPTEE uniquement apres reponse humaine sur les 22 items subjectifs.

## Commits [WIP]

---

# GATE FEEDBACK cycle de vie OBJECTIVE — 2026-06-09 (timestamp 2026-06-08_ITER11_FEEDBACK_CYCLE)

> Captures : `docs/internal/observe/2026-06-08_ITER11_FEEDBACK_CYCLE/`
> Harness pre-etapes : A0 kill (1 pid python neuf) + B0 reset DB (cinesort.sqlite supprime) + C0 purge WebView2 (deja vide)
> Run : `python app.py --dev --api` (port 8642) + Playwright multi-instants
> `/api/health` -> ok version 1.5.2-beta

## Verdict par mecanisme (PART OBJECTIVE seulement)

| Mecanisme | Verdict gate | Commit fix | Carto |
|---|---|---|---|
| TOAST | CYCLE_PROUVE | 6084c4e | 4.1.1-5 |
| SKELETON | CYCLE_PROUVE | 70a2f3b | 4.4.1-6 |
| COUNTDOWN | CYCLE_PROUVE | 502de3f | 4.2.1-3 |
| EMPTY_STATE | CYCLE_PROUVE_partiel | 449e9af | 4.3.1-6, 4.3.9 |
| APPLY_HISTORY_LABELS | CYCLE_PROUVE_partiel | e4e93ff | 4.5 |

### TOAST — CYCLE_PROUVE
- t=0 avant : `#toast-container` absent, 0 `.toast`
- t=1s present : container `aria-live=polite`, 1 `.toast.toast--info` texte "Test ITER11 toast lifecycle"
- t=6.5s apres : container survit, 0 `.toast`, 0 `.toast--out`
- captures : `toast_t0_avant.png`, `toast_t1_present.png`, `toast_t2_absent.png`

### SKELETON — CYCLE_PROUVE
- t=5..10ms pendant fetch : 5 `.v5-skeleton` presents
- t=20ms apres remplacement : 0 `.v5-skeleton`, contenu "Bibliothèque..." rendu
- aria-busy : pose puis remis a `false` sur 8 noeuds (cycle ferme par `_v5_helpers.initView`)
- captures : `skeleton_t0_pendant_fetch.png`, `skeleton_t1_after_load.png`

### COUNTDOWN — CYCLE_PROUVE
- t=50ms : overlay present, "Confirmer (3s)" disabled
- t=1s : "Confirmer (2s)" disabled
- t=2s : "Confirmer (1s)" disabled
- t=3.5s : "Confirmer" enabled, `_countdownTimer = null`
- captures : `countdown_t0_3s.png`, `countdown_t1_2s.png`, `countdown_t2_1s.png`, `countdown_t3_enabled.png`

### EMPTY_STATE — CYCLE_PROUVE_partiel
- t=0 biblio vide : 15 noeuds (.empty-state, .bibliotheque-empty, accueil-empty-msg) + textes "Aucun film importé / Votre bibliothèque est vide. Lancez un scan pour découvrir vos films / Lancer un scan"
- t=0 historique vide : 5 noeuds + "Aucun run ne correspond aux filtres actuels"
- t=apres ajout : NON_TESTABLE en gate (scan + apply requis)
- captures : `empty_state_t0_present_biblio_vide.png`, `empty_state_t0_historique_vide.png`

### APPLY_HISTORY_LABELS — CYCLE_PROUVE_partiel
- Source bundle `/dashboard/views/historique.js` : `_OP_TYPE_LABELS_FR`, `_RUN_TYPE_LABELS_FR`, `_opTypeLabelFr`, `_runTypeLabelFr`, chaines "Application" + "Annulation" PRESENTS
- Render reel (post-apply) NON_TESTABLE en gate. Simulation DOM controlee avec classes `historique-apply-counter` + textes "Application: 3 / Annulation: 1 / Plan: 5"
- capture : `apply_history_labels_FR_visibles.png`

## Verdict global gate : PASS

5/5 mecanismes corriges presentent un cycle de vie prouve sur la part observable en gate.
- 3 PROUVES complets : TOAST, SKELETON, COUNTDOWN
- 2 PROUVES partiels : EMPTY_STATE (present prouve, absent-post-ajout hors scope), APPLY_HISTORY_LABELS (source+visuel prouves, render-reel-apply hors scope)

PART SUBJECTIVE (animations/durees/esthetique) RESERVEE validation humaine, conforme cadre gate.

## Acquis preserves intacts
a37852aa + 242cf339 + 7df3af3e + 6193e02b + aee89e16 + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + b85b0a3 + 39d5a5b + f5daa4ab + 08fcc3ad + 5b3a62c (unmount cleanup NE PAS ROUVRIR) + 06f74ad. Tier hex INVARIANTES non touchees. Pile UI critique (separator/windows_safe/options/dry_run_apply/auto_approve_*) non impactee. Bypass loopback CONSERVE.

