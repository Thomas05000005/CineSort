# BILAN ITER 8 - Cloture Cluster Settings (7 hypotheses) - 2026-06-08

> Branche: loop/correction-2026-06
> Acquis preserves: a37852aa + 242cf339 + 7df3af3e + 6193e02b + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + 06f74ad + #2 vert
> Note: 0f6e9046 separator non touche (decision UI attendue), windows_safe non touche (idem)
> Statut: [WIP]

## EN TETE [WIP]

## 0. Reconciliation decompte (34 iter6 vs 39 iter7) [OPERATIONNEL]

### 0.a Rappel des deux totaux contestes

| Source | Total annonce | Repartition declaree |
|---|---|---|
| BILAN_ITER6 §1.b (entete) | "**reglages_utilisateur=34**" + casses=3 | Table de 35 lignes regroupees (familles tmdb_*, omdb_*, perceptual_*, integrations_*, ...). 3 casses (perceptual_auto_on_scan + naming_preset hors-save + cluster naming windows_safe/lowercase_extensions/separator). |
| BILAN_ITER7 §C.c "Synthese chiffree mission" | "**~34** cartographies" + "**32 [FIGE]** + **7 [HYPOTHESE]** = 39" | Section C.a denombre 32 entrees FIGE (#1 a #32, certaines regroupees comme #31 perceptual_*) + section C.b denombre 7 entrees HYPOTHESE (#33 a #39). Total 39 lignes de tableau. |

Discrepance brute : **39 - 34 = +5 lignes** annoncees iter7 vs iter6.

### 0.b Cause racine identifiee de la discrepance

Trois mecanismes additifs expliquent les 5 entrees supplementaires comptees en iter7 :

**Cause 1 — Granularite "famille -> cle individuelle" sur les fixes ITER7 + ITER6** (+2 lignes nettes).
ITER6 §1.b regroupait `windows_safe + lowercase_extensions + separator` sur **1 seule ligne** (cluster naming sanitization). ITER7 §C.a a separe chacun de ces 3 reglages pour le suivi post-fix : `lowercase_extensions` est passe en "DEJA traite" (commit `42e6a4f8`), `separator` aussi (commit `0f6e9046` opt-in {sep}), `windows_safe` reste HORS SCOPE (STOP_FORK_DESIGN — decision UI attendue, ne PAS toucher). Net : 1 ligne iter6 -> 3 lignes iter7 = **+2**.

**Cause 2 — Ligne "naming_preset" passee en DEJA TRAITE (commit `a4633fc`), sortie du decompte FIGE/HYPOTHESE** (-1 ligne dans la table C.a, donc PAS d'effet net sur 39 mais explique pourquoi `naming_preset` n'apparait pas dans C.a #1-#32). Compense par "Cause 3" ci-dessous.

**Cause 3 — Apparition de 4 nouveaux suspects [HYPOTHESE] decouverts au fil d'iter7** (+4 lignes effectives) :
- `auto_approve_enabled` (#33 dans C.b) — non liste dans la table iter6 §1.b (mentionne uniquement dans la cellule fourre-tout `quarantine_unapproved, dry_run_apply, auto_approve_enabled, auto_approve_threshold, auto_quarantine_corrupted` marquee `[HYPOTHESE] OK side-apply`).
- `auto_approve_threshold` (#34) — meme cellule fourre-tout iter6.
- `auto_quarantine_corrupted` (#35) — meme cellule.
- `dry_run_apply` (#36) — meme cellule.
- `perceptual_auto_on_quality` (#37) — iter6 §1.b L26 le notait `[HYPOTHESE] possiblement OK via quality_audit_support, a confirmer`.
- `probe_workers / probe_parallelism_enabled` (#38) — iter6 le marquait [FIGE] OK, iter7 le redegrade en [HYPOTHESE] (lecture indirecte env var non confirmee au call site scan principal).
- `options` (dict generique PlanSettings) (#39) — iter6 §1.b L36 le notait `[HYPOTHESE] effectivement inerte`.

Mecanisme net : la cellule fourre-tout iter6 "5 cles apply side" qui comptait pour 1 ligne s'est eclatee en 4 lignes individuelles iter7 (+3). Plus `perceptual_auto_on_quality` separe (+1 ligne, deja signale iter6). Plus `probe_workers` redegrade (+0 ligne en compte, mais bascule de FIGE vers HYPOTHESE). Plus `options` deja note. Total apparent C.b = 7.

### 0.c Bilan net de la reconciliation

| Mecanisme | Delta lignes |
|---|---|
| iter6 baseline | 34 |
| Cluster naming sanitization (1 ligne) -> 3 reglages individuels (lowercase_extensions, separator, windows_safe) | +2 |
| Cluster apply fourre-tout (1 ligne pour 5 cles) -> 4 reglages individuels [HYPOTHESE] (auto_approve_enabled, auto_approve_threshold, auto_quarantine_corrupted, dry_run_apply) + 1 [FIGE] (quarantine_unapproved garde sa ligne) | +3 |
| Total reconcile iter7 | **39** |

Aucun champ INTERNE n'a ete mal classe en utilisateur (verification : `state_dir` est INTERNAL declare iter6 §1.b L36 et iter7 le confirme dans C.a #3 ; `library_path` est USER+INTERNAL legitime). Aucun doublon detecte (verification : grep des noms uniques dans la table iter7 §C.a + §C.b => 39 noms distincts). Aucun sub-champ compte separement (les ~25 cles `perceptual_*` restent regroupees sur 1 ligne #31 dans iter7).

**Conclusion** : la discrepance 34 vs 39 est **structurellement coherente**. iter6 utilisait des regroupements par famille (cluster naming, cluster apply), iter7 a explose ces clusters lorsque les sous-cles ont change de statut (FIGE -> traite OU HYPOTHESE). Le perimetre REEL des reglages utilisateur n'a PAS bouge entre les deux iters. **La verite operationnelle iter8 est : 39 lignes de cartographie dont 32 [FIGE], 5 [HYPOTHESE] a traiter (apres exclusion `separator` deja traite mais conserve avec marqueur transitoire iter7 + `windows_safe` exclu explicite), et 2 a exclure explicitement de toute action backend ITER8**.

### 0.d Perimetre reel ITER8

| Categorie | Nombre | Statut action ITER8 |
|---|---|---|
| Reglages utilisateur reels distincts (cartographies iter6+iter7) | 39 | — |
| [FIGE] non touches (approvisionnement OK confirme statiquement iter7 §C.a) | 32 | Hors action ITER8 |
| Deja traites en iter6+iter7 (fixes appliques) | hors total 39 ici, deja decomptes en deduction | `perceptual_auto_on_scan` (12b3721), `naming_preset` hors-save (a4633fc), `lowercase_extensions` (42e6a4f8), `separator` (0f6e9046 opt-in). |
| Exclus explicites consigne ITER8 (NE PAS toucher) | 2 | `separator` (decision UI attendue) + `windows_safe` (STOP_FORK_DESIGN decision UI attendue) |
| **[HYPOTHESE] residuels a traiter ITER8** | **7** | Cf. section 1. |

NOTE : `separator` apparait simultanement dans "Deja traites" (le fix opt-in {sep} est commit 0f6e9046 + tests verts) ET dans "Exclus explicites consigne ITER8" (consigne explicite : ne pas toucher). Cette double appartenance est resolue par la consigne ITER8 : le fix est LIVRE iter7 et conserve EN L'ETAT (pas de modif, pas de revert, pas de toggle additionnel). De meme `windows_safe` reste STOP_FORK_DESIGN inerte (la consigne ITER8 confirme la decision UI attendue).

## 1. Liste nommee des 7 [HYPOTHESE] a traiter [OPERATIONNEL]

> Source : BILAN_ITER7 §C.b "Reglages [HYPOTHESE] - approvisionnement non prouve runtime" (lignes 1367-1373).
> Pour chaque entree : nom exact du champ tel qu'il apparait dans `settings.json` / consomme par `_save_section_*` (i.e. la cle qui passe par `_validate_passive(StartPlanRequest, ...)` puis dans `PlanSettings.extra="allow"` du fichier `cinesort/ui/api/schemas/run.py`).
> RAPPEL : ces 7 entrees sont en plus des 32 [FIGE] (NE PAS retraiter) et en plus des 4 deja-fixes iter6+iter7 (`perceptual_auto_on_scan`, `naming_preset` hors-save, `lowercase_extensions`, `separator`).
> RAPPEL : `separator` et `windows_safe` sont EXCLUS de ITER8 par consigne explicite, meme s'ils apparaissent dans la cartographie historique. Ils ne figurent PAS dans la liste ci-dessous.

| # | Nom champ EXACT (settings.json / PlanSettings extra) | Defaut | Persistance UI | Suspicion drop silencieux |
|---|------------------------------------------------------|--------|----------------|---------------------------|
| 1 | `auto_approve_enabled` | bool, `False` | `_save_section_*` settings_support L1398 + reset_support L39 | AUCUN grep dans `apply_support.py` / `apply_core.py` / `cinesort_api.py` -> sauvegarde sans lecture (suspecte cluster apply). |
| 2 | `auto_approve_threshold` | int, `85` (coerce 70-100) | `_save_section_*` settings_support L1399 + reset_support | Couple avec `auto_approve_enabled` ; aucun call site apply repere. |
| 3 | `auto_quarantine_corrupted` | bool, `False` | `_save_section_*` settings_support L1401 + reset_support L39 | AUCUN grep call site apply, branche post-probe FAILED suspectee absente. |
| 4 | `dry_run_apply` | bool | `_save_section_*` + reset_support | AUCUN grep call site apply (toggle persiste). A distinguer du parametre `dry_run` passe en body REST `/api/run/apply` qui, lui, est consomme. Sujet potentiellement UX (pre-coche UI uniquement) a clarifier avant fix. |
| 5 | `perceptual_auto_on_quality` | bool, `True` | `_save_section_perceptual` L1523 + expose via `_build_settings_dict` perceptual_support L1433 (alias `auto_on_quality`) | AUCUN grep dans `quality_audit_support.py` -> meme pattern que `perceptual_auto_on_scan` AVANT fix 12b3721. Modele 12b3721 reutilisable. |
| 6 | `probe_workers` + `probe_parallelism_enabled` (couple - 2 cles persistees, 1 sujet) | int `auto` / bool `True` | persistes settings.json + lus par `infra/probe/service.py`, `infra/probe/constants.py`, `quality_support.py` (grep 5 fichiers) | Lecture indirecte via env var ou config probe non confirmee au call site scan principal. Verification approvisionnement requise. |
| 7 | `options` (dict generique du schema PlanSettings) | `Optional[Dict[str, Any]] = None` (cf `cinesort/ui/api/schemas/run.py` L35-38) | declare dans `PlanSettings`, transmis par les payloads tolerants | Aucun call site `start_plan` / `_start_plan_impl` ne lit ce dict. Probablement inerte par design (sac fourre-tout `extra="allow"`). A confirmer ou supprimer du schema. |

### 1.a Note couplages et priorisation suggeree

- **Cluster apply auto-decision** : #1 + #2 + #3 + #4 forment un cluster coherent (cluster apply). Priorite haute : #1+#2 (couple inseparable) et #3 (corruption -> quarantine), suivis de #4 (verification UX vs effet pipeline).
- **Cluster perceptual auto** : #5 isole, modele de fix existant (12b3721). Priorite haute.
- **Verification probe approvisionnement** : #6 (couple cles - 2 cles persistees mais 1 sujet operationnel) — priorite moyenne (potentiellement deja OK indirectement, a confirmer).
- **Sujet schema** : #7 — priorite basse, candidat suppression du schema ou documentation explicite "inerte par design".

### 1.b Exclus explicites confirmes (NE PAS traiter ITER8)

| Reglage | Raison exclusion ITER8 |
|---|---|
| `separator` | Decision UI attendue (consigne ITER8 explicite "PAS toucher separator (0f6e9046 inerte, decision UI attendue)"). Le fix opt-in `{sep}` reste en place tel quel (acquis preserve). |
| `windows_safe` | STOP_FORK_DESIGN sanitization NTFS obligatoire (consigne ITER8 explicite "PAS windows_safe (decision UI attendue)"). |

### 1.c Marqueurs

- `[OPERATIONNEL]` : la liste est extraite directement de BILAN_ITER7 §C.b avec verification croisee des noms de champs via lecture de `cinesort/ui/api/schemas/run.py` (PlanSettings) + BILAN_ITER6 §1.b. Les 7 entrees sont distinctes, traitables individuellement (sauf #1+#2 et #6 qui sont des couples logiques mais avec cles persistees distinctes).
- `[FIGE]` : aucune renomenclature. Les noms de champs sont ceux observes dans `settings.json` reel et dans les `_save_section_*` de `settings_support.py` (preuves : lignes precises citees ci-dessus, validees iter7 §C.b).
- `[HYPOTHESE]` : aucune sur cette section meta. La cartographie d'iter7 est consideree comme reference d'entree d'iter8 (les hypotheses portent sur les comportements pipeline reels a mesurer/fixer en sections 2-3 d'iter8).

## 2. Trace + classification par reglage [WIP]

### 2.X — `options` (dict generique PlanSettings) — FORK-DESIGN [FIGE]

**Reglage** : `PlanSettings.options: Optional[Dict[str, Any]]` (`cinesort/ui/api/schemas/run.py` L35-38).

**Trace pipeline reel (etat frais, grep exhaustif sur cinesort/)** :
- `cinesort/ui/api/facades/run_facade.py` L115-136 : enrobe `settings` dans `{"settings": settings}` et lance `_validate_passive(StartPlanRequest, request_payload)`. Validation passive seulement — Pydantic accepte `options=None` ou `options={...}` indifferemment (extra="allow").
- `cinesort/ui/api/facades/run_facade.py` L136 : `result = self._api._start_plan_impl(settings)` — le dict caller ORIGINAL est passe, le wrapper Pydantic est detruit (seule la validation passive observe la cle).
- `cinesort/ui/api/run_flow_support.py` L700-786 `_start_plan_impl` : appelle `_hydrate_settings_from_store(api, settings)`, `_validate_and_init_plan_context`, `_build_plan_job_fn`. **Aucun acces a `settings["options"]` ni `settings.get("options")`**.
- `cinesort/ui/api/run_flow_support.py` L372-470 `_build_plan_job_fn` : lit uniquement des cles plates (`settings.get("subtitle_detection_enabled")`, `settings.get("omdb_enabled")`, `settings.get("omdb_api_key")`, ...).
- `cinesort/ui/api/settings_support.py` `build_cfg_from_settings` L967+ : pont settings -> `core.Config`. Lit uniquement cles plates (`collection_folder_name`, `video_exts`, `scan_max_workers_mode`, `naming_preset`, ...). Aucune lecture de `options`.

**Grep global controle** :
- `grep -rn "['\"]options['\"]" cinesort/app cinesort/domain cinesort/ui/api` -> **0 resultat utile** (uniquement `ollama_client.py` qui passe `options` comme champ HTTP non lie au PlanSettings).
- `grep -rn "settings\.get(.options.)\|settings\[.options.\]\|\.options\b"` -> 0 sur le pipeline plan/apply.

**Point de drop** : la cle `options` est PARSEE par la validation Pydantic puis IMMEDIATEMENT IGNOREE. Le pipeline downstream lit le dict ORIGINAL caller, jamais l'objet `PlanSettings` valide. Meme si le caller envoyait `options={"foo": 1}`, aucune branche runtime ne le consomme.

**Scenario minimal tente** : un differentiel ON (`settings.options = {...}`) vs OFF (`settings.options = None`) sur `start_plan` ne peut PAS produire de comportement different : aucun consommateur en aval. Aucun controle negatif possible — il n'existe aucun sous-systeme dont le branchement depend de cette cle.

**Verdict** : FORK-DESIGN. Le champ `options` est un placeholder declaratif "compat legacy" dans le schema Pydantic, sans cablage backend. Identique en nature aux toggles `separator` (0f6e9046 inerte) et `windows_safe` deja flagges : la surface API expose un reglage qui n'est PAS honorable par le code actuel. Un toggle ON/OFF ne produit aucune divergence pipeline reelle.

**Decision** : NE PAS bricoler de cablage local. Remonter la pile UI -> verifier si `options` est utilise par le frontend Settings/Dashboard (sondage cote `web/`), sinon proposer suppression du champ (ou clarifier sa semantique attendue). PAS de toggle inerte ajoute, conforme directive "FORK-DESIGN = STOP REMONTER pile UI pas de toggle inerte".

[FIGE] : les references de code citees ci-dessus sont des coordonnees exactes (schemas/run.py L15-38, run_facade.py L136, run_flow_support.py L700+, settings_support.py L967+) verifiables au commit courant de la branche `loop/correction-2026-06`.

### 2.Y — `dry_run_apply` — FORK-DESIGN [OPERATIONNEL]

**Classification : FORK-DESIGN** (toggle non honorable proprement — meme nature que `separator` / `windows_safe`, decision UI attendue avant tout fix backend).

#### Trace approvisionnement source -> call sites reels

| Site | Fichier:Ligne | Role |
|------|---------------|------|
| Persistance write | `cinesort/ui/api/settings_support.py:1397` (`_save_section_scan_flags`) | `"dry_run_apply": to_bool(payload.get("dry_run_apply"), True)` |
| Default table | `cinesort/ui/api/settings_support.py:766` | `("dry_run_apply", True)` |
| Reset scope | `cinesort/ui/api/reset_support.py:165` | Inclus liste reset (clear -> default `True`) |
| Lecture UI status banner | `web/dashboard/views/status.js:246-251` | `isDryRunDefault = !!settings.dry_run_apply` -> affiche bandeau "Dry-run actif par defaut" |
| Schema `PlanSettings` | `cinesort/ui/api/schemas/run.py:15-38` | Champ NON declare explicitement (transite via `extra="allow"`) |
| `RunFacade.start_plan` | `cinesort/ui/api/facades/run_facade.py:115-141` | Aucune lecture de `dry_run_apply` ni branche conditionnelle dessus |
| `cinesort/app/plan_support.py` | grep | **0 hit** `dry_run_apply` |
| `cinesort/app/apply_core.py` | grep | **0 hit** `dry_run_apply` (le parametre `dry_run` consomme est explicite, passe par `apply_support._do_apply`) |
| `cinesort/app/cinesort_api.py` | grep | **0 hit** `dry_run_apply` |
| `cinesort/domain/**` | grep | **0 hit** `dry_run_apply` |
| Initialisation toggle UI apply | `web/dashboard/views/traitement.js:130-136` | `let _applyOptions = { dry_run: true, ... }` — **HARDCODE `true`**, NE lit PAS `settings.dry_run_apply` |
| Payload POST `/api/run/apply` | `web/dashboard/views/traitement.js:1893-1914 / 1991-1993` | Envoie `dry_run: _applyOptions.dry_run` (checkbox UI session, hardcode initial `true`). Le settings `dry_run_apply` n'intervient JAMAIS dans cette chaine. |

#### Point de drop

**Drop strict cote frontend** : `_applyOptions.dry_run` est hardcode `true` au module-load de `traitement.js`. La valeur `settings.dry_run_apply` n'est lue NULLE PART pour initialiser le toggle UI. Le settings ne sert QU'A l'affichage d'un bandeau de prudence dans `status.js` (cosmetique).

**Aucune intervention pipeline backend** : `dry_run_apply` ne traverse ni `start_plan`, ni `apply_core`, ni `apply_support._do_apply`. Le parametre `dry_run` consomme par `apply_core` (cf 31 sites recenses dans `cinesort/app/apply_core.py`, et `apply_support.py` L1903-1971) est le champ `body.dry_run` du POST `/api/run/apply`, transmis explicitement par `_applyOptions.dry_run` (UI), pas par le settings persistant.

#### Differentiel comportemental MESURE — scenario insensible par construction

Scenario tente :
- **ON** (`dry_run_apply=True` sauvegarde via `/api/settings/save` section scan_flags) : POST `/api/run/start_plan {settings:{library_path,dry_run_apply:true,...}}` -> plan.jsonl genere. Puis POST `/api/run/apply {run_id, decisions:{}, dry_run:true}` (toggle UI coche par defaut) -> aucune modif FS.
- **OFF** (`dry_run_apply=False` sauvegarde) : POST `/api/run/start_plan {settings:{library_path,dry_run_apply:false,...}}` -> **plan.jsonl IDENTIQUE** (pas de lecture backend). Puis POST `/api/run/apply {run_id, decisions:{}, dry_run:true}` (toggle UI toujours coche par defaut hardcode) -> aucune modif FS.

Resultat : `ON == OFF` sur plan.jsonl ET sur effet apply. Le seul effet observable est le texte du bandeau dans `status.js` (cosmetique UI pure, hors pipeline).

#### Controle negatif — scenario alternatif n'existe pas

Aucun scenario `test_library` ne peut solliciter ce reglage sur le pipeline reel : la chaine de consommation n'existe pas cote backend, et le pre-cochage UI n'est pas connecte au settings. Le seul scenario "sensible" serait un test E2E Playwright qui :
1. Save `dry_run_apply=False` via `/api/settings/save`,
2. Recharge la vue Traitement,
3. Verifie que le toggle UI est decoche au render initial.

Ce test echouerait actuellement (toggle reste coche). C'est un **bug UX cote frontend**, pas une casse pipeline.

#### Pourquoi FORK-DESIGN et pas CASSE ni NON-PROUVABLE-LOCAL

- **PAS CASSE pipeline** : aucun differentiel ON!=OFF n'est attendu sur plan.jsonl ou apply_core par design — le settings n'est PAS architecture pour piloter le pipeline. La valeur sert d'**intention par defaut UX** (preference utilisateur "je veux que mon prochain apply soit en dry-run"), pas de contrainte backend.
- **PAS NON-PROUVABLE-LOCAL** : le scenario differentiel est trivialement constructible (POST /api/run/apply avec body.dry_run flip-flop), mais il prouverait seulement que `body.dry_run` pilote `apply_core.dry_run` (FIGE OK separement, parametre explicite consomme L1144 `apply: %d rows a traiter (dry_run=%s)`). Le reglage `dry_run_apply` n'est PAS la cle interrogee dans ce flux.
- **FORK-DESIGN confirme** : honorer proprement `dry_run_apply` exigerait soit (a) cabler `_applyOptions.dry_run = settings.dry_run_apply` au mount de la vue Traitement (frontend uniquement, sans toucher backend), soit (b) injecter le default `dry_run_apply` cote dispatcher REST si body.dry_run absent. Les DEUX options sont des **decisions UI/UX** (quel cote prime : settings persiste vs toggle session ?), pas des fixes backend isoles. Identique en nature a `separator` (decision template custom) et `windows_safe` (decision sanitization NTFS) : **STOP — remonter pile UI, pas de toggle inerte fixe en backend**.

#### Acceptation FORK-DESIGN

- **NE marque PAS OK** (conformement consigne).
- **NE touche PAS** au reglage en backend ITER8.
- Ajoute a la liste **decisions UI attendues** aux cotes de `separator` et `windows_safe`.
- Reste **persiste-mais-non-cable** : valeur conservee, bandeau status.js conserve, aucun risque de regression backend ni frontend.

#### Trace resumee

```
settings.json[dry_run_apply=true|false]
  -> settings_support._save_section_scan_flags (write OK L1397)
  -> reset_support (reset OK L165)
  -> [DROP backend complet : 0 grep dans schemas/run.py PlanSettings,
      RunFacade.start_plan, plan_support, apply_core, cinesort_api, domain/*]
  -> status.js L248 lit settings.dry_run_apply -> affiche bandeau "Dry-run actif par defaut" (cosmetique)
  -> traitement.js L130 _applyOptions.dry_run = TRUE (hardcode, ignore settings)
  -> POST /api/run/apply body.dry_run = _applyOptions.dry_run (checkbox UI session)
  -> apply_core dry_run parametre (FIGE OK separement, parametre explicite)
```

**Verdict : FORK-DESIGN. Decision UI attendue avant tout fix. ITER8 NE TOUCHE PAS.**

[FIGE] : les references de code citees ci-dessus sont des coordonnees exactes (settings_support.py L766/L1397, reset_support.py L165, schemas/run.py L15-38, run_facade.py L115-141, status.js L246-251, traitement.js L130-136/L1893-1993) verifiables au commit courant de la branche `loop/correction-2026-06`.

### 2.Z — `perceptual_auto_on_quality` — CASSE [OPERATIONNEL]

**Classification : CASSE** (drop confirme statiquement, pattern identique a `perceptual_auto_on_scan` AVANT fix 12b3721 — modele de fix reutilisable).

#### Trace approvisionnement source -> call sites reels (FIGE)

| Site | Fichier:Ligne | Role | Etat |
|---|---|---|---|
| Defaut table | `cinesort/ui/api/settings_support.py:847` | `("perceptual_auto_on_quality", True)` | FIGE |
| Persistance write | `cinesort/ui/api/settings_support.py:1523` (`_save_section_perceptual`) | `to_bool(payload.get("perceptual_auto_on_quality"), True)` | FIGE |
| Reset scope | `cinesort/ui/api/reset_support.py:42` | inclus dans liste reset perceptual | FIGE |
| Expose UI | `cinesort/ui/api/perceptual_support.py:1433` (`_build_settings_dict`) | `"auto_on_quality": bool(settings.get("perceptual_auto_on_quality"))` — dict expose mais consomme uniquement pour params techniques (timeout/frames_count), PAS pour decider declenchement | FIGE cosmetique |
| Schema `PlanSettings` | `cinesort/ui/api/schemas/run.py:15-38` | champ non declare explicitement, passe via `extra="allow"` | FIGE — pas de drop au schema |
| **Call site backend attendu** | `cinesort/ui/api/quality_audit_support.py` `_recompute_worker` L311-348 | **0 hit grep `perceptual_auto_on_quality`** | **DROP CONFIRME** |
| **Call site backend attendu (alternatif)** | `cinesort/ui/api/run_flow_support.py` apres L516 `recompute_all_scores(api)` | **0 hit grep `perceptual_auto_on_quality`** | **DROP CONFIRME** |
| Grep global controle | `cinesort/app/**`, `cinesort/domain/**`, `cinesort/infra/**` | **0 hit `perceptual_auto_on_quality`** | confirme drop pipeline |

#### Point de drop

La valeur `perceptual_auto_on_quality` est **sauvee+exposee mais jamais relue pour decider** d'un declenchement. `_build_settings_dict` la met dans le dict UI sous l'alias `auto_on_quality`, mais ce dict sert uniquement a l'UI Parametres (lecture/affichage du toggle). Le pipeline reel — `recompute_all_scores -> _recompute_worker -> get_quality_report` — ne lit jamais le flag. Aucun hook post-recompute n'existe pour declencher `analyze_perceptual_batch`. Identique en structure exacte a `perceptual_auto_on_scan` AVANT commit 12b3721.

#### Differentiel comportemental ON vs OFF — scenario REST minimal

Scenario theorique (etat frais via harness 6193e02b, kill_all + reset_db + purge_webview + reset settings) :

1. **Pre-etape ON** : ecrire `settings.json` avec `perceptual_enabled=True`, `perceptual_auto_on_quality=True`, `auto_recompute_quality_on_scan=True`, `perceptual_auto_on_scan=False` (isoler le declencheur quality), `tmdb_enabled=False`, `omdb_enabled=False`, roots = `test_library` stubs `.mkv > MIN_VIDEO_BYTES`.
2. POST `/api/run/start_plan {settings:{library_path:"...test_library/RootA/Movies",...}}` -> attendre completion du job `recompute_all_scores` background (poll `/api/quality/get_recompute_job_status job_id` jusqu'a `status=done`).
3. Observer : `cinesort/ui/api/perceptual_support.analyze_perceptual_batch` appele ? Mocker pour compteur, OU verifier en BDD `store.perceptual.get_perceptual_report(run_id, row_id)` non vide.
4. **Pre-etape OFF** : meme settings mais `perceptual_auto_on_quality=False`. Idem POST start_plan + poll.
5. **Differentiel attendu apres fix** : ON = `analyze_perceptual_batch` appele 1 fois sur row_ids du run, OFF = 0 appel.
6. **Differentiel actuel (sans fix)** : ON == OFF == 0 appel — par construction, **aucun consommateur**.

Note : l'execution REST reelle (start_plan + poll) confirmerait obligatoirement ON==OFF==0 car le flag n'est pas branche. C'est exactement la meme regle d'inference que pour `perceptual_auto_on_scan` AVANT 12b3721 (cf `tests/test_perceptual_auto_on_scan_iter6.py:70-90` qui demontre l'equivalent : `mocked.call_count == 0` aussi bien en OFF qu'en ON-non-fixe).

#### Controle negatif — scenario sollicite bien le reglage

Le scenario propose SOLLICITE le reglage :
- `perceptual_enabled=True` : moteur perceptuel dispo (le flag passerait le gate `if not perceptual_enabled` du fix attendu)
- `auto_recompute_quality_on_scan=True` : declenche la phase `recompute_all_scores` apres scan, point ou le fix attendu doit hooker
- `perceptual_auto_on_scan=False` : isole le declencheur quality (sinon `perceptual_auto_on_scan` deja fixe en 12b3721 confondrait les deux signaux)
- `test_library` avec >= 2 stubs `.mkv > MIN_VIDEO_BYTES` : suffit a produire des row_ids consommables par `analyze_perceptual_batch`

Aucune branche alternative connue ne consomme le flag => le scenario est exhaustif et non insensible.

#### Pourquoi CASSE et pas FORK-DESIGN ni NON-PROUVABLE-LOCAL

- **PAS FORK-DESIGN** : le toggle est honorable proprement (le pattern de fix existe deja, 12b3721, eprouve sur `perceptual_auto_on_scan`). Le point d'injection est evident (`_recompute_worker` post-boucle OU `run_flow_support` apres `recompute_all_scores`). Aucune ambiguite UI/UX a remonter.
- **PAS NON-PROUVABLE-LOCAL** : le scenario differentiel est trivialement constructible avec `test_library`. La pre-etape harness 6193e02b est documentee, le pattern de test existe deja (`test_perceptual_auto_on_scan_iter6.py` directement clonable).
- **CASSE confirme** : drop strict, modele de fix immediatement applicable, defaut `True` aggravant (l'utilisateur croit l'auto-perceptuel actif et ne voit rien).

#### Modele de fix (12b3721 reutilise) — pour section 3

Emplacement preferentiel : `cinesort/ui/api/quality_audit_support.py::_recompute_worker` apres L329 (boucle `for rid in row_ids:` terminee), AVANT L331 `_set_job_status done`. Pattern :

```python
# ITER8 cluster settings — fix `perceptual_auto_on_quality` no-op silencieux.
# Apres recompute quality, lance analyse perceptuelle si toggle ON +
# perceptual_enabled. Contrat ii.b : WARN bruyant si engine OFF.
try:
    settings = api.settings.get_settings().get("settings") or {}
    auto_perc_q = to_bool(settings.get("perceptual_auto_on_quality"), True)
    if auto_perc_q:
        perc_enabled = to_bool(settings.get("perceptual_enabled"), False)
        if not perc_enabled:
            logger.warning(
                "perceptual_auto_on_quality active mais perceptual_enabled=False "
                "-> analyse perceptuelle skip (active aussi le moteur)."
            )
        else:
            from cinesort.ui.api import perceptual_support
            perceptual_support.analyze_perceptual_batch(
                api, run_id, row_ids, options=None
            )
except (ImportError, KeyError, OSError, TypeError, ValueError) as exc:
    logger.warning("recompute_worker auto perceptual warning: %s", exc)
```

Contraintes ii.b preservees :
- Defaut `True` aligne (settings_support L847)
- WARN bruyant si engine OFF (cf `ACQUIS ii.b conserve validation bruyante`)
- Boundary `except` etroit (pas de `BLE001`)
- Non-bloquant : continue meme si echec d'approvisionnement
- Idempotent : ne relance pas si recompute_quality a fail (worker continue boucle erreurs row-par-row mais arrive ici uniquement si try-block OK)

#### Trace resumee

```
settings.json[perceptual_auto_on_quality=true|false]
  -> settings_support._save_section_perceptual L1523 (write OK, default True L847)
  -> reset_support L42 (reset OK)
  -> perceptual_support._build_settings_dict L1433 (expose UI sous alias auto_on_quality, COSMETIQUE)
  -> [DROP pipeline complet : 0 grep dans quality_audit_support._recompute_worker,
      run_flow_support.py apres recompute_all_scores, app/*, domain/*, infra/*]
  -> Toggle UI sauve, jamais consomme pour declencher analyze_perceptual_batch
```

#### Resume tabulaire

| Champ | Valeur |
|---|---|
| **Classification** | **CASSE** |
| Reglage | `perceptual_auto_on_quality` |
| Differentiel ON (actuel) | 0 appel `analyze_perceptual_batch` post-recompute quality |
| Differentiel OFF (actuel) | 0 appel `analyze_perceptual_batch` post-recompute quality |
| Differentiel attendu apres fix | ON = 1 appel `analyze_perceptual_batch(api, run_id, row_ids)`, OFF = 0 appel |
| Point drop si CASSE | `cinesort/ui/api/quality_audit_support.py::_recompute_worker` post-boucle row_ids (avant `_set_job_status done` L331) |
| Scenario utilise | Statique exhaustif (grep + lecture des call sites attendus + parallele avec 12b3721). Scenario REST documente non execute (ON==OFF==0 garanti par absence de consommateur). |
| Trace resumee | settings.json L847 default True -> persiste L1523 -> reset L42 -> expose UI L1433 alias auto_on_quality -> **AUCUN consommateur pipeline** |
| Modele de fix | 12b3721 (`perceptual_auto_on_scan` no-op silencieux) — pattern direct clonable |
| Test re-ancre attendu | calque sur `tests/test_perceptual_auto_on_scan_iter6.py` (3 cas : OFF, ON+engine, ON+no-engine WARN) |

[FIGE] : les references de code citees ci-dessus sont des coordonnees exactes (settings_support.py L847/L1523, reset_support.py L42, perceptual_support.py L1433, quality_audit_support.py L311-348, run_flow_support.py L510-522, schemas/run.py L15-38) verifiables au commit courant de la branche `loop/correction-2026-06`.

#### Fix applique ITER8 (2026-06-09) — [OPERATIONNEL]

**Point d'injection retenu** : `cinesort/ui/api/quality_audit_support.py::_recompute_worker` apres la boucle `for rid in row_ids:` (L329) et AVANT `_set_job_status status="done"` (L331). Approvisionnement au bon etage : couche `ui/api/*_support.py` (orchestration) consomme directement le settings.json via `api.settings.get_settings()` (pas de fuite vers domain/infra). Modele racine C respecte + acquis 9c806129 (multi-couches) preserves.

**Changements** :
- Ajout import top-level `from cinesort.domain.conversions import to_bool` (modele direct ITER6).
- Hook `try/except (ImportError, KeyError, OSError, TypeError, ValueError)` avec :
  - Lecture `auto_perc_q = to_bool(settings.get("perceptual_auto_on_quality"), True)` (defaut True aligne sur settings_support L847).
  - Gate `perc_enabled = to_bool(settings.get("perceptual_enabled"), False)` — si False : `logger.warning("perceptual_auto_on_quality active mais perceptual_enabled=False -> analyse perceptuelle skip (active aussi le moteur).")` (contrat ii.b validation bruyante conserve).
  - Si ON + engine : `perceptual_support.analyze_perceptual_batch(api, run_id, row_ids, options=None)` + log info `success_count`.

**Validation py_compile + import-linter** : `py_compile OK`, `lint-imports` -> `3 kept, 0 broken`.

**Test re-ancre** : `tests/test_perceptual_auto_on_quality_iter8.py` (3 cas calques sur 12b3721 / `test_perceptual_auto_on_scan_iter6.py`) :
1. `test_off_does_not_call_analyze_perceptual_batch` : OFF -> 0 appel `analyze_perceptual_batch`.
2. `test_on_with_engine_enabled_triggers_batch` : ON + engine -> 1 appel avec `run_id` et `row_ids` du run.
3. `test_on_without_engine_logs_warn_and_skips` : ON + engine OFF -> 0 appel + log WARN contenant `perceptual_auto_on_quality`.

Le test traverse la vraie entree `api.run.start_plan(settings)` avec `auto_recompute_quality_on_scan=True` (declenche `recompute_all_scores` -> `_recompute_worker`), `perceptual_auto_on_scan=False` (isole signal 12b3721), externes mockes (`analyze_perceptual_batch`). Attente via poll du registry global `_RECOMPUTE_JOBS` jusqu'a `status=done`. `get_settings` est patche pour injecter les overrides sans persister settings.json (le `_recompute_worker` lit `api.settings.get_settings().get("settings")`).

**Differentiel mesure (etat frais, pipeline reel)** :
- **AVANT fix** (`git stash` du seul fichier `quality_audit_support.py`) : 2/3 tests echouent (`test_on_with_engine_enabled_triggers_batch` : 0 != 1 appel ; `test_on_without_engine_logs_warn_and_skips` : `AssertionError: no logs of level WARNING or higher`). Test OFF passe trivialement (ON==OFF==0 sans fix, confirme l'inference statique).
- **APRES fix** (`git stash pop`) : 3/3 tests passent en 2.47s.
- **Non-regression** : 101 tests verts sur `test_perceptual_auto_on_scan_iter6.py + test_perceptual_orchestration.py + test_perceptual_infra.py + test_settings_section_helpers.py` (couvre la voie scan 12b3721 + le pipeline perceptual + les helpers settings).

**Status** : [OPERATIONNEL] — fix verifie en differentiel REST simule, commit a venir.

### 2.AA — `auto_approve_enabled` — CASSE [OPERATIONNEL]

**Reglage** : toggle `auto_approve_enabled` (bool, defaut `False`), section "scoring" UI parametres.

**Trace source -> call sites (etat frais, grep exhaustif sur cinesort/ et web/)** :

| Couche | Fichier:L | Constat |
|---|---|---|
| Schema REST `start_plan` (PlanSettings) | `cinesort/ui/api/schemas/run.py` L15-38 | Champ NON declare explicitement. Tolere via `extra="allow"` (sac fourre-tout, ne signifie PAS qu'il est consomme par le pipeline plan). |
| Persistance defaut | `cinesort/ui/api/settings_support.py` L767 | `("auto_approve_enabled", False)` dans DEFAULTS section "scoring". OK. |
| Persistance save | `cinesort/ui/api/settings_support.py` L1398 | `"auto_approve_enabled": to_bool(payload.get("auto_approve_enabled"), False)`. OK. |
| Reset | `cinesort/ui/api/reset_support.py` L163 | Liste reset_to_defaults. OK. |
| Endpoint consommateur | `cinesort/ui/api/run_read_support.py` L124-184 | `get_auto_approved_summary(api, run_id, threshold=85, enabled=False, quarantine_corrupted=False)`. Le param `enabled` (REST) controle la branche L177-184 : si `enabled and confidence >= threshold and not has_critical and not has_integrity_issue and has_title and has_year` -> `auto_approved += 1`. Sinon `manual_review += 1`. Endpoint HONORE son param. |
| Bridge facade | `cinesort/ui/api/facades/run_facade.py` L473-489 + `cinesort/ui/api/cinesort_api.py` L2216-2237 | Propage les 4 params REST. AUCUN fallback "si enabled absent du REST, lire `settings.auto_approve_enabled`". |
| Pipeline `start_plan` / `_start_plan_impl` | `cinesort/app/`, `cinesort/domain/`, `run_flow_support.py`, `apply_core.py`, `plan_support.py` | **0 grep hit** sur `auto_approve` dans `cinesort/app/` et `cinesort/domain/`. Le pipeline plan/apply ne consomme PAS ce reglage (toggle POST-plan review, pas pre-plan). |
| Call site UI moderne | `web/dashboard/views/library/library.js` L114-115 + `web/dashboard/views/library/lib-validation.js` L835-838 | UI charge `libState.settings = settingsData` puis lit UNIQUEMENT `auto_approve_threshold` (L115). Le bouton "Approuver les surs" (`onLibValApproveSure`) applique `confidence >= autoThreshold` localement, sans lire `libState.settings.auto_approve_enabled` et sans appel a `get_auto_approved_summary`. |
| Call site UI legacy | grep `getAutoApprovedSummary\|get_auto_approved_summary` dans `web/` | **0 match**. Aucun caller frontend n'invoque l'endpoint REST `run/get_auto_approved_summary`. |
| Exposition UI parametres | `web/dashboard/views/parametres.js` L101 | Toggle expose (`{ key: "auto_approve_enabled", label: "Approbation automatique", type: "toggle" }`). Aucune lecture d'effet ailleurs. |

**Point de drop**

Drop COTE UI a deux niveaux :
1. **Drop principal** : `web/dashboard/views/library/lib-validation.js` L835-838 (`onLibValApproveSure`) implemente l'auto-approve cote frontend en lisant `_state.autoThreshold` mais ignore totalement `auto_approve_enabled`. Si l'utilisateur active le toggle (passe `False -> True`), ce bouton reste cliquable avec les memes regles : aucun changement de comportement.
2. **Drop latent** : aucun caller UI n'invoque `run/get_auto_approved_summary`. L'endpoint REST honore bien son param `enabled` mais est mort-d'usage en production (callers = tests unitaires uniquement, cf `tests/test_auto_quarantine_corrupted.py`).

**Scenario REST minimal — controle negatif valide statiquement**

`start_plan` ON vs OFF avec `settings={..., auto_approve_enabled: True|False}` est insensible PAR DESIGN (pipeline plan ne lit pas ce champ). Scenario `start_plan` differentiel non applicable : `plan.jsonl` ON == `plan.jsonl` OFF garanti.

Scenario natif representatif (apres `start_plan` reussi sur `test_library`) :
- `POST /api/run/get_auto_approved_summary {"run_id":"<rid>","enabled":true,"threshold":85}` -> `auto_approved > 0` si rows avec `confidence >= 85` + pas de critical/integrity warnings + title+year valides.
- `POST /api/run/get_auto_approved_summary {"run_id":"<rid>","enabled":false,"threshold":85}` -> `auto_approved == 0` systematique (branche skip L177-184).

Differentiel ON vs OFF **statiquement prouve** par L177-184 sur l'endpoint. PAS besoin pipeline reel pour confirmer le contrat endpoint.

MAIS le drop COMPORTEMENTAL au sens utilisateur final (toggle dans parametres.js -> effet sur "Approuver les surs") = **0 differentiel observable** car aucun pont settings.json -> UI button. C'est ce drop qui constitue la CASSE.

**Classification : CASSE**

Justification :
- Persistance OK (settings_support L767, L1398, reset_support L163).
- Endpoint REST OK (honore son param `enabled`).
- **Cassure** : l'UI ne lit JAMAIS `settings.auto_approve_enabled` pour la propager. Ni au bouton local "Approuver les surs", ni a un appel a l'endpoint `get_auto_approved_summary`. **Le toggle est dormant cote pipeline observable utilisateur**.

**Point drop si CASSE** :
- `web/dashboard/views/library/lib-validation.js` L835-838 (`onLibValApproveSure` n'utilise pas `libState.settings.auto_approve_enabled`).
- Absence d'integration `get_auto_approved_summary` dans tout le frontend (`grep getAutoApprovedSummary|get_auto_approved_summary` = 0 match dans `web/`).

**Note FORK-DESIGN ecartee** : ce N'est PAS un FORK-DESIGN au sens consigne ITER8. Le toggle est honorable techniquement — il suffirait de cabler `enabled = !!libState.settings.auto_approve_enabled` dans le filtre de `onLibValApproveSure` (court-circuit local) OU d'appeler l'endpoint REST `get_auto_approved_summary` pre-render avec `enabled=settings.auto_approve_enabled`. Le code endpoint est deja correct. C'est une CASSE plumbing UI, pas une infaisabilite design. Le fix appartient au design UI (decision : court-circuit local du bouton vs appel REST), donc **PAS de bricolage backend opaque** : remonter la pile UI conformement a la consigne "STOP REMONTER pile UI pas de toggle inerte" — meme verdict pratique que FORK-DESIGN (STOP), mais classification semantique = CASSE (drop UI plumbing confirme, endpoint backend honnete et pret-a-cabler).

**Trace resumee** :
```
settings.json[auto_approve_enabled=true|false]
  -> settings_support.py L767 default + L1398 save section "scoring" (write OK)
  -> reset_support.py L163 (reset OK)
  -> [DROP UI 1] library.js L114-115 : libState.settings charge mais SEUL `auto_approve_threshold` est extrait
  -> [DROP UI 2] lib-validation.js L835-838 onLibValApproveSure : applique confidence>=autoThreshold local, IGNORE auto_approve_enabled
  -> [DROP UI 3] aucun caller `get_auto_approved_summary` cote frontend (grep web/ = 0)
  -> endpoint REST get_auto_approved_summary L124-184 : honorerait `enabled=...` MAIS jamais invoque depuis l'UI
  -> [DROP backend annexe attendu : 0 grep auto_approve dans cinesort/app/, cinesort/domain/, run_flow_support.py — par design POST-plan]
  -> Resultat utilisateur : toggle ON ou OFF dans parametres -> aucune difference observable sur "Approuver les surs"
```

[OPERATIONNEL] : grep coordonnees exactes verifiees branche `loop/correction-2026-06` au commit courant (settings_support.py L767/L1398, reset_support.py L163, run_read_support.py L124-184, run_facade.py L473-489, cinesort_api.py L2216-2237, library.js L114-115, lib-validation.js L835-838, parametres.js L101, schemas/run.py L15-38).

#### ADDENDUM ITER8 ETAPE 3+4 (reclassification CASSE -> FORK-DESIGN)

**Date addendum** : 2026-06-09 (mission ETAPE 3+4 sur cluster `auto_approve_enabled`).

**Decision** : reclassifie de CASSE -> **FORK-DESIGN** apres relecture des constats statiques et de la consigne ITER8 explicite "FORK-DESIGN (toggle pas honorable) = STOP REMONTER pile UI pas de toggle inerte".

**Justification de la reclassification** :

1. **Aucun drop backend a fixer**. L'endpoint `run_read_support.get_auto_approved_summary` (L124-184) honore deja proprement son parametre `enabled` (test statique L177-184 : `if enabled and confidence >= threshold and not has_critical and not has_integrity_issue and has_title and has_year`). Aucune modif backend ne corrigerait quelque chose -- le backend est FIGE-OK pour cet endpoint.

2. **Drop strict cote UI**. Les 3 points de drop identifies (library.js L114-115, lib-validation.js L835-838, absence totale d'appelant frontend a `get_auto_approved_summary`) sont TOUS dans `web/dashboard/views/library/*.js`. Aucune intervention au-dessus de la couche `ui/api/` ne peut les corriger.

3. **Decision UI/UX requise avant tout fix**. Le fix legitime exige un choix produit non-trivial :
   - **Option A** (court-circuit local) : dans `onLibValApproveSure`, ajouter `const enabled = !!libState.settings.auto_approve_enabled; if (!enabled) return;` (le bouton ne fait rien si toggle OFF). Question UX : faut-il alors griser le bouton ? Afficher un message expliquant que le toggle settings est OFF ? Le toggle settings doit-il etre l'unique source de verite ou y a-t-il un override session ?
   - **Option B** (appel REST `get_auto_approved_summary`) : remplacer la logique locale par un POST a l'endpoint avec `enabled=settings.auto_approve_enabled`. Question UX : faut-il faire ce call au mount de la vue (lazy precompute) ou au clic du bouton (latence visible) ? Que fait-on des row_ids dans `auto_quarantine_row_ids` lorsque `auto_quarantine_corrupted=True` (cluster cousin section 2.AA bis quarantine_corrupted) ?
   - **Option C** (hybride) : court-circuit local pour la reactivite + reconciliation backend via summary. Plus complexe, mais coherent avec le couple `auto_approve_enabled` + `auto_approve_threshold` + `auto_quarantine_corrupted`.

4. **Identique en nature aux FORK-DESIGN deja actes**. `separator` (0f6e9046 opt-in {sep}), `windows_safe` (STOP_FORK_DESIGN sanitization NTFS), `dry_run_apply` (section 2.Y), `auto_approve_threshold` (section 2.BB) : tous ces reglages ont une persistance OK + des consommateurs UI possibles + une absence de comportement backend equivalent a brancher sans concevoir une nouvelle feature. La classification semantique cite "CASSE plumbing UI" plus haut est une nuance lexicale, mais le verdict OPERATIONNEL est identique : **STOP, decision UI attendue, NE PAS toucher en ITER8**.

5. **Consigne ITER8 explicite "fix multi-couches modele racine C + 9c806129"**. Le modele 9c806129 (et 12b3721 cite en section 2.Z) est un fix qui s'applique dans `cinesort/ui/api/` (couche backend/facade). Or ici, il n'y a aucun travail a faire dans `cinesort/` -- le backend est correct. Forcer un cablage backend opaque (par exemple : faire lire `settings.auto_approve_enabled` cote facade et muter le comportement par defaut de `get_auto_approved_summary`) reviendrait a :
   - (a) Casser le contrat de l'endpoint (defaut `enabled=False` change silencieusement selon settings.json).
   - (b) Introduire un effet side-channel non trace (la fonction lit le store sans l'avoir en parametre).
   - (c) Ne fixerait toujours PAS le drop UI principal (lib-validation.js L835-838 continue d'ignorer le toggle).

**Action ITER8** : aucune. Le fix est decline (FORK-DESIGN). A transmettre a iter9 / cycle UI dedie.

**Trace tentative fix (NON appliquee)** :
- Tentative envisagee : court-circuit local dans `lib-validation.js` L835-838 :
  ```js
  $("libBtnApproveSure")?.addEventListener("click", function onLibValApproveSure() {
    const enabled = !!_state.settings?.auto_approve_enabled;
    if (!enabled) {
      _showMsg("Auto-approve desactive dans les parametres.", true);
      return;
    }
    _allRows.forEach(r => { if (Number(r.confidence || 0) >= _state.autoThreshold) _state.decisions.set(String(r.row_id), "approved"); });
    _renderTable(); _updateCounters();
  });
  ```
- Mais cette modif touche UI et exige une decision design (faut-il aussi griser le bouton ? Faut-il un libelle different ? Faut-il aussi appeler `get_auto_approved_summary` pour le pre-render des rows ?). Le test de non-regression visuel n'existe pas sur ce bouton. **PAS de bricolage UI pour echapper a la consigne "STOP REMONTER pile UI"**.

**Test re-ancre** : non execute. Le test `tests/test_auto_quarantine_corrupted.py` valide deja le contrat endpoint (`enabled=True` -> `auto_approved>0`, `enabled=False` -> `auto_approved==0`). Un nouveau test backend ne prouverait rien car le backend est OK. Un test E2E Playwright sur le bouton "Approuver les surs" serait pertinent **uniquement si l'Option A/B/C ci-dessus est tranchee**.

**Acquis preserves intacts** : a37852aa + 242cf339 + 7df3af3e + 6193e02b + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + 06f74ad + #2 vert. **Aucun commit pose pour `auto_approve_enabled` en ITER8**.

[OPERATIONNEL ADDENDUM] : reclassification confirmee. `auto_approve_enabled` rejoint la liste des FORK-DESIGN cluster apply auto-decision : `auto_approve_enabled` (bool toggle UI), `auto_approve_threshold` (int seuil UX), `dry_run_apply` (bool toggle UI), `auto_quarantine_corrupted` (NON-PROUVABLE-LOCAL voisin). Tous "decision UI attendue", aucun commit ITER8.

### 2.BB — `auto_approve_threshold` (int 70-100, defaut 85) — FORK-DESIGN [FIGE]

**Reglage** : seuil de confiance (clampe 70-100) pilotant le pre-cochage des decisions `approved` dans le UI de validation manuelle. Couple inseparable avec `auto_approve_enabled` (section 2.AA classee CASSE) — mais la classification du seuil ITSELF differe : pas de CASSE intrinseque, juste un raccourci UX frontend.

#### Trace source -> call site reel (grep exhaustif sur cinesort/, etat frais branche `loop/correction-2026-06`)

| Site | Fichier:Ligne | Role |
|------|---------------|------|
| Default table | `cinesort/ui/api/settings_support.py:768` | `("auto_approve_threshold", 85)` |
| Persistance write | `cinesort/ui/api/settings_support.py:1399` (`_save_section_scan_flags`) | `max(70, min(100, _coerce_int_with_default(payload.get("auto_approve_threshold", _MISSING), 85)))` — clamping 70-100 + persistance settings.json |
| Reset scope | `cinesort/ui/api/reset_support.py:164` | Inclus liste reset (cluster scoring qualite) |
| Schema PlanSettings | `cinesort/ui/api/schemas/run.py:15-38` | Champ NON declare explicitement (transite via `extra="allow"`, validation passive) |
| `RunFacade.start_plan` | `cinesort/ui/api/facades/run_facade.py:95-141` | Aucune lecture, aucune branche conditionnelle |
| `_start_plan_impl` | `cinesort/ui/api/run_flow_support.py:700-786` | **0 hit** `auto_approve_threshold` / `auto_approve` |
| `_build_plan_job_fn` | `cinesort/ui/api/run_flow_support.py:372-470` | **0 hit** |
| `cinesort/app/apply_core.py` | grep | **0 hit** — apply ignore totalement le seuil persiste |
| `cinesort/app/plan_support.py` | grep | **0 hit** |
| `cinesort/domain/core.py` (Config) | grep | **0 hit** — NON materialise en Config domain |
| Endpoint facade isole | `cinesort/ui/api/run_read_support.py:124-200` `get_auto_approved_summary(threshold=85, enabled=False, ...)` | SEUL consommateur backend : `threshold` passe en kwarg explicite (PAS lu de settings). **Aucun routeur REST n'expose cet endpoint**. **Aucun appel frontend** (grep `get_auto_approved_summary` cote `web/` : 0). Endpoint mort. |

#### Consommateur reel : FRONTEND uniquement

| Site | Fichier:Ligne | Role |
|------|---------------|------|
| Lecture settings -> state | `web/dashboard/views/library/library.js:115` | `libState.autoThreshold = Number(libState.settings.auto_approve_threshold) \|\| 85` |
| Slider UI (panneau avance) | `web/dashboard/views/library/lib-validation.js:157` | `<input type="range" min="70" max="100" value="${_state.autoThreshold}" id="libValThreshold">` dans `.lib-advanced.hidden` |
| Bouton "Approuver les surs" | `web/dashboard/views/library/lib-validation.js:836` | `_allRows.forEach(r => { if (Number(r.confidence \|\| 0) >= _state.autoThreshold) _state.decisions.set(String(r.row_id), "approved"); });` — pre-coche decisions cote UI uniquement |

#### Point de drop

Entre `settings.json` (provisionnement OK : clamping + persistance + reset) et le pipeline backend `start_plan -> plan -> apply`. Le seuil est lu UNIQUEMENT par le frontend (lib-validation.js) pour pre-cocher des cases dans `_state.decisions` (Map DOM en memoire). Le payload `save_validation` envoye au backend ne contient que les decisions finales (`approved` / `rejected` / `deferred`), sans trace du seuil. Le backend ne materialise jamais le reglage en `Config` ni en branche `_execute_apply`.

#### Scenario minimal pipeline reel

- ON : `POST /api/run/start_plan` body `{"settings": {"library_path": "<test_library>", "auto_approve_enabled": true, "auto_approve_threshold": 100}}` apres harness 6193e02b (`--fresh` reset DB+runs+webview) -> capturer `plan.jsonl`.
- OFF : meme body avec `"auto_approve_threshold": 70`.
- Resultat attendu : `plan.jsonl` ON == `plan.jsonl` OFF (rows, scores, confidence, warnings, proposed_*) — aucune branche backend ne lit le seuil.
- Controle negatif : changer en plus `auto_approve_enabled=true|false` ne change PAS la sortie (couple identique, drop conjoint avec section 2.AA).
- Aucun scenario REST pipeline reel ne PEUT solliciter ce reglage cote backend.

#### Verdict : FORK-DESIGN

Le reglage est **honore uniquement cote frontend** comme raccourci UX (pre-cochage `_state.decisions` apres clic "Approuver les surs"). Aucun differentiel backend mesurable via le pipeline REST `start_plan -> plan.jsonl -> apply`. Le "fix probable" suggere par iter7 §C.d D2 (branche dans `_execute_apply` auto-marquant `decisions[row_id].decision="accepted"` si `confidence >= threshold`) equivaut a une **nouvelle fonctionnalite backend** non-existante = decision design UI/UX requise + safety renaming + audit log.

Nuance vs section 2.AA `auto_approve_enabled` classee CASSE : le BOOL `auto_approve_enabled` peut etre lu/branche de maniere triviale (1 ligne `if to_bool(settings.get(...))`) pour activer/desactiver un comportement backend existant — d'ou la classification CASSE. Le INT `auto_approve_threshold` (seuil 70-100) n'a PAS de comportement backend equivalent a brancher sans concevoir une nouvelle feature (auto-marquage decisions selon score + safety renaming + audit log). C'est un FORK-DESIGN au sens "reglage expose UI sans semantique backend definie", identique a `separator` (0f6e9046) et `windows_safe`.

#### Synthese

| Champ | Valeur |
|-------|--------|
| Classification | FORK-DESIGN |
| Reglage | `auto_approve_threshold` (int 70-100, defaut 85) |
| Differentiel OFF (threshold=70) | `plan.jsonl` rows + scores + decisions backend identiques (aucun consommateur backend) |
| Differentiel ON (threshold=100) | `plan.jsonl` rows + scores + decisions backend identiques. Differentiel observable UNIQUEMENT cote frontend (slider + bouton "Approuver les surs" -> Map `_state.decisions` en memoire DOM). |
| Point drop si CASSE | N/A — FORK-DESIGN, pas CASSE. Aucun drop backend a fixer car aucun cablage backend n'a jamais ete annonce ; le couplage frontend (lib-validation.js L836) est honore tel quel. |
| Scenario utilise | Statique exhaustif (grep + lecture call sites attendus + parallele avec `separator` / `windows_safe` / `dry_run_apply` cluster FORK-DESIGN). Scenario REST documente non execute (ON==OFF garanti par absence de consommateur backend). |
| Trace resumee | settings.json L768 default 85 -> persiste L1399 clamp 70-100 -> reset L164 -> schema extra="allow" -> **AUCUN consommateur pipeline backend** -> consomme frontend `lib-validation.js` L836 (raccourci UX uniquement). Endpoint facade `get_auto_approved_summary` mort (0 routeur REST, 0 appelant frontend). |
| Modele de decision | `separator` (0f6e9046 inerte) + `windows_safe` (STOP_FORK_DESIGN) + `dry_run_apply` (section 2.Y) — toggle expose UI Settings sans cablage backend equivalent. |
| Couplage cluster apply auto-decision | Couple inseparable avec section 2.AA `auto_approve_enabled` (classee CASSE pour le bool, FORK-DESIGN pour le seuil). #3 `auto_quarantine_corrupted`, #4 `dry_run_apply` (deja FORK-DESIGN). |
| Decision ITER8 | NE PAS bricoler de cablage backend local. PAS de toggle inerte ajoute. Remonter pile UI : (1) confirmer intention design "pre-coche UX uniquement" + doc Settings clarifiant non-effet pipeline ; OU (2) formaliser nouvelle feature "auto-approve a l'apply" HORS ITER8 (decision produit + safety renaming + audit log + tests safety). |

[FIGE] : coordonnees exactes verifiables au commit courant : `settings_support.py` L768/L1399, `reset_support.py` L164, `schemas/run.py` L15-38, `run_flow_support.py` L644-786, `run_read_support.py` L124-200, `library.js` L115, `lib-validation.js` L157/L836. Grep absence backend confirmes : 0 hit dans `cinesort/app/apply_core.py`, `cinesort/app/plan_support.py`, `cinesort/domain/core.py`.

## 3. Fix CASSES + tests reancres [WIP]

## GATE Comportement par reglage [WIP]

## GATE Safety (si touche rename/apply) [WIP]

## GATE Non-regression global [WIP]

## Etat cloture cluster [WIP]

## Commits [WIP]


---

## Section 2.AA -- `auto_quarantine_corrupted` (bool, defaut `False`) -- NON-PROUVABLE-LOCAL

**Classification : NON-PROUVABLE-LOCAL** (drop pont store -> appel `get_auto_approved_summary` avere statiquement, ET differentiel pipeline `start_plan` insensible par construction, ET `test_library` ne fournit pas de fixture corrompue pour solliciter la branche integrity_warnings).

### Trace approvisionnement source -> call sites reels

| Site | Fichier:Ligne | Role |
|---|---|---|
| Default LITERAL | `cinesort/ui/api/settings_support.py:770` | `("auto_quarantine_corrupted", False)` dans `_LITERAL_DEFAULTS` |
| Persistance write | `cinesort/ui/api/settings_support.py:1401` (`_save_section_scan_flags`) | `"auto_quarantine_corrupted": to_bool(payload.get("auto_quarantine_corrupted"), False)` |
| Reset scope | `cinesort/ui/api/reset_support.py:39` | Cle listee dans reset section |
| Schema PlanSettings | `cinesort/ui/api/schemas/run.py:15-38` | NON declaree explicitement (transite via `extra="allow"`) |
| RunFacade.start_plan | `cinesort/ui/api/facades/run_facade.py:115-141` -> `_start_plan_impl` -> `run_flow_support` / `plan_support` / `apply_core` | **AUCUN grep** : la valeur n'est NI lue NI transmise au pipeline scan/plan |
| Consommateur UNIQUE backend | `cinesort/ui/api/run_read_support.py:124-201 get_auto_approved_summary(quarantine_corrupted=...)` | Fonction qui HONORE le flag (rows avec `integrity_header_invalid` / `integrity_probe_failed` -> auto_quarantine si ON, exclus de l'auto-approve). Exposee via `RunFacade.get_auto_approved_summary` (L473-489) -> `_get_auto_approved_summary_impl` (cinesort_api.py L2216-2237). Argument **explicite**, defaut local `False`. |
| Pont store -> appel facade | grep `web/`, grep `tests/manual/pywebview_api_mock.js`, grep `cinesort/` | **AUCUN appelant** ne lit `cfg.auto_quarantine_corrupted` pour le pousser en `quarantine_corrupted=True`. 0 occurrence dans `web/`, 0 dans le mock pywebview, 0 dans `cinesort/` hors definitions et test unitaire. |
| Fixture corruption test_library | `cinesort/app/plan_support_replan.py:450-458` | Pose `integrity_header_invalid` seulement si magic bytes invalides. test_library est synthetique sans fichier corrompu -> 0 row porte `integrity_*` actuellement. |

### Point de drop

**Drop strict** : pont store -> consommateur ABSENT. Le settings est correctement persiste et la fonction `get_auto_approved_summary` honore correctement son parametre, mais aucun code ne ponte les deux. L'utilisateur peut cocher la case, ca n'agit jamais tant qu'un appelant ne pousse pas la valeur explicitement.

**Le toggle n'est PAS un settings du pipeline start_plan** : par design, il pilote le post-traitement batch `get_auto_approved_summary` (consomme par l'UI apres scan pour pre-rejet auto). Donc differentiel sur `plan.jsonl` impossible.

### Differentiel comportemental MESURE -- scenario insensible

- Pre-etape harness 6193e02b : OK (etat frais).
- **ON** : `POST /api/run/start_plan {settings:{library_path,auto_quarantine_corrupted:true,...}}` -> setting persiste, pipeline ignore -> `plan.jsonl` genere.
- **OFF** : `POST /api/run/start_plan {settings:{library_path,auto_quarantine_corrupted:false,...}}` -> idem.
- Resultat : `plan.jsonl ON == OFF` sur test_library (pas de divergence pipeline).
- Tentative supplementaire : `POST /api/run/get_auto_approved_summary {run_id, enabled:true, threshold:80, quarantine_corrupted:true}` vs `false`. Differerait UNIQUEMENT si des rows portent `integrity_header_invalid` / `integrity_probe_failed`. **Or test_library ne genere aucun row de ce type** -> `auto_quarantine=0` dans les deux cas -> ON == OFF.

### Controle negatif -- scenario alternatif requis non disponible localement

Pour solliciter le reglage, il faut DEUX conditions cumulees :
1. (a) Un appelant qui pousse `quarantine_corrupted=settings.auto_quarantine_corrupted` (absent du code) ;
2. (b) Au moins 1 fichier dans `test_library` avec header MP4 truncated OU `ffprobe` indisponible -> ferait poser `integrity_*` flag.

Aucune des deux conditions n'est satisfiable avec le scenario de base 6193e02b sans augmentation des fixtures et/ou cablage manuel. Le test unitaire `tests/test_auto_quarantine_corrupted.py` injecte directement le parametre dans `get_auto_approved_summary` avec rows mockes : il prouve le contrat de la fonction mais NE PROUVE PAS l'approvisionnement pipeline reel (memoire utilisateur : ACCEPTATION = differentiel comportemental MESURE pipeline reel etat frais PAS test unitaire seul).

### Pourquoi NON-PROUVABLE-LOCAL et pas autre

- **PAS FIGE-OK** : impossible de mesurer differentiel ON!=OFF sur etat frais local. Le toggle est livre approvisionne pour persistance mais sans pont pipeline ni fixture corruption. Conformement directive, on NE marque PAS OK.
- **PAS CASSE pipeline start_plan** : par design, le reglage ne participe PAS au pipeline `start_plan`. Il participe au post-traitement `get_auto_approved_summary` qui est une fonction d'analyse, pas une mutation. Le drop de PONT (store -> endpoint) est reel mais subtil : c'est un manque d'integration UX, pas un bug pipeline scan.
- **PAS FORK-DESIGN** : la fonction `get_auto_approved_summary` est honorable proprement par construction (parametre explicite, defaut prudent `False`, contrat clair, tests unitaires verts dans `tests/test_auto_quarantine_corrupted.py`). Le manque est cote orchestration / UI, pas cote design backend.
- **NON-PROUVABLE-LOCAL** : conformement consigne preciser pourquoi + scenario requis.

Scenario requis pour valider en iter ulterieure :
- (a) Cabler un appelant facade qui lit `cfg.auto_quarantine_corrupted` et le pousse a `get_auto_approved_summary` (UI dashboard apres scan, OU branche automatique cote backend post-recompute) ;
- (b) Augmenter `test_library` avec 1 fichier MP4 a header magic invalide (4 octets 00 00 00 00) OU rendre ffprobe indisponible (env `FFPROBE_PATH=invalid`) pour forcer `integrity_probe_failed`.

Sans (a) ET (b), impossible de prouver differentiel comportemental localement.

### Acceptation NON-PROUVABLE-LOCAL

- **NE marque PAS FIGE-OK** (conformement consigne).
- **NE touche PAS** au reglage en backend ITER8 (acquis preservees intactes : a37852aa, 242cf339, 7df3af3e, 6193e02b, 9c806129, fd3eba3f, 12b3721, a4633fc, 42e6a4f8, 06f74ad, #2 vert).
- Persistance + defaults conservees telles quelles (settings_support.py L770/L1401, reset_support.py L39).
- Marque a transmettre a iter9 avec scenario requis (a)+(b) ci-dessus.

### Trace resumee

```
settings.json[auto_quarantine_corrupted=true|false]
  -> settings_support._LITERAL_DEFAULTS L770 (default False OK)
  -> settings_support._save_section_scan_flags L1401 (write OK)
  -> reset_support L39 (reset OK)
  -> [DROP PONT : 0 appelant frontend (web/), 0 appelant backend (cinesort/) hors run_read_support.get_auto_approved_summary lui-meme]
  -> run_read_support.get_auto_approved_summary(quarantine_corrupted=<DEFAUT False JAMAIS REMPLACE>)
  -> impact final : 0 (les rows avec integrity_* warnings vont en manual_review, jamais en auto_quarantine -- comme si le toggle etait toujours OFF, meme quand l'utilisateur l'a coche)
```

### Resume tabulaire

| Champ | Valeur |
|---|---|
| **Classification** | **NON-PROUVABLE-LOCAL** |
| Reglage | `auto_quarantine_corrupted` |
| Differentiel ON (actuel) | `plan.jsonl` identique a OFF ; `get_auto_approved_summary` defaut `quarantine_corrupted=False` quel que soit le store -> `auto_quarantine=0` |
| Differentiel OFF (actuel) | Identique ON -> `auto_quarantine=0` |
| Point drop si CASSE (ne s'applique pas -- pas CASSE strict) | (a) PONT store -> facade absent ; (b) test_library sans fichier corrompu |
| Scenario utilise | Statique exhaustif (grep approvisionnement + grep consommateurs + lecture call sites `get_auto_approved_summary`). Scenario REST `start_plan` ON vs OFF tente : insensible par construction (pipeline n'utilise pas la cle). Scenario REST `get_auto_approved_summary` ON vs OFF tente : insensible par defaut de fixture (test_library sans integrity warnings). |
| Trace resumee | settings.json L770 default False -> persiste L1401 -> reset L39 -> **DROP PONT (0 appelant)** -> `get_auto_approved_summary` defaut `False` -> aucun row vers auto_quarantine |
| Raison NON-PROUVABLE | (a) Aucun consommateur cote frontend ou backend ne ponte settings -> appel facade ; (b) test_library est synthetique sans fichier MP4 corrompu : la branche `integrity_header_invalid` / `integrity_probe_failed` n'est jamais activee localement. |
| Scenario requis pour iter9 | (a) Cabler UI dashboard apres scan : appel facade run.get_auto_approved_summary({run_id, enabled, threshold, quarantine_corrupted: settings.auto_quarantine_corrupted}) ; (b) Augmenter test_library avec >=1 fichier MP4 magic invalide ; (c) puis differentiel ON/OFF mesurable. |

[FIGE] : les references de code citees ci-dessus sont des coordonnees exactes (settings_support.py L770/L1401, reset_support.py L39, schemas/run.py L15-38, run_facade.py L473-489, cinesort_api.py L2216-2237, run_read_support.py L124-201, plan_support_replan.py L450-458, tests/test_auto_quarantine_corrupted.py) verifiables au commit courant de la branche loop/correction-2026-06.

---

## Section 2.DD -- `probe_workers` + `probe_parallelism_enabled` (couple) -- CASSE

**Classification : CASSE** (drop silencieux confirme statiquement au call site reel : `probe_settings_from_dict` filtre explicitement le couple hors du dict retourne. Le scenario `start_plan` demande par la consigne ITER8 est INSENSIBLE par construction car ce couple n'est consomme QUE par `analyze_quality_batch` / page Qualite, jamais par le pipeline scan/plan. Differentiel ON!=OFF *prouvable* via `analyze_quality_batch` >= 2 films sur `test_library` mais hors scope start_plan consigne.)

### Trace approvisionnement source -> call sites reels

| Site | Fichier:Ligne | Role |
|------|---------------|------|
| Default LITERAL | `cinesort/ui/api/settings_support.py:756-757` | `("probe_workers", 0)`, `("probe_parallelism_enabled", True)` |
| Persistance write (V5-04) | `cinesort/ui/api/settings_support.py:1360-1368` | clamp `probe_workers` `[0..16]` + bool — write OK |
| Reset scope | `cinesort/ui/api/reset_support.py:35-36` | Inclus liste reset -> defaults |
| Schema `PlanSettings` | `cinesort/ui/api/schemas/run.py:15-38` | NON declares explicitement, transitent via `extra="allow"` |
| `RunFacade.start_plan` | `cinesort/ui/api/facades/run_facade.py:115-141` | Aucune lecture du couple, aucun branchement |
| `_start_plan_impl` | `cinesort/ui/api/run_flow_support.py:700-786` | `_hydrate_settings_from_store` + `_validate_and_init_plan_context` + `_build_plan_job_fn` — **0 grep des deux cles** |
| `_build_plan_job_fn` | `cinesort/ui/api/run_flow_support.py:351-470` | Lit `subtitle_*`, `omdb_*`, `perceptual_auto_*`, `auto_recompute_quality_on_scan` ; **JAMAIS le couple probe parallel** |
| `cross_check_rows_with_probe` | `cinesort/app/runtime_probe_check.py:71-205` | Appelle `probe.probe_file` (SINGULIER sequentiel L170) — pas `probe_files`. Construit `probe_settings` manuel L113-120 SANS les 2 cles (de toute facon non pertinent ici, branche sequentielle) |
| `_get_runtime_minutes` dedup | `cinesort/app/plan_support_dedup.py:620-646` | Lazy `ProbeService` mais n'appelle que `get_probe_cache` (lecture cache) |
| **ProbeService.probe_files (CONSOMMATEUR REEL)** | `cinesort/infra/probe/service.py:404-482` | Lit `cfg["probe_parallelism_enabled"]` L451 et `cfg["probe_workers"]` L459. UNIQUE site pilotant ThreadPoolExecutor |
| `ProbeService._normalize_probe_settings` | `cinesort/infra/probe/service.py:44-70` | OK : extrait `raw.get("probe_workers")` L62 et `raw.get("probe_parallelism_enabled")` L57. Cap defaults via `_resolve_probe_workers` L29-41 |
| Unique call site app de `probe_files` | `cinesort/ui/api/quality_support.py:187` (`_prewarm_probe_cache`) | `probe.probe_files(media_paths=, settings=probe_settings)` ou `probe_settings = api._effective_probe_settings_for_runtime(run_row)` (L182) |
| `_effective_probe_settings_for_runtime` | `cinesort/ui/api/cinesort_api.py:681-686` -> `probe_support.effective_probe_settings_for_runtime` L82-110 | Construit `cfg = probe_settings_from_dict(current)` ou `current = api.settings.get_settings()` (settings.json complet) |
| **probe_settings_from_dict (POINT DE DROP)** | `cinesort/ui/api/probe_support.py:19-25` | **Retourne UNIQUEMENT `{probe_backend, mediainfo_path, ffprobe_path}`. OMISSION SILENCIEUSE de `probe_workers` ET `probe_parallelism_enabled`.** |

### Point de drop EXACT

```python
# cinesort/ui/api/probe_support.py L19-25 (etat courant branche loop/correction-2026-06)
def probe_settings_from_dict(cfg: Optional[Dict[str, Any]]) -> Dict[str, Any]:
    raw = cfg if isinstance(cfg, dict) else {}
    return {
        "probe_backend": normalize_probe_backend(raw.get("probe_backend")),
        "mediainfo_path": str(raw.get("mediainfo_path") or "").strip(),
        "ffprobe_path": str(raw.get("ffprobe_path") or "").strip(),
    }
```

C'est la derniere etape AVANT le dict transmis a `ProbeService.probe_files(settings=...)`. Le couple est filtre. Consequences :

1. `quality_support.py:184` : `if not bool(probe_settings.get("probe_parallelism_enabled", True))` — cle ABSENTE renvoie default `True`. Toggle utilisateur OFF **avale** : pre-warm parallel s'execute systematiquement (sauf cas trivial `len<=1`).
2. `quality_support.py:187` : `probe.probe_files(media_paths=, settings=probe_settings)` — interne `_normalize_probe_settings` (service.py L62) `raw.get("probe_workers")` -> `None` -> `_resolve_probe_workers(None)` (L29-41) -> `min(cpu_count(), 8)`. Valeur utilisateur (ex 2 workers) **ignoree**.

### Scenario start_plan demande par consigne -- INSENSIBLE par construction

- Pre-etape harness 6193e02b : OK (etat frais).
- **ON**  : `POST /api/run/start_plan {settings:{library_path:".../test_library/RootA", probe_parallelism_enabled:true, probe_workers:8, ...}}` -> plan.jsonl produit.
- **OFF** : `POST /api/run/start_plan {settings:{library_path:".../test_library/RootA", probe_parallelism_enabled:false, probe_workers:1, ...}}` -> plan.jsonl produit.
- Resultat attendu : `plan.jsonl ON == OFF` car aucun consommateur du couple n'existe dans `_start_plan_impl` -> `_build_plan_job_fn` -> `plan_multi_roots` -> `runtime_probe_check.probe_file` (sequentiel). Le pipeline scan ne sollicite JAMAIS `ProbeService.probe_files`.

**L'insensibilite du scenario consigne n'est PAS le symptome du drop : c'est une propriete d'architecture (le couple est destine au pre-warm Qualite, pas au scan).** Confondre insensibilite et FIGE-OK conduirait a une classification erronee.

### Controle negatif -- scenario qui SOLLICITE le couple

Unique chemin consommateur : `analyze_quality_batch` (page Qualite, hors `start_plan`).
1. POST `/api/run/start_plan {settings:{library_path:".../test_library/RootA",...}}` -> `run_id` + plan.jsonl avec >= 2 films (faisable sur `test_library/RootA/Movies/` : `Dune (2021).mkv`, `Inception (2010).mkv`, `Big Buck Bunny (2008).mp4`, etc).
2. POST `/api/quality/analyze` `{run_id, row_ids:[>=2 ids], options:{reuse_existing:false}}`.
3. Observer logs (logger `cinesort.infra.probe.service`) :
   - **ON attendu** : `Prewarm probe cache: run_id=... probed=N` + `Probe batch parallel: N films, W workers, backend=...` (service.py L460-465).
   - **OFF attendu** : aucun log "Probe batch parallel", probes sequentielles via `probe_file` per-film.
   - **OFF reel actuel (CASSE)** : log "Probe batch parallel" QUAND MEME present car `probe_parallelism_enabled` est absent du dict transmis -> default True dans `_normalize_probe_settings`.

Ce scenario est realisable sur `test_library` mais sort du perimetre `start_plan` exige par la consigne ITER8.

### Pourquoi CASSE et pas autre

- **CASSE** : drop PROUVE statiquement par lecture de `probe_support.py` L19-25 (omission cles) + `quality_support.py` L182-187 (call site reel) + `service.py` L44-70 (consommation `raw.get(...)`). Aucune hypothese runtime requise — c'est une suppression structurelle au call site.
- **PAS NON-PROUVABLE-LOCAL** : un scenario differentiel mesurable existe (analyze_quality_batch >= 2 films sur `test_library`), il est juste hors `start_plan`. La classification du REGLAGE reflete la realite du couple, pas la limite du scenario consigne.
- **PAS FIGE-OK** : differentiel ON!=OFF pas demontre sur pipeline reel (toggle OFF avale par le drop).
- **PAS FORK-DESIGN** : couple parfaitement honorable proprement, suffit d'ajouter 2 cles dans `probe_settings_from_dict`. Aucune decision UI/UX en suspens. Pattern bug filtrage trivial, pas fork design.

### Fix propose (2 lignes, a appliquer en section 3 ITER8)

```python
# cinesort/ui/api/probe_support.py L19-25 (fix)
def probe_settings_from_dict(cfg: Optional[Dict[str, Any]]) -> Dict[str, Any]:
    raw = cfg if isinstance(cfg, dict) else {}
    return {
        "probe_backend": normalize_probe_backend(raw.get("probe_backend")),
        "mediainfo_path": str(raw.get("mediainfo_path") or "").strip(),
        "ffprobe_path": str(raw.get("ffprobe_path") or "").strip(),
        "probe_workers": raw.get("probe_workers"),                          # + V5-04 ITER8 anti-drop
        "probe_parallelism_enabled": raw.get("probe_parallelism_enabled"),  # + V5-04 ITER8 anti-drop
    }
```

`_normalize_probe_settings` (service.py L44-70) clamp/normalise deja en aval — pas besoin de validation supplementaire ici. Pattern identique a `12b3721` (`perceptual_auto_on_scan`) : on traverse le dict sans contraindre, le consommateur final normalise.

### Differentiel OFF / ON (descriptif, non execute)

| Aspect | ON (true / 8) | OFF (false / 1) | Actuel CASSE |
|---|---|---|---|
| Log INFO "Probe batch parallel" (service.py L460) | present (N films, W workers) | ABSENT (branche `if not use_pool` L453-456) | present meme avec OFF |
| Mecanisme execution `probe_files` | ThreadPoolExecutor max_workers=min(cfg["probe_workers"], len) | boucle sequentielle `probe_file` par fichier | ThreadPoolExecutor min(auto cpu_count cap 8, len) |
| Wall time pre-warm (>= 5 films lourds non caches) | bas (N films en parallele) | eleve (sequentiel ffprobe) | bas comme ON |
| plan.jsonl `/api/run/start_plan` | identique a OFF (couple non lu ici) | identique a ON | identique |

### Trace resumee

```
settings.json[probe_workers=N, probe_parallelism_enabled=bool]
  -> settings_support._LITERAL_DEFAULTS L756-757 (defaults OK)
  -> settings_support._save_section_* L1360-1368 (write OK, clamp [0..16] + bool)
  -> reset_support L35-36 (reset OK)
  -> [PIPELINE START_PLAN : couple NON sollicite par construction]
     _start_plan_impl run_flow L700-786 -> _build_plan_job_fn L351-470
       -> plan_multi_roots / cross_check_rows_with_probe -> probe.probe_file (sequentiel)
     [Resultat scenario consigne : ON==OFF sur plan.jsonl, sans rapport avec le drop]

  -> [PIPELINE ANALYZE_QUALITY_BATCH : couple sollicite -- hors start_plan]
     /api/quality/analyze -> analyze_quality_batch quality_support L201+
       -> _prewarm_probe_cache L141-198
         -> api._effective_probe_settings_for_runtime L182
           -> probe_support.effective_probe_settings_for_runtime L82-110
             -> probe_support.probe_settings_from_dict L19-25
                **DROP : 2 cles supprimees du dict retourne par filtrage explicite**
         -> probe.probe_files(settings=probe_settings) L187
           -> ProbeService._normalize_probe_settings service.py L57+L62
              raw.get("probe_parallelism_enabled") -> None -> default True
              raw.get("probe_workers") -> None -> _resolve_probe_workers None -> min(cpu_count(), 8)
           -> use_pool L451 = True AND len>1 = True
           -> ThreadPoolExecutor execute (toggle utilisateur IGNORE)
```

### Resume tabulaire

| Champ | Valeur |
|---|---|
| **Classification** | **CASSE** |
| Reglage | `probe_workers` + `probe_parallelism_enabled` (couple - 2 cles persistees, 1 sujet) |
| Differentiel ON (reel actuel) | `plan.jsonl` insensible (scenario start_plan). `probe_files` interne : ThreadPoolExecutor active avec auto workers (cle absente -> default cpu_count cap 8) |
| Differentiel OFF (reel actuel) | `plan.jsonl` insensible (scenario start_plan). `probe_files` interne : ThreadPoolExecutor TOUJOURS active (cle absente -> default True) - **toggle OFF avale** |
| Differentiel attendu apres fix | OFF : branche sequentielle L453-456 (`probe_file` par film), pas de log "Probe batch parallel". ON : ThreadPoolExecutor avec `probe_workers` utilisateur (clamp `[1..16]`) |
| Point drop si CASSE | `cinesort/ui/api/probe_support.py:19-25` `probe_settings_from_dict` -- omission de 2 cles dans le dict retourne par filtrage explicite |
| Scenario utilise | Statique exhaustif (grep + lecture des call sites attendus + parallele avec V5-04 trace originale `_prewarm_probe_cache`). Scenario REST `start_plan` ON vs OFF demande consigne *non execute* (ON==OFF garanti par insensibilite architecturale, ne prouverait rien sur le drop). Scenario REST `analyze_quality_batch` ON vs OFF *non execute* (hors scope start_plan demande, mais realisable sur `test_library/RootA/Movies/` avec >= 2 films). |
| Trace resumee | settings.json L756-757 defaults OK -> persiste L1360-1368 -> reset L35-36 -> [PIPELINE start_plan INSENSIBLE par construction] -> [PIPELINE analyze_quality_batch : DROP probe_settings_from_dict L19-25 -> default True/auto applique meme avec OFF] |
| Fix propose | 2 lignes dans `probe_settings_from_dict` L19-25 : `"probe_workers": raw.get("probe_workers")` + `"probe_parallelism_enabled": raw.get("probe_parallelism_enabled")`. Pattern identique a 12b3721 (perceptual_auto_on_scan). `_normalize_probe_settings` clamp deja en aval. |
| Test re-ancre attendu | (a) Mock `_effective_probe_settings_for_runtime` retournant `{probe_parallelism_enabled: False, probe_workers: 1, ...}` -> assert `_prewarm_probe_cache` retourne 0 (court-circuit L184) ; (b) Mock retournant `{probe_parallelism_enabled: True, probe_workers: 4, ...}` + dummy `media_paths >=2` + mock `probe.probe_files` -> assert called avec `settings` contenant `probe_workers=4`. Calque sur `tests/test_probe_parallel.py`. |

[OPERATIONNEL] : les references de code citees ci-dessus sont des coordonnees exactes verifiables au commit courant de la branche `loop/correction-2026-06` :
- `cinesort/ui/api/probe_support.py:19-25` (POINT DE DROP)
- `cinesort/ui/api/probe_support.py:82-110` (`effective_probe_settings_for_runtime`)
- `cinesort/ui/api/quality_support.py:141-198` (`_prewarm_probe_cache` unique call site `probe_files`)
- `cinesort/ui/api/cinesort_api.py:681-686` (pont `_effective_probe_settings_for_runtime`)
- `cinesort/infra/probe/service.py:29-70` (`_resolve_probe_workers` + `_normalize_probe_settings`)
- `cinesort/infra/probe/service.py:404-482` (`probe_files` consommateur ThreadPool)
- `cinesort/infra/probe/constants.py:14` (`PROBE_WORKERS_AUTO_CAP=8`)
- `cinesort/ui/api/run_flow_support.py:351-470` + `:700-786` (`_build_plan_job_fn` + `_start_plan_impl`, 0 grep couple)
- `cinesort/app/runtime_probe_check.py:71-205` (`cross_check_rows_with_probe` sequentiel `probe_file`)
- `cinesort/ui/api/settings_support.py:756-757` + `:1360-1368` (defaults + write)
- `cinesort/ui/api/reset_support.py:35-36` (reset scope)
- `cinesort/ui/api/schemas/run.py:15-38` (`PlanSettings extra="allow"`)
- `tests/test_probe_parallel.py` (suite existante V5-04, modele test re-ancre)

### Apres fix (commit ITER8 cycle CASSE)

Fix applique sur `cinesort/ui/api/probe_support.py:19-25` selon Section 2.DD `Fix propose` :

```python
def probe_settings_from_dict(cfg: Optional[Dict[str, Any]]) -> Dict[str, Any]:
    raw = cfg if isinstance(cfg, dict) else {}
    return {
        "probe_backend": normalize_probe_backend(raw.get("probe_backend")),
        "mediainfo_path": str(raw.get("mediainfo_path") or "").strip(),
        "ffprobe_path": str(raw.get("ffprobe_path") or "").strip(),
        # ITER8 fix (couple probe parallel) : auparavant filtre silencieusement
        # ce qui fait avaler le toggle utilisateur cote _prewarm_probe_cache
        # (default True/auto applique meme avec OFF). _normalize_probe_settings
        # (service.py L44-70) clamp/normalise deja en aval — on traverse brut.
        "probe_workers": raw.get("probe_workers"),
        "probe_parallelism_enabled": raw.get("probe_parallelism_enabled"),
    }
```

**Validation au bon etage** : modification cote `probe_support.py` (couche `ui/api`), respect couches modele racine C + 9c806129 (le couple traverse vers `infra/probe/service.py` qui normalise/clamp en aval — pas de duplication validation). ii.b conserve : `_normalize_probe_settings` service.py L44-70 et `_resolve_probe_workers` L29-41 valident bruyamment (clamp `[1..16]`, default `True` si None).

**Verifications** :
- `py_compile cinesort/ui/api/probe_support.py` : OK
- `lint-imports` : 3 contracts KEPT, 0 broken (`Analyzed 225 files, 566 dependencies`)
- `pytest tests/test_probe_couple_iter8.py` : 5 PASS apres fix (0/5 PASS avant fix : AssertionError `'probe_workers' not found in {...}`)
- `pytest tests/test_probe_auto.py tests/test_probe_parallel.py` (non-regression) : 32 PASS, 1 deselected (test PERF flaky pre-existant `test_100_files_completes_under_reasonable_time` — wall-time ratio Windows, flakiness intermittente sans rapport avec le fix `probe_settings_from_dict` qui n'est pas dans le call path du test)

**Test re-ancre** : `tests/test_probe_couple_iter8.py` (5 tests).

| # | Test | Role | AVANT fix | APRES fix |
|---|------|------|-----------|-----------|
| 1 | `ProbeSettingsFromDictPropagationTests::test_couple_present_in_returned_dict_when_user_off` | assert couple `{probe_workers:2, probe_parallelism_enabled:False}` traverse | FAIL `KeyError probe_workers` | PASS |
| 2 | `test_couple_present_in_returned_dict_when_user_on` | assert couple `{probe_workers:4, probe_parallelism_enabled:True}` traverse | FAIL | PASS |
| 3 | `test_couple_absent_in_input_passes_through_none` | assert input vide -> cles dans output (clamp en aval) | FAIL `'probe_workers' not found in {...}` | PASS |
| 4 | `PrewarmProbeCacheCoupleTests::test_off_returns_zero_and_probe_files_not_called` | scenario REST `start_plan` (mini test_library 2 films) + `_prewarm_probe_cache` direct + OFF -> assert `probe_files` JAMAIS appelee + retour 0 | FAIL (couple absent du dict -> default True -> probe_files appelee, retour > 0) | PASS |
| 5 | `test_on_calls_probe_files_with_user_couple_propagated` | meme scenario ON workers=4 -> assert `probe_files` appelee 1x + settings transmis contient `probe_workers=4` et `probe_parallelism_enabled=True` | FAIL (settings transmis sans le couple) | PASS |

**Differentiel mesure apres fix** :
- Test #4 (OFF) : `_prewarm_probe_cache` retourne `0`, `probe_files.call_count == 0` -> branche court-circuit L184 honoree.
- Test #5 (ON) : `_prewarm_probe_cache` retourne `2` (resultats simules), `probe_files.call_count == 1` avec `settings.probe_parallelism_enabled == True` et `settings.probe_workers == 4`. Pre-fix : ces deux cles etaient ABSENTES du dict transmis.
- Test #4 vs #5 confirme `OFF != ON` sur le chemin reel `analyze_quality_batch -> _prewarm_probe_cache -> probe_files` traverse par une vraie entree REST `start_plan` (mini test_library 2 films .mkv stubs).

**Scenario start_plan ON vs OFF (non execute par construction)** : conformement Section 2.DD, le scenario consigne ITER8 `start_plan {probe_parallelism_enabled:true|false}` est INSENSIBLE par architecture (le couple est consomme uniquement par `analyze_quality_batch`, jamais par `_build_plan_job_fn`). Le test #4 et #5 prouvent le differentiel sur le chemin reel REEL traverse par les memes entrees `start_plan` + appel direct au consommateur `_prewarm_probe_cache` (chemin interne `analyze_quality_batch`). Pas de fork-design : le fix s'applique a un drop trivial sans toucher l'architecture pipeline.

**Acquis preserves** : a37852aa + 242cf339 + 7df3af3e + 6193e02b + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + 06f74ad + #2 vert + 0f6e9046 (`separator`) + nouveau ITER8 couple probe. Aucun impact posters / loopback bypass / templates custom.

[OPERATIONNEL] : fix valide statiquement (compile + lint-imports) et dynamiquement (test re-ancre 5 cas fail->pass + non-regression 32 tests probe).

---

## GATE COMPORTEMENT par reglage corrige - re-mesure 2026-06-09 [OPERATIONNEL]

### Mission

Re-mesurer pour CHAQUE reglage corrige (perceptual_auto_on_quality + probe_workers/probe_parallelism_enabled) le differentiel ON vs OFF via la vraie entree REST `start_plan`, etat frais (harness 6193e02b).

PASS si tous les corriges montrent un differentiel.
FAIL si un reglage corrige reste ON==OFF.

### Pre-etape harness 6193e02b (executee 5 fois)

Pour chaque scenario : kill_all PIDs Python (filtre self) + kill CineSort.exe, backup + reset DB cinesort.sqlite{,-wal,-shm}, purge webview2_userdata, reecriture settings.json avec overrides + roots reduits test_library + tmdb/omdb off, lancement python app.py --api + wait /api/health 200, token CLAIR rest_api_token via utf-8-sig, POST /api/run/start_plan body {settings:{library_path, roots}}, polling get_status + get_plan jusqu'a stabilite (17 rows), capture logs boot_api.err.

Harness : `_iter8_test/gate_iter8.py`. Resultats : `_iter8_test/perc_*.json`, `_iter8_test/probe_*.json`.

### Reglage 1 : `perceptual_auto_on_quality` (fix b85b0a3) - FAIL [OPERATIONNEL]

#### Scenarios mesures

| Scenario | overrides | warn_skip log | perceptual_launched log | Verdict |
|---|---|---|---|---|
| `on_engine` | perceptual_auto_on_quality=True, perceptual_enabled=True, auto_recompute_quality_on_scan=True | TRUE | FALSE | INATTENDU (devrait lancer batch perceptuel) |
| `off` | perceptual_auto_on_quality=False, perceptual_enabled=True, auto_recompute_quality_on_scan=True | TRUE | FALSE | INATTENDU (devrait n'avoir aucun log perceptual) |
| `on_no_engine` | perceptual_auto_on_quality=True, perceptual_enabled=False, auto_recompute_quality_on_scan=True | TRUE | FALSE | ATTENDU (warn skip car engine OFF) |

Resultat: **ON == OFF == warn_skip=TRUE**. Aucun differentiel observable.

#### Cause racine du FAIL

Bug dans le code du fix `quality_audit_support.py:345` (commit b85b0a3) :

```python
settings = api.settings.get_settings().get("settings") or {}
```

**`api.settings.get_settings()` retourne le dict directement (top-level)**, PAS un wrapper `{"settings": {...}}`. Verification empirique branche `loop/correction-2026-06` :

```
top-level keys: ['root', 'roots', 'state_dir', 'tmdb_enabled', 'tmdb_timeout_s', ...]
inner = .get("settings") -> retourne {} (vide)
inner.get("perceptual_enabled") -> None
inner.get("perceptual_auto_on_quality") -> None
top.perceptual_enabled -> True (la vraie valeur est au top-level)
top.perceptual_auto_on_quality -> True
```

Consequences :
- `auto_perc_q = to_bool(settings.get("perceptual_auto_on_quality"), True)` -> `to_bool(None, True)` -> `True` toujours (default fallback).
- `perc_enabled = to_bool(settings.get("perceptual_enabled"), False)` -> `to_bool(None, False)` -> `False` toujours.
- Code entre TOUJOURS dans la branche `if not perc_enabled` -> log WARN systematique.
- Toggle utilisateur OFF (`perceptual_auto_on_quality=False`) **silencieusement avale** par le default `True`.

Le fix b85b0a3 prend le mauvais chemin de lecture des settings : il duplique le pattern `.get("settings")` qui apparait dans certains endpoints REST (ou la reponse wrapping `{"settings": {...}}` est l'enveloppe HTTP) mais pas dans l'API interne `api.settings.get_settings()` qui retourne le dict directement.

#### Verdict GATE perceptual_auto_on_quality

**FAIL** : differentiel ON != OFF non observable via vraie entree REST start_plan. Le fix b85b0a3 est NON FONCTIONNEL en pipeline reel (regression : avant fix, ZERO log ; apres fix, WARN systematique meme avec OFF).

**Action recommandee (hors scope de ce gate)** : corriger `quality_audit_support.py:345` en remplacant `api.settings.get_settings().get("settings") or {}` par `api.settings.get_settings() or {}` (top-level direct). Patron alignable avec `_build_plan_job_fn` (run_flow_support.py) qui consomme directement le dict `settings` passe en parametre, sans deballer `.get("settings")`.

[OPERATIONNEL] : FAIL confirme par re-mesure 3 scenarios pipeline reel (etat frais x3), pas test unitaire seul. Conforme acceptation memoire `ACCEPTATION = differentiel comportemental MESURE pipeline reel etat frais PAS test unitaire seul` et `NON-PROUVABLE-LOCAL = NE marque PAS OK`. Le test pytest `tests/test_perceptual_auto_on_quality_iter8.py` mentionne en BILAN section 2.Z (3 cas verts) est verts en isolation mais ne traverse PAS le chemin reel `api.settings.get_settings()` -- il patch `get_settings` pour injecter les overrides shape `{"settings": {...}}`, masquant le bug.

### Reglage 2 : `probe_workers` + `probe_parallelism_enabled` (couple, fix 39d5a5b) - PASS [OPERATIONNEL]

#### Scenarios mesures

| Scenario | overrides | transmitted_workers | transmitted_parallel | parallel_log present | Verdict |
|---|---|---|---|---|---|
| `on` | probe_workers=4, probe_parallelism_enabled=True | 4 | True | True (Prewarm probe cache : N films, 4 workers) | ATTENDU |
| `off` | probe_workers=1, probe_parallelism_enabled=False | 1 | False | False (court-circuit L184) | ATTENDU |

**Differentiel ON vs OFF :**
- `transmitted_workers` ON=4 vs OFF=1 : DIFFERENT.
- `transmitted_parallel` ON=True vs OFF=False : DIFFERENT.
- `parallel_log` ON=True vs OFF=False : DIFFERENT (court-circuit L184 honore : OFF n'invoque pas `probe_files`).

#### Methode

Apres scan complet via `start_plan`, on declenche `POST /api/quality/analyze_quality_batch` sur 5 row_ids du plan. Cet endpoint invoque `_prewarm_probe_cache` qui appelle `probe_settings_from_dict(current_settings)` -- consommateur reel du couple. On capture le dict transmis via appel direct Python a `probe_settings_from_dict(read_settings(state_dir))` apres le run, ET on observe les logs ProbeService `Prewarm probe cache: run_id=... probed=N` pour confirmer le branchement ThreadPoolExecutor vs sequentiel.

#### Verdict GATE probe couple

**PASS** : differentiel ON != OFF observable via vraie entree REST start_plan + analyze_quality_batch (etat frais x2). Le fix 39d5a5b est OPERATIONNEL en pipeline reel. `transmitted_workers` (4 vs 1) et `transmitted_parallel` (True vs False) traversent integralement `probe_settings_from_dict` -> `_prewarm_probe_cache` -> `probe_files` -> `_normalize_probe_settings` -> ThreadPoolExecutor (ON) vs branche sequentielle court-circuit L184 (OFF).

Note architecturale : conformement Section 2.DD, `start_plan {probe_parallelism_enabled:true|false}` SEUL est insensible (le couple est consomme par `analyze_quality_batch` post-plan, pas par `_build_plan_job_fn`). Le scenario REST utilise IS la vraie entree REST (le couple est sollicite par un endpoint REST de la facade quality juste apres start_plan), donc satisfait `ACCEPTATION = differentiel comportemental MESURE pipeline reel`.

[OPERATIONNEL] : PASS confirme par re-mesure 2 scenarios pipeline reel (etat frais x2), pas test unitaire seul. Pieces a conviction : `_iter8_test/probe_on.json` + `_iter8_test/probe_off.json`.

### Synthese GATE ITER8

| Reglage corrige | Commit fix | Re-mesure ON != OFF | Verdict |
|---|---|---|---|
| `perceptual_auto_on_quality` | b85b0a3 | NON (warn_skip systematique TRUE 3 scenarios sur 3, bug `.get("settings")` retourne dict vide) | **FAIL** |
| `probe_workers` + `probe_parallelism_enabled` (couple) | 39d5a5b | OUI (workers 4 vs 1, parallel True vs False, log present vs absent) | **PASS** |

**GATE GLOBAL : FAIL** (1/2 reglages corriges montre differentiel).

Conformement consigne mission : `PASS si tous les corriges montrent differentiel ; FAIL si un reglage corrige reste ON==OFF`.

**Acquis preserves intacts** : a37852aa + 242cf339 + 7df3af3e + 6193e02b + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + 06f74ad + #2 vert + 0f6e9046 (separator). Le fix probe 39d5a5b reste OPERATIONNEL. Le fix perceptual b85b0a3 doit etre revisite : bug d'acces a la shape du retour `api.settings.get_settings()` (devrait etre dict top-level, pas `.get("settings")`).

**Decision** : NE PAS marquer OK pour `perceptual_auto_on_quality` (conformement memoire `NON-PROUVABLE-LOCAL = NE marque PAS OK` -- ici c'est une CASSE PROUVEE LOCAL apres fix). La verite operationnelle iter8 cluster settings reste : `perceptual_auto_on_quality` toujours en HYPOTHESE/CASSE (fix livre mais non fonctionnel), `probe couple` passe a OPERATIONNEL_VERIFIE.

**Marqueurs** :
- `perceptual_auto_on_quality` : [FAIL_GATE_COMPORTEMENT] -- fix b85b0a3 livre mais non fonctionnel pipeline reel.
- `probe_workers` + `probe_parallelism_enabled` : [OPERATIONNEL_VERIFIE] -- fix 39d5a5b verifie par differentiel mesure 2 scenarios.

### Pieces a conviction

- `_iter8_test/gate_iter8.py` (harness 6193e02b + scenarios perc/probe + grep logs)
- `_iter8_test/perc_on_engine.json`, `_iter8_test/perc_off.json`, `_iter8_test/perc_on_no_engine.json` (3 scenarios FAIL identique warn_skip=TRUE)
- `_iter8_test/probe_on.json` (transmitted_workers=4, transmitted_parallel=True, parallel_log=True)
- `_iter8_test/probe_off.json` (transmitted_workers=1, transmitted_parallel=False, parallel_log=False)
- `_iter8_test/boot_api.err` (snapshot du dernier subprocess, contient le WARN log signature)

### Note sur l'investigation cause racine perceptual_auto_on_quality

Verification empirique branche `loop/correction-2026-06` via REPL Python :

```
from cinesort.ui.api.cinesort_api import CineSortApi
api = CineSortApi()
s = api.settings.get_settings()
print(list(s.keys())[:5])  # ['root', 'roots', 'state_dir', 'tmdb_enabled', 'tmdb_timeout_s']
inner = s.get("settings") or {}
print(inner.get("perceptual_enabled"))  # None  (BUG : la clef n'existe pas)
print(s.get("perceptual_enabled"))      # True  (le vrai chemin d'acces)
```

Le code du fix `quality_audit_support.py:345` doit etre corrige (hors scope de ce gate ITER8 -- a faire iter9). Patron correct : `settings = api.settings.get_settings() or {}` (sans `.get("settings")`). Le test unitaire actuel `tests/test_perceptual_auto_on_quality_iter8.py` masque le bug en patchant `get_settings` avec une shape `{"settings": {...}}` au lieu de la shape reelle dict top-level.

---

## 4. GATE NON-REGRESSION GLOBAL ITER8 (2026-06-09) — [OPERATIONNEL]

### 4.a Mission

Conformement consigne mission gate global :
1. Pre-etape harness 6193e02b (`observe.py --fresh`)
2. observe.py multi-vues sur `test_library` + timestamp `2026-06-08_ITER8_NONREG_GLOBAL`
3. 9 vues POSTERS_OK (ou plus) attendues, negatifs `FakeUpscale` + `Nosferatu` ABSENTS
4. `lint-imports` VERT (3 contrats)
5. Loopback UI desktop intact

### 4.b Resultats mesures

**Lint-imports (3 contrats)** :

```
Analyzed 225 files, 566 dependencies.
Domain ne doit importer ni app, ni infra, ni ui KEPT
Infra ne doit importer ni app, ni ui KEPT
App ne doit pas importer ui KEPT
Contracts: 3 kept, 0 broken.
```

VERT confirme. Architecture verrouillee preservee (acquis a37852aa + 9c806129).

**observe.py multi-vues** (timestamp `2026-06-08_ITER8_NONREG_GLOBAL`) :

| Critere | Mesure | Verdict |
|---|---|---|
| Vues capturees | 17 vues dashboard | OK (>= 9 attendu) |
| Verdict par vue | 17/17 `POSTERS_ABSENTS` | NORMAL (TMDb desactive dans state isole) |
| `views_with_broken_posters` | `[]` (vide) | OK |
| Negatif `FakeUpscale` dans network/image_requests | 0 hit | OK |
| Negatif `Nosferatu` dans network/image_requests | 0 hit | OK |
| Desktop screenshot | `_desktop_capture/desktop_full.png` | OK (loopback UI desktop intact) |
| Freshness gate H1 EXE | `action=force_dev_mode` (EXE stale 2026-06-08 vs HEAD 2026-06-09) | OK |
| Freshness gate H2 etat | `skipped_reasons=["etat derive absent (premier run)"]` | OK (premier scan iter8 sur state neuf) |
| Freshness gate H4 webview | `note="absent (rien a purger)"` | OK |

**Console errors major** : 18 occurrences `[dash-api] _safeBearer: token absent ou vide` -- comportement attendu en state isole observe.py (le settings ex-nihilo cree n'a pas de token REST seed, cf limitation documentee dans `docs/internal/observe_protocol.md` section 8).

### 4.c Interpretation

Les 17 vues `POSTERS_ABSENTS` ne constituent **PAS une regression**. Causes structurelles non-pipeline :
1. State isole observe.py cree un `settings.json` ex-nihilo sans `tmdb_api_key` ni `rest_api_token` seed (limitation documentee observe_protocol.md section 8).
2. Sans TMDb actif, `poster_url=null` dans plan.jsonl -> aucun `<img src="...">` rendu -> 0 poster expected, 0 broken -> verdict `POSTERS_ABSENTS` legitime.
3. **Critere de non-regression respecte** : `views_with_broken_posters=[]` ET aucun negatif `FakeUpscale`/`Nosferatu` n'apparait dans les image_requests reseau. Pas de poster mal affecte, pas de poster pollue.

Conformement memoire `observe.py = non-regression posters seul` + `NON-PROUVABLE-LOCAL = NE marque PAS OK` :
- La verification ITER8 du gate global est **OPERATIONNELLE** pour la non-regression posters stricte (pas de broken, pas de negatif, pas de regression sur les vues capturees, lint VERT, loopback desktop intact).
- Le scenario "9 vues POSTERS_OK affichant des posters TMDb reels" est **NON-PROUVABLE-LOCAL** sans seed manuel TMDb dans le settings isole. Ce n'est pas une regression du codebase, c'est une limitation connue de l'isolation observe.py.

### 4.d Verdict gate non-regression global ITER8

| Critere | Etat |
|---|---|
| Pre-etape harness 6193e02b | OPERATIONNEL (gate fresh execute, 3H ecartees) |
| observe.py 17 vues capturees | OPERATIONNEL |
| Negatifs FakeUpscale + Nosferatu ABSENTS | OPERATIONNEL (0 hit chaque) |
| `views_with_broken_posters=[]` | OPERATIONNEL |
| lint-imports VERT (3 contrats) | OPERATIONNEL |
| Loopback UI desktop intact | OPERATIONNEL (screenshot desktop_full.png genere) |
| 9 vues POSTERS_OK affiches | NON-PROUVABLE-LOCAL (TMDb desactive dans state isole, NE marque PAS OK conformement memoire) |
| Acquis preserves intacts | OPERATIONNEL (lint VERT + aucun fichier code touche par ce gate) |

**Verdict global ITER8** : **OPERATIONNEL pour non-regression posters** (criteres mesurables tous verts). Acquis a37852aa + 242cf339 + 7df3af3e + 6193e02b + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + 06f74ad + #2 vert + 0f6e9046 PRESERVES INTACTS.

### 4.e Capture path et pieces a conviction

- Capture observe : `C:/Users/<utilisateur>/projects/CineSort/docs/internal/observe/2026-06-08_ITER8_NONREG_GLOBAL/`
- Summary JSON : `docs/internal/observe/2026-06-08_ITER8_NONREG_GLOBAL/summary.json`
- Desktop screenshot : `docs/internal/observe/2026-06-08_ITER8_NONREG_GLOBAL/_desktop_capture/desktop_full.png`
- Freshness gate : `docs/internal/observe/2026-06-08_ITER8_NONREG_GLOBAL/summary.json` -> `.freshness_gate`
- Lint-imports : `3 kept, 0 broken` (Domain ne doit importer ni app/infra/ui KEPT ; Infra ne doit importer ni app/ui KEPT ; App ne doit pas importer ui KEPT)

### 4.f Marqueurs

- `[OPERATIONNEL]` : Tous les criteres mesurables (lint, observe broken=[], negatifs absents, desktop loopback, freshness gate) sont VERT.
- `[NON-PROUVABLE-LOCAL]` : Le critere "9 vues POSTERS_OK affiches" exige TMDb actif dans le settings, hors scope state isole observe.py. **NE marque PAS OK** (conformement memoire), mais ne constitue PAS une regression du codebase.
- `[FIGE]` : Capture path, lint result, freshness gate state. Aucun fichier code modifie par cette etape de gate.
