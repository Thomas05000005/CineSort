# Audit Claude - 2026-05-26 - Couche all (niveau modere)

**Modele** : Opus 4.7 (audit-bot automatique)
**Modules audites** : 160 (cinesort/app, cinesort/domain, cinesort/infra, cinesort/ui/api)
**Categories couvertes** : 46/46 (focus prioritaire 1, 4, 9, 14, 15, 17, 24, 30, 31, 32, 35, 37, 41, 47)
**Methodologie** : 4 agents Explore en parallele (1 par couche) + self-critique pass + dedup vs issues ouvertes/fermees.

## Resume executif

Audit cible niveau "modere" : confidence >= 0.7, severite >= 2. Le precedent audit (2026-05-24) etant tres recent et ayant deja absorbe la majorite des findings critiques (#404-#415 + PRs #407-#409, #416), ce run s'est concentre sur :

- **Resistance DoS aux entrees externes** (fichiers video crafted, serveurs Jellyfin malveillants, statics surdimensionnes).
- **Defense in depth securite** (CORS warning, path traversal residuel, isolation_level explicite).
- **Self-critique aggressive** : 25 findings remontes par les agents, 19 ecartes (faux positifs / deja documentes / mitigations en place), 6 retenus comme issues + 3 traites en PR.

### Top 6 findings retenus
| Sev | Categorie | Module | Issue / PR |
|-----|-----------|--------|------------|
| 2 | 4 SEC + 32 RES | infra/rest_server.py:540 `_read_static_bytes` | #423 |
| 2 | 4 SEC + 47 ARCH | infra/rest_server.py:914 CORS=* + 0.0.0.0 | #424 / PR #429 |
| 2 | 4 SEC + 32 RES | infra/jellyfin_client.py (5 sites) size check after decode | #425 |
| 2 | 4 SEC + 32 RES | infra/probe/normalize.py:36 `_to_int` no magnitude bound | #426 / PR #430 |
| 2 | 4 SEC (CWE-22) | ui/api/export_support.py:145 `last_done_run_id` | #427 / PR #431 |
| 1 | 9 DB | infra/db/connection.py `isolation_level` not explicit | #428 |

## Par categorie

Findings remontes par les agents Explore puis filtres lors de la self-critique pass.

### 1 - BUGS_LATENT_PYTHON
3 findings remontes / 2 ecartes (mitigation deja en place : try/except ValueError englobant) / 1 retenu pour suivi (mel_analysis span fallback theorique).

### 4 - SECURITY
8 findings remontes / 3 ecartes (faux positifs : timeout=30 sur subprocess pas HTTP, _SIM_CACHE deja sous lock, integrations_facade clampe dans `_impl`) / 5 retenus = **#423 #424 #425 #426 #427**.

### 9 - MIGRATIONS_DB
2 findings remontes / 1 retenu (#428 isolation_level explicit). 1 ecarte (savepoint naming reuse — non-bloquant SQLite).

### 14 - ERROR_HANDLING
4 findings remontes / 4 ecartes :
- `film_support.py:129 r.json()` => `except ValueError` ligne 145 deja couvert (faux positif).
- `library_audit_support.py:232 r.json()` => `except Exception` ligne 251 deja couvert (faux positif).
- `_dispatch_plugin_hook` swallow => pattern delibere "ne pas bloquer pour un plugin" (faux positif).
- `RunState.log` silencieux sur write fail => acceptable best-effort.

### 17 - DONNEES_ORPHELINES
1 finding remonte / 1 ecarte (deja issue #381 `sous-scores mel/spectral_flatness`).

### 24 - OFFLINE_FIRST + 41 NETWORK_RESILIENCE
2 findings remontes / 1 retenu (#425 jellyfin size after decode) / 1 ecarte (circuit-breaker half-open : design delibere documente ligne 79 du code).

### 30 - PYWEBVIEW_JS_API
1 finding remonte sur CORS / retenu (#424 + PR #429).

### 32 - RESOURCE_EXHAUSTION
3 findings remontes / 3 retenus (#423 #425 #426).

### 47 - ARCHITECTURE_INVARIANTS
1 finding remonte (CORS wildcard sans warning admin) / retenu.

### Autres categories (10, 11, 15, 16, 19, 22-46)
0 finding nouveau remonte au-dessus du seuil. Couverture deja saturee par les audits precedents (cf #66-#103, #173-#291, #382-#415).

## Self-critique pass

25 findings bruts remontes par les agents Explore. Filtres appliques :

| Filtre | Supprimes |
|--------|-----------|
| FILTRE 1 (REALITE) | 0 |
| FILTRE 2 (IDIOME) | 1 (`float == 0.0` sur somme de carres, mathematiquement sur) |
| FILTRE 3 (CONFIDENCE < 0.7) | 2 (mutable defaults dataclass vague, fs_safety thread leak documente) |
| FILTRE 4 (DEDUP cross-categories) | 0 |
| FILTRE 5 (SEVERITE incoherente) | 0 |
| FILTRE 6 (ACTIONABILITE) | 1 (mel composite_score confidence flag, suggestion non-concrete) |
| FILTRE 7 (DEJA MITIGE) | 9 (circuit-breaker half-open, _SIM_CACHE FIFO, fs_safety thread, integrations_facade clamp, film_support json, library_audit_support json, _dispatch_plugin_hook silent, RunState.log silent, plan_support videos[0]) |
| FILTRE 8 (PROPORTIONNALITE) | 0 |

Soit **13 findings supprimes / 6 retenus**. Issues nouvelles : #423-#428 (6). PRs nouvelles : #429-#431 (3).

## Par module

### cinesort/infra/rest_server.py
- 4 SEC + 32 RES, **#423** : `_read_static_bytes` lit le fichier sans verifier la taille (DoS local possible).
- 4 SEC + 47 ARCH, **#424** + **PR #429** : aucun warning au boot si `CORS=*` et `host=0.0.0.0` simultanement. PR ajoute un `logger.warning` defense in depth.

### cinesort/infra/jellyfin_client.py
- 4 SEC + 32 RES, **#425** : pattern `if len(resp.content) > 10MB: raise` execute APRES decode dans 5 sites (lignes 177, 194, 248, 274, 337+). Solution : streaming `iter_content` + check Content-Length avant.

### cinesort/infra/probe/normalize.py
- 4 SEC + 32 RES, **#426** + **PR #430** : `_to_int` accepte des strings de digits arbitrairement longues. Fix livre : borne magnitude 10^15 + 16 digits + tests dedies dans `tests/test_probe_normalize.py`.

### cinesort/ui/api/export_support.py
- 4 SEC (CWE-22), **#427** + **PR #431** : `last_done_run_id` venant de la DB injecte directement dans le Path. PR livree : validation `api._is_valid_run_id` avant la construction.

### cinesort/infra/db/connection.py
- 9 DB, **#428** : `isolation_level` non defini explicitement (defaut implicite DEFERRED). Severite 1 : defense in depth pour Python 3.13.

## Statistiques

- Modules audites : 160
- Findings totaux remontes : 25 (Domain agent: 7, UI/API agent: 15, Infra agent: 12, App agent: 3 — note: certains se recoupaient)
- Findings retenus apres self-critique : 6 (24%)
- **Issues creees : 6** (#423, #424, #425, #426, #427, #428)
- **PRs ouvertes : 3** (#429 CORS warning, #430 _to_int magnitude, #431 export run_id validation)
- Findings dejà connus (dedup) : 9 (#327, #381, #409, #410, #411, #414, #415, #76, #173)

## Tendance vs audit precedent (2026-05-24)

Audit du 2026-05-24 etait transverse (lazy imports stdlib). Audit "all" precedent : 2026-05-23 (rapport `2026-05-23-all.md`).

Comparaison qualitative :
- Surface securite : encore reduite (5 findings vs ~10 le 2026-05-23, indique progression sur les patterns critiques).
- DoS resilience : 3 findings nouveaux (lecture statics, jellyfin size, probe int magnitude) tous defenses in depth, pas exploit reel.
- Architecture/pattern : aucun finding sur facade/repository/import-linter (couche stabilisee).

Prochains audits transverse possibles :
- Cycle complete des facades (verifier que toutes les 6 facades implementent `__getattr__` proxy).
- Audit des `unlink` / `shutil.rmtree` (chemin canonique, atomicite, journal).
- Property-based testing sur les conversions (probe normalize, naming, scene_parser).

## Annexes

### Faux positifs documentes (ne pas re-creer)

- `film_support.py:129` `r.json()` : ValueError CATCHE ligne 145 (`except (OSError, ImportError, KeyError, TypeError, ValueError)`). Re-tester avant tout futur audit.
- `library_audit_support.py:232` `r.json()` : Exception CATCHE ligne 251 (`except Exception as exc`).
- `perceptual_support.py:1218,1266 timeout=30` : c'est un `subprocess.run` (ffmpeg local), pas un HTTP timeout. Clamp_timeout non applicable.
- `integrations_facade.py timeout_s` : clampe dans les `_impl` correspondants (lignes 1091, 1107, 1280, 1296, 1382, 1480 de cinesort_api.py).
- `_SIM_CACHE` FIFO eviction : deja sous `_SIM_CACHE_LOCK` (lignes 101-104).
- `_circuit_breaker.py` half-open : design delibere documente ligne 79 (`Pas d'etat half_open ... pour rester simple`).
- `fs_safety.py` thread leak : documente ligne 11 (`Le thread leake (daemon, mourra avec le process)`).
- `plan_support.py:654` `videos[0]` : protege par `if not videos: continue` ligne 687.
- `apply_support.py:1615` `client.refresh_library` : protege par `except (IntegrationError, OSError, requests.RequestException)` ligne 1619.

[audit-bot:2026-05-26-INDEX]
