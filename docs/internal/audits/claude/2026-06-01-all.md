# Audit Claude - 2026-06-01 - Couche all

**Modele** : Opus 4.7 (thinking budget 32K)
**Niveau** : modere
**Modules audites** : ~15 sondes profondes parmi les ~165 du periphique
**Categories couvertes** : 46/46 (revue ciblee, pas exhaustive sur chaque module)

## Resume executif

Audit modere du 2026-06-01 sur la couche `all`. Decouvert 3 findings nouveaux dont 2 BUG fiabilite (severite 3) dans le module `cinesort/app/watcher.py` (jamais audite en profondeur depuis sa creation) et 1 dette technique (severite 1) dans `cinesort/app/radarr_sync.py`. Les 3 findings ont donne lieu a issues + PRs.

**Top 3 findings du jour** :

1. **rel(watcher)** [#487] : `_previous_snapshot = current` grave AVANT `_trigger_scan` early-return (roots inaccessibles) -> changement reel detecte mais perdu, plus jamais rejoue. Asymetrique avec le cas "scan running" deja gere. PR de fix : #495.
2. **fix(watcher)** [#488] : `except (KeyError, TypeError, ValueError)` autour de `start_plan` rate `AttributeError`/`OSError`/`RuntimeError` -> thread daemon `cinesort-watcher` meurt silencieusement. PR de fix : #495 (commune).
3. **refactor(radarr-sync)** [#490] : constante orpheline `_UPGRADE_ENCODE_FLAGS` jamais utilisee + commentaire d'intent flou. PR de fix : #497.

## Par categorie

(Categories non listees -> 0 nouveaux findings sur ce run, ou couvertes par audits precedents)

- **(5) CONCURRENCE** : 1 finding (watcher snapshot race avec early-return)
- **(11) DETTE TECHNIQUE** : 1 finding (radarr constante orpheline)
- **(14) ERROR HANDLING** : 1 finding (watcher except trop etroit)
- **(33) MICROCOPY / SOURCE OF TRUTH** : 1 finding (radarr, dedup avec #11)
- **(24) OFFLINE-FIRST** : 1 finding (watcher, dedup avec #5)
- **(47) ARCHITECTURE INVARIANTS** : 0 nouveau finding (CI import-linter passe vert)

## Par module audite

### cinesort/app/watcher.py (206 LOC)
- **severite 3** | categorie 5+24 | line 147 | `FolderWatcher.run` | snapshot grave avant `_trigger_scan` early-return | fix: `_trigger_scan -> bool` puis update conditionnel
- **severite 3** | categorie 14 | line 204 | `FolderWatcher._trigger_scan` | except (KeyError,TypeError,ValueError) trop etroit | fix: `except Exception` + `logger.exception`

### cinesort/app/radarr_sync.py (197 LOC)
- **severite 1** | categorie 11 | line 19 | `_UPGRADE_ENCODE_FLAGS` | constante orpheline + substring matching incoherent | fix: supprimer constante, documenter le substring

### Autres modules sondes (pas de nouveau finding non-couvert)
- `cinesort/app/_dir_utils.py` (24 LOC) : trivial, OK
- `cinesort/app/_fuzzy_utils.py` (101 LOC) : doc minore sur index post-normalisation, low-confidence
- `cinesort/app/_path_utils.py` (13 LOC) : trivial, OK
- `cinesort/app/disk_space_check.py` (138 LOC) : OK (note: `cfg.root` single root, deja tracee par #471 famille)
- `cinesort/app/email_report.py` (158 LOC) : OK, SSL strict + timeout + try/finally quit() propre
- `cinesort/app/job_runner.py` (480 LOC) : OK, race conditions TOCTOU bien gerees (try/regen run_id)
- `cinesort/app/notify_service.py` (180 LOC) : finding deja trace par #453 (evaluate_js hors main thread)
- `cinesort/app/move_journal.py` (194 LOC) : OK, journal write-ahead correct
- `cinesort/app/move_reconciliation.py` (183 LOC) : OK
- `cinesort/app/cleanup.py` (386 LOC) : OK, lazy import documente
- `cinesort/app/jellyfin_sync.py` (278 LOC) : OK, backoff exponentiel + multi-library OK
- `cinesort/app/omdb_cross_check.py` (182 LOC) : OK, branches bien guardees
- `cinesort/app/runtime_probe_check.py` (219 LOC) : OK
- `cinesort/app/plugin_hooks.py` (197 LOC) : finding deja trace par #408 (PYTHONPATH whitelist)
- `cinesort/app/apply_audit.py` (282 LOC) : OK, journal JSONL thread-safe propre
- `cinesort/app/watchlist.py` (192 LOC) : OK
- `cinesort/app/plan_support.py` (2190 LOC) : sonde partielle, pas de nouveau finding majeur. Categorise low-confidence apres self-critique : la complexite du module merite un audit dedie ulterieur.
- `cinesort/infra/single_instance.py` (185 LOC) : OK, lock OS-aware bien fait
- `cinesort/infra/state.py` (115 LOC) : OK, atomic_write_json correct
- `cinesort/infra/network_utils.py` (103 LOC) : OK, SSRF guard adequate
- `cinesort/infra/integration_errors.py` (40 LOC) : trivial, OK
- `cinesort/ui/api/_responses.py` (132 LOC) : OK
- `cinesort/ui/api/_validators.py` (118 LOC) : OK
- `cinesort/domain/duplicate_compare.py` (464 LOC) : OK, low-confidence note sur `_file_size` heuristique (commente)
- `cinesort/domain/scan_helpers.py` (370 LOC) : OK

## Self-critique pass

15 candidats findings identifies en lecture, 12 retires apres self-critique :

| Filtre | Findings retires |
|---|---|
| 1 - Realite (imagine) | 0 |
| 2 - Idiomatique justifie | 4 (try/except boundary-top patterns, magic numbers `range(10)`, etc.) |
| 3 - Confidence < 0.70 | 3 (low-confidence : disk_space_check single-root, plan_support complexity, _file_size heuristic) |
| 4 - Dedup cross-categories | 2 (watcher snap = REL + CONCURRENCE -> garde REL) |
| 5 - Severite recalibree | 1 (radarr substring : pousse de 2 a 1) |
| 6 - Actionable | 1 (idee vague "plan_support a decouper") |
| 7 - Etat actuel deja mitige | 1 (notify_service: deja #453) |
| 8 - Proportionnalite multi-PR | 0 |

**Findings emis** : 3 (watcher x2, radarr x1).
**Findings deja connus enrichi** : 2 commentaires implicites via mention dans body issues (#453, #408 non re-commentes pour eviter spam, >7j sans activite respectee).

## Statistiques

- **Modules audites** : 25 sondes profondes (parmi les ~165 du periphique).
- **Findings totaux** : 3 (high 0, med 2, low 1).
- **Issues creees** : 3 (#487, #488, #490).
- **PRs ouvertes** : 2 (#495 watcher [closes #487, #488], #497 radarr [closes #490]).
- **Findings deja connus (dedup)** : 2 (notify_service #453, plugin_hooks #408).
- **Self-critique drop** : 12 findings.

## Tendance vs audit precedent (2026-05-31 transverse)

Le rapport precedent etait focalise transverse (architecture). Aujourd'hui retour a "all" avec sondes profondes ciblees. Pas de regression d'invariants architecturaux detectee (import-linter CI vert sur main).

Les modules `cinesort/app/*` qui n'avaient pas eu d'audit profond depuis longtemps revelent ces 2 bugs fiabilite asymetriques dans `watcher.py` (lifecycle relativement recent). A reauditer apres merge des PRs pour confirmer la non-regression.
