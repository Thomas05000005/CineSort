# Audit Claude — 2026-07-03 — Couche « all »

**Modele** : Opus 4.8 (thinking max)
**Personas actives** : ARCHITECT + SECURITY + PERF + CORRECTNESS (rotation multi-persona)
**Couche** : `all` (domain + app + infra + ui/api)
**Niveau** : MODERE (fixes evidents uniquement, petites PRs, aucun refactor profond)
**Categories couvertes** : echantillonnage transverse sur 46 categories (accent 3 dette, 7/13/15 perceptuel, 40 SSRF, 44 AI-readiness, 47 architecture)
**Issues creees** : 0 nouvelle
**PRs creees** : 0 fix (uniquement ce rapport)

## Resume executif

Audit MODERE de la couche complete. **Zero nouveau finding confirme** au seuil de
confiance >= 70 % apres application des 8 filtres de self-critique. Ce n'est pas un
run « vide » : c'est la signature d'une **cible d'audit saturee**. Le backlog
`audit-bot:` (~200 issues ouvertes) + ~90 PRs couvrent deja l'immense majorite de
la surface actionnable, et les cinq grandes vagues de correction (M–R, juin 2026)
plus les cycles adversariaux R1–R8 ont assaini les couches chaudes.

Chaque candidat inspecte ce jour etait soit **deja documente** (issue/PR existante),
soit **sous le seuil de confiance**, soit **du code idiomatique** que les filtres
2 et 7 imposent d'ecarter. Conformement a la regle anti-spam de `audit-prompt.md`,
**aucune issue ni PR n'a ete ouverte pour du bruit** — creer un doublon coute plus
cher que ne rien creer.

Le convention du depot veut qu'un jour a zero finding ne genere **pas** de fichier
JSONL (cf. absence de `2026-05-12`, `2026-05-13`, `2026-05-20` dans `findings/`).
Ce rapport respecte cette convention : aucun `2026-07-03-all.jsonl` n'est produit.

## Perimetre inspecte ce jour

### Modules lus en integralite (21)

| Couche | Modules |
|--------|---------|
| domain | `conversions.py`, `codec_ranks.py`, `confidence_thresholds.py`, `tiers_helpers.py`, `edition_helpers.py`, `film_identity.py`, `custom_rules.py`, `duplicate_multi_signal.py`, `genre_rules.py`, `mkv_title_check.py`, `title_ambiguity.py` |
| domain/perceptual | `upscale_detection.py`, `grain_classifier.py` |
| app | `retention_cleanup.py`, `apply_audit.py`, `_fuzzy_utils.py`, `_local_candidate.py`, `watchlist.py` |
| infra | `network_utils.py` |

### Sweeps de patterns transverses (couche complete)

| Sweep | Cible | Resultat |
|-------|-------|----------|
| Mutable default args (`=[]`, `={}`, `=set()`) | domain+app+infra+ui/api | **0 hit** |
| Bare `except:` (hors `Exception`/`BaseException`) | domain+app+infra+ui/api | **0 hit** (tous les excepts sont types) |
| Identity smell `== None/True/False` | domain+app+infra+ui/api | **0 hit reel** (6 hits, tous en docstring/commentaire) |
| Chaine `.get(x)[...]` risque NoneType | domain+app | **0 hit reel** (1 faux positif : `.get(k, default)`) |

## Findings par filtre de dedup (FILTRE 7 — deja mitige)

Candidats identifies puis **ecartes** car deja couverts :

1. **`domain/conversions.py`** — bug `to_optional_bool` deja porte par **PR #666**.
2. **`domain/genre_rules.py:245`** — malus resolution `height > 0 and height < 1080`
   (exclut 4K natif du malus, comportement discutable) = **issue #682** existante.
3. **`infra/network_utils.py`** — deux gaps SSRF (link-local IPv6, DNS rebinding)
   deja traces par **issues #518 et #624**. La politique « LAN autorise, seul le
   metadata cloud est bloque » est un choix documente et intentionnel (usage perso
   Plex sur la meme machine), pas un defaut.
4. **`app/watchlist.py`** — fuzzy sans strictesse d'annee deja **issue #584**.
5. **`domain/tiers_helpers.py`** — seuils 70/66/55/40 calibres v1.5.7 (853 films),
   couleurs hex absentes (invariant memoire user respecte) — rien a signaler.

## Candidats sous le seuil de confiance (ecartes par FILTRE 3)

- **`domain/title_ambiguity.py:76-90`** — `detect_title_ambiguity` compte les
  candidats par titre normalise sans exiger des `tmdb_id` distincts. Un meme film
  present a la fois en source `tmdb` et `nfo_tmdb` pourrait declencher une fausse
  « ambiguite ». Confiance ~55 % : l'effet est benin (le boost contextuel promeut
  quand meme le bon candidat) et un dedup par `tmdb_id` en amont (cote appelant,
  non lu ce jour) le neutralise probablement. **Sous 70 % → non ouvert.** A
  reverifier si un audit futur lit le call-site.

## Statistiques

| Metrique | Valeur |
|----------|------:|
| Modules lus en integralite | 21 |
| Sweeps de patterns transverses | 4 |
| Findings bruts candidats | 6 |
| dont deja mitiges (FILTRE 7) | 5 |
| dont sous seuil confiance (FILTRE 3) | 1 |
| **Findings nouveaux confirmes (>= 70 %)** | **0** |
| Issues nouvelles | 0 |
| PRs de fix | 0 |
| Doublons stricts detectes | 5 |

## Self-critique pass (8 filtres — cf. audit-prompt.md §2.6)

- **Filtre 1 (realite)** : chaque candidat verifie par lecture du code reel + `grep`.
- **Filtre 2 (idiome)** : les excepts types, les `.get(k, default)`, les gardes
  `if not x: return` sont idiomatiques — ecartes.
- **Filtre 3 (confidence)** : le seul candidat neuf (title_ambiguity) est a ~55 %,
  sous le seuil 70 % → ecarte.
- **Filtre 4 (dedup cross-cat)** : aucun finding retenu, rien a fusionner.
- **Filtre 5 (severite)** : aucune escalade artificielle — les 5 deja-mitiges
  restent a leur severite d'origine dans leurs issues.
- **Filtre 6 (actionabilite)** : rien de nouveau d'actionnable ce jour.
- **Filtre 7 (etat actuel)** : 5 candidats supprimes car deja couverts par
  #666 / #682 / #518 / #624 / #584.
- **Filtre 8 (proportionnalite)** : aucune PR ouverte — ne pas produire de churn
  sur une base saturee est le choix proportionne.

## Comparaison avec l'audit precedent

Le dernier rapport est `2026-05-24-transverse.md` (couche transverse, 0 issue
nouvelle, 2 PRs polish #391/#392). Tendance confirmee : la surface actionnable
continue de se reduire. Le 24 mai concluait deja « couche transverse en etat
sain » ; l'audit `all` du 3 juillet etend ce constat a domain/app/infra/ui.

**Conclusion** : la valeur d'un audit sur cible saturee est de **confirmer
l'absence de regression** et de **ne pas polluer le backlog**. Les invariants
architecturaux (3 contrats import-linter), les gardes SSRF, les seuils de scoring
calibres et les couches perceptuelles restent conformes. Prochain gain marginal :
lire les call-sites de `title_ambiguity` pour trancher le candidat a 55 %, et
poursuivre l'echantillonnage des modules `ui/api/*_support.py` non encore relus.

## Annexe — fichiers de ce run

- Ce rapport : `docs/internal/audits/claude/2026-07-03-all.md`
- JSONL findings : **aucun** (0 finding — convention depot, cf. 2026-05-12/13/20)
