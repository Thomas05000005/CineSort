# Audit Claude - 2026-06-13 - Couche all

**Modele** : Opus 4.7 (thinking max)
**Modules audites** : 152 (couche all)
**Categories couvertes** : 46/46
**Niveau** : modere
**Ouverture PRs** : true

## Resume executif

Top findings critiques (severite >= 2) :

1. **`bug(quality-score,ui-library)` (#586, sev 3)** : seuils `confidence_label` desyncs entre domain (75/50 FR) et UI (85/60 EN). Le commentaire UI ment : "Reproduit la grille". Confidence 0.95.
2. **`perf(title-helpers)` (PR #587, sev 1)** : `@lru_cache` manquant sur `_norm_for_tokens` hot loop `core.py:1110-1111`. Confidence 0.87.
3. **`refactor(ui-api)` (sev 2)** : `test_reset()` directement sur `CineSortApi` au lieu de `RuntimeFacade` (lie a #483). Confidence 0.78, non-issue dedie.

Aucun BLOCKER (severity 4) detecte sur ce run.

## Par categorie (sample)

- **(1) BUGS LATENTS** : 0 nouveau (3 candidates filtres comme defensifs faux positifs : `int(...)` deja garde en amont).
- **(3) PERFORMANCE** : 1 nouveau (`_norm_for_tokens` lru_cache, PR #587).
- **(11) CODE MORT** : 0 nouveau confirme (les findings agents non verifies via tests/ ont ete filtres).
- **(14) ERROR HANDLING** : 1 (radarr_client raise sans from, sev 1).
- **(17) ORPHAN DATA** : 0 nouveau (la plupart deja documentes #535, #576).
- **(19) COHERENCE BACKEND<->UI** : 1 nouveau critique (#586 desync confidence_label).
- **(47) ARCH INVARIANTS** : 1 mineur (test_reset hors facade, lie a #483).

## Par module

### cinesort/domain/title_helpers.py
- **Sev 1 / Categorie 3** / `_norm_for_tokens:226`
- Description : fonction pure appelee dans hot loops sans cache.
- Suggestion fix : `@lru_cache(maxsize=512)` (applique dans PR #587).

### cinesort/ui/api/library_support.py
- **Sev 3 / Categorie 19** / `_confidence_label_from_value:879`
- Description : grille (85/60, EN) divergent du domain (75/50, FR).
- Suggestion fix : factoriser via `domain.confidence_label()` public (issue #586).

### cinesort/infra/log_scrubber.py
- **Sev 1 / Categorie 12** / `reset_for_tests:166`
- Description : `_ROTATING_INSTALLED` reference avant declaration ligne 179.
- Suggestion fix : promouvoir la decl en tete de module.
- Note : self-critique baisse a 0.65 (pas un bug runtime). Pas d'issue cree.

### cinesort/infra/radarr_client.py
- **Sev 1 / Categorie 14** / `_post,_get:122,140`
- Description : `raise ValueError('Response too large')` sans `from exc` (asymetrie vs omdb_client:308).
- Suggestion fix : harmoniser le chaining + typer en `RadarrError`.
- Note : confidence 0.85 mais effort minime, pas d'issue dedie (laissera trace dans JSONL).

### cinesort/ui/api/cinesort_api.py
- **Sev 2 / Categorie 47** / `test_reset:1716`
- Description : methode publique directe (regression facade).
- Lie a issue #483 (5 methodes residuelles).
- Suggestion fix : deplacer dans `RuntimeFacade` + adapter snapshot test.

## Statistiques

- Modules audites : 152
- Findings totaux post-filtre : 5 (sev 3 : 1, sev 2 : 1, sev 1 : 3)
- Findings supprimes par self-critique : 9
  - 3 faux positifs (`int()` sans garde mais entree deja int)
  - 1 doublon avec PR ouverte (#533 sur scene_parser)
  - 1 doublon avec PR #546 (omdb cache size limit)
  - 1 non-bug runtime (log_scrubber globals order)
  - 1 marginal (CSV vide whitespace - cas <0.85 confidence)
  - 2 ambigus low-value
- Issues creees : 1 (#586)
- PRs ouvertes : 1 (#587 perf title_helpers)
- Findings deja connus (dedup) : ~12 issues ouvertes touchent les memes modules

## Personas utilises

- DB : 1 finding (#586 confidence_label)
- PERFORMANCE : 1 finding (PR #587)
- ARCHITECT : 1 finding (test_reset)
- RELIABILITY : 1 finding (radarr raise from)
- (SECURITY, UX, COMPLIANCE) : 0 nouveau finding ce run (les patterns critiques sont deja sous issue : #564 path traversal export, #563 SMTP downgrade, #555 QR SVG XSS).

## Tendance vs audit precedent (2026-06-12)

- Audit 2026-06-12 : 3 issues ouvertes (#583, #584, #585) + 2 PRs (#581, #582).
- Audit 2026-06-13 : 1 issue (#586), 1 PR (#587).
- Tendance : la couche all converge. Beaucoup de findings residuels deja captures. Le nouveau finding majeur (#586) emerge d'une analyse cross-couche (domain vs UI).

## Methodologie

- Multi-agent : 4 sub-agents Explore en parallele (un par couche : app, domain, infra, ui/api).
- Self-critique pass : filtres applique (confiance >= 0.85, dedup avec issues #423-#585 + PRs ouvertes, verification realite via Read).
- Verification anti-faux-positifs : Read direct des lignes citees + grep des callers.
- Pas d'execution de tests (env GitHub Actions limite ruff/pytest local) : CI validera PR #587.

## Reference

- Issue : #586 (confidence_label desync)
- PR : #587 (perf lru_cache _norm_for_tokens)
- JSONL : `docs/internal/audits/findings/2026-06-13-all.jsonl`
