# Audit Claude - 2026-05-23 - Couche all

**Modele** : Claude Opus 4.7 (thinking budget eleve)
**Niveau** : modere (fixes evidents/safe, PRs petites)
**Ouverture PRs** : true
**Modules cibles** : 154 (cinesort/{app,domain,infra,ui/api})
**Personas actives** : SECURITY / RELIABILITY / ARCHITECT / DB / PERFORMANCE

## Resume executif

L'audit du 2026-05-23 confirme la maturite avancee du codebase : sur 50+ findings bruts
remontes par 4 agents Explore (1 par couche), 3 seulement passent les filtres de
self-critique (etape 2.6). Aucun finding critique. Les patterns "classiques" (SSRF,
path traversal, secrets en log, race conditions, busy-wait, lazy imports) sont tous
couverts par les 30+ audits precedents (cf issues #64-291 majoritairement fermees).

Les 3 retenus sont des raffinements de severite 1 (STYLE), tous safe-fix :

### Top findings actionnables

| # | Module:line | Severity | Confidence | Action |
|---|-------------|----------|------------|--------|
| 1 | `cinesort/ui/api/cinesort_api.py:1286` | 1 | 0.95 | PR #385 (clamp_timeout symetrie) |
| 2 | `cinesort/ui/api/run_flow_support.py:694` | 1 | 0.85 | PR #386 (clamp last_log_index) |
| 3 | `cinesort/domain/perceptual/composite_score_v2.py:586` | 1 | 0.85 | Issue #387 (rename video_data) |

## Par categorie (couverture 46/46)

| Categorie | Findings retenus | Notes |
|-----------|------------------|-------|
| 1. Bugs latents Python | 0 | dataclass field(default_factory) idiomatique, pas mutable default |
| 2. Bugs domaine | 0 | sentinel patterns deja audites (#18, #325) |
| 3. Performance | 0 | re.compile audite #323, O(n^2) audite #29, #47, #74 |
| 4. Securite | 0 | SSRF/SQL/CWE-22 deja patches #64-73, #240 |
| 5. Concurrence | 0 | notify_service.start_drain_timer single-thread par design (filtre 7) |
| 6. UI/Frontend | 0 | XSS deja patches #67, #236 |
| 7. A11y | 0 | hors scope backend |
| 8. I18n | 0 | #239 deja ouvert |
| 9. Migrations DB | 0 | transactions OK + rollback #80 fixe |
| 10. Dette technique | 2 | clamp_timeout drift (#385), naming video_data (#387) |
| 11. Code mort | 0 | regle CRITIQUE verification tests respectee |
| 12. Patterns Python | 0 | code idiomatique |
| 13. Typing mypy | 0 | non audite cette passe |
| 14. Error handling | 1 | last_log_index clamp (#386) |
| 15. Logging | 0 | log_scrubber complet apres #291 |
| 16. Tests | 0 | bonne couverture |
| 17. Donnees orphelines | 0 | #381 deja ouvert pour sub-scores mel/spectral |
| 18-46 | 0 | couvertes par audits anterieurs |

## Par module

### cinesort/ui/api/cinesort_api.py:1286

- `_get_plex_libraries_impl` utilise `max(1, min(30, timeout_s))` au lieu de
  `clamp_timeout(timeout_s)` (helper Audit C7 P1, range 1-60s, fallback robuste).
- 4 autres call sites Plex/Radarr/OMDb/Jellyfin sont symetriques avec le helper.
- **Severite** : STYLE (1) - drift symetrie + plus permissif que les autres
- **Action** : PR #385.

### cinesort/ui/api/run_flow_support.py:694

- `get_status(api, run_id, last_log_index=0)` expose via `RunFacade.get_status`,
  accessible REST + js_api. Sans clamp, `last_log_index=-5000` renvoie tous les
  logs au lieu d'une page (deque cape a 5000).
- Ligne 738 : `last_log_index + len(logs)` renvoie un `next_log_index` incoherent.
- Pas de DoS critique (deque borneee) mais contournement pagination + UX bug.
- **Severite** : STYLE (1)
- **CWE** : CWE-20 Improper Input Validation
- **Action** : PR #386.

### cinesort/domain/perceptual/composite_score_v2.py:586

- Regle 6 fake-lossless : `video_data = (normalized_probe or {}).get("audio") or []`
  - le nom de variable contredit le contenu.
- Aucun bug runtime, fonction marche correctement.
- Lisibilite / AI-readiness uniquement (categorie 44).
- **Severite** : STYLE (1)
- **Action** : Issue #387.

## Self-critique (filtres appliques)

L'audit initial des 4 agents Explore a remonte ~14 findings bruts. Apres self-critique :

- **6 supprimes par filtre 7 MITIGATION** :
  - `email_report.py SMTP timeout` -> deja en cours dans PR #380 (ouverte)
  - `_http_utils.py POST in DEFAULT_RETRY_METHODS` -> AGENT HALLUCINATION : ligne 15 ne contient PAS POST (seulement GET, HEAD, OPTIONS, PUT, DELETE). #286 deja ferme.
  - `_http_utils.py:47 raise_on_status=False` -> design choix documente, callers utilisent raise_for_status. Mitigation en place.
  - `notify_service.start_drain_timer race` -> commentaire explicite "A appeler depuis app.py apres set_window() donc depuis le main thread" - mitigation par design single-thread.
  - `rest_server.py:47` -> agent confondait avec _http_utils.py
  - `jellyfin_sync.py:64 mutable field` -> idiomatique dataclass field(default_factory=list), filtre 2

- **3 supprimes par filtre 4 DEDUP** :
  - `perceptual_support cache hit orphan` -> #237 deja couvre
  - `jellyfin_sync.restore_watched docstring` -> #289 deja couvre la cause root
  - `apply_support.py:1286 Plex timeout` -> meme racine que #385 (clamp_timeout)

- **2 supprimes par filtre 3 CONFIDENCE < 0.70** :
  - `library_support.py:431 page validation` -> int(page or 1) + max(1, ...) suffit
  - `jellyfin_sync.py:214 documentation invariant` -> 0.72 mais valeur ajoutee marginale

Restent **3 findings actionnables** -> 2 PRs (#385, #386) + 1 issue (#387).

## Verification "code mort"

Aucune suppression proposee cette passe (categorie 11 vide). La regle CRITIQUE
du audit-prompt.md sur la verification `grep tests/` + code prod + imports
dynamiques avant suppression est respectee.

## Tendance vs audits precedents

| Date | Modules audites | Findings bruts | Retenus | PRs | Issues |
|------|------------------|----------------|---------|-----|--------|
| 2026-05-12 | 161 | ~80 | 30+ | 8 | 22+ |
| 2026-05-17 | transverse | 9 | 5 | 1 | 5 |
| 2026-05-21 | 161 | 19 | 6 | 3 | 3 |
| 2026-05-22 | 161 | 80 | 3 | 1 | 2 |
| **2026-05-23** | **154** | **~14** | **3** | **2** | **1** |

Le ratio findings/modules continue de baisser (de 0.5 a 0.09), reflet d'une codebase
qui converge vers une dette technique residuelle stable. Les fixes restants sont des
raffinements de severite 1 (STYLE/dette legere) plutot que des bugs runtime.

## Outils 2026 mentionnes dans les fixes

- `ruff format/check` (CI obligatoire pre-push) - non disponible localement cette passe
- audit-bot fingerprints (sha256 8-char) pour dedup robuste
- helpers `clamp_timeout` / `clamp_non_negative_int` deja en place dans `_validators.py`

## Statistiques

- Modules audites : 154
- Findings bruts : ~14
- Findings retenus apres self-critique : 3
- Issues creees : 1 (#387)
- PRs ouvertes : 2 (#385, #386)
- Findings deja connus (dedup) : 11
- Issues anterieures referencees : 0 (toutes deja closes/independantes)

## Process

- 4 agents Explore lances en parallele (1 par couche : app / domain / infra / ui-api)
- Self-critique pass tres severe vs 30+ audits precedents
- Verification systematique des findings contre le code reel avant creation issue/PR
- Aucune action sans verification dedup vs `gh issue list --state all`

🤖 Audit automatique - 2026-05-23
