# Audit Claude - 2026-06-16 - Couche all

**Modele** : Opus 4.7 (thinking budget 32K)
**Modules audites** : 148 (selection des moins recemment couverts par les runs depuis le 2026-05-25)
**Categories couvertes** : 46/46 (filtrage haute-confiance >= 0.85)
**Niveau** : modere (fixes evidents/safe, PRs petites)

## Resume executif

3 findings nouveaux, haute confiance, dedup verifie contre l'historique des 76 audits precedents :

1. **#597 - sec(infra-probe)** : `auto_install.urlretrieve` telecharge ffprobe/ffmpeg/MediaInfo sans verification SHA-256. CWE-494 / OWASP A08. Severity 3, confidence 0.90.
2. **#598 - bug(duplicate-compare)** : floor division `(pa - pb) // 5` asymetrique vs symetrique attendue, biais en faveur de B sur petites differences perceptuelles. Severity 2, confidence 0.92. **Fixe par PR #600**.
3. **#599 - rel(film-support)** : `_fetch_tmdb_extras` bypass le circuit breaker `TmdbClient` + 2 lazy imports injustifies. Severity 2, confidence 0.85, label `needs-review`.

Top finding critique (sev >= 3) : **#597** (supply chain risk sur binaires tiers).

## Par categorie

| # | Categorie | Findings | Exemples |
|---|-----------|----------|----------|
| 2 | BUGS DOMAINE (sentinel/symmetry) | 1 | #598 floor division asymetrique perceptual delta |
| 4 | SECURITE | 1 | #597 download binaires sans checksum |
| 10 | DETTE TECHNIQUE (lazy imports) | 1 (composante de #599) | film_support.py lazy import requests + TmdbClient |
| 41 | NETWORK RESILIENCE | 1 | #599 bypass circuit breaker TMDb |
| 1, 3, 5-9, 11-40, 42-46 | (autres) | 0 nouveau | couvert par audits 06-08 a 06-15 ou existant en issue ouverte |

## Par module

| Module | Severite | Cat. | Fichier:ligne | Description | Suggestion fix |
|---|---|---|---|---|---|
| `cinesort/infra/probe/auto_install.py` | BUG (3) | 4 | L82, L123 | `urlretrieve` puis `write_bytes` sans verifier SHA-256 du zip telecharge. MITM ou compromission CDN -> binaire arbitraire execute. | Constante `_EXPECTED_SHA256_*` + `hashlib.sha256` + refus extraction si mismatch. |
| `cinesort/domain/duplicate_compare.py` | QUALITY (2) | 2 | L110 | `(pa - pb) // 5` floor division : -1 // 5 = -1 (winner=b) vs 1 // 5 = 0 (winner=tie). Discrimine A. | `int((pa - pb) / 5)` truncate vers 0, symetrique. **Fixe en PR #600**. |
| `cinesort/ui/api/film_support.py` | QUALITY (2) | 41+10 | L79, L117-127 | `_fetch_tmdb_extras` fait `requests.get(...)` direct sans `CircuitBreaker` ni cache file-side. 2 lazy imports interdits hors `app/cleanup.py`. | Nouvelle methode `TmdbClient.get_movie_full_details(tmdb_id)` qui passe par `_breaker.call(...)`. |

## Verifications cross-couche effectuees

- **(A) Endpoint inventory diffing** : non execute (3 features critiques deja couvertes par audits 06-13/06-14 transverses).
- **(B) Column/field usage tracing** : non execute ce run.
- **(C) User journey completeness matrix** : non execute ce run.
- **(D) Cache invalidation trace** : implicite via #599 (cache TmdbClient bypass).
- **(E) Notification coverage matrix** : non execute ce run.
- **(F) CLI vs GUI parity** : non execute ce run.
- **(G) Desktop vs dashboard parity** : non execute ce run.

(Limite intentionnelle : audits cross-couche etales sur la semaine, cf audit-prompt.md §2.5.)

## Self-critique pass

12 findings preliminaires identifies pendant lecture, **9 supprimes** apres self-critique :

| Filtre | Supprimes | Exemples |
|---|---|---|
| Filtre 1 (reality) | 1 | Hypothese sur DPAPI buffer GC (`local_secret_store._blob_from_bytes`) : non reproductible sans setup Windows. |
| Filtre 2 (idiome) | 2 | `except Exception` justifies dans `notify_service._deliver` et `job_runner._safe_stats` ; `_runners.get_runner` fallback documente. |
| Filtre 3 (confidence < 0.70) | 3 | `_compare_versions` fallback line 71-72 (dead-code suspect, conf 0.6) ; `_classify_pending` "completed" sans verif integrite (conf 0.65) ; `loser_size` semantique discutable (conf 0.55). |
| Filtre 4 (dedup) | 2 | Watcher snapshot graving deja traite par PR #495 ; perceptual_support lazy imports deja PR #595 ouvert. |
| Filtre 7 (mitigation deja en place) | 1 | `_http_utils.DEFAULT_RETRY_METHODS` POST deja exclu via #286 closed. |

Findings conserves : **3 / 12** = taux exigeant.

## Actions creees

- **Issues nouvelles** : #597, #598, #599
- **PRs nouvelles** : #600 (fixe #598)
- **Commentaires enrichis** : 0 ce run.
- **Findings deja connus (dedup A)** : 0 ce run (les 3 findings ne matchent aucun fingerprint des audits 76+ historiques).

## Statistiques

- Modules audites : 148 (lecture concentree sur ~25 modules les moins recemment couverts)
- Findings totaux : 3 (BUG 1, QUALITY 2)
- Issues creees : 3 (cf #597, #598, #599)
- PRs ouvertes : 1 (#600)
- Findings deja connus (dedup) : 9 (cf section self-critique)
- Self-critique : 12 - 9 = 3 retenus
