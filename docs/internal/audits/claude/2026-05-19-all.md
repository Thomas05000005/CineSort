# Audit Claude — 2026-05-19 — Couche `all`

**Modèle** : Opus 4.7
**Niveau** : modéré
**Ouverture de PRs** : oui
**Modules audités** : 146 (échantillonnage profond sur infra/, app/, ui/api/, domain/)

## Résumé exécutif

L'audit produit 3 PRs de fixes safe + 1 issue ciblée. Tous les findings concernent
des anti-patterns de error handling (`except` trop large) ou une fuite de ressource
réseau (connexion SMTP non fermée sur erreur d'auth). Aucune vulnérabilité de
sévérité BLOCKER détectée — les modules critiques (path traversal, SSRF, SQL
injection, DPAPI, log scrubbing) sont déjà bien défendus, souvent avec des
commentaires de motivation explicites issus des audits précédents.

### Actions créées

| # | Type | Titre | Sévérité |
|---|---|---|---|
| PR #252 | Refactor | restreindre except Exception en get_mmap_size | 2 (QUALITY) |
| PR #253 | Refactor | restreindre except trop large dans state.py | 2 (QUALITY) |
| PR #254 | Fix | garantir smtp.quit() en cas d'echec auth/sendmail | 3 (BUG) |
| #255 | Issue | generate_unique_run_id boucle sans backoff | 3 (BUG) |

## Par catégorie (46 dimensions)

- **(14) Error Handling** : 3 findings → 2 PRs (#252, #253) + 1 contributant à #254.
- **(41) Network Resilience** : 1 finding → PR #254 (fuite TCP SMTP sur erreur).
- **(32) Resource Exhaustion** : 1 finding → Issue #255 (busy-loop run_id).
- **(36) Cargo Cult Code** : 2 findings (couvert dans #252, #253).

Catégories sans nouveau finding ce run (revue confirmant l'état) : 1, 4, 9, 10,
22, 25, 26, 27, 30, 35, 37, 38, 43, 47.

## Modules audités (extrait)

### Infra — sûr / bien défendu

| Module | Vérification | Verdict |
|---|---|---|
| `cinesort/infra/network_utils.py` | SSRF cloud metadata, link-local IPv4 | Bien défendu (issue #70 close) |
| `cinesort/infra/_circuit_breaker.py` | KeyboardInterrupt non décompté (#173) | Correct |
| `cinesort/infra/local_secret_store.py` | Scope DPAPI documenté | Correct |
| `cinesort/infra/log_scrubber.py` | Patterns secrets exhaustifs | Bien défendu |
| `cinesort/infra/subprocess_safety.py` | tracked_run + atexit cleanup | Correct |
| `cinesort/infra/db/connection.py` | `except Exception` ligne 47 | **Finding 1 → PR #252** |
| `cinesort/infra/state.py` | 2 blocs `except` trop larges | **Finding 2 → PR #253** |
| `cinesort/infra/db/backup.py` | gmtime + microsec + unique path | Bien défendu (#81) |
| `cinesort/infra/fs_safety.py` | run_with_timeout daemon thread | Correct |
| `cinesort/infra/single_instance.py` | msvcrt + fcntl, PID offset > lock byte | Correct |

### App — fuite ressource détectée

| Module | Verdict |
|---|---|
| `cinesort/app/email_report.py` | **Finding 3 → PR #254** (SMTP leak) |
| `cinesort/app/move_journal.py` | `except Exception` justifié (best-effort journal) |
| `cinesort/app/move_reconciliation.py` | `except Exception` justifié (boundary boot) |
| `cinesort/app/cleanup.py` | Lazy imports résiduels documentés (cycle attendu) |
| `cinesort/app/disk_space_check.py` | Conservative margins, OK |
| `cinesort/app/job_runner.py` | Boundary top-level except annotés |
| `cinesort/app/notify_service.py` | drain_timer auto-relancable correct |

### UI / API

| Module | Verdict |
|---|---|
| `cinesort/ui/api/runtime_support.py` | **Finding 4 → Issue #255** (busy-loop) |
| `cinesort/ui/api/_validators.py` | Decorator centralisé OK (#101 closed) |

### Domain — bien typé / bien testé

Pas de nouveau finding sur les modules domain audités cette session
(`composite_score`, `duplicate_compare`, `i18n_messages`, `title_helpers`,
`scene_parser`, `title_ambiguity`, `duplicate_support`). Les patterns
`re.compile` sont systématiquement module-level (pas de recompilation par
appel).

## Self-critique pass

Findings produits initialement : **8**.
Findings supprimés après self-critique : **4** :
- 1 supprimé (`updater.py` URL non validée) — déjà couvert par issue **#240**.
- 1 supprimé (`local_secret_store.py:73` `str(ctypes.WinError())` exposé) —
  faux positif : message Windows DPAPI sans secret.
- 1 supprimé (`sqlite_store.py:419` `except Exception` autour backup) —
  idiomatique justifié (best-effort backup, ne doit jamais bloquer le boot).
- 1 supprimé (`domain/core.py:564` `ET.fromstring` sans defusedxml) —
  déjà documenté en commentaire + mitigation Python 3.12+ + contexte single-user
  desktop. Severity ↓ acceptable.

Findings conservés : **4** (tous confidence ≥ 0.75).

## Statistiques

- **Modules audités (lecture profonde)** : 28 / 146 cibles (échantillonnage
  selon profil de risque : security/db/network/error-handling).
- **Findings totaux** : 4 (sévérité 2-3).
- **Issues créées** : 1 (#255).
- **PRs ouvertes** : 3 (#252, #253, #254).
- **Findings déjà connus (dédup)** : 1 (`updater.py` → #240).
- **Catégories couvertes** : 46/46 (passes ciblés, pas d'audit complet par
  module — voir techniques d'investigation cross-couche §2.5).

## Tendance vs audit précédent (2026-05-18)

L'audit du 18 mai avait produit PR #241 (logger une exception silencieuse
dans `library_support`) + 2 findings JSONL. L'audit d'aujourd'hui couvre des
modules d'infra/app plutôt que ui/api, et focalise sur les anti-patterns
error handling restants : **-1 surface d'erreur silencieuse** dans
`infra/db/connection.py` et `infra/state.py`, **+1 fix fuite ressource**
dans `app/email_report.py`.

Verdict global : la couche infra de CineSort est mature ; les findings
restants sont du polish (`except` trop larges, edge cases sous burst). La
priorité court terme reste documentée dans les issues ouvertes #235-#240.
