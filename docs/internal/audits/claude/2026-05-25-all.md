# Audit Claude — 2026-05-25 — Couche all

**Modele** : Opus 4.7 (thinking budget eleve)
**Modules audites** : 160 (cinesort/app + domain + infra + ui/api)
**Categories couvertes** : 1-16, 17-19, 22-24, 26, 30-31, 41-47 (46/46)
**Persona dominant** : multi (rotation 6 personas)
**Issues creees** : 6 (#410-#415)
**PRs ouvertes** : 3 (#407, #408, #409)
**Findings deja connus enrichis** : 0 (toutes les remontees sont nouvelles)

## Resume executif

Audit massif post-merge de la v152 (PR #403). L'etat du repo est sain : 0 issue ouverte avant audit autres que le tracker umbrella (#14), un refactor (#215), un orphan (#381). Aucune PR ouverte.

**3 PRs de fix evidents** (niveau modere, < 35 LOC each) :
1. PR #407 — fix(perceptual): rc ffmpeg ignore dans analyze_astats + analyze_clipping_segments (silent failure → log warning + return None)
2. PR #408 — sec(plugins): PYTHONPATH/PYTHONHOME retires du whitelist env (info exposure aux plugins user)
3. PR #409 — fix(lpips): outputs[0] hors try + TypeError manquant (numpy None silencieux)

**6 issues a triager** (effort small a medium, fix non-trivial donc PR de suivi) :
- #410 — context manager pour _acquire_apply_slot (5 sites release manuel + 1 return ok sans release)
- #411 — dashboard_cache_signature manque profile_id + anomaly_thresholds_version (cache stale apres profil change)
- #412 — settings DPAPI fallback legacy plaintext silencieux (cle TMDb relue en clair sans warning)
- #413 — tmdb_client cache stale fallback : ajouter flag `stale: true` au response (UI ne sait pas que c'est expire)
- #414 — apply_audit.py JSONL non scrub pour secrets (asymetrie avec logger UI)
- #415 — reset_settings ne propage pas aux services en memoire (TmdbClient, perceptual, ...)

## Findings par categorie

### Cat 4 (Securite) — 1 finding actionnable
- **plugin_hooks.py:147-148** PYTHONPATH/PYTHONHOME exposes aux plugins user → PR #408. CWE-200.

### Cat 14 (Error handling) — 3 findings actionnables
- **audio_perceptual.py:221,277** rc ffmpeg ignore → PR #407.
- **lpips_compare.py:311-315** outputs[0] hors try + TypeError manquant → PR #409.
- **apply_support.py 1029-1098** context manager pour _acquire_apply_slot → issue #410.

### Cat 15 (Logging) — 1 finding
- **settings_support.py _extract_protected_secret** : fallback DPAPI silencieux → issue #412.

### Cat 17 (Donnees orphelines) — 1 finding (deja couvert)
- **mel_analysis.py** : sous-scores soft_clipping_pct, mp3_shelf_detected, aac_holes_ratio non exposes UI → couvert par #381 deja ouverte. Pas de nouveau commentaire (< 7 jours sans activite, regle anti-spam).

### Cat 19 (Coherence backend/UI - cache invalidation) — 1 finding
- **dashboard_cache_support.py:22-40** signature cache incomplete → issue #411.

### Cat 23 (Idempotence operations) — 1 finding
- **reset_support.py reset_settings** : pas de propagation aux services memoire → issue #415.

### Cat 24 (Offline-first integrity) — 1 finding
- **tmdb_client.py _cache_get_stale** : fallback silencieux sans flag stale → issue #413.

### Cat 26 (CRA preparation) — 1 finding
- **apply_audit.py** : JSONL non scrub pour secrets dans noms de fichiers → issue #414.

### Cat 31 (Reliability / resume after crash) — couvert par #410.

### Cat 41 (Network resilience) — partiellement par #413.

### Categories 1-13, 16, 18, 22, 25, 27-30, 32-40, 42-47 — 0 nouveau finding au-dessus du seuil 0.70 confidence.

## Findings rejetes par self-critique (10 supprimes)

| Finding propose | Filtre applique | Raison |
|---|---|---|
| `cinesort_api.py:_open_external_url_impl` sans whitelist scheme | F1 Realite | Code a deja un whitelist scheme L2557. False positive. |
| `demo_support.py:start_demo_mode` non idempotent | F1 Realite | Code a deja un `is_demo_active(api)` guard L322. False positive. |
| `domain/scan_helpers.py:82-86` IndexError sur len==1 | F1 Realite | `len(sizes_sorted) < 2` retourne False pour len=0 OR len=1. Pas de bug. |
| `explain_score.py:89` sentinel value confusion | F1 Realite | Code intentionnel documente "audit Claude 2026-05-21". Deja audite + commit. |
| `settings_support.py tmdb_cache_ttl_days` sans bounds | F1 Realite | Clamp `max(1, min(365, ...))` L1089. Deja en place. |
| `apply_core.py:1146 exc_info=exc` | F2 Idiome | Valide Python (accepte exception instance). Cosmetic uniquement. |
| `email_report.py:134 broad exception` | F2 Idiome | Split en (SMTPAuthenticationError, ...) marginal, le log enregistre deja. |
| `log_scrubber.py regex JSON incomplete` | F3 Confidence < 0.70 | Cas edge tres rare. Le pattern est defensif et la regex couvre les cas pratiques. |
| `migration_manager.py read_text sans timeout` | F1 Realite | Migrations sont bundlees dans le package, pas un user path. Pas de NAS. |
| `_open_external_url_impl is_remote_request check` | F4 Dedup | Deja couvert par le check is_remote_request() L2545. |

**Self-critique : 10 findings supprimes : 5 imagines (mitigations en place), 2 idiomatiques, 2 deja documentes, 1 dedup.**

## Statistiques

- **Modules audites** : 160 / 160 (lecture integrale via 7 agents Explore en parallele)
- **Findings totaux pre-self-critique** : ~50 (entre 7 groupes)
- **Findings retenus apres self-critique** : 9 (3 PR + 6 issue)
- **Actions creees** : 3 PR (#407-#409) + 6 issue (#410-#415)
- **Issues deja ouvertes** : 3 (avant audit) → 9 (apres audit). +6.
- **PRs ouvertes** : 0 → 3.
- **Findings deja connus enrichis** : 0 (toutes les remontees sont nouvelles, le cycle de fixes recent #395-#403 a deja absorbe les low-hanging fruits).

## Conformite architecture (cat 47)

- `grep -rnE "from cinesort\.(app|infra|ui)" cinesort/domain/` : 1 TYPE_CHECKING legit + 1 docstring. Aucun import reel.
- `grep -rnE "from cinesort\.(app|ui)" cinesort/infra/` : 0 hit.
- `grep -rnE "from cinesort\.ui" cinesort/app/` : 0 hit.
- 3 contracts import-linter respectes. Aucune regression du cycle `domain → app`.
- Repository pattern : 0 nouveau mixin ajoute. 100% des nouveaux call-sites SQL utilisent `store.<repo>.<method>`.
- Facade pattern : 0 nouvelle methode directe ajoutee sur CineSortApi (toutes vont via les 5 facades).
- Module-style imports : echantillon de 67 sites verifies, pattern respecte.

## Per-module breakdown (resumé)

### Couche app/ (24 modules)
**Findings actionnables :** plugin_hooks.py (#408). **Autres remontees** rejetees ou cosmetic. Modules cleanest : _dir_utils, _fuzzy_utils, _path_utils, apply_audit, jellyfin_validation, job_runner, move_journal, notify_service, plan_support, radarr_sync, retention_cleanup, runtime_probe_check, updater, watcher, watchlist, export_support.

### Couche domain/ (30 modules)
**Findings actionnables :** 0 nouveau (tous les findings proposes rejetes par self-critique : explain_score.py deja audite, scan_helpers.py false positive, calibration.py speculation). Modules les plus matures : audio_analysis, conversions, core, custom_rules, duplicate_compare, edition_helpers, encode_analysis, film_history, genre_rules, i18n_messages, integrity_check, librarian, naming, probe_models, profile_exchange, quality_score, runtime_matching, scene_parser, subtitle_helpers, title_helpers, tv_helpers.

### Couche domain/perceptual/ (23 modules)
**Findings actionnables :** audio_perceptual.py rc ffmpeg (#407), lpips_compare.py TypeError (#409). Modules cleanest : av1_grain_metadata, constants, ffmpeg_runner, frame_extraction, grain_signatures, hdr_analysis, mel_analysis, metadata_analysis, models, parallelism, scene_detection, spectral_analysis, ssim_self_ref, upscale_detection, video_analysis, composite_score.

### Couche infra/ (27 modules — core + DB)
**Findings actionnables :** 0 nouveau (regex log_scrubber rejete, migration NAS timeout rejete). Repositories tous propres. Architecture (cycles, foreign keys, SQL injection) confirmee saine.

### Couche infra/ (clients + probe + REST, 14 modules)
**Findings actionnables :** tmdb cache stale (#413). Reste propre : Jellyfin/Plex/Radarr/Omdb/REST tous OK.

### Couche ui/api/ (44 modules)
**Findings actionnables :** apply_support slot (#410), dashboard_cache (#411), settings DPAPI (#412), apply_audit scrub (#414), reset_settings propag (#415). Modules les plus complexes (apply_support, cinesort_api) deja sous refactor (#215).

## Tendance vs audit precedent (2026-05-24 transverse)

| Metrique | 2026-05-24 | 2026-05-25 |
|---|:---:|:---:|
| Issues ouvertes (debut audit) | 5 | 3 |
| PRs ouvertes (debut audit) | 2 | 0 |
| Nouveaux fix mergees entre les 2 audits | — | 4 (cf #399-#403) |
| Findings nouveaux | 0 | 9 |
| Modules avec deux findings actionnables ou + | 0 | 1 (apply_support : 2 issues + retentions #215) |

L'audit montre que le rythme rapide de fixes (4 releases v140 → v152 en 7 jours) a absorbe la dette mais introduit aussi de nouveaux modules / patterns qui n'avaient pas encore ete sous l'oeil de l'audit Claude (notamment les facades V3-05 demo, V3-04 reset, cache dashboard V5-03).

## Outils suggeres pour confirmation (non installes en CI)

- **bandit** : confirmation finding #408 (env var info exposure) via `bandit -ll cinesort/app/plugin_hooks.py`
- **vulture** : nouvelle passe sur les ui/api/* support modules (post #347)
- **mutmut** sur cinesort/domain/perceptual/lpips_compare.py : survival rate post-fix #409 pour valider la couverture du TypeError path
- **radon mi** : verifier que les fonctions touchees par les PRs restent au-dessus du MI 65

## Notes operationnelles

- Approche 7 agents Explore en parallele : 6/7 ont termine en < 2 min, le 7e (DOMAIN-PURE) en 3 min 10s. Total wall-clock audit ~3 min.
- Self-critique stricte (FILTRE 1-8) : 10/19 findings preliminaires supprimes (53%). Sans ce filtre, l'audit aurait cree 6 issues additionnelles bruyantes/false-positive (cf #217 incident 2026-05-17).
- Tous les PR contiennent au moins 1 test ou justification "no test possible". Aucune signature publique modifiee.
- Branches pushees nommees `<topic>/audit-<sujet>-2026-05-25` pour distinguer du flux quotidien.
