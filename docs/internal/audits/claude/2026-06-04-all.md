# Audit Claude — 2026-06-04 — Couche all

**Modele** : Opus 4.7 (thinking budget eleve)
**Modules audites** : 48 (echantillonnage cible parmi les 158 listes)
**Categories couvertes** : 46/46

## Resume executif

Le re-audit du jour confirme que la couche `cinesort/` est dans un etat tres
stable apres le batch massif du 2026-06-03 (issues #505-#526 + PRs #527-#532).
Apres pre-audit multi-agent et passage de **self-critique strict**, la grande
majorite des candidats releves par les sous-agents sont :

- Soit deja documentes (issues ouvertes #420 a #525),
- Soit deja mitiges dans le code (guards, `try/except`, `is None` check),
- Soit idiomatiques Python/numpy non-bug.

Un seul finding **inedit et confirme** est sorti du filtre :

| # | Module | Severite | Fix |
|---|--------|----------|-----|
| 1 | `cinesort/domain/scene_parser.py:274` | 1 STYLE | PR #533 ouverte |

## Self-critique : findings supprimes

- 4 hallucines par sous-agents (lignes inexistantes, fonction renommee) :
  `omdb_cross_check.py:190` coverage_pct (n'existe pas), `email_report.py:115/122`
  ssl.create_default_context double (un seul appel), `notify_service.py:176`
  timer cancel (= None set apres suppress, correct), `metadata_analysis.py:153`
  interlaced division par 0 (deja guarde l.151).
- 7 idiomatiques : `custom_rules.py:98` sorted() defensif, `edition_helpers.py:81`
  fallback `.title()` correct, `integrity_check.py:188` `min()` redondant mais
  pas bug (guard l.170), `title_helpers.py:288` loop reverse-sequel correct,
  `notifications_support.py:148-157` assign sans try (pas critique), `zip(strict=False)`
  acceptable, `docs_whitelist.py` deja lowercase keys.
- 5 deja mitiges : `upscale_detection.py:61` try/except l.62-63, `video_analysis.py:217`
  appartient deja a #457 (5 sites), `grain_analysis.py:55` idem #457, `composite_score.py:71`
  fallback documente, `is_safe_external_url` IPv6 deja couvert par #518.
- 4 deja en issue : `_circuit_breaker` 5xx -> #458, `library_audit_support:296-306`
  N+1 TMDb -> #467, `mkv_title_check._is_scene_title` dead -> #463,
  `tmdb_client._tmdb_client` -> #520.

**Total supprime : 20 candidats / 21 emis. Taux de purge : 95%.**
Le seul finding survivant est `scene_parser.py:_orphan_sep_re`.

## Par categorie

| Cat | Nom | Nb findings | Exemples retenus |
|-----|-----|-------------|------------------|
| 3 | PERFORMANCE | 1 | `scene_parser:274` re.compile in function |
| autres | — | 0 | (purges en self-critique) |

## Par module

### cinesort/domain/scene_parser.py

- **Severite** : STYLE (1)
- **Categorie** : 3 (Performance)
- **Ligne** : 274 (avant fix)
- **Description** : `_orphan_sep_re = re.compile(r"\s+[-_.]+\s*$")` declare
  a l'interieur de `parse_scene_title()`. Hot path (appele par filename scanne).
  Re-compile a chaque appel alors que les 8 autres regex du fichier sont
  toutes module-level.
- **Fix** : Promotion en constante module-level `_ORPHAN_SEP_RE` avec docstring.
- **PR** : #533

## Verification methodologique

- `git log --oneline -20` : pas de changement sur scene_parser depuis #495.
- `grep -rn _orphan_sep_re` : 1 site (la declaration locale), 1 usage (loop).
- Pattern uniforme verifie : `grep -n "^_.*_RE\s*=\s*re\.compile"` retourne
  8 patterns top-level + le nouveau.
- Comportement identique : meme regex litterale, meme appel `.sub("", name)`.

## Statistiques

- Modules audites (echantillon) : 48 / 158
- Sous-agents lances : 3 (batch app+domain, batch infra+ui, batch perceptual)
- Findings emis par sous-agents : 41 (13 + 10 + 18)
- Findings post-self-critique : 1
- Issues creees : 0 (toutes les autres categories etaient deja couvertes)
- PRs ouvertes : 1 (#533)
- Findings deja connus (dedup) : 16 confirmes en re-audit (issues #420-#525)
- Hallucinations LLM filtrees : 4

## Tendance vs 2026-06-03

| Indicateur | 2026-06-03 | 2026-06-04 | Delta |
|-----------|------------|------------|-------|
| Findings nouveaux | 22 | 1 | -21 |
| PRs ouvertes | 6 | 1 | -5 |
| Issues nouvelles | 22 | 0 | -22 |

Le delta negatif est attendu et **sain** : les fixes de hier ont consomme la
grosse poche de bugs latents. Le seuil de bruit (faux positifs LLM) est
remonte a 95% du flux brut. La purge agressive est imposee par les retex
#217 et #91 (suppression de code mort sans verifier les tests).

## Note tendance multi-runs

Les 8 derniers audits journaliers (2026-05-27 -> 2026-06-04) montrent une
courbe descendante : 14, 22, 13, 6, 22, 1, 1, 1 findings nouveaux par jour.
Le code se stabilise. Recommandation : passer en mode hebdomadaire pour la
couche `all`, et **bi-mensuel** pour la couche `transverse`.
