# Audit Claude — 2026-07-04 — Couche : all

**Modele** : Opus 4.8 (thinking max, effort ultra)
**Persona dominant** : ARCHITECT + RELIABILITY (categorie 11 code mort + 12 patterns Python)
**Modules audites** : ~200 modules (cinesort/app, cinesort/domain, cinesort/infra, cinesort/ui/api)
**Categories couvertes** : balayage des 46 categories, focus 11 (code mort), 12 (patterns Python), 1 (bugs latents), 2 (securite OWASP/CWE)
**Issues creees** : 1 (#708)
**PRs creees** : 2 (#709 fix import mort, cette PR rapport)

## Resume executif

Passe complete niveau **modere** sur les ~200 modules des 4 couches. Le codebase
est **tres mature** : audite quotidiennement depuis ~2 mois (le rapport de la veille,
PR #707 du 2026-07-03, remontait **0 finding**), avec 543 commits sur les 30 derniers
jours et 5+ vagues adversariales (R1-R7, hotfix1-7) qui ont deja epuise la majorite
des bugs latents classiques.

Ce run confirme la tendance : **1 seul finding neuf** de faible severite (import mort
`_EXTENDED_EDITIONS`), traite en PR #709. Les patterns de bugs habituels
(`== None`, bare `except`, mutable default args, `shell=True`, divisions non gardees,
dette `TODO/FIXME`) sont **essentiellement absents** apres verification par grep sur
tout `cinesort/`.

| Metrique | Valeur |
|----------|-------:|
| Modules balayes | ~200 (4 couches) |
| Findings neufs confirmes | 1 |
| dont severity STYLE (1) | 1 |
| dont severity BUG (3) / BLOCKER (4) | 0 |
| Issues nouvelles | 1 (#708) |
| PRs code | 1 (#709) |
| Doublons stricts filtres | 0 |

## Finding neuf

### A1 — Import mort `_EXTENDED_EDITIONS` dans `runtime_hard_filter.py` (F401)

- **Fichier** : `cinesort/domain/runtime_hard_filter.py:33`
- **Categorie** : 11 (code mort) / F401 (pyflakes, regle ruff active)
- **Severity** : 1 (STYLE) — **Confidence** : 0.97
- **Persona** : RELIABILITY

Ligne d'import originale :

```python
from cinesort.domain.runtime_matching import _EXTENDED_EDITIONS, _is_extended_edition
```

Seul `_is_extended_edition` est utilise (ligne 105, dans `evaluate_runtime_hard_filter`).
`_EXTENDED_EDITIONS` (un `set` defini dans `runtime_matching.py:43`) n'est **jamais
reference** dans le module.

**Verification code mort (regle categorie 11)** :
- `grep -n "_EXTENDED_EDITIONS" cinesort/domain/runtime_hard_filter.py` → seule
  occurrence = la ligne d'import (33). Aucun usage.
- `grep -rn "_EXTENDED_EDITIONS" tests/` → une seule occurrence dans
  `tests/test_runtime_hard_filter_v77.py:138`, et c'est un **commentaire**, pas un
  import depuis ce module. Aucun test ne depend de la re-exportation via
  `runtime_hard_filter`.
- La constante reste definie et exportee par `runtime_matching.py` (source de verite) :
  supprimer l'import ne casse aucun autre consommateur.

**Fix** (PR #709) : retirer `_EXTENDED_EDITIONS,` de l'import, ne garder que
`_is_extended_edition`. Diff d'une seule ligne, aucun changement de comportement.

**Note maintainer** : F401 est une regle **active** dans la config ruff
(`pyproject.toml`, `select = ["E","F","W","I", ...]`) et le job `ruff check` de
`ci.yml` est **bloquant**. Qu'un F401 ait atteint `main` suggere que le commit R8
squash `650d162` (pousse directement, pas via PR) a pu **contourner la CI** — a
verifier cote branche de protection. Le fix passe par une PR normale qui re-soumet
au gate.

## Filtres appliques (etape 2.6 audit-prompt.md)

- **Filtre 1 (realite)** : finding verifie en lisant le code reel + grep croise
  (module + tests). 0 finding purement theorique retenu.
- **Filtre 2 (idiome)** : rien retenu sur du code idiomatique. Un import inutilise
  n'est PAS idiomatique (F401).
- **Filtre 3 (confidence >= 0.70)** : le finding est a 0.97.
- **Filtre 4 (dedup cross-categories)** : finding unique, aucun chevauchement.
- **Filtre 5 (severite)** : STYLE (1), pas d'escalade artificielle — c'est un import
  mort sans effet runtime.
- **Filtre 6 (actionabilite)** : fix concret pousse en PR #709.
- **Filtre 7 (etat actuel)** : les gros chantiers historiques (cycle domain→app,
  mixins SQLite, lazy imports, fonctions >100L) restent traites/suivis ailleurs
  (#83 closed, #85, #215) — pas re-remontes.
- **Filtre 8 (proportionnalite)** : PR #709 = 1 LOC.

## Verifications negatives (patterns absents — 0 finding)

Balayage grep sur tout `cinesort/` :

| Pattern | Regle | Resultat |
|---------|-------|----------|
| `== None` / `!= None` | E711 | 0 hit code prod |
| bare `except:` | E722 | 0 hit code prod |
| mutable default args (`def f(x=[])`, `={}`) | B006 | 0 hit code prod |
| `subprocess(... shell=True)` | S602 | 0 hit |
| `eval(` / `exec(` sur input | S307 | 0 hit |
| dette `TODO`/`FIXME`/`XXX` non tracee | cat 10 | quasi nul, pas de dette neuve |
| `except ...: pass` (swallow silencieux) | SIM105 | conformes / whitelistes |

## Comparaison avec audit precedent

| Date | Couche | Findings neufs | PR fix |
|------|--------|:-------------:|:------:|
| 2026-07-01 | all | 0 | #697 (rapport) |
| 2026-07-03 | all | 0 | #707 (rapport) |
| **2026-07-04** | **all** | **1** | **#709** |

**Conclusion** : codebase toujours en etat sain. Le seul finding est un residu
cosmetique (import mort) probablement introduit par un squash pousse hors PR. Aucun
bug fonctionnel, securite ou concurrence detecte sur cette passe.

## Annexe — fichiers de ce run

- Code : `cinesort/domain/runtime_hard_filter.py` (PR #709, 1 LOC)
- Ce rapport : `docs/internal/audits/claude/2026-07-04-all.md`
- JSONL findings : `docs/internal/audits/findings/2026-07-04-all.jsonl`
