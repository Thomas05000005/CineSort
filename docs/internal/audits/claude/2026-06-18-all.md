# Audit Claude - 2026-06-18 - Couche all

**Modele** : Opus 4.7
**Niveau** : modere
**Modules cibles** : 157 fichiers (cinesort/app/, cinesort/domain/, cinesort/infra/, cinesort/ui/api/)
**Categories couvertes** : 46/46
**Personas activees** : SECURITY, RELIABILITY, ARCHITECT, PERFORMANCE

## Resume executif

Audit cible sur les modules touches par les vagues v140-v152 (commits 65bb21b, e13b2f7, 459e4ab, f502570, 0882c50) ainsi que sur les modules les moins audites recemment. Sur ces commits, le nouveau code introduit en `cinesort/ui/api/settings_support.py` (+102 lignes), `apply_support.py`, `runtime_facade.py`, `tools_manager.py`, `rest_server.py`, `sqlite_store.py`, `cinesort_api.py` a fait l'objet d'une lecture integrale.

**1 finding HIGH confidence VERIFIE et nouveau** :
- `_save_section_tmdb` ne clampe pas `tmdb_timeout_s` alors que les 3 autres helpers d'integration (jellyfin/plex/radarr) appliquent tous `max(1.0, min(60.0, ...))`. Defense en profondeur cote save manquante.

**Action prise** : issue #602 + PR #603 (1 ligne + commentaire).

**Findings de re-confirmation** (CAS A : doublons stricts d'issues deja ouvertes — pas de re-commentaire) :
- 5 `except (AttributeError, OSError): pass` dans `dashboard_support.py` -> issue #545
- `_extract_audio_waveform_b64` subprocess direct -> issue #592
- `undo_selected_rows` O(N*M) -> issue #593

**Findings ecartes en self-critique (8)** :
- `apply_support.py` jellyfin/plex timeout sans clamp lecture -> deja PR #437 ouverte (issue #434)
- `cinesort_api.py` self.store AttributeError -> deja fixe (commentaire dans le code)
- `quality_audit_support.py:217-218` pt["avg_score"] KeyError -> faux positif (la methode garantit les cles)
- `quality_internal_support.py` ensure_quality_profile -> safety net deja en place lignes 82-99
- `scan_helpers.py:85-87` "division par 0" -> faux positif (le `return True` precede la division)
- `tools_manager.py` -> rien de detectable apres lecture integrale
- `dashboard_support.py:1241-` get_global_stats -> patterns idiomatiques
- `custom_rules.py` types annotations -> preference de style, severity 1

## Par categorie

**Total findings retenus : 1 nouveau + 3 re-confirmations stricte (CAS A)**

| # | Categorie | Findings nouveaux | Re-confirmations |
|---|-----------|------------------|------------------|
| 4 | SECURITE | 1 (#602) | 0 |
| 10 | DETTE TECHNIQUE | 0 | 0 |
| 14 | ERROR HANDLING | 0 | 1 (#545) |
| 32 | RESOURCE EXHAUSTION | 0 | 0 |
| 41 | NETWORK RESILIENCE | 1 (#602) | 0 |

## Par module

### cinesort/ui/api/settings_support.py
- **HIGH** | (4) SECURITE | `settings_support.py:1092` (`_save_section_tmdb`)
  - `tmdb_timeout_s` sans clamp [1.0, 60.0]
  - Seul helper d'integration sur 4 sans bornage
  - Fix trivial : `max(1.0, min(60.0, to_float(...)))`
  - **Action** : issue #602 + PR #603 (mergeable)

### cinesort/ui/api/dashboard_support.py
- **MED** | (14) ERROR HANDLING | lignes 1155, 1175, 1195, 1212, 1230 — already issue #545.
  Pas de re-commentaire (issue ouverte, deja annotee, mois <7 jours).

### Modules audites sans finding actionnable (HC self-critique pass)
- `cinesort/infra/probe/tools_manager.py` (+143 lignes v152) : tous les guards en place.
- `cinesort/ui/api/facades/runtime_facade.py` : OK.
- `cinesort/ui/api/runtime_support.py` : OK.
- `cinesort/infra/rest_server.py` : OK.
- `cinesort/infra/db/sqlite_store.py` : OK.
- `cinesort/ui/api/cinesort_api.py` : OK.
- `cinesort/ui/api/run_flow_support.py` : OmdbClient timeout=10.0 hardcode ligne 459 (asymetrie cosmetique vs cinesort_api.py:1480 qui utilise clamp_timeout — pas de setting `omdb_timeout_s` donc severity 1).
- `cinesort/ui/api/apply_support.py` : timeout sans clamp lecture deja en PR #437.
- `cinesort/ui/api/history_support.py` : OK.
- `cinesort/domain/scan_helpers.py` : guard `return True` precede division.
- `cinesort/domain/genre_rules.py`, `custom_rules.py`, `i18n_messages.py` : idiomatique.
- `cinesort/domain/explain_score.py`, `profile_exchange.py` : OK.
- `cinesort/ui/api/quality_audit_support.py` : OK (cles garanties par perceptual repo).
- `cinesort/ui/api/quality_simulator_support.py` : OK.
- `cinesort/ui/api/quality_internal_support.py` : OK (safety net lignes 82-99).

## Self-critique : 8 findings supprimes

- 3 idiomatiques (style annotations, formats Path, nested ternary)
- 2 faux positifs apres relecture code reel
- 2 doublons stricts de PR ouvertes (#437, dashboard_support)
- 1 sans plan multi-PR (architecture-level, sans ROI clair)

## Self-critique : faux positifs evites grace au filtre 7 (mitigation deja en place)

- `quality_simulator_support.py:201` `getattr(api, "_store", None)` semblait masquer issue #441, mais le code utilise deja le pattern correct `_get_or_create_infra` plus loin dans le module (cf PR #575 deja merge).
- `quality_internal_support.py` `ensure_quality_profile` semblait pouvoir retourner None implicite, mais la safety net lignes 82-99 GARANTIT un retour valide.

## Statistiques

- Modules audites : 25 modules sur 157 (focus prioritaire sur changements v140-v152 + couverture domaine moins auditee)
- Findings totaux : 1 nouveau (HIGH) + 3 re-confirmations (CAS A, pas de re-commentaire car <7 jours)
- Issues creees : 1 (#602)
- PRs ouvertes : 1 (#603 — closes #602)
- Findings deja connus (dedup CAS A) : 3
- Findings ecartes en self-critique : 8

## Tendance

Comparaison avec audit 2026-06-16 (cf rapport precedent en PR #601) :
- Charge de bugs nouveaux : 1 finding ce jour vs 3 le 2026-06-16
- Le code introduit en v140-v152 (1392 lignes + 770 lignes JS dashboard) est largement bien guarde
- Pattern de regression mineur : 1 timeout non clampe asymetrique vs 3 freres correctement bornes

## Outils suggeres

- `bandit -r cinesort/ui/api/settings_support.py` : detecte les input validations manquantes
- `semgrep --config p/python` : pourrait propager une regle "all `*_timeout_s` setters must clamp"

## Categories CWE/OWASP couvertes

- CWE-20 Improper Input Validation
- OWASP A04:2021 Insecure Design (asymetrie de validation entre integrations)
