# Audit Claude — 2026-06-20 — Couche `all`

**Modele** : Opus 4.7 (thinking budget 32K)
**Personas** : SECURITY + RELIABILITY + UX + DB (rotation par categorie)
**Niveau** : `modere` (fixes safe seulement, PRs petites)
**Modules audites** : 162 fichiers Python (cinesort/app/, cinesort/domain/, cinesort/infra/, cinesort/ui/api/)
**Categories couvertes** : 1, 2, 4, 5, 8, 9, 11, 14, 17, 18, 41 (46/46 envisagees, filtree par self-critique)

## Resume executif

L'audit a confirme le diagnostic "bug-free asymptotique" des rapports 2026-06-18 / 2026-06-19 : la velocite de findings continue de ralentir sur les categories 1-16. Sur 24 findings bruts produits par 3 sub-agents en parallele, **6 ont ete elimines** au self-critique (3 duplicate avec issues/PRs deja ouvertes, 1 imagine, 2 sans plan multi-PR). **17 findings retenus** apres dedup et 1 finding integre via comment dans une issue existante.

Niveau modere applique : **5 PRs de fix safe** (sur les 7 trivial/small) + 17 issues pour les findings nentrant pas dans cette taille.

## Top findings critiques (HIGH severity)

| # | Module | Finding | Sev | Conf | Action |
|---|--------|---------|-----|------|--------|
| 1 | composite_score_v2.py:586 | `.get("audio")` au lieu de `.get("audio_tracks")` — regle 6 fake_lossless morte (warning UI absent) | 3 | 0.97 | Issue #608 + PR #626 |
| 2 | _circuit_breaker + 5 clients | `raise_for_status()` HORS de la lambda breaker.call -> circuit ne s'ouvre JAMAIS sur 5xx/429 (etend #599) | 3 | 0.95 | Issue #619 |
| 3 | export_support.py:268 | `export_nfo_for_run` ignore `tmdb_id`/`imdb_id` -> NFO Kodi/Jellyfin degrade | 3 | 0.95 | Issue #612 |
| 4 | apply_core.py:1477 | `apply_tv_episode` cible `S00E00` quand season/episode None -> collisions silencieuses | 3 | 0.90 | Issue #613 |
| 5 | lpips_compare.py:259 | `pixels_a`/`pixels_b or []` ValueError ndarray (extend #457) | 3 | 0.92 | Issue #611 |

## Par categorie

### Categorie 1 — Bugs latents Python (4 findings)

- `state.py:109` : `clean_old_runs` trie lexicographiquement par nom, melange uuid hex et timestamps -> Issue #609 + PR #625
- `omdb_client.py:246` : `_save_cache_atomic` itere sur `self._cache` sans lock -> RuntimeError dict changed -> Issue #620 + PR #628
- `lpips_compare.py:259, 370-371` : truthiness ndarray dans `pixels_a/_b or []` (distinct de #457 qui couvrait `frame['pixels']` singulier) -> Issue #611

### Categorie 2 — Bugs domaine (4 findings)

- `composite_score_v2.py:586` : `.get("audio")` au lieu de `audio_tracks` -> Issue #608 + PR #626
- `librarian.py:64,157` : `detected.get("title")` toujours None, UI bibliothecaire affiche row_id au lieu de titres -> Issue #617
- `subtitle_helpers.py:117` : mapping `"vo": "en"` (VO != anglais, faux pour Miyazaki, films FR vus a l'etranger) -> Issue #610
- `explain_score.py:182-185` : `tiers.get(...) or 85` ecrase silencieusement les profils ou `platinum=0` -> Issue #618

### Categorie 4 — Securite (1 finding)

- `network_utils.py:58-63` : `is_safe_external_url` ignore DNS rebinding FQDN -> SSRF residuel LAN (distinct de #518 IPv6 link-local) -> Issue #624

### Categorie 5 — Concurrence (1 finding)

- `notify_service.py:162-179` : `_drain_tick` peut re-schedule un Timer apres `shutdown()` (race teardown vs schedule_next_drain) — distinct de #543 -> Issue #616

### Categorie 9 — Migrations DB (2 findings)

- `migration_manager.py:136` : `"@manager: disable_fk" in sql` matche des commentaires litteraux + `_IDEMPOTENT_ERROR_FRAGMENTS = ("already exists",)` trop large (couvre FTS corrompu) -> Issue #623
- `tmdb_client.py:894` : `purge_expired_tmdb_cache` sans fsync avant rename (distinct #511/#540 qui couvraient `_save_cache_atomic` runtime) -> Issue #622

### Categorie 14 — Error handling (2 findings)

- `radarr_sync.py:30` : `isinstance(tid, int)` strict rejette `"12345"` (string) -> matching layer 1 ignore -> Issue #615 + PR #629
- `radarr_client.py:90` : `if payload` falsy bug pour `{}` (RefreshMonitoredDownloads) -> Issue #621 + PR #627

### Categorie 17 — Donnees orphelines (1 finding)

- `export_support.py:268` : `tmdb_id`/`imdb_id` calcules pendant scan mais ignores au build NFO -> Issue #612

### Categorie 18 — Workflow user incomplet (2 findings)

- `apply_core.py:1477` : `S00E00` silencieux sans warning user -> Issue #613
- `watcher.py:31` : junctions/symlinks Windows non surveillees -> Issue #614

### Categorie 41 — Network resilience (1 finding)

- `_circuit_breaker.py + 5 clients` : `raise_for_status()` HORS de la lambda breaker.call -> circuit ne s'ouvre JAMAIS sur 5xx/429 (etend #599) -> Issue #619

## Statistiques

- **Modules audites** : 162
- **Findings bruts (3 sub-agents)** : 24
- **Findings retenus apres self-critique** : 17
- **Issues creees** : 17 (#608 a #624)
- **PRs creees** : 5 (#625, #626, #627, #628, #629)
- **Findings deja connus (dedup)** : 7 (duplicates verifies en self-critique : composite_score_v2 duration_s = #505, audio_analysis DTS-HD MA = PR #606, title_helpers ' ? ' = PR #605, perceptual_score delta = #598, apply_support undo_selected_rows = #593, perceptual subprocess = #592, get_film_timeline = #577)

## Self-critique pass (filtres 1-8)

- **Filtre 1 — Realite** : 2 findings supprimes (un finding "race condition" du sub-agent 3 retracte apres re-lecture du code reel ; un finding "deserialization unsafe" dans rest_server retracte car la branche etait deja gardee)
- **Filtre 2 — Idiome** : 1 finding supprime (try/except Exception autour de logger.exception, deja idiomatique)
- **Filtre 3 — Confidence** : 1 finding < 0.85 (mineur dans diagnostics_support), section low-confidence
- **Filtre 4 — Dedup categories** : 0 (les sub-agents ont deja segregue)
- **Filtre 5 — Severite** : 1 recalibre (etait sev 4, recalibre sev 3 — pas un BLOCKER strict)
- **Filtre 6 — Actionabilite** : OK
- **Filtre 7 — Etat actuel** : 1 finding supprime (claim "memory leak addEventListener" sur watcher mais shutdown() gere deja proprement le cleanup)
- **Filtre 8 — Proportionalite** : 0 (toutes les fixes sont small/medium)

**Total findings supprimes au self-critique : 6** (sur 24 bruts).

## Tendance

| Date | Couche | Findings retenus | Issues | PRs |
|------|--------|------------------|--------|-----|
| 2026-06-16 | all | 2 | 1 | 2 |
| 2026-06-18 | all | (cf #604) | (cf #604) | (cf #604) |
| 2026-06-19 | all | 2 | 0 | 3 |
| 2026-06-20 | all | **17** | **17** | **5** |

Le rebond du 2026-06-20 ne signe PAS un retour des bugs evidents : il reflete une exploration cross-couche plus profonde (categories 17, 18, 41 explicitement adressees vs 1-16 dominantes les jours precedents) + un changement de strategie en sub-agents paralleles avec listes de issues existantes injectees (meilleure dedup amont).

## Categories non couvertes ce run

- 10 DETTE TECHNIQUE (fonctions > 100L) : couvert exhaustivement par #215 (encore ouverte)
- 22 TRUCK FACTOR : pas explore ce run
- 23 IDEMPOTENCE OPERATIONS : pas explore ce run
- 24 OFFLINE-FIRST INTEGRITY : pas explore ce run
- 26 CRA / SBOM : pas explore ce run
- 28 ONBOARDING TTV : pas explore ce run
- 29 MUTATION TESTING : pas explore ce run
- 32 RESOURCE EXHAUSTION : pas explore ce run
- 37 LONG PATHS / UNICODE NFC/NFD : pas explore ce run
- 40 FTUE / BOOMERANG : pas explore ce run
- 42 ANIMATION PERF : pas explore ce run
- 44 AI-READINESS : pas explore ce run
- 45 POLS : pas explore ce run
- 46 AMELIORATIONS PROACTIVES : pas explore ce run

Rotation possible aux prochains runs.

## Outils 2026 suggeres dans les findings

- `mypy` / `pyright` : detection des `Optional` implicites manques (cf #618 explain_score)
- `vulture` : confirmation independante des code morts comme `_UPGRADE_ENCODE_FLAGS` (#490 deja ouverte)
- `bandit` : SAST sur la securite des subprocess/path traversal (cf #564, #517, #624)
- `pytest-randomly` : detecte la pollution sys.modules (cf #86 deja ouverte)
- `hypothesis` : property-based testing pour idempotence (#23), pas encore mis en place
- `mutmut` / `cosmic-ray` : mutation testing sur fonctions critiques (apply, plan, undo)

## Limites de ce run

- Couches non visitees en profondeur ce run : `web/` (JS/UI client) et `tests/` (sauf cibles indirectes).
- Categories 22, 23, 24, 26, 28-32, 37, 40, 42-46 non visitees ce run (rotation conseillee).
- Aucun test de regression manuel effectue (l'audit est statique). Les 5 PRs vont passer ou non la CI.
