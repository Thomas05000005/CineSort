# Audit Claude — 2026-06-11 — Couche `all`

**Modele** : Opus 4.7 (thinking max)
**Parametres** : couche=all, niveau=modere, ouvre_PRs=true
**Modules audites** : 162 (cf MODULE_LIST audit-prompt) + rotation Explore agent
  sur 12 modules peu explores recemment (4 app, 4 perceptual, 4 infra/probe)
**Categories couvertes** : 46/46 (focus 1, 3, 11, 14, 15, 17, 47)

## Resume executif

Pipeline en mode "exhaustion controlee" : 4 findings RETENUS apres self-critique
sur 13 findings candidats (3 agents Explore). Aucun blocker, aucun CVE,
aucune regression architecturale. Cycle `domain -> app` toujours brise (verifie).

**Actions creees** :
- **PR #575** — fix(quality-simulator) : `_get_or_create_infra(api._state_dir)` au lieu
  de `getattr(api,"_store",None)`. Closes #441 (bug latent depuis 2026-05-28,
  simulateur de preset entierement non fonctionnel en prod).
- **PR #579** — fix(perceptual-av1) : `logger.warning` sur `JSONDecodeError` dans
  `_run_ffprobe_streams`. Closes #578 (cas asymetrique avec les autres except
  qui loggent deja).
- **Issue #576** — orphan(film-history) : `scan_count` + `apply_count` calcules mais
  jamais consommes cote UI. Decision produit requise (Option A : exposer dans
  film-detail.js, Option B : retirer du contrat).
- **Issue #577** — perf(film-history) : N+1 dans `get_film_timeline` sur les
  apply operations (1 query/batch * runs). Fix recommande @lru_cache 60s, ou
  batch query au niveau ApplyRepository.
- **Issue #578** — rel(perceptual-av1) : voir PR #579 ci-dessus.

## Par categorie (focus self-critique)

| Cat | Findings candidats | Retenus | Ecartes |
|-----|--------------------|---------|---------|
| 1 BUG_LATENT_PYTHON | 1 (spectral div/(ws-1)) | 0 | 1 — confidence 0.75 |
| 3 PERFORMANCE | 3 (N+1 film, rglob winget, lower-in-loop) | 1 (#577) | 2 — opt-in / micro-perf |
| 11 CODE_MORT | 3 (av1 fields, runner type, scan_count) | 1 (#576) | 2 — debug-intent / type-style |
| 14 ERROR_HANDLING | 2 (av1 log, _safe_file_mtime) | 1 (#578 + PR #579) | 1 — style |
| 15 LOGGING | 1 (av1 log) | (dedup avec #578) | 0 |
| 17 ORPHAN_DATA | 2 (scan_count, av1 grain_seed) | 1 (#576) | 1 — debug-only |
| 47 ARCHITECTURE | 1 (api._store inexistant) | 1 (PR #575) | 0 |

## Self-critique : 9 findings ecartes (filtres 1-8)

L'agent Explore en rotation a propose 13 findings ; 4 retenus, 9 ecartes :

1. **REALITE/IDIOME** (filtres 1+2) : `genres_low`/`companies_low` recrees par regle
   dans `_rule_matches` (`grain_signatures.py:105-114`). Conditionne le ROI sur
   la taille de GRAIN_SIGNATURES_EXCEPTIONS. Micro-perf, non actionnable seul.
2. **REALITE** : `_safe_file_mtime` sans log (`tools_manager.py:139`) — defensif
   par design, propose de logger les `OSError` sur `path.stat()` qui sont
   typiquement attendus (fichier vient d'etre supprime).
3. **IDIOME** : `int(rc or 0)` dans `tools_manager.py:558` — defensif typique
   pour un runner externe. `runner()` retourne `Tuple[int,str,str]` typing.
4. **CONFIDENCE < 0.7** : `compute_spectrogram(samples, window_size=1)` division
   par 0 (`spectral_analysis.py:161`). Constante interne 4096, path public
   improbable. Skipped.
5. **IDIOME** : `RunnerFn` sans annotation des exceptions levees (`tooling.py:12`)
   — typing.Callable n'exprime pas les exceptions, c'est une limite de
   l'ecosysteme, pas un bug.
6. **DEJA MITIGE** (filtre 7) : `pkg_root.rglob(exe)` sans maxdepth dans
   `tools_manager.py:177` est opt-in (`scan_winget_packages=False` par defaut).
   Aucun probleme en mode normal.
7. **IDIOME** : `except (KeyError, ValueError, json.JSONDecodeError)` dans
   `mediainfo_backend.py:41` et `ffprobe_backend.py:58` — le `KeyError` est
   defensif, accepte par convention dans le codebase.
8. **CONFIDENCE 0.85** : `Av1FilmGrainInfo.grain_seed/ar_coeff_lag/num_y_points`
   stocks mais jamais lus en aval — verifie par `grep`, mais l'intention "debug"
   est documentee dans le commentaire `raw_params`. Acceptable.
9. **CONFIDENCE < 0.85** : `edition_helpers.py:70-77` Anniversary suffix
   title-casing — cas edge tres specifique, pas verifiable sans cas reels.

## Par module (findings retenus)

### `cinesort/ui/api/quality_simulator_support.py`
- **Persona** : RELIABILITY · **Cat** 47 · **Severite** 3 (BUG) · **Conf** 0.95
- Ligne 201, 230 : `getattr(api, "_store", None)` / `api._store.run.get_latest_run()`
  -> `CineSortApi` n'expose pas `_store`. Simulateur entierement mort en prod.
- Fix : `store, _runner = api._get_or_create_infra(api._state_dir)`.
- **PR #575** ouverte. **Closes #441**.

### `cinesort/domain/film_history.py`
- **Persona** : UX · **Cat** 17 · **Severite** 2 (QUALITY) · **Conf** 0.92
- Ligne 128-130, 159, 201, 229-230 : `scan_count`/`apply_count` calcules et
  exposes par `get_film_timeline()`, jamais consommes cote `web/`.
- Action : **Issue #576** ouverte (decision produit Option A vs B).

- **Persona** : PERFORMANCE · **Cat** 3 · **Severite** 2 · **Conf** 0.88
- Ligne 192-199 : N+1 sur `list_apply_operations_by_row` (1 query/batch/run).
- Action : **Issue #577** ouverte (fix @lru_cache rapide, ou batch query).

### `cinesort/domain/perceptual/av1_grain_metadata.py`
- **Persona** : RELIABILITY · **Cat** 14+15 · **Severite** 2 · **Conf** 0.92
- Ligne 137-140 : `except (json.JSONDecodeError, ValueError): return None` sans
  log. Asymetrie avec les except adjacents qui loggent en warning.
- Fix : `logger.warning(... %s, exc)` (3 LOC).
- **PR #579** ouverte. **Closes #578**.

## Statistiques

- Modules audites : 162 (formal) + 12 (focus Explore rotation)
- Findings totaux candidats : 13
- Findings retenus : 4 (1 PR-clos #441, 1 PR-clos #578, 2 issues)
- Issues creees : 3 (#576, #577, #578)
- PRs ouvertes : 2 (#575, #579)
- Findings deja connus (dedup CAS A) : 0 (les issues persistantes #459, #443
  sont laissees inchangees — voir Tendance ci-dessous)
- Findings ecartes par self-critique : 9 (3 idiome, 2 confidence<0.85, 2 deja
  mitiges, 2 realite)

## Tendance

| Date | Issues | PRs | Findings retenus |
|------|--------|-----|------------------|
| 2026-06-08 | 8 | 3 | 11 |
| 2026-06-09 | 0 | 1 | 1 |
| 2026-06-10 | 0 | 2 | 2 |
| 2026-06-11 | 3 | 2 | 4 |

Rebondissement leger : 4 findings vs 2 hier, mais aucun n'est nouveau au-dela
de bugs structurels deja documentes par persona RELIABILITY/PERFORMANCE.

### Issues persistantes non retraitees ce run

- **#443** (toggle `include_subtitles`) : decision produit requise, label
  `needs-discussion`. Aucun changement.
- **#459** (secrets masques en backend) : fix plus large (nouvel accessor
  interne `read_settings_raw`), pas un quick-fix safe pour le niveau modere.
  Aucun changement.

## Strategie 2026-06-12

- Continuer auto-PR sur #459 (faut une revue archi : exposer `read_settings_raw`
  ou injecter le store des secrets par DI).
- Approche cross-couche sur User Journey "TMDb match incertain" (categorie
  18) — non explorer ce run faute de temps.
- Verifier categorie 27 (Authenticode signing) au prochain run hebdo
  transverse (preparation v1.0 GA).
