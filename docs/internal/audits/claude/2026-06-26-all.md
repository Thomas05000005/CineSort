# Audit Claude — 2026-06-26 — Toutes couches (échantillon ciblé)

**Modèle** : Opus 4.8 + thinking max
**Niveau** : modéré (corrections sûres/évidentes uniquement, petites PRs)
**Persona dominant** : RELIABILITY (catégorie 23 idempotence)
**Modules audités** : ~28 modules échantillonnés sur les couches domain / app / infra / ui (priorité aux modules peu couverts par le backlog d'issues existant)
**Catégories couvertes** : 23 (idempotence), 7 (data-loss/intégrité), 12 (patterns Python), 31 (sécurité subprocess/env), 47 (architecture invariants)
**Issues créées** : 1 nouvelle (#673)
**PRs créées** : 0

## Résumé exécutif

Le dépôt reste dans un **état exceptionnellement sain**. Sur ~28 modules lus intégralement (helpers domain, orchestration app, infra DB/intégrations, support ui/api), **une seule** observation de confiance ≥ 0,70 genuinement nouvelle a été identifiée — et c'est le **jumeau exact** d'une issue acceptée la veille (#671). Tous les autres modules échantillonnés sont propres : gardes TOCTOU, écritures atomiques (`os.replace` + tmp sibling), `tracked_run` pour les subprocess, whitelist d'env vars plugins, anti-SSRF poster proxy, anti-data-loss rollback avec vérification SHA1 — le tout déjà en place et documenté.

La densité du backlog audit-bot (671+ issues, la plus récente datée du 2026-06-25) confirme que les passes précédentes ont déjà capturé l'essentiel des défauts de surface. La valeur marginale d'un nouvel audit réside désormais dans la **détection de jumeaux** : un défaut accepté dans un module a souvent un clone dans le module sœur que la passe d'origine n'a pas couvert. C'est précisément le cas ici.

| Métrique | Valeur 2026-06-26 |
|----------|:-----------------:|
| Modules échantillonnés (lecture intégrale) | ~28 |
| Findings nouveaux confiance ≥ 0,70 | 1 |
| Doublons évités (dédup pré-création) | ≥ 2 (#638 merge_metadata `_is_empty`, #542 omdb normalize perf) |
| Violations contracts import-linter | 0 (aucune réintroduite) |

## Findings par catégorie

### Catégorie 23 — Idempotence (1 finding nouveau)

1. **#673 (créée)** — `cinesort/app/omdb_cross_check.py` : `cross_check_rows_with_omdb` réécrit `row.confidence = max(0, min(100, confidence + bonus))` de façon **additive**, sans garde d'idempotence. Strictement le jumeau de **#671** (`cross_check_rows_with_probe`, accepté le 2026-06-25, confiance 0,72). Aggravation propre à OMDb : la branche **divergence (-25)** n'est pas bornée par le garde `confidence >= threshold → skip` (qui ne borne que les bonus positifs), donc une ré-invocation empile les pénalités jusqu'à 0. Latent (appelé une fois par run dans `run_flow_support.job_fn`), d'où **needs-review**, cohérent avec le triage de #671. Correctif mutualisable avec #671 (flag `_omdb_crosschecked` par row, ou recompute depuis une base stockée).

## Doublons évités (dédup pré-création)

- **merge_metadata `_is_empty` traite 0/0.0 comme vide** (`app/merge_metadata.py:109`) → déjà **#638**. Comportement par ailleurs documenté dans le docstring de la fonction.
- **`_normalize_title_for_compare` perf (re.sub × N)** (`app/omdb_cross_check.py:47`) → déjà **#542** ; partiellement atténué depuis (regex `_NON_ALPHANUM_RE` désormais pré-compilée au niveau module, ligne 44).
- **subtitle `vo` → `en`** (`domain/subtitle_helpers.py`) → déjà **#610**.

## Modules échantillonnés jugés propres (extrait)

- `domain/` : codec_ranks, confidence_thresholds, tiers_helpers, edition_helpers, genre_rules, tv_helpers, subtitle_helpers, scan_helpers, perceptual/upscale_detection, perceptual/scene_detection.
- `app/` : _dir_utils, disk_space_check, _local_candidate, _fuzzy_utils, _path_utils, quarantine_ttl, merge_metadata, apply_rollback (anti-data-loss SHA1 + backup tmp), plugin_hooks (whitelist env + tracked_run), omdb_cross_check, runtime_probe_check, move_reconciliation, jellyfin_sync.
- `infra/` : db/pragma_profile, db/nas_validation, integrations/poster_proxy (anti-SSRF).
- `ui/api/` : _validators (clamp_timeout / clamp_non_negative_int).

## Notes non actionnables (sous le seuil de confiance, non filées)

- `jellyfin_sync._compute_retry_delay` : le commentaire (ligne 24) annonce « 5+10+20+40+60 = 135s » mais la somme réelle des délais est 5+5+10+20+40 = 80s. Nit de documentation pur, aucun impact comportemental — non filé (catégorie cosmétique).
- `tv_helpers._XE_RE` : le commentaire sur le faux positif « Movie.1080p.2x04 » est légèrement trompeur (le `\b` protège bien) mais la regex est correcte — non filé.

## Conclusion

État sain confirmé sur l'échantillon. Recommandation de traitement : **regrouper #671 et #673** dans un même correctif d'idempotence (les deux phases cross-check partagent le même anti-pattern additif), après confirmation qu'un chemin replan/retry peut ré-invoquer ces phases sur des PlanRow déjà en mémoire.
