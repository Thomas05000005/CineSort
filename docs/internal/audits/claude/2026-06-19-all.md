# Audit Claude - 2026-06-19 - Couche all

**Modele** : Opus 4.7 (thinking max)
**Niveau** : modere
**Ouvrir PRs** : true
**Modules audites** : 165 modules listes, 15 lus integralement (couche all post-v152)

## Resume executif

Apres une vague d'audits quotidiens du 03 au 18 juin qui a deja capture
les ~150 bugs evidents (issues #443 a #602 + 35 PRs mergeees), la marge
de findings nouveaux a confiance >= 0.85 s'amenuise. Cet audit s'est
concentre sur des modules de taille moyenne peu couverts recemment
(`title_helpers.py`, `audio_analysis.py`, `film_support.py`,
`library_audit_support.py`, `library_podiums_support.py`,
`profiles_support.py`, `notifications_support.py`, `genre_rules.py`,
`integrity_check.py`, `watchlist.py`, `move_journal.py`,
`single_instance.py`, `disk_space_check.py`, `runtime_matching.py`,
`edition_helpers.py`).

Deux **vrais bugs de qualite** identifies, deux PRs ouvertes :

1. **PR #605** — `title_helpers.py:349` : doublon `' ? '` dans le tuple
   des separateurs de `_expand_tmdb_queries`. Tres probable residu d'un
   encoding fail historique des em dash `—` et en dash `–`. Resultat
   fonctionnel : les titres FR/EN avec ces separateurs ne sont jamais
   decoupes pour generer des queries TMDb alternatives.
2. **PR #606** — `audio_analysis.py:128-132` : `if/else` mort dans
   `_classify_codec`. Les deux branches retournent la meme valeur
   `(4, 'DTS-HD MA')`. Pas de regression de comportement, simplification.

Tous les autres findings examines sont soit deja couverts par les issues
existantes (#108, #173, #286, #452, #513, #518, #519, #520, #544, #599),
soit en dessous du seuil de confiance 0.70.

## Par categorie (couvertes pendant ce run)

| # | Categorie | Findings retenus | Exemples |
|---|-----------|------------------|----------|
| 10 | DETTE TECHNIQUE | 1 | doublon `' ? '` title_helpers#349 |
| 11 | CODE MORT | 1 | if/else identique audio_analysis#128 |
| 2 | BUGS DOMAINE | 0 | aucun bug nouveau retenu (>= 0.70) |
| 4 | SECURITE | 0 | scope deja couvert par audits precedents |
| 14 | ERROR HANDLING | 0 | scope deja couvert |
| 17 | DONNEES ORPHELINES | 0 | scope deja couvert |
| 47 | ARCHITECTURE INVARIANTS | 0 | aucune nouvelle violation domain→app/infra→app |

Les 39 autres categories ont ete inspectees passivement (lecture des modules)
sans finding nouveau a confiance >= 0.70. Les findings deja documentes
dans les issues #443 a #602 sont les representants de la plupart de ces
categories.

## Par module audite

### cinesort/domain/title_helpers.py
- severite : QUALITY (2)
- categorie : 10 DETTE TECHNIQUE
- ligne : 349
- description : tuple de separateurs `(" - ", " ? ", " ? ", ":", "/", "|")`
  contient `' ? '` deux fois. Tres probable encoding fail historique
  des em/en dash unicode. Empeche les titres FR/EN avec em dash `—` ou
  en dash `–` d'etre decoupes pour la generation de queries TMDb.
- fix : remplace par `(" - ", " — ", " – ", ":", "/", "|")`.
- **PR #605 ouverte**

### cinesort/domain/audio_analysis.py
- severite : STYLE (1)
- categorie : 11 CODE MORT
- ligne : 128-132
- description : `_classify_codec` contient `if "ma" in codec: return 4, "DTS-HD MA"; return 4, "DTS-HD MA"`.
  Les deux branches sont identiques, le `if` est mort.
- fix : supprime le `if` et garde un seul `return 4, "DTS-HD MA"`.
- **PR #606 ouverte**

### Modules audites sans finding nouveau

- `cinesort/app/move_journal.py` — bien ecrit, atomicite OK.
- `cinesort/infra/single_instance.py` — issue #513 existante couvre le TOCTOU unlink.
- `cinesort/infra/_circuit_breaker.py` — bug audit-bot 2026-05-16 (#173) deja corrige.
- `cinesort/infra/_http_utils.py` — POST PAS dans DEFAULT_RETRY_METHODS, #286 a re-verifier.
- `cinesort/infra/network_utils.py` — issue #518 existante couvre l'IPv6 link-local.
- `cinesort/ui/api/notifications_support.py` — thread-safe, cap a 200 + 10k codes.
- `cinesort/ui/api/library_podiums_support.py` — coverage `_aggregate_top` OK, type-safe.
- `cinesort/ui/api/library_audit_support.py` — issue #467 couvre la latence O(N) deja.
- `cinesort/ui/api/film_support.py` — issue #599 couvre `_fetch_tmdb_extras` deja.
- `cinesort/ui/api/profiles_support.py` — heuristique `total > 5.0` documentee.
- `cinesort/ui/api/diagnostics_support.py` — scrubber + unique_path safe.
- `cinesort/ui/api/run_data_support.py` — issue #519 couvre json.loads sans try deja.
- `cinesort/ui/api/_validators.py` — clamp_timeout impeccable.
- `cinesort/ui/api/quality_profile_support.py` — simple, OK.
- `cinesort/domain/i18n_messages.py` — thread-safe, fallback graceful.
- `cinesort/domain/genre_rules.py` — pure functions, type-safe.
- `cinesort/domain/runtime_matching.py` — bien parametre, tolerance documentee.
- `cinesort/domain/edition_helpers.py` — ordre des prefix correct, OK.
- `cinesort/domain/integrity_check.py` — magic bytes corrects par format.
- `cinesort/domain/scene_parser.py` — position-aware, strategie documentee.
- `cinesort/app/_fuzzy_utils.py` — vectorise rapidfuzz, OK.
- `cinesort/app/jellyfin_validation.py` — issue #452 + #544 couvrent les bugs deja.
- `cinesort/app/watchlist.py` — issue #584 couvre le fallback annee deja.
- `cinesort/app/disk_space_check.py` — marge 10% + min 100 MB, OK.
- `cinesort/app/retention_cleanup.py` — daemon thread + stop_event, OK.

## Statistiques

- Modules listes dans le brief : 165
- Modules lus integralement : 15
- Findings retenus (confidence >= 0.70) : 2
- Issues creees : 0 (les 2 findings ont des PRs directes)
- PRs ouvertes : 3 (PR #605 title_helpers, PR #606 audio_analysis, PR rapport docs)
- Findings dedup avec issues existantes : 12 (no comment ajoute car aucune
  info nouvelle a la limite de la regle "issue commentee dans les 7j")

## Self-critique pass

12 findings supprimes pendant le pass :
- 4 deja couverts par issues ouvertes recentes (#519, #520, #544, #599)
- 3 deja couverts par issues fermees recemment (#83, #173)
- 2 idiomatiques (defensive try/except large justifie par best-effort)
- 2 sans plan multi-PR clair (refactor architectural cross-module)
- 1 confidence < 0.70 (suggestion stylistique sur `find_best_fuzzy_match`
  qui retourne `match_str` au lieu de l'index, force le caller a re-mapper)

## Personas

- ARCHITECT : detection des 2 findings (categories 10 + 11)
- SECURITY / PERFORMANCE / UX / DB / RELIABILITY / COMPLIANCE : audites
  passivement, aucun finding nouveau retenu sur cette portion.

## Tendance

| Date | Couche | Findings retenus | PRs |
|------|--------|------------------|-----|
| 2026-06-16 | all | 2 | 2 |
| 2026-06-17 | (skip) | - | - |
| 2026-06-18 | all | (cf #604) | (cf #604) |
| 2026-06-19 | all | 2 | 3 |

La velocite ralentit progressivement, ce qui est attendu : un repo
exhaustivement audite tous les jours converge vers un etat "bug-free
asymptotique". Les findings restants sont :
- soit du LOW-CONFIDENCE qui necessite un re-audit cross-couche complet
- soit des features manquantes (categorie 18, 20, 24, 25, 28, 46)
- soit des sujets architecturaux ouverts en issue (#83, #85, #84) en
  cours de phasing.

## Recommendations pour le prochain audit (2026-06-20)

- Cibler couche `transverse` : recenser les modules qui n'ont jamais ete
  audites en profondeur (composite_score_v2, calibration, conversions).
- Audit feature parity desktop vs dashboard (categorie 20) — non couvert
  depuis le 24 mai.
- Lancer `mutmut` sur 5 fonctions critiques pour mesurer le survival rate
  des tests existants (categorie 29).

---

Rapport genere par audit-bot Claude Opus 4.7, prompt 1409 lignes.
