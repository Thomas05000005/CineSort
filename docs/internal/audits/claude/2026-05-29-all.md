# Audit Claude - 2026-05-29 - Couche all

**Modèle** : Opus 4.7 · **Niveau** : modéré · **Modules audités** : 153 (toute la couche `cinesort/`)
**Méthode** : 6 sous-agents (app / domain / perceptual / infra / ui-api ×2) en lecture intégrale, puis vérification manuelle des findings haute sévérité + self-critique.

## Résumé exécutif (top findings)

1. **#456 (BLOCKER)** — `tmdb_support` appelle `api._normalize_user_path` qui n'existe pas comme méthode d'instance → `AttributeError` non capturée → vignettes TMDb et recherche manuelle TMDb cassées en prod (masqué par mocks de test).
2. **#457 (BUG)** — Truthiness `ndarray` sur `frame["pixels"]` (`if not pixels:` / `if pixels:`) → `ValueError` sur tout film → analyse vidéo + grain perceptuelle silencieusement cassée (5 sites).
3. **#458 (BUG)** — Circuit breaker TMDb/OMDb ne compte jamais les 5xx (`raise_for_status` hors breaker + `raise_on_status=False`) → ne s'ouvre jamais sur service down (defeat de #76).
4. **#459 (BUG)** — `get_settings()` renvoie les secrets masqués (`••••••••`) ; consommés tels quels par des modules backend (timeline Jellyfin, TMDb posters/sagas) → auth échoue silencieusement.
5. **#462 (BUG)** — Pré-check espace disque compte les rows **rejetées** (`decision_presence` au lieu de `ok=True`) → faux « espace insuffisant ».
6. **#460 (QUALITY)** — `_preview_naming_template` : branche « film réel » morte (3 appels cassés avalés) → `sample_row_id` inopérant.
7. **#463 (QUALITY)** — `check_container_title` flague tout écart de titre conteneur (scene-detection `_is_scene_title` morte) → faux positifs `mkv_title_mismatch`.
8. **#461 (QUALITY, sec)** — `del` des buffers ctypes propriétaires avant l'appel DPAPI (UAF latent, Windows).
9. **#466 (QUALITY, sec)** — ffprobe/MediaInfo téléchargés et exécutés sans checksum/signature (CWE-494).
10. **#465 (QUALITY)** — `row_id` basé sur `hash()` salé par `PYTHONHASHSEED` → non déterministe → décisions utilisateur potentiellement perdues.

## Par couche / persona

| Couche | Findings retenus | PRs |
|---|---|---|
| ui/api | #456, #459, #460, #464, #462, #471, #472 (+ PR #473, #474) | 2 |
| domain/perceptual | #457, #470 (+ PR #474) | — |
| domain | #463, #469 | — |
| infra | #458, #461, #466, #468, #472 | — |
| app | #462, #465 | — |

Personas : SECURITY (#461, #466), RELIABILITY (#456-#465, #468, #470), PERFORMANCE (#467), DB (#468, #472), UX (#471, PR #473).

## Par module (détail)

- `ui/api/tmdb_support.py:37,129` — **high** — cat 3 — `api._normalize_user_path` AttributeError → #456.
- `domain/perceptual/{video_analysis,grain_analysis}.py` (5 sites) — **high** — cat 1 — truthiness ndarray ValueError → #457.
- `infra/tmdb_client.py:164` + `omdb_client.py:302` — **high** — cat 41 — breaker n'attrape pas les 5xx → #458.
- `ui/api/settings_support.py:1042` (+ consommateurs) — **high** — cat 19 — secrets masqués consommés en interne → #459.
- `ui/api/apply_support.py:1071` — **high** — cat 2 — disque compte rows rejetées → #462.
- `ui/api/cinesort_api.py:1525` — **med** — cat 11 — preview naming branche morte → #460.
- `ui/api/cinesort_api.py:1206` — **med** — cat 2 — jellyfin validate_connection sans check ok → #464.
- `domain/mkv_title_check.py:35` — **med** — cat 11 — scene-detection morte + faux positifs → #463.
- `infra/local_secret_store.py:61,109` — **med** — cat 1 — del buffers ctypes (UAF) → #461.
- `app/plan_support.py:1608,1890` — **med** — cat 1 — row_id hash() non déterministe → #465.
- `infra/probe/auto_install.py:82,123` — **med** — cat 4 — pas de checksum binaires → #466.
- `ui/api/library_audit_support.py:296` — **med** — cat 3 — N appels TMDb séquentiels → #467.
- `infra/db/backup.py:245` — **med** — cat 9 — restore ne purge pas -wal/-shm → #468.
- `domain/naming.py:396` — **med** — cat 2 — folder_matches_template ignore placeholders → #469.
- `domain/perceptual/hdr_analysis.py:160` — **med** — cat 2 — HLG avant DV (DV 8.4) → #470.
- `ui/api/history_support.py:359` — **low** — cat 2 — open_path mono-root → #471.
- `infra/db/repositories/quality.py:401` — **low** — cat 10 — seuils tiers hardcodés → #472.
- `ui/api/dashboard_support.py:1143` — **low** — cat 2 — insight status running/RUNNING → **PR #473**.
- `ui/api/perceptual_support.py:1132` — **med** — cat 1 — précédence opérateur None → **PR #474**.

## Findings low-confidence / documentés (non filés)

- `composite_score_v2.py:139` — seuil 720p `height>=680` (conf 0.62) : marges déjà irrégulières, possiblement intentionnel.
- `state.py:77` — `write_text_safe` non protégé (conf 0.78) : naming trompeur, impact mineur.
- `cinesort_api.py:1286` — `get_plex_libraries` n'unmask pas le token (conf 0.78) : sous-cas de #459.
- `film_history.py:176` — delta score calculé en ordre run mais re-trié par ts (conf 0.72).
- `custom_rules.py:145` — `_act_score_mult` tronque au lieu d'arrondir (conf 0.70, biais à la baisse).
- `duplicate_compare.py:437` — `_best_audio` sans départage bitrate (conf 0.72, divergence vs quality_score).
- `scan_helpers.py:183` — garde `depth >= 0` toujours vraie (conf 0.80, cosmétique).

## Self-critique

Findings supprimés / dégradés : **5**.
- 1 **imaginé** (réalité) : « drain timer notifications jamais démarré » — faux, `app.py:723` appelle `start_drain_timer` (l'agent cherchait `cinesort/ui/app.py` inexistant).
- 1 **doublon** : `_drain_tick` hors main thread = déjà #453.
- 1 **idiomatique** : `audio_analysis` `best_rank=0` — comportement correct après relecture.
- 2 **low-confidence** (< 0.70) déplacés en section documentée : `composite_score_v2 height>=680`, `NOTIFYICONDATA struct alignment`, `mel mp3_shelf negative drop`.

## Statistiques

- Modules audités : **153**
- Findings retenus (≥ 0.70) : **24** (high 5, med 12, low 7)
- Issues créées : **17** (#456-#472)
- PRs ouvertes : **2** (#473, #474)
- Commentaires d'enrichissement (CASE C) : **2** (PR #436, issue #449)
- Findings déjà connus (dédup) : nombreux (perceptual reshape #449/#435/#436, secrets/DPAPI #412, circuit breaker #76, etc.)
- Catégories couvertes : 46/46

## Tendance

Run précédent (2026-05-28) : 14 issues. Ce run : 17 issues + 2 PRs. La majorité des nouveaux findings sont des **bugs de cohérence backend↔UI** (attributs/clés inexistants, secrets masqués, sentinels) plutôt que des bugs algorithmiques — signe que la couche `ui/api` mérite une passe de tests d'intégration sans mocks d'attributs internes.
