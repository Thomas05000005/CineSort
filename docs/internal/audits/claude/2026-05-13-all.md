# Audit Claude — 2026-05-13 — Couche all (niveau modere)

**Modele** : Opus 4.7 (thinking max)
**Contexte** : re-audit post ultra-audit du 2026-05-12 qui a livre 32 issues (#64-95) + 4 PRs sur la couche `all`. Cette session cible des findings **NOUVEAUX** non couverts.
**Modules examines** : ciblage `cinesort/app/*` recents (watcher, plugin_hooks, watchlist, email_report, jellyfin_validation, radarr_sync, move_reconciliation, disk_space_check) + `cinesort/ui/api/*` (quality_simulator, quality_profile, perceptual, notifications) — 23 modules examines en detail sur les 130 listes.

## Resume executif

5 findings nouveaux haute confiance (>= 0.80) apres self-critique :

1. **[SEV 3]** `cinesort/app/jellyfin_validation.py:20` — `int(tid)` sans try/except crash tout le rapport Jellyfin si un Candidate a un `tmdb_id` non-numerique (NFO corrompue, tag IMDb herite). Fix : try/except `(TypeError, ValueError)`. **PR #97**.
2. **[SEV 3]** `cinesort/ui/api/quality_simulator_support.py:24,89-91,135` — `_SIM_CACHE` (dict module-level) est mute sans verrou par `ThreadingHTTPServer`. Risque `RuntimeError: dictionary changed size during iteration` sur l'eviction FIFO + perte d'entrees. Fix : `threading.Lock`. **PR #99**.
3. **[SEV 2]** `cinesort/app/move_reconciliation.py:86,144,178` — 3 sites `except Exception` puis `_logger.warning("...: %s", exc)` jettent le traceback. Module de recovery post-crash : preserver le traceback est critique. Fix : `_logger.exception()`. **PR #98**.
4. **[SEV 2]** `cinesort/app/plugin_hooks.py:122` — `env = dict(os.environ)` partage **toute** l'env du parent (incluant `OPENAI_API_KEY`, `GITHUB_TOKEN` herites de la shell) avec les scripts plugin. **Issue #102** (security CWE-200, choix de whitelist demande discussion).
5. **[SEV 1]** `cinesort/ui/api/*` 23 sites sur 9 modules dupliquent `if not api._is_valid_run_id(run_id): return {"ok": False, "message": "run_id invalide."}`. **Issue #101** (refactor decorateur).

## Par categorie (categories actives ce run)

| # | Categorie | Findings nouveaux | Exemples |
|---|-----------|-------------------|----------|
| 2 | BUGS_DOMAINE | 1 | `int(tid)` jellyfin_validation crash sur tmdb_id non-numerique |
| 4 | SECURITE | 1 | plugin_hooks fuite env parent |
| 5 | CONCURRENCE | 1 | `_SIM_CACHE` race condition (ThreadingHTTPServer) |
| 10 | DETTE_TECHNIQUE | 2 | Boilerplate `_is_valid_run_id` x23 ; `radarr_sync` double `.get()` |
| 12 | PATTERNS_PYTHON | 1 | `disk_space_check` `//` vs `/` incoherent |
| 14 | ERROR_HANDLING | 1 | `move_reconciliation` 3 sites `_logger.warning(\"...: %s\", exc)` |
| 15 | LOGGING | 1 | 250 returns `{ok: False}` sans logging (mesure 2026-05-13) |

Toutes les autres categories : 0 finding nouveau, **deja couvert** par l'audit precedent (cf issues #64-95).

## Self-critique : findings supprimes (filtres F1-F6)

| Filtre | Count | Exemples supprimes |
|--------|-------|---------------------|
| F1 REALITE (imagine) | 4 | `jellyfin_validation.py:90` "manque .strip()" — `local_title` est deja strip ligne 66 ; `notify_service.py:103-109` race hook — pattern lecture-puis-appel acceptable ; 2 autres |
| F2 IDIOME | 2 | `move_journal.py:155 if self._fn is None:` etait deja correct ; `watcher.py:61-62 rsplit` format strict garanti ligne 35 |
| F3 CONFIDENCE | 5 | mises en low-confidence (pas en issue) : `notifications_support.py:233-248`, `library_support.py:52-55`, `quality_simulator_support.py:127` (validation overrides), 2 autres |
| F4 DEDUP cross-categ. | 3 | doublons trouves entre persona PERFORMANCE et categorie 41 NETWORK |
| F5 SEVERITE recalibrage | 0 | (severites OK) |
| F6 ACTIONABILITE | 1 | suggestion `_normalize_path` extraction etait deja faite (audit precedent) |

Total **15 findings supprimes** sur ~25 candidats brut.

## Actions creees

### PRs (4) — niveau modere, fixes safe < 30 LOC

| PR | Titre | Fichiers | Tests | Categorie |
|----|-------|----------|-------|-----------|
| #97 | fix(jellyfin-validation): _extract_local_tmdb_id resiste aux tmdb_id non-numeriques | 2 | +1 test | 2 BUG |
| #98 | refactor(reconciliation): logger.exception pour conserver le traceback | 1 | — | 14 ERROR |
| #99 | fix(quality-simulator): thread-safety du cache _SIM_CACHE | 2 | +1 test | 5 CONCURRENCE |
| #100 | refactor(audit): petits polish radarr_sync + disk_space_check | 2 | — | 10/12 polish |

### Issues (3) — meta refactor / discussion produit

| Issue | Titre | Effort |
|-------|-------|--------|
| #101 | refactor(api): factoriser le boilerplate `_is_valid_run_id` (23 sites, 9 modules) | Medium |
| #102 | security(plugins): plugin_hooks expose tout l'environnement parent aux scripts utilisateur | Small (mais discussion whitelist) |
| #103 | logging(api): 250 sites `return {"ok": False, ...}` ne loggent rien | Large (migration progressive) |

### Enrichissements sur issues existantes

Aucun enrichissement utile identifie sur les issues #64-95 — l'audit du 12 mai etait deja exhaustif sur les angles couverts. La presence session porte sur des modules / patterns differents (plugins, simulator cache, reconciliation traceback).

## Statistiques

- Modules audites en detail : 23 (cible pragmatique sur les 130 listes, focalise sur les modules recents et les supports UI API peu audites)
- Findings nouveaux haute confiance : 8 (5 actionnes en PR + 3 en issue)
- Findings supprimes par self-critique : 15
- PRs ouvertes : 4 (#97, #98, #99, #100)
- Issues ouvertes : 3 (#101, #102, #103)
- Issues deja existantes apres dedup : 32 (#64-95 — non re-commentees, audit precedent fresh < 24h)

## Tendance vs audit precedent (2026-05-12)

- **Categorie 4 SECURITE** : +1 finding (plugin env fuite). Toutes les autres securite (#64-72) deja couvertes.
- **Categorie 5 CONCURRENCE** : +1 finding (`_SIM_CACHE` race). #49 (lpips_compare race) closed, ce nouveau est un autre site.
- **Categorie 10 DETTE** : +2 findings precis (`_is_valid_run_id` x23, `radarr_sync` double get). Complement utile sur les chiffres globaux deja signales (162 lazy imports #83).
- **Categorie 14 ERROR_HANDLING** : +1 finding precis (move_reconciliation x3 sites). #80 (rollback DB) reste a faire.
- **Categorie 15 LOGGING** : +1 finding meta (250 returns silencieux), nouveau angle.

Aucun finding du run 2026-05-12 invalide. Les 32 issues d'hier restent toutes pertinentes.

## Outils suggeres (cf audit-prompt.md ETAPE 2.9)

- `bandit` : confirmerait CWE-200 plugin env (cat 4)
- `pytest-freethreaded` : detecterait `_SIM_CACHE` race (cat 5)
- `vulture` : confirmerait le double `.get()` radarr_sync comme dead expression candidate
- `radon cc` : cible les 9 modules `*_support.py` pour mesurer si la factorisation `_is_valid_run_id` reduirait la complexite cyclomatique
