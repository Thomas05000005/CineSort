# Audit patterns R6 — bugs confirmes (verif adversariale)

Date 2026-06-14. 33 trouvailles confirmees (real=true), 18 apres dedoublonnage/synthese.


## HIGH

- [HIGH] cinesort/ui/api/library_support.py:1448 (write) + film_support.py:155-183 / library_support.py:210 + app/apply_core.py (read) — Le choix manuel d'un candidat TMDb (set_film_tmdb_candidate, toast 'Renommage mis a jour') est perdu : tmdb_id/titre/jaquette reviennent au match auto au reload et l'apply renomme avec l'ancien match. — Cause : l'override est ecrit uniquement dans la table film_tmdb_overrides ; aucun lecteur de prod ne la consulte (get_tmdb_override film_modal.py:189 a 0 appelant), chosen_tmdb_id n'est pose que transitoirement dans get_film_full (film_support.py:317), jamais persiste dans la PlanRow/plan.jsonl ni lu par _build_library_rows ni par apply_core. — Correctif : dans _get_film_full_impl/_build_library_rows ET dans apply_core.apply_rows, injecter row['chosen_tmdb_id']=get_tmdb_override(...)['tmdb_id'] (+ titre/annee/confidence) avant resolution, ou persister le choix directement dans la PlanRow/plan.jsonl.

- [HIGH] cinesort/app/apply_core.py:93-98 + cinesort/infra/db/repositories/film_modal.py:81-124 + library_actions_support.py:74-177 — 'Marquer pour suppression' (modal et bulk) n'a aucun effet : aucun film n'est jamais deplace vers _user_marked_for_deletion/, dossier jamais cree, promesse UI silencieusement non tenue. — Cause : DEUX mecanismes write-only divergents et non consommes (modal -> table SQLite film_marked_for_deletion ; bulk -> deletion_marks.json), leurs lecteurs (list_marked_for_deletion/is_marked_for_deletion, _read_deletion_marks) n'ont aucun appelant de prod, et apply_core ne reference jamais marked/deletion (0 occurrence). — Correctif : unifier sur la table DB, charger store.film_modal.list_marked_for_deletion(run_id) au debut de l'apply et router ces row_ids vers un deplacement reel dans _user_marked_for_deletion/ (analogue au traitement des doublons perdants apply_core.py:934+) ; supprimer le mecanisme JSON.

- [HIGH] web/dashboard/views/qij.js:114,127,128,167-169 — Les 3 KPI du header Qualite/QIJ (films, score moyen, Platinum %) affichent toujours 0, et la fleche de tendance reste toujours '→'. — Cause : le frontend lit stats.total_films/avg_score/premium_pct/trend a la racine alors que get_global_stats (dashboard_support.py:1703-1708) niche ces cles sous summary.* (et summary.trend est deja un glyphe, pas 'up'/'down') ; le garde hasData reste vrai via v2_tier_distribution donc le bloc KPI est bien rendu a 0/0/0. — Correctif : const s = stats.summary || {} puis lire s.total_films/s.avg_score/s.premium_pct, et afficher s.trend directement.

- [HIGH] web/dashboard/components/film-detail.js:436-446 (et meme cause web/dashboard/views/film-detail.js:140-167,260-298) — Onglet Apercu/hero du Modal Detail Film : Resolution/Codec/Bitrate/Duree absents, 'Pistes audio: 0', 'Sous-titres: 0', HDR 'SDR' pour tout film reellement scanne. — Cause : le front lit probe.video/probe.audio/probe.subtitles/probe.duration_s alors que get_film_full renvoie metrics brut (film_support.py:260) dont la forme reelle est plate sous probe.detected.* (width/height/video_codec/bitrate_kbps/audio_tracks_count/duration_s, quality_score.py:1632-1649) ; _hdrLabel lit aussi has_hdr10/has_dv vs cles hdr10/hdr_dolby_vision. — Correctif : lire const det = probe.detected || {} (det.width/height, det.video_codec, det.bitrate_kbps, det.audio_tracks_count, det.duration_s, probe.subtitles_embedded), ou normaliser cote backend un objet probe canonique. Note : le nombre de sous-titres n'est pas dans metrics -> l'ajouter au backend ou retirer la ligne.


## MEDIUM

- [MEDIUM] cinesort/ui/api/history_support.py:306-324 (backend) + web/dashboard/views/historique.js:241-282,658-777,868 (front) — Inspecteur Historique inutilisable : onglet 'Films' affiche toujours '(detail non disponible pour ce run)', onglet 'Doublons' toujours 'Detail des groupes non disponible', et la recherche par titre de film ne matche jamais (avec en prime des appels reseau inutiles _prefetchFilmsForSearch a chaque terme, risque de rate-limit). — Cause : le dict run retourne par _get_history_stats_impl ne contient ni cle films, ni duplicates_decided, ni duplicates_skipped ; cote front les gardes Array.isArray(data.films)/duplicates_* sont donc toujours fausses, _filmsCacheByRun jamais peuple, branches vides systematiques. — Correctif : enrichir le dict run avec films [{film_id/title/tier/score/decision}] (depuis quality_reports) et duplicates_decided/skipped (depuis perceptual_reports/decisions) ; baser le filtre 'missing' du prefetch sur _historyStatsCache.has.

- [MEDIUM] web/dashboard/views/accueil.js:760-776,385-405,1065-1071 — Pendant un scan, le hero et la carte CTA affichent 'Scan en cours sur 0 films (0/0)' avec barre figee a 0% et aucun ETA tout au long du scan malgre le polling 2s. — Cause : _extractScanProgress lit la progression dans payload.run_info (total_rows/current_index/phase/eta_min) mais get_dashboard (dashboard_support.py:721-760) ne produit jamais run_info ni ces cles au top-level ; seul active_run_id est present (carte affichee mais non alimentee). — Correctif : exposer dans get_dashboard un bloc de progression construit depuis le RunState actif, ou faire pointer le polling vers run/get_status (direction prevue, commentaire dashboard_support.py:762-763).

- [MEDIUM] cinesort/infra/db/repositories/film_modal.py:104,212 (unmark_for_deletion / clear_tmdb_override) — Impossible d'annuler une action de correction manuelle : un film marque par erreur pour suppression ne peut jamais etre demarque (ni par film ni en bulk) et partira dans le bucket suppression au prochain apply ; un override TMDb errone ne peut pas revenir au match auto. — Cause : les methodes repo unmark/clear existent mais n'ont aucun appelant (uniquement 2 docstrings mensongeres promettant la reversibilite, library_support.py:1390/1506) ; aucun impl cinesort_api.py, aucune methode LibraryFacade, aucun endpoint/bouton UI ; les undo du front ciblent run/undo_last_apply (post-apply, mecanisme distinct). — Correctif : ajouter _unmark_for_deletion_impl/_clear_tmdb_override_impl, les exposer sur LibraryFacade, cabler les endpoints library/* et les boutons UI ; a minima corriger les docstrings.

- [MEDIUM] cinesort/ui/api/dashboard_support.py:1608,1611-1619 (KPI sous summary 1705-1707) — Score moyen (Qualite), total films (Accueil/Contexte) et l'ETA de la modale 're-calculer les scores' sont gonfles/fausses : un film present dans N scans est compte N fois. — Cause : get_global_stats calcule total_films/avg_score/premium_pct cumules sur lim runs (defaut 20) alors que la distribution par tier a deja ete corrigee au dernier run (fix R6-F limit_runs=1) non applique au summary ; chaque run est un snapshot complet independant. — Correctif : aligner sur R6-F en calculant ces 3 KPI a partir du dernier run uniquement (runs_summary[0], quality_counts.get(run_ids[0])).

- [MEDIUM] web/dashboard/views/qualite.js:1034-1035 + cinesort/ui/api/quality_audit_support.py:331-336 — 'Re-calculer les scores' affiche un toast vert '✓ Scores re-calcules (N/N)' meme quand tous les films echouent (probe ffprobe absent, medias deplaces). — Cause double : le worker incremente processed meme en cas d'echec (processed+=1 L336) donc status='done' ; et get_quality_report retourne {ok:False} sans lever d'exception, donc le compteur errors reste 0 et le front ne lit jamais data.errors. — Correctif : (1) backend inspecter le retour res.get('ok') et incrementer errors sinon ; (2) front lire data.errors et choisir type error/warn/success en consequence.

- [MEDIUM] web/dashboard/components/film-detail.js:185-197,910 + bibliotheque.js:1589 + cinesort/infra/integrations/poster_proxy.py:641-728 — 'Rafraichir le poster depuis TMDb' (fiche film) et 'Recharger les jaquettes' (bibliotheque, films deja identifies) : le poster change une fois puis l'ancien revient au re-render, et le toast 'N jaquettes recuperees' est mensonger. — Cause : posterSrc privilegie toujours l'URL proxy /api/poster?id=&size= (keyee id+size, sans cache-bust, Cache-Control immutable 30j) ; le force_refresh ne vise que get_tmdb_posters (cache JSON TMDb), pas le fichier image disque du proxy ; _state.data.poster_url (fallback masque) est un no-op pour l'affichage. — Correctif : invalider le cache disque du proxy pour size+id lors du refresh ET appliquer un token de version au src proxy au re-render, ou faire primer l'URL TMDb directe fraiche pour le row rafraichi.

- [MEDIUM] web/shared/components.css:6599 (.qualite-drawer) et :926 (.v5-top-bar-theme-menu) — Le drawer 'Filtres Qualite' (monte sur document.body) et le menu de theme de la top-bar laissent transparaitre le contenu vivant de la page derriere eux (libelles peu lisibles). — Cause : panneaux poses avec background: var(--surface-1), qui vaut rgba(255,255,255,0.035) (~3.5%, quasi transparent dans tous les themes), sans overlay opacifiant ni backdrop-filter sur le menu (contrairement a .v5-modal/.v5-palette qui reposent sur un overlay plein-ecran). — Correctif : donner un fond opaque (background: var(--bg-raised), defini dans les 5 themes) au lieu de var(--surface-1).

- [MEDIUM] web/shared/components.css:472 (.v5-select) / :5530 (.parametres-select) + web/dashboard/styles.css:385 (.input) — Sur Windows/Chromium en theme sombre, le popup natif des <select> (tri Journal QIJ, filtres Qualite Etat/Tier/Score, selects de Parametres) s'affiche en theme clair (fond blanc, texte sombre/illisible). — Cause : aucune de ces classes ne declare color-scheme:dark ; la seule occurrence existe sur .v5-input (components.css:418, fix R6-E) non migree vers .v5-select/.input/.parametres-select, et il n'y a aucun color-scheme global. — Correctif : ajouter color-scheme:dark a .v5-select, .input et .parametres-select (ou au :root).

- [MEDIUM] web/dashboard/views/status.js:937-991 + parametres.js:1139,2168-2173,2263 + cinesort/ui/api/settings_support.py:1208-1269 — Les boutons 'Afficher la cle'/'Copier la cle' (Statut Acces distant) et l'oeil/copie du champ Cle d'acces (Parametres) exposent/copient '••••••••' au lieu du Bearer reel -> 401 sur l'appareil distant. — Cause : get_settings masque rest_api_token (_SECRET_FIELDS -> _SECRET_MASK = 8 puces) et l'endpoint POST settings/reveal_rest_token n'existe pas (seulement 2 commentaires) ; le front lit la valeur masquee comme si c'etait le token. — Correctif : implementer reveal_rest_token (localhost-only via is_remote_request) appele par les boutons afficher/copier, ou retirer ces boutons et ne garder que le QR ; a minima desactiver afficher/copier quand value === masque.


## LOW

- [LOW] web/dashboard/views/qualite.js:1875-1882 (bibliotheque.js) + cinesort/ui/api/library_support.py:951-956 + bibliotheque.js:728-764 — Le preset Playlist livre 'DNR partiel detecte' affiche un toast de succes mais n'applique aucun filtre (grille non filtree). — Cause : _applyPlaylist ignore filters.warnings (forEach warnings vide) et _buildFilters n'emet jamais de cle warnings, donc le POST part avec filters={} alors que le backend sait filtrer par warnings. — Correctif : propager _state.playlistWarnings depuis _applyPlaylist et l'emettre dans _buildFilters (filters.warnings), le vider au reset des filtres.

- [LOW] web/dashboard/views/processing.js:769 — Apres un Apply reussi, le recap affiche toujours 'Apply termine. 0 operation.' quel que soit le nombre de renommages/deplacements. — Cause : res.data?.done est lu mais le payload de run/apply (apply_support.py:2162) renvoie {ok, result, apply_batch_id} sans cle done (la dataclass ApplyResult expose renames/moves/errors, pas done). — Correctif : const r = res.data?.result || {}; const done = (r.renames||0)+(r.moves||0)+(r.collection_moves||0).

- [LOW] cinesort/infra/db/repositories/perceptual.py:218-260 (get_global_score_v2_trend, count_v2_tier_since) — Le KPI 'films analyses' (trend QIJ) et l'insight Home 'N films Platinum ajoutes ce mois' sur-comptent : re-scanner la meme biblio le meme jour gonfle les compteurs (1 film compte plusieurs fois). — Cause : COUNT(*) sans DISTINCT/dedup par film, alors que la table perceptual_reports a une PK (run_id,row_id) et chaque scan cree un nouveau run_id -> autant de rows que de runs pour un meme film, sans pruning des runs anterieurs. — Correctif : dedupliquer par film (COUNT(DISTINCT row_id) ou derniere analyse par film via MAX(ts)/ROW_NUMBER), ou scoper l'insight au dernier run comme les insights #2-4.

- [LOW] web/dashboard/components/film-detail.js:333,990 + web/dashboard/views/doublons.js:342,596 + qualite.js:234-236 — Les vignettes de poster (candidats TMDb, recherche manuelle, cartes/inspecteur Doublons, grille Reject Qualite) affichent l'icone d'image cassee du navigateur en cas d'echec de chargement (poster_path perime/404, CDN bloque). — Cause : ces <img> pointent vers des URL TMDb directes (image.tmdb.org) 404-ables et n'ont ni onerror ni listener 'error' en phase capture ; le placeholder n'est rendu que si l'URL est vide. Le fix R6-H (onerror/listener capture) n'a ete applique qu'au poster principal et aux cartes Bibliotheque. — Correctif : ajouter le meme onerror inline ou un listener container en phase capture remplacant l'<img> par le placeholder existant.

- [LOW] web/dashboard/components/film-detail.js:887-915 — 'Rafraichir le poster depuis TMDb' quand aucune cle API TMDb n'est configuree affiche un toast generique trompeur 'L'action n'a pas pu etre effectuee. Reessayer ?' (incite a reessayer en vain). — Cause : get_tmdb_posters renvoie {ok:True, posters:{}, reason:'tmdb_not_configured'} (tmdb_support.py:55) ; _refreshPosterUnit ne teste que data.ok===false et ne lit jamais data.reason, donc tombe dans le throw generique, contrairement au sibling bibliotheque.js:1583 qui gere le cas. — Correctif : apres resolution de data, if (data?.reason === 'tmdb_not_configured') { toast warn 'Cle TMDb non configuree (Parametres > TMDb)'; return; }.
