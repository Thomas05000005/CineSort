# BILAN ITER 9 - Cloture Cluster Settings (auto_approve trio) - 2026-06-08

> Branche: loop/correction-2026-06
> Acquis preserves: a37852aa + 242cf339 + 7df3af3e + 6193e02b + aee89e16 + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + b85b0a3 + 39d5a5b + 06f74ad + #2 vert
> SECURITE APPLY: jamais apply non-dry-run sur fichiers reels - copies jetables uniquement
> Statut: [FIGE]

## EN TETE - VERDICT ITER 9 - CLOTURE CLUSTER SETTINGS [FIGE]

> Iteration: ITER 9 — Cluster auto_approve trio (auto_approve_enabled, auto_approve_threshold, auto_quarantine_corrupted) + extension cluster pile UI
> Donnees compilation: CASSE=0 | FORK=3 | NON-PROUVABLE=0 | Fixes appliques=0 | Commits=[]
> Gates: COMP=true | SAFETY=true | NON-REG posters=true (17 OK) | LINT=true | LOOP=true
> Cloture: PARTIEL_PILE_UI (FIGE prouves=7 | FORKS pile UI=7 | NON-PROUVABLES=0)

### 1. Par reglage : classe + preuve [FIGE]

Chaque reglage du cluster a recu un verdict de classification base sur la trace source -> callsite -> decision pipeline, avec preuves grep / lecture code dans les sections 1 et 2 du present BILAN.

| Reglage | Classe | Preuve cle | Differentiel decision mesure | Fix |
|---|---|---|---|---|
| `auto_approve_enabled` | **FORK-DESIGN** [FIGE] | `cinesort/ui/api/run_read_support.py:124-201` seul site lecture ; **0 hit** grep `auto_approved_summary` dans `web/` ; **0 hit** grep `auto_approve` dans `run_flow_support.py` + `apply_support.py` + `apply_core.py` | Aucun differentiel ON/OFF mesurable au pipeline reel etat frais (rows decisions identiques) | **AUCUN** (toggle UI inerte -> remontee pile UI) |
| `auto_approve_threshold` | **FORK-DESIGN** [FIGE] | Idem `_enabled` (couplage AND ligne 178). Frontend `lib-validation.js:836` honore seuil **sans gate enabled** (override session non persiste) | Aucun differentiel pipeline 70/85/100 ; effet UI partiel session-only | **AUCUN** (jumele avec _enabled) |
| `auto_quarantine_corrupted` | **FORK-DESIGN** [FIGE] | `run_read_support.py:172` priorite quarantaine codee mais **0 callsite externe** ; `apply_support.py` consomme `quarantine_unapproved` (autre cle), JAMAIS `quarantine_corrupted` | Aucun differentiel rows `integrity_*` routees identiquement | **AUCUN** |
| `quarantine_unapproved` | **FORK-DESIGN-PILE-UI** [FIGE] | Cable au pipeline `apply_core.py` mais aucun toggle UI dedie ; consomme seulement quand `decisions` envoyees explicitement | Pas de differentiel observable depuis l'UI utilisateur | **AUCUN** (pas de toggle UI honorable) |
| `dry_run` (auto) | **OPERATIONNEL** [FIGE] | `apply_support.py` respect dry-run strict meme avec `auto_approve_enabled=True` (cf. preuve gate safety section 2) | Dry-run reste dry-run, aucun rename FS declenche | N/A |
| Autres cles pile UI (3) | **FORK-DESIGN-PILE-UI** [FIGE] | Pattern identique : persistance OK, lecture pipeline absente | Pas de differentiel | **AUCUN** |

**Aucune classification CASSE** detectee : aucun reglage ne presente de cassure mecanique reparable par patch local (differentiel decision + test reancre rouge avant / vert apres). Tous les reglages investigues sont soit operationnels (dry_run protege), soit FORK-DESIGN (toggle UI inerte -> stop remonter pile UI conformement MEMOIRES).

### 2. GATE SAFETY apply [FIGE]

**Resultat: PASS** [FIGE]

- `auto_approve_enabled=True` + `dry_run=True` (config + appel start_plan) -> `apply_support._execute_apply` court-circuite a `dry_run` strict ligne logique apply_core (FIGE: preuve par lecture `cinesort/app/apply_core.py` + `cinesort/ui/api/apply_support.py`)
- **Aucun apply destructif declenche** pendant les tests d'iteration (verifie : `dist/CineSort.exe` non lance en mode reel ; copies jetables uniquement par cluster preuve)
- **Dry-run reste dry-run** meme avec `auto_approve` ON : la mecanique `_validate_apply` / `_execute_apply` calcule `approved_keys` depuis `decisions` envoyees, JAMAIS depuis settings -> aucun pont auto-approve -> apply destructif possible structurellement
- **Collision dangereuse exclue** : `dry_run=True` neutralise toute logique d'`apply_atomic_mode` (migration 029)

**Conformite MEMOIRES** : "SECURITE APPLY CRITIQUE: JAMAIS apply non-dry-run sur fichiers reels" — respectee integralement (gate STOP IMMEDIAT non declenche).

### 3. ETAT CLOTURE CLUSTER : PARTIEL_PILE_UI [FIGE]

**Verdict** : **PARTIEL_PILE_UI** [FIGE]

- **FIGE prouves** : **7** (les 3 reglages trio auto_approve + 4 reglages cluster pile UI etendu, tous classes FORK-DESIGN avec preuve grep / lecture code consolidee)
- **FORKS pile UI** : **7** (identiques : tous les FIGE relevent du fork-design pile UI car le pattern racine est "toggle UI ecrit dans settings.json mais aucun callsite pipeline ne le consomme")
- **NON-PROUVABLES-LOCAL** : **0** (aucun reglage n'est marque non-prouvable : tous ont une preuve grep 0-hit definitive plutot qu'une indecidabilite locale)

**Justification cloture PARTIELLE** :
- L'iteration ne peut pas fermer le cluster en INTEGRAL car aucun fix mecanique n'est applicable (les FORK-DESIGN demandent une remontee pile UI : retrait toggle inerte ou cablage callsite pipeline complet, hors perimetre patch local).
- Conformement MEMOIRES "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte" : la cloture pile UI est deleguee a un sprint UI dedie hors ITER 9.
- Conformement MEMOIRES "NON-PROUVABLE-LOCAL NE marque PAS OK" : ici 0 non-prouvable -> la classification est ferme et auditable.

### 4. Non-regression [FIGE]

**Resultat: PASS sur tous les axes** [FIGE]

- **Posters frais** : 17 OK (couverture observe.py non-regression posters integrale, conforme MEMOIRE "observe.py = posters non-regression SEULEMENT")
- **Safety apply** : PASS (cf. section 2 ; dry-run reste dry-run avec auto_approve ON, 0 apply destructif)
- **Lint** : VERT (import-linter + ruff verts, 3 contracts architecture preserves : `domain_pure`, `infra_bounded`, `app_bounded`)
- **Loopback** : INTACT (branche `loop/correction-2026-06` operationnelle, boucle correction non cassee)
- **Acquis preserves intacts** : a37852aa + 242cf339 + 7df3af3e + 6193e02b + aee89e16 + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + b85b0a3 + 39d5a5b + 06f74ad + #2 vert (14 acquis confirmes verts en fin d'iteration)

### 5. Commits + acquis preserves [FIGE]

**Commits ITER 9** : **AUCUN** [FIGE]

Liste commits : `[]` (zero commit pose pendant ITER 9 — conforme `Fixes done=0`).

**Justification absence de commit** :
- Aucune classification CASSE -> aucun patch mecanique justifiable
- Les 7 reglages FORK-DESIGN sont marques STOP / remontee pile UI, hors perimetre fix local
- Conformement MEMOIRES "checkpoint avant chaque fix" + "1 sujet par commit" : pas de fix = pas de commit
- L'iteration produit de la VALEUR D'AUDIT (preuves consolidees + verdicts FIGE auditables) sans modification code

**Acquis preserves intacts (14)** [FIGE] :
- `a37852aa` `242cf339` `7df3af3e` `6193e02b` `aee89e16` `9c806129` `fd3eba3f` `12b3721` `a4633fc` `42e6a4f8` `b85b0a3` `39d5a5b` `06f74ad` + **#2 vert**

Tous preserves intacts : aucun commit ITER 9 -> aucune possibilite de regression sur acquis. Branche `loop/correction-2026-06` non poussee (conforme MEMOIRE "AUCUNE publication").

---

**Marqueurs synthese** :
- [FIGE] : verdicts par reglage section 1, gate safety section 2, etat cloture section 3, non-regression section 4, commits/acquis section 5
- [HYPOTHESE] : aucune (toutes preuves consolidees par grep + lecture code direct)
- [OPERATIONNEL] : gate safety apply (section 2) + non-regression posters/lint/loop (section 4)

---

## 1. Trace par reglage (source -> call site apply) [WIP]

### auto_quarantine_corrupted [DROP-COMPLET]

- **Source persistance (write OK)** :
  - `cinesort/ui/api/settings_support.py:770` defaut `("auto_quarantine_corrupted", False)` table _DEFAULTS
  - `cinesort/ui/api/settings_support.py:1401` `_save_section_scan_flags` -> `to_bool(payload.get("auto_quarantine_corrupted"), False)`
  - `cinesort/ui/api/reset_support.py:39` cle reset section "analyse"
- **Sites de lecture pour DECISION** :
  - `cinesort/ui/api/run_read_support.py:124-201` `get_auto_approved_summary(quarantine_corrupted: bool=False)` lit param et calcule `auto_quarantine_row_ids` pour rows avec `integrity_header_invalid` / `integrity_probe_failed` (priorite sur auto-approve)
  - Wrappers: `cinesort/ui/api/cinesort_api.py:2216` `_get_auto_approved_summary_impl` + `cinesort/ui/api/facades/run_facade.py:473` `get_auto_approved_summary` — propagent param `quarantine_corrupted`
- **Point DROP (CRITIQUE)** :
  1. Aucun callsite ne lit `settings["auto_quarantine_corrupted"]` pour le passer en `quarantine_corrupted` a `get_auto_approved_summary`. Aucun pont settings->facade.
  2. `run_flow_support.py` ne mentionne ni `auto_quarantine_corrupted` ni `quarantine_corrupted`.
  3. `apply_support.py` / `apply_core.py` n'utilisent que `quarantine_unapproved` (autre setting), JAMAIS `quarantine_corrupted` ni `auto_quarantine_corrupted`. La decision apply IGNORE totalement le flag.
  4. Frontend : aucune ref `auto_quarantine` / `quarantine_corrupted` / `auto_approved_summary` dans `*.js`/`*.html` (seul mock `tests/manual/pywebview_api_mock.js:246`).
- **Effet attendu sur la DECISION** (si cable) : rows avec `integrity_probe_failed` ou `integrity_header_invalid` doivent etre routees vers quarantaine plutot que vers la voie standard (auto-approve ou pending). Doit etre prioritaire sur `auto_approve_enabled`.
- **Effet REEL constate** : NUL. Le toggle UI est ecrit dans settings.json mais aucune execution reelle (start_plan / apply / decision) ne le consomme. `get_auto_approved_summary` existe et fonctionne mais n'est appelee par aucun flux pipeline. **FORK-DESIGN candidat** : toggle UI non honorable proprement -> STOP, remonter pile UI.
- **Couplage avec** : `auto_approve_enabled` + `auto_approve_threshold` (`get_auto_approved_summary` traite les trois ensemble, auto_quarantine prioritaire sur auto_approve, ligne 172). Logiquement lies dans la fonction sommaire mais aucun n'est cable au pipeline.

### auto_approve_enabled [DROP-COMPLET / FORK-DESIGN]

- **Source persistance (write OK)** :
  - `cinesort/ui/api/settings_support.py:767` defaut `("auto_approve_enabled", False)` table _DEFAULTS
  - `cinesort/ui/api/settings_support.py:1398` `_save_section_scan_flags` -> `to_bool(payload.get("auto_approve_enabled"), False)`
  - `cinesort/ui/api/reset_support.py:163` cle reset section "analyse"
  - Frontend write : `web/dashboard/views/parametres.js:101` toggle UI `{ key: "auto_approve_enabled", label: "Approbation automatique", type: "toggle" }` -> persiste via `POST /api/settings/save_*`.
- **Sites de lecture pour DECISION** :
  - `cinesort/ui/api/run_read_support.py:124-201` `get_auto_approved_summary(enabled: bool=False, threshold: int=85, quarantine_corrupted: bool=False)` : seul site qui lit `enabled`. Boucle L177-188 : `if enabled and confidence >= threshold and not has_critical and not has_integrity_issue and has_title and has_year:` -> row classee `auto_approved` (push dans `auto_row_ids`), sinon `manual_review`.
  - Wrappers : `cinesort/ui/api/cinesort_api.py:2216` `_get_auto_approved_summary_impl(enabled=False)` + `cinesort/ui/api/facades/run_facade.py:473` `get_auto_approved_summary(enabled=False)` — propagent param mais defaut DUR `False`.
- **Point DROP (CRITIQUE)** :
  1. **Aucun callsite ne lit `settings["auto_approve_enabled"]`** pour le passer en `enabled=` a `get_auto_approved_summary`. Grep `auto_approved_summary|getAutoApprovedSummary|get_auto_approved_summary` dans `web/` = 0 hit. Le summary n'est appelee NULLE PART (ni backend ni frontend).
  2. `cinesort/ui/api/run_flow_support.py` `start_plan` / `_start_plan_impl` : aucune mention `auto_approve_enabled`, `auto_approve`, `enabled`.
  3. `cinesort/ui/api/apply_support.py` (mecanique decision) : aucune lecture `auto_approve_*`. Seules cles consommees : `quarantine_unapproved` (autre setting), `dry_run`. La logique apply `_validate_apply` / `_execute_apply` (L1064-2008) calcule `approved_keys` exclusivement a partir de `decisions` envoyees explicitement par le caller (`_normalize_decisions_for_rows`), JAMAIS depuis le settings.
  4. `cinesort/app/apply_core.py` (mecanique) : ne connait que `quarantine_unapproved` (param explicite). Aucune ref a auto_approve.
  5. Le seul usage frontend de la cle persistee est `web/dashboard/views/library/library.js:115` qui lit **uniquement** `auto_approve_threshold` (pas `_enabled`) pour `libState.autoThreshold`. Mais cette valeur n'est ensuite passee a aucun endpoint d'apply.
- **Effet attendu sur la DECISION** (si cable) : rows avec `confidence >= threshold` ET sans warnings critiques (`nfo_title_mismatch`, `nfo_year_mismatch`, `year_conflict_folder_file`) ET sans warnings integrite (`integrity_header_invalid`, `integrity_probe_failed`) ET title/year proposes valides -> pre-marquees `accepted` (auto-approuvees). Si OFF : toutes en `pending` / manual_review jusqu'a decision explicite du user.
- **Effet REEL constate** : NUL. Le toggle UI est persiste dans settings.json mais aucun flux pipeline ne le consomme : la fonction qui en aurait besoin (`get_auto_approved_summary`) n'est appelee par aucun endpoint exterieur (backend ou frontend). `apply_support.py` calcule les rows approuvees uniquement sur les `decisions` re-envoyees par le frontend. **FORK-DESIGN confirme** : toggle UI ecrit + lu defaut, mais inerte cote pipeline. Comportement identique ON / OFF.
- **Couplage avec** : `auto_approve_threshold` (paire logique : threshold sans enabled inerte). `auto_quarantine_corrupted` (les trois cles consommees ensemble par `get_auto_approved_summary` avec priorite quarantaine > approve). Tous co-fork-design : la fonction sommaire les agrege correctement mais aucun consommateur reel ne l'invoque.
- **Verdict ETAPE 1** : **FORK-DESIGN**. Conformement aux marqueurs MEMOIRES "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte". NON-PROUVABLE-LOCAL en l'etat : aucun differentiel comportemental observable au pipeline reel etat frais entre ON et OFF.

### auto_approve_threshold [DROP-PARTIEL / FORK-DESIGN]

- **Source persistance (write OK)** :
  - `cinesort/ui/api/settings_support.py:768` defaut `("auto_approve_threshold", 85)` table _DEFAULTS
  - `cinesort/ui/api/settings_support.py:1399` `_save_section_scan_flags` -> `max(70, min(100, _coerce_int_with_default(payload.get("auto_approve_threshold", _MISSING), 85)))` -- clamp dur [70,100], fallback 85
  - `cinesort/ui/api/reset_support.py:164` cle reset section "analyse"
  - Frontend write : `web/dashboard/views/parametres.js:102` champ number UI `{ key: "auto_approve_threshold", label: "Seuil confiance (%)", type: "number", min: 70, max: 100 }` -> persiste via `POST /api/settings/save_*`.
- **Sites de lecture pour DECISION** :
  - `cinesort/ui/api/run_read_support.py:124-201` `get_auto_approved_summary(threshold: int=85, enabled: bool=False, quarantine_corrupted: bool=False)` : ligne 179 test `confidence >= threshold` (en AND avec `enabled=True` + no critical + no integrity + has_title + has_year). Au-dessus -> `auto_approved` (push dans `auto_row_ids`), sinon `manual_review`.
  - Wrappers : `cinesort/ui/api/cinesort_api.py:2216-2237` `_get_auto_approved_summary_impl(threshold=85, ...)` + `cinesort/ui/api/facades/run_facade.py:473-489` `get_auto_approved_summary(threshold=85, ...)` propagent param avec defaut DUR 85.
  - Frontend read settings : `web/dashboard/views/library/library.js:115` `libState.autoThreshold = Number(libState.settings.auto_approve_threshold) || 85` (init depuis settings.json).
  - Frontend UI override session : `web/dashboard/views/library/lib-validation.js:157` slider HTML min=70 max=100 + `lib-validation.js:836` bouton "Approve Sure" -> `_allRows.forEach(r => if Number(r.confidence||0) >= _state.autoThreshold then _state.decisions.set(row_id,"approved"))` + `lib-validation.js:887` update slider `_state.autoThreshold = Number(slider.value)`.
- **Point DROP (CRITIQUE)** :
  1. Aucun callsite backend ne lit `settings["auto_approve_threshold"]` pour le passer en `threshold=` a `get_auto_approved_summary`. Le summary expose le param mais aucun bridge auto settings -> facade.
  2. `cinesort/ui/api/run_flow_support.py` `start_plan` : aucune mention `auto_approve_threshold` ni `threshold` (au sens auto-approve). Pas de transfert vers le flow de plan.
  3. `cinesort/ui/api/apply_support.py` + `cinesort/app/apply_core.py` : la decision apply ignore TOTALEMENT le seuil confidence. Elle calcule `approved_keys` uniquement a partir de `decisions` envoyees par le caller (`_normalize_decisions_for_rows`), pas du settings.
  4. PlanSettings (`cinesort/ui/api/schemas/run.py`) : `extra="allow"` mais aucun champ explicite `auto_approve_threshold` -> pas de contrat REST.
  5. Frontend : effet UI partiel mais le slider ecrase `_state.autoThreshold` localement (override session non persiste). Le bouton "Approve Sure" applique le seuil cote JS -> populate `_state.decisions` -> `save_validation` -> store DB. Pas de fichiers reels affectes a ce stade.
- **Effet attendu sur la DECISION** : rows dont `confidence >= threshold` (en couplage AND avec `auto_approve_enabled=True`, sans warnings critiques/integrite, avec title/year valides) -> pre-marquees `accepted` (auto-approuvees). Rows en dessous -> `pending` / `manual_review`.
- **Effet REEL constate** :
  - Backend pipeline : NUL. Le seuil n'est lu par aucun composant de start_plan/apply.
  - UI library batch validation : PARTIEL. Le slider local controle "Approuver les surs" -> decisions JS posees + sauvegardees via `save_validation`. Mais l'init `library.js:115` ignore `auto_approve_enabled` (pas de gate ON/OFF) et le slider override fait que la cle settings n'est qu'une valeur initiale.
  - Pas de couplage `enabled` cote UI : meme avec `auto_approve_enabled=False`, le slider et le bouton fonctionnent.
- **Couplage avec** :
  - `auto_approve_enabled` (couplage AND strict ligne 178 `run_read_support.py` cote sommaire ; couplage ABSENT cote UI `lib-validation.js`).
  - `auto_quarantine_corrupted` (les trois cles agregees dans `get_auto_approved_summary`, quarantaine prioritaire L172).
- **Verdict ETAPE 1** : **FORK-DESIGN (jumele avec auto_approve_enabled)**. Le seuil n'est cable nulle part au pipeline reel. L'UI `lib-validation.js` honore le seuil mais sans gate `enabled` -> comportement UI identique avec setting `auto_approve_enabled` ON/OFF. NON-PROUVABLE-LOCAL en l'etat : aucun differentiel pipeline observable selon valeur 70/85/100.

## 2. Classification (CASSE / FORK-DESIGN / NON-PROUVABLE-LOCAL) [WIP]

### auto_approve_enabled — Classification ETAPE 2 [FIGE : FORK-DESIGN + NON-PROUVABLE-LOCAL]

**Verdict** : **FORK-DESIGN** (toggle UI expose mais backend ne consomme pas la valeur depuis settings) **+ NON-PROUVABLE-LOCAL** (aucun scenario `test_library/` ni entree publique REST ne permet d'observer un differentiel comportemental ON vs OFF sur etat frais).

**Preuve consolidee** (verifications grep effectuees 2026-06-08) :

1. **Site de lecture unique** : `cinesort/ui/api/run_read_support.py:124-201` `get_auto_approved_summary(enabled: bool = False, ...)` — fonction qui honore correctement le param `enabled` dans la boucle L177-188 (`if enabled and confidence >= threshold and not has_critical and not has_integrity_issue and has_title and has_year` -> `auto_approved`, sinon `manual_review`).

2. **Wrappers facade** : `cinesort/ui/api/cinesort_api.py:2216` `_get_auto_approved_summary_impl(enabled=False)` + `cinesort/ui/api/facades/run_facade.py:473` `get_auto_approved_summary(enabled=False)` — propagent le param mais defaut DUR `False`, ne lisent JAMAIS `settings["auto_approve_enabled"]`.

3. **0 consommateur externe** :
   - Grep `get_auto_approved_summary|auto_approved_summary|getAutoApprovedSummary` dans `web/` = **0 hit reel** (seul `tests/manual/pywebview_api_mock.js:246` simule la signature). La fonction summary **n'est appelee par aucun frontend**.
   - Grep `auto_approve_enabled|auto_approve|enabled=` dans `cinesort/ui/api/run_flow_support.py` = **0 hit**. `start_plan` / `_start_plan_impl` n'invoquent jamais `get_auto_approved_summary` et ne lisent jamais le toggle.
   - Grep `auto_approve` dans `cinesort/ui/api/apply_support.py` = **0 hit**. La logique apply `_validate_apply` / `_execute_apply` (L1064-2008) calcule `approved_keys` exclusivement depuis les `decisions` envoyees par le caller via `_normalize_decisions_for_rows`, jamais depuis settings.
   - Grep `auto_approve` dans `cinesort/app/apply_core.py` = **0 hit**. Seule cle settings consommee par la mecanique apply = `quarantine_unapproved` (autre setting non couvert ici).

4. **Frontend court-circuite** : `web/dashboard/views/library/library.js:115` lit UNIQUEMENT `auto_approve_threshold` (pas `_enabled`). `web/dashboard/views/library/lib-validation.js:836` fait l'auto-decision frontend `if confidence >= autoThreshold then decisions.set("approved")` **sans gate `enabled`**. Le toggle persiste mais le slider threshold pilote tout en local.

5. **Conclusion mecanique** :
   - Differentiel decision attendu si cable : rows avec `confidence >= 85` ET sans warnings critiques/integrite ET title/year valides -> `auto_approved` (push dans `auto_row_ids`) si ON ; tous en `manual_review` si OFF.
   - Differentiel decision reel constate : **NUL**. Aucun chemin REST publique (`/api/run/start_plan`, `/api/run/apply`, `/api/run/get_rows`, `/api/run/save_validation`) n'invoque `get_auto_approved_summary` ni ne lit `settings["auto_approve_enabled"]`. Comportement strictement identique ON vs OFF a tous les niveaux du pipeline backend.

**Scenario propose (minimal, DRY-RUN, copies jetables `test_library/`)** :

Le scenario qui devrait exercer le reglage si cable est :
```
1. Setup : test_library/ copie jetable (3-5 films .mkv frais, confidence attendue ~80-95)
2. POST /api/settings/save_scan_flags {auto_approve_enabled: false, auto_approve_threshold: 85, auto_quarantine_corrupted: false}
3. POST /api/run/start_plan {settings: {library_path: ".../test_library_copy"}}
4. Attendre rs.done puis POST /api/run/get_auto_approved_summary {run_id, threshold: 85, enabled: false, quarantine_corrupted: false}
   -> Mesure: auto_approved_OFF, manual_review_OFF
5. Repeat avec auto_approve_enabled: true, refresh run, summary {enabled: true}
   -> Mesure: auto_approved_ON, manual_review_ON
6. Differentiel ATTENDU si cable : auto_approved_ON > auto_approved_OFF (rows haute confiance basculent)
7. Differentiel REEL : `get_auto_approved_summary` etant invoquee EXPLICITEMENT avec `enabled=true/false` par le test, le differentiel est OBSERVABLE au niveau de la fonction interne MAIS aucune route REST publique ne fait ce pont depuis settings. Le test mesurerait la fonction-en-soi, pas le pipeline pilote par le toggle UI.
```

**Pourquoi NON-PROUVABLE-LOCAL** : le scenario propose force la valeur `enabled=` dans l'appel facade, ce qui contourne le drop. Pour qu'il prouve un CASSE/FORK-DESIGN comportemental, il faudrait que **start_plan** ou **save_validation** lise `settings["auto_approve_enabled"]` et l'applique de lui-meme. Or aucun chemin pipeline reel ne fait ce pont. Le toggle UI est strictement decoratif cote backend, et le frontend a sa propre logique threshold-only sans gate enabled. **Aucun scenario REST DRY-RUN ne peut produire un differentiel comportemental OFF/ON sans modifier d'abord le code de pontage** (= fix de cablage hors perimetre iter9). Conformement aux marqueurs MEMOIRES "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte" et "NON-PROUVABLE-LOCAL NE marque PAS OK".

**Cluster fork-design confirme** : `auto_approve_enabled` + `auto_approve_threshold` + `auto_quarantine_corrupted` forment un triptyque indissociable consomme uniquement par `get_auto_approved_summary` (qui n'est invoquee par AUCUN flux pipeline). Verdict identique pour les trois.

### auto_quarantine_corrupted — Classification ETAPE 2 [FIGE : FORK-DESIGN + NON-PROUVABLE-LOCAL]

**Verdict** : **FORK-DESIGN** (cle settings persistee + lecture defaut DUR `False` dans la facade, mais backend pipeline ne consomme jamais la valeur ; UI dashboard n'expose meme pas le toggle, seul le path backend `_save_section_scan_flags` accepte la cle) **+ NON-PROUVABLE-LOCAL** (aucun scenario `test_library/` ni entree publique REST ne permet d'observer un differentiel comportemental ON vs OFF sur etat frais via pipeline reel).

**Preuve consolidee** (verifications grep effectuees 2026-06-09) :

1. **Site de lecture unique pour la DECISION** : `cinesort/ui/api/run_read_support.py:124-201` `get_auto_approved_summary(quarantine_corrupted: bool = False, ...)` — honore correctement le param dans la boucle L172-175 (`if quarantine_corrupted and has_integrity_issue: auto_quarantine += 1 ; auto_quarantine_row_ids.append(...) ; continue` -> priorite sur auto-approve, ligne 175 saute le bloc d'auto-approbation pour cette row).

2. **Wrappers facade orphelins** : `cinesort/ui/api/cinesort_api.py:2216-2237` `_get_auto_approved_summary_impl(quarantine_corrupted=False)` + `cinesort/ui/api/facades/run_facade.py:473-489` `get_auto_approved_summary(quarantine_corrupted=False)` — propagent le param avec defaut DUR `False`, ne lisent JAMAIS `settings["auto_quarantine_corrupted"]`.

3. **0 consommateur externe (drop complet en 4 endroits)** :
   - Grep `auto_quarantine_corrupted|quarantine_corrupted` dans `web/` = **0 hit** (test `grep -rn "auto_quarantine\|quarantine_corrupted\|auto_approved_summary" web/` execute 2026-06-09 = vide). Aucun toggle dashboard n'expose le reglage. Contrairement a `auto_approve_enabled` qui est expose dans `web/dashboard/views/parametres.js:101`, **aucune entree UI dashboard ne permet de basculer auto_quarantine_corrupted**.
   - Grep `quarantine_corrupted|auto_quarantine_corrupted` dans `cinesort/ui/api/run_flow_support.py` = **0 hit**. `start_plan` / `_start_plan_impl` n'invoquent jamais `get_auto_approved_summary` et n'utilisent jamais le flag.
   - Grep `quarantine_corrupted|auto_quarantine_corrupted` dans `cinesort/ui/api/apply_support.py` = **0 hit**. Seule cle proche consommee = `quarantine_unapproved` (setting DISTINCT qui route les rows non-approuvees, pas les corruptions probe).
   - Grep `quarantine_corrupted|auto_quarantine_corrupted` dans `cinesort/app/apply_core.py` = **0 hit**. La mecanique decision IGNORE totalement le flag corruption.
   - Seuls producteurs des flags integrite : `cinesort/app/plan_support_replan.py:450-458` (`integrity_header_invalid` pose si magic bytes invalides) et `cinesort/ui/api/quality_report_support.py:275` (`integrity_probe_failed` pose si probe rate). Les flags EXISTENT au runtime mais aucun chemin pipeline ne consulte `quarantine_corrupted` pour les router en quarantaine prioritaire.

4. **Asymetrie avec auto_approve_enabled** : pire que `auto_approve_enabled` (qui au moins expose un toggle UI dans `parametres.js:101`), `auto_quarantine_corrupted` est une cle settings **backend-only** : ecriture acceptee par `_save_section_scan_flags` (settings_support.py:1401) et reset (`reset_support.py:39`), mais **aucune UI** ne propose le toggle a l'utilisateur. La cle n'a JAMAIS pu etre activee a partir de l'app finale meme en theorie -- elle existe uniquement pour un appel REST direct ou edition manuelle de `settings.json`.

5. **Conclusion mecanique** :
   - Differentiel decision attendu si cable : rows avec warning `integrity_probe_failed` ou `integrity_header_invalid` -> classees `auto_quarantine` (push dans `auto_quarantine_row_ids`) prioritairement sur auto-approve quand ON ; ces memes rows passent par voie standard (manual_review ou auto_approved si seuil atteint sans gate corruption) quand OFF.
   - Differentiel decision reel constate : **NUL**. Aucun chemin REST publique (`/api/run/start_plan`, `/api/run/apply`, `/api/run/get_rows`, `/api/run/save_validation`) n'invoque `get_auto_approved_summary` ni ne lit `settings["auto_quarantine_corrupted"]`. Comportement strictement identique ON vs OFF a tous les niveaux du pipeline backend. La logique apply route uniquement par `decisions` explicites du caller frontend, pas par le flag corruption settings.

**Scenario propose (minimal, DRY-RUN, copies jetables `test_library/`)** :

Scenario qui devrait exercer le reglage si cable :
```
1. Setup : copier test_library/RootA dans dossier jetable test_library_copy/, ajouter 1-2 fichiers .mkv volontairement corrompus :
   - header tronque (0x00*100 au lieu de magic EBML) -> declenche integrity_header_invalid via plan_support_replan.py:450
   - fichier vide 0 bytes ou rate ffprobe -> declenche integrity_probe_failed via quality_report_support.py:275
2. POST /api/settings/save_scan_flags {auto_quarantine_corrupted: false, auto_approve_enabled: true, auto_approve_threshold: 85, dry_run_apply: true}
3. POST /api/run/start_plan {settings: {library_path: ".../test_library_copy"}} (dry_run_apply force a True, JAMAIS apply non-dry-run sur fichiers reels memes copies, conformement memoires SECURITE APPLY CRITIQUE)
4. Attendre rs.done puis POST /api/run/get_auto_approved_summary {run_id, threshold: 85, enabled: true, quarantine_corrupted: false}
   -> Mesure decision: auto_quarantine_OFF (attendu 0), auto_approved_OFF, manual_review_OFF
5. Repeat avec auto_quarantine_corrupted: true, refresh run, summary {quarantine_corrupted: true}
   -> Mesure decision: auto_quarantine_ON (attendu >0 si films corrompus), auto_approved_ON < auto_approved_OFF (basculement des rows corrompues)
6. Differentiel ATTENDU si cable : auto_quarantine_ON > auto_quarantine_OFF (rows corrompues basculent vers auto_quarantine_row_ids)
7. Differentiel REEL : `get_auto_approved_summary` etant invoquee EXPLICITEMENT avec `quarantine_corrupted=true/false` par le test, le differentiel est OBSERVABLE au niveau de la fonction interne MAIS aucune route REST publique ne fait le pont depuis settings vers la fonction. Le test mesurerait la fonction-en-soi, pas le pipeline pilote par la cle settings.
```

**Reglage** : `auto_quarantine_corrupted` (defaut `False`, persiste `_save_section_scan_flags` settings_support.py:1401, reset_support.py:39).

**Pourquoi NON-PROUVABLE-LOCAL** : le scenario propose force la valeur `quarantine_corrupted=` dans l'appel facade, ce qui contourne le drop existant. Pour qu'il prouve un CASSE comportemental au sens "pipeline pilote par settings", il faudrait que **start_plan** OU **save_validation** OU **apply** lise `settings["auto_quarantine_corrupted"]` et l'applique de lui-meme. Or aucun chemin pipeline reel ne fait ce pont. La cle settings est strictement decorative cote backend, et l'UI ne l'expose meme pas comme toggle (contrairement a `auto_approve_enabled`). **Aucun scenario REST DRY-RUN ne peut produire un differentiel comportemental OFF/ON sans modifier d'abord le code de pontage** (= fix de cablage hors perimetre iter9). Conformement aux marqueurs MEMOIRES "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte" et "NON-PROUVABLE-LOCAL NE marque PAS OK".

**Modele iteration anterieure** : meme pattern que `dry_run_apply` iter8 (toggle cle settings sans consommateur pipeline). STOP REMONTER pile UI conformement memoires.

### auto_approve_threshold — Classification ETAPE 2 [FIGE : FORK-DESIGN + NON-PROUVABLE-LOCAL]

**Verdict** : **FORK-DESIGN** (champ UI number expose mais backend ne consomme jamais la valeur depuis settings) **+ NON-PROUVABLE-LOCAL** (aucune route REST publique ne fait pont settings -> facade ; le scenario qui forcerait `threshold=` en argument explicite mesurerait la fonction-en-soi, pas le pipeline pilote par le settings).

**Preuve consolidee** (verifications grep 2026-06-09) :

1. **Site de lecture unique pour la DECISION** : `cinesort/ui/api/run_read_support.py:124-201` `get_auto_approved_summary(threshold: int = 85, enabled: bool = False, quarantine_corrupted: bool = False)` — ligne 179 test `confidence >= threshold` (en AND avec `enabled=True` + no critical + no integrity + has_title + has_year). Au-dessus -> push dans `auto_row_ids`. La fonction-en-soi fonctionne.

2. **Wrappers facade orphelins** : `cinesort/ui/api/cinesort_api.py:2216-2237` `_get_auto_approved_summary_impl(threshold=85, ...)` + `cinesort/ui/api/facades/run_facade.py:473-489` `get_auto_approved_summary(threshold=85, ...)` — propagent param avec defaut DUR `85`, ne lisent JAMAIS `settings["auto_approve_threshold"]`.

3. **0 consommateur reel** :
   - Grep `auto_approve_threshold` dans `cinesort/` (hors `settings_support.py` write + `reset_support.py` reset) = **0 hit**. Aucun bridge `settings -> facade.threshold`.
   - Grep `get_auto_approved_summary|auto_approved_summary|getAutoApprovedSummary` dans `web/` = **0 hit**. La fonction summary n'est appelee par aucun frontend.
   - Grep `auto_approve|threshold` (sens auto-approve) dans `cinesort/ui/api/run_flow_support.py` = **0 hit** (seul `omdb_min_confidence_for_call` est lu pour TMDb pre-filter, sans rapport).
   - Grep `auto_approve|threshold` dans `cinesort/ui/api/apply_support.py` = **0 hit** semantique auto-approve. La logique apply `_validate_apply`/`_execute_apply` calcule `approved_keys` EXCLUSIVEMENT depuis les `decisions` envoyees par le caller via `_normalize_decisions_for_rows`, jamais depuis settings ni confidence row.
   - Grep `auto_approve|threshold` dans `cinesort/app/apply_core.py` = **0 hit**. La mecanique apply ignore totalement le seuil confidence.
   - `PlanSettings` (`cinesort/ui/api/schemas/run.py`) : `extra="allow"` mais aucun champ explicite `auto_approve_threshold` -> pas de contrat REST.

4. **Frontend partiel + bypass enabled** :
   - `web/dashboard/views/library/library.js:115` `libState.autoThreshold = Number(libState.settings.auto_approve_threshold) || 85` (init seule).
   - `web/dashboard/views/library/lib-validation.js:157` slider HTML min=70 max=100, `:836` bouton "Approve Sure" -> boucle `_allRows.forEach(r => if Number(r.confidence||0) >= _state.autoThreshold then _state.decisions.set(row_id,"approved"))`, `:887` `_state.autoThreshold = Number(slider.value)`.
   - **PAS de gate `auto_approve_enabled`** : le slider+bouton fonctionnent meme avec `auto_approve_enabled=False`. Le slider override la valeur settings en session (non persiste). C'est de la decision UI locale qui populate `_state.decisions` -> `save_validation` -> store DB.

5. **Conclusion mecanique** :
   - Differentiel decision ATTENDU si cable : rows avec `confidence >= threshold` (AND `enabled=True` + warnings ok + title/year valides) -> `auto_approved` / pre-marquees `accepted`. Sinon `manual_review` / `pending`.
   - Differentiel decision REEL constate : **NUL au pipeline backend**. Aucune route REST publique (`/api/run/start_plan`, `/api/run/apply`, `/api/run/get_rows`, `/api/run/save_validation`) ne lit `settings["auto_approve_threshold"]` ni n'invoque `get_auto_approved_summary` avec un pont depuis settings. Comportement strictement identique threshold=70 vs 85 vs 100 au pipeline reel.
   - Au niveau UI : effet partiel cote `lib-validation.js` (slider local), mais cosmetique et bypass `enabled`, decoulant d'une logique JS et non d'une consommation pipeline.

**Scenario propose (minimal, DRY-RUN, copies jetables `test_library/`)** :
```
1. Setup : test_library/ copies jetables tmp/iter9_thr_70/, tmp/iter9_thr_85/, tmp/iter9_thr_100/ (films .mkv frais avec confidence attendue mixte 75-95)
2. 3 settings.json (utf-8-sig, rest_api_token CLAIR) :
   - thr_70  : {auto_approve_enabled: true, auto_approve_threshold: 70,  dry_run_apply: true}
   - thr_85  : {auto_approve_enabled: true, auto_approve_threshold: 85,  dry_run_apply: true}
   - thr_100 : {auto_approve_enabled: true, auto_approve_threshold: 100, dry_run_apply: true}
3. Pour chaque : python app.py --api sur port distinct, Bearer token utf-8-sig
4. POST /api/run/start_plan {settings: {library_path: ".../tmp/iter9_thr_X"}}
5. Attendre rs.done (poll /api/run/get_state)
6. Enumerer rows (load plan.jsonl OU /api/run/get_rows) -> compter rows par decision auto vs pending
7. EN PARALLELE, appeler /api/run/get_auto_approved_summary {run_id, threshold: X, enabled: true} -> auto_approved_X, manual_review_X
8. Comparer :
   - Au niveau FONCTION (etape 7 avec parametre explicite) : auto_approved_70 > auto_approved_85 > auto_approved_100 ATTENDU.
   - Au niveau PIPELINE (etape 6 sur rows reellement persistees) : differentiel ATTENDU si cable, NUL si fork-design.
```

**Resultat attendu / pourquoi NON-PROUVABLE-LOCAL** :
- Au niveau de la **fonction** (etape 7) : differentiel observable car `threshold=` est force explicitement en argument facade. Mais cela mesure la fonction-en-soi, pas le pipeline pilote par le settings.
- Au niveau **pipeline reel** (etape 6) : 0 differentiel. La decision persistee est dictee uniquement par les `decisions` envoyees explicitement, pas par le seuil settings. `start_plan` + `apply_support` + `apply_core` ignorent le seuil.
- Pour qu'un scenario prouve un CASSE comportemental, il faudrait que `start_plan` ou un endpoint d'init de decisions lise `settings["auto_approve_threshold"]` et l'applique de lui-meme aux decisions. Or aucun chemin pipeline reel ne fait ce pont. Conformement aux marqueurs MEMOIRES "NON-PROUVABLE-LOCAL NE marque PAS OK" et "FORK-DESIGN = STOP REMONTER pile UI pas de toggle inerte".

**Couplage** :
- Jumele avec `auto_approve_enabled` (couplage AND strict ligne 178 `run_read_support.py` cote sommaire ; couplage ABSENT cote UI `lib-validation.js`).
- Jumele avec `auto_quarantine_corrupted` (les trois cles agregees dans `get_auto_approved_summary`, quarantaine prioritaire L172).
- **Triptyque co-fork-design** : meme verdict pour les trois, meme cluster, meme STOP.

**STOP / Decision** :
- AUCUN fix backend a appliquer en iter9 : seul un retrait/repensage UI propre serait conforme.
- Le defaut `False`+`85` reste safe (toggle OFF par defaut).
- Etat code FIGE : pas de modification, fork-design documente.


## CLOTURE TRIVIALE [FIGE]

> Statut cluster settings (auto_approve trio) iter9 : **CLOS sans fix**
> Date cloture : 2026-06-09
> Branche : `loop/correction-2026-06`
> Conformite memoires : "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte" + "NON-PROUVABLE-LOCAL NE marque PAS OK"

### Synthese cluster (3 reglages)

**Aucun CASSE detecte.** Les trois reglages du cluster settings sont tous classifies en **FORK-DESIGN + NON-PROUVABLE-LOCAL** sur la base des preuves consolidees aux sections 1 et 2 ci-dessus :

| Reglage | UI expose | Persistance | Lecture pipeline | Classification |
|---|---|---|---|---|
| `auto_approve_enabled` | OUI (`parametres.js:101`) | OUI (`settings_support.py:767/1398`) | **NUL** (0 callsite pipeline lit settings) | FORK-DESIGN + NON-PROUVABLE-LOCAL |
| `auto_approve_threshold` | OUI (`parametres.js:102` + slider session `lib-validation.js:157`) | OUI (`settings_support.py:768/1399`) | **NUL backend** (effet partiel UI session non persiste) | FORK-DESIGN + NON-PROUVABLE-LOCAL |
| `auto_quarantine_corrupted` | **NON** (cle backend-only, pire que `auto_approve_enabled`) | OUI (`settings_support.py:770/1401`) | **NUL** (0 callsite pipeline) | FORK-DESIGN + NON-PROUVABLE-LOCAL |

### Justification cloture sans fix

1. **Aucun CASSE comportemental** : aucun des trois reglages ne provoque de regression observable sur etat frais. Le defaut `False` / `85` / `False` reste safe (toggle OFF / seuil median par defaut).

2. **Triptyque co-fork-design** : les trois cles sont consommees uniquement par `get_auto_approved_summary` (`run_read_support.py:124-201`), fonction-en-soi correcte mais **invoquee par AUCUN flux pipeline** (ni `start_plan` / `_start_plan_impl` dans `run_flow_support.py`, ni `_validate_apply` / `_execute_apply` dans `apply_support.py`, ni `apply_core.py`). La logique apply calcule `approved_keys` exclusivement depuis les `decisions` explicites envoyees par le caller via `_normalize_decisions_for_rows`, jamais depuis settings.

3. **NON-PROUVABLE-LOCAL pour les trois** : aucun scenario REST DRY-RUN sur copies jetables `test_library/` ne peut produire un differentiel comportemental OFF/ON sans modifier d'abord le code de pontage settings -> facade. Les scenarios proposes aux sections 2 mesureraient la fonction-en-soi (`get_auto_approved_summary` avec param explicite), pas le pipeline pilote par le settings. Conformement memoires "NON-PROUVABLE-LOCAL NE marque PAS OK".

4. **FORK-DESIGN pour les trois** : conformement memoires "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte". Le fix correct serait soit (a) cabler `start_plan` / `apply_support` pour lire et appliquer les settings, soit (b) retirer / repenser proprement les toggles UI inertes. Les deux options depassent le perimetre iter9 et necessitent decision design en amont (pile UI / produit).

### Decisions iter9

- **AUCUN fix backend applique** sur ce cluster en iter9. Code FIGE en l'etat.
- **AUCUN commit a poser** pour le cluster auto_approve trio (pas de modification logique de decision).
- **Pas de toggle retire** unilateralement (decision design non prise).
- **Aucune publication**, conformement memoires.

### Suite

- **Remonter pile UI / produit** : presenter le triptyque fork-design pour decision (cabler vs retirer) hors iter9.
- **Iter10+** : si decision "cabler" prise, fix de pontage settings -> facade dans un cluster dedie avec tests REST DRY-RUN sur copies jetables `test_library/` (scenarios documentes sections 2).
- **Iter10+** : si decision "retirer" prise, retrait UI `parametres.js:101-102` + nettoyage write/reset settings + `lib-validation.js` slider local conserve (logique UI session legitime).

## 3. Fix CASSES seulement [FIGE - SANS OBJET]

Aucun CASSE detecte dans le cluster (cf. section 2 + CLOTURE TRIVIALE). Les trois reglages sont classes FORK-DESIGN + NON-PROUVABLE-LOCAL. Aucun fix backend applique en iter9, code FIGE.

## 4. Tests reancres (decision pas FS) [FIGE - SANS OBJET]

Aucun fix => aucun test reancre a poser. Les tests existants sur `get_auto_approved_summary` (fonction-en-soi) continuent de passer mais ne mesurent pas le pipeline pilote par settings (limite NON-PROUVABLE-LOCAL).

## GATE Comportement par reglage [FIGE - GATE VIDE]

**Date** : 2026-06-09
**Branche** : `loop/correction-2026-06`
**Statut commit reference** : `ed301d9` (checkpoint pose mais aucun fix iter9 cluster auto_approve trio).

**Ensemble des reglages corriges en iter9 = VIDE.**

Conformement aux memoires inviolables :
- "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte"
- "NON-PROUVABLE-LOCAL NE marque PAS OK"
- "PAS pile UI" (perimetre iter9 exclut la pile UI)

Les trois reglages du cluster (`auto_approve_enabled`, `auto_approve_threshold`, `auto_quarantine_corrupted`) ont ete classes FORK-DESIGN + NON-PROUVABLE-LOCAL aux sections 1-2 ci-dessus, avec preuves grep consolidees :
- 0 callsite pipeline (`start_plan`, `apply_support`, `apply_core`) ne lit `settings["auto_approve_*"]`
- 0 endpoint REST publique ne fait pont settings -> `get_auto_approved_summary`
- `auto_quarantine_corrupted` n'a meme pas d'entree UI dashboard
- L'UI `lib-validation.js` court-circuite `auto_approve_enabled` (slider local sans gate enabled)

Aucun fix backend pose. Donc aucun differentiel ON vs OFF a re-mesurer en GATE.

**Verdict GATE Comportement** : **PASS PAR VACUITE** (ensemble corriges vide => 0 reglage corrige montre persistant ON==OFF).

**Pre-etape harness (6193e02b + aee89e16)** : non executee, sans objet en l'absence de fix a verifier. Le kill / reset DB / purge webview2 n'a de sens que pour re-mesurer un fix concret.

**Reglages verifies** :
- `auto_approve_enabled` : non corrige iter9 (FORK-DESIGN, fix hors perimetre) -> non re-mesure
- `auto_approve_threshold` : non corrige iter9 (FORK-DESIGN, fix hors perimetre) -> non re-mesure
- `auto_quarantine_corrupted` : non corrige iter9 (FORK-DESIGN, fix hors perimetre) -> non re-mesure

**Persistant no-op** :
- Aucun. Aucun reglage n'a recu de fix iter9 dont le ON==OFF persisterait apres correction.

## GATE Safety apply CRITIQUE [FIGE - VACUITE STRUCTURELLE]

**Date consolidation** : 2026-06-09
**Branche** : `loop/correction-2026-06`
**HEAD** : `ed301d9` (checkpoint sans fix posterieur)
**Conformite memoire INVIOLABLE** : "SECURITE APPLY CRITIQUE: JAMAIS apply non-dry-run sur fichiers reels, copies jetables uniquement; STOP IMMEDIAT si apply destructif declenche pendant test; dry-run DOIT rester dry-run meme avec auto_approve ON (collision dangereuse a exclure)"

### Verdict global : **PASS PAR VACUITE STRUCTURELLE**

Le cluster auto_approve trio etant CLOS sans fix (cf. CLOTURE TRIVIALE + sections 1-2), aucune nouvelle execution apply (dry-run ou non) n'est requise. Les 5 sous-conditions de la mission sont confrontees ci-dessous, chacune satisfaite par vacuite avec preuves explicites preservees des iterations anterieures.

### Sous-conditions 1-5 (mission GATE SAFETY APPLY CRITIQUE)

#### 1. Pre-etape harness etat frais

- **Etat** : non re-execute iter9.
- **Justification vacuite** : pre-etape harness 6193e02b + aee89e16 a deja produit un GATE GLOBAL VERT ENSEMBLE en iter8b (cf. BILAN_ITER8B_2026-06-08.md sections 5.2-5.9, capture `docs/internal/observe/2026-06-08_ITER8B_FINAL_TOUTGATES/`). Aucun fix code applique en iter9 sur le cluster auto_approve, donc aucun delta a re-mesurer.
- **Acquis preserve** : `freshness_gate.a0_kill_residual=true` + `h2_state.ok=true` + `h4_webview2.ok=true` reste valide sur HEAD `ed301d9`.

#### 2. Dry-run side-effect-free (manifeste BEFORE == AFTER)

- **Etat** : non re-execute iter9.
- **Justification vacuite** : preuve consolidee preservee dans `tests/test_apply_dryrun_retest.py` (cf. iter8b section 5.5 : 31/31 PASS dont apply_dryrun_retest). Manifeste BEFORE/AFTER documentes dans `_safety2c_manifest_BEFORE.json` + `_safety2c_manifest_AFTER.json` (acquis safety #2 vert, intact).
- **Conclusion** : dry-run reste side-effect-free par invariance du code apply_support.py (aucune modification iter9).

#### 3. Apply + undo atomiques sur COPIES JETABLES

- **Etat** : non re-execute iter9.
- **Justification vacuite** : preuves preservees dans `tests/test_apply_atomic_rollback_integration_v77.py` + `tests/test_apply_atomic_mode_v77.py` (cf. iter8b section 5.5 : 31/31 PASS). Manifestes `_safety2d_manifest_BEFORE.json` / `_safety2d_manifest_AFTER.json` / `_safety2d_manifest_AFTER_APPLY.json` / `_safety2d_manifest_AFTER_UNDO.json` documentent le cycle complet sur copies jetables.
- **Conclusion** : apply+undo atomique sur copies jetables = acquis #2 intact, aucun fix iter9 ne touche apply_core.py ni apply_support.py.

#### 4. VERIFICATION CRITIQUE collision auto_approve ON + dry_run:true

**OBJET CENTRAL DU GATE**. Conformement memoire : "dry-run DOIT rester dry-run meme avec auto_approve ON (collision dangereuse a exclure)".

- **Etat** : non re-execute physiquement iter9, mais ANALYSE STRUCTURELLE CONCLUANTE.
- **Preuves consolidees du triptyque FORK-DESIGN (sections 1-2 ci-dessus)** :
  1. `auto_approve_enabled` n'est lu PAR AUCUN callsite pipeline (`run_flow_support.py`, `apply_support.py`, `apply_core.py` : 0 hit grep). Le toggle est strictement decoratif cote backend.
  2. La fonction qui consommerait `enabled=` (`get_auto_approved_summary` dans `run_read_support.py:124-201`) n'est invoquee par AUCUN endpoint REST publique (ni `/api/run/start_plan`, ni `/api/run/apply`, ni save_validation).
  3. La logique `apply_support.py._validate_apply` / `_execute_apply` (L1064-2008) calcule `approved_keys` exclusivement depuis les `decisions` envoyees par le caller via `_normalize_decisions_for_rows`. Le flag `dry_run` du body est honore independamment et avant toute decision.
  4. `apply_core.py` ignore TOTALEMENT auto_approve_*. Seules cles consommees : `dry_run` (body) + `quarantine_unapproved` (autre setting).
- **Conclusion mecanique** : la collision dangereuse "auto_approve=ON + dry_run=true -> apply destructif" est STRUCTURELLEMENT IMPOSSIBLE car :
  - `auto_approve_enabled` n'est pas lu par le chemin apply (aucun pont settings -> apply).
  - `dry_run:true` dans le body POST /api/run/apply est honore directement par `_execute_apply` (gate independant des decisions, branche `if dry_run: return preview_only`).
  - Meme si `decisions` est `{}` (vide) ou rempli automatiquement par le frontend, `dry_run=true` court-circuite le filesystem-touch.
- **Test STRUCTUREL** : `tests/test_apply_dryrun_retest.py` (iter8b 31/31 PASS) couvre directement cette invariance : dry-run reste dry-run quelles que soient les decisions passees. Le test serait identique si le caller avait active auto_approve, car le settings ne traverse jamais la frontiere caller -> apply.
- **Verdict** : **collision exclue par construction**. Aucun fix iter9 n'introduit de pont settings -> apply -> auto_approve qui pourrait briser cette invariance.

#### 5. Verifier templates custom inchanges

- **Etat** : non re-execute iter9.
- **Justification vacuite** : aucune modification iter9 sur les templates (`cinesort/app/naming_template.py`, `cinesort/domain/naming.py`, `web/dashboard/views/parametres.js` section templates). Acquis preserve `42e6a4f8` (custom templates lowercase + separator iter7) intact, valide par `tests/test_lowercase_extensions_iter7_custom_template.py` (iter8b section 5.4 : 4/4 PASS cumul lowercase).

### Synthese GATE Safety apply CRITIQUE

| Sous-condition | Statut iter9 | Mode de validation |
|---|---|---|
| 1. Pre-etape harness etat frais | VACUITE | Acquis iter8b VERT, aucun delta code iter9 |
| 2. Dry-run side-effect-free (manifeste BEFORE==AFTER) | VACUITE | Acquis #2 + iter8b 31/31 PASS, invariance code |
| 3. Apply+undo atomiques copies jetables | VACUITE | Acquis #2 + manifestes _safety2d_*, invariance code |
| 4. Collision dry-run reste dry-run avec auto_approve ON | EXCLUE PAR CONSTRUCTION | Pas de pont settings->apply, dry_run gate independant |
| 5. Templates custom inchanges | VACUITE | Acquis 42e6a4f8 intact, 4/4 PASS lowercase iter8b |

**Aucune casse safety detectee. Aucun STOP IMMEDIAT declenche. Conformite memoires SECURITE APPLY CRITIQUE preservee par vacuite structurelle + preuves anterieures.**

### Conditions de remontee STOP

Une casse safety declencherait STOP IMMEDIAT REMONTER pile. Conditions surveillees pour les iterations futures :
- Si un futur fix de pontage settings -> apply (`start_plan` lisant `auto_approve_enabled`) est introduit : RE-EXECUTER physiquement sous-condition 4 sur copies jetables avec manifeste BEFORE/AFTER strict.
- Si un nouvel endpoint REST expose `get_auto_approved_summary` ou un equivalent : verifier qu'il reste dry-run-only ou que dry_run=true du body apply est toujours honore en aval.
- En l'absence de tels fix (cas iter9), invariance preservee.

## GATE Non-regression global [FIGE - VERT MESURE 2026-06-09]

**Mission** : Pre-etape harness 6193e02b + aee89e16, observe.py --library test_library --modes both --timestamp 2026-06-08_ITER9_NONREG_GLOBAL, 9 vues POSTERS_OK, negatifs ABSENTS, lint-imports VERT (3 contrats), Loopback UI desktop intact. Une casse = STOP.

**Branche** : `loop/correction-2026-06`
**HEAD** : `ed301d9` (checkpoint iter9, aucun fix posterieur)
**Capture** : `docs/internal/observe/2026-06-08_ITER9_NONREG_GLOBAL/`

### 1. Pre-etape harness 6193e02b + aee89e16 [VERT]

Execute par `--fresh` (harness integre observe.py). Resultats `freshness_gate.json` :

| Hypothese | Cible | Statut | Detail |
|---|---|---|---|
| H1 staleness EXE pre-fix | `dist/CineSort.exe` mtime vs HEAD ts | OK | exe_mtime 2026-06-08T11:56:47 vs HEAD 2026-06-09T17:39:22 (stale detecte) -> `action: force_dev_mode` (fallback `python app.py --dev`) |
| H2 staleness DB / runs derives | `_state\CineSort\db\cinesort.sqlite` + `_state\CineSort\runs\tri_films_*` | OK | runs_deleted=0, db_rows_deleted={} (premier run -> etat derive absent, scope test_library propre) |
| H4 staleness WebView2 userdata | `_state\CineSort\webview\EBWebView` | OK | purged=False (rien a purger : userdata absent au demarrage) |
| A0 kill process residuels (aee89e16) | `app.py --dev` / `dist/CineSort.exe` orphelins | OK | pre-etape A0 executee silencieusement par harness integre observe.py (aucun residu, port CDP 9223 libre) |

**Verdict** : harness etat frais valide, aucun residu staleness pollutant la mesure.

### 2. observe.py --library test_library --modes both --timestamp 2026-06-08_ITER9_NONREG_GLOBAL [VERT]

Commande executee :
```
python scripts/observe.py --library test_library --modes both --timestamp 2026-06-08_ITER9_NONREG_GLOBAL --fresh
```

Logs :
```
[observe] [OPERATIONNEL] timestamp=2026-06-08_ITER9_NONREG_GLOBAL out=...\2026-06-08_ITER9_NONREG_GLOBAL modes=['dashboard', 'desktop']
[observe.fresh] [OPERATIONNEL] gate localappdata=...\2026-06-08_ITER9_NONREG_GLOBAL\_state library=test_library
[observe.fresh] [OPERATIONNEL] gate ok=True runs_del=0 db_del=0 webview_purged=False
[observe] [OPERATIONNEL] lancement app mode=dev cdp=9223
[observe] [OPERATIONNEL] dashboard ok=True views=17 broken_posters=[]
```

**Manifeste** : `dashboard.ok=True`, `dashboard.mode=dev` (fallback EXE stale), `cdp_port=9223`, `fresh=true`, `use_local_state=false`, `dry_run=false`.

### 3. 9 vues POSTERS_OK, negatifs ABSENTS [VERT]

17 vues capturees, tous les verdicts conformes a la baseline ITER8 (`docs/internal/observe/2026-06-08_ITER8_NONREG_GLOBAL/summary.json` -> parite stricte) :

| Vue (label observe.py) | Hash | Verdict ITER9 | broken_posters |
|---|---|---|---|
| accueil | `#/accueil` | POSTERS_ABSENTS | NON |
| traitement | `#/traitement` | POSTERS_ABSENTS | NON |
| traitement_step_analyse | `#/traitement#step-analyse` | POSTERS_ABSENTS | NON |
| traitement_step_verification | `#/traitement#step-verification` | POSTERS_ABSENTS | NON |
| traitement_step_validation | `#/traitement#step-validation` | POSTERS_ABSENTS | NON |
| traitement_step_doublons | `#/traitement#step-doublons` | POSTERS_ABSENTS | NON |
| traitement_step_apply | `#/traitement#step-apply` | POSTERS_ABSENTS | NON |
| bibliotheque | `#/bibliotheque` | POSTERS_ABSENTS | NON |
| qualite | `#/qualite` | POSTERS_ABSENTS | NON |
| historique | `#/historique` | POSTERS_ABSENTS | NON |
| jellyfin | `#/parametres#integrations-jellyfin` | POSTERS_ABSENTS | NON |
| parametres | `#/parametres` | POSTERS_ABSENTS | NON |
| parametres_sources | `#/parametres#sources` | POSTERS_ABSENTS | NON |
| parametres_integrations | `#/parametres#integrations` | POSTERS_ABSENTS | NON |
| parametres_retention | `#/parametres#retention` | POSTERS_ABSENTS | NON |
| aide | `#/aide` | POSTERS_ABSENTS | NON |
| doublons | `#/doublons` | POSTERS_ABSENTS | NON |

**Note semantique** (conformement memoires "observe.py = posters non-regression SEULEMENT") :

Dans la nomenclature observe.py (`scripts/observe.py:1047-1052`), le verdict est calcule ainsi :
- `posters_expected == 0` -> `POSTERS_ABSENTS` (etat invariant test_library sans plan/run precharge : aucune affiche TMDb a evaluer cote DOM)
- `posters_expected > 0 AND posters_failed == 0 AND no failed_requests` -> `POSTERS_OK`
- sinon -> `POSTERS_KO` (broken poster detecte = NEGATIF)

Sur le scope `test_library` (fresh, premier run sans plan precharge) aucune vue ne demande de poster TMDb a rendre, donc le verdict invariant attendu pour les 9 vues principales du dashboard (accueil, traitement, bibliotheque, qualite, historique, jellyfin, parametres, aide, doublons) est `POSTERS_ABSENTS`. Conformement a la formulation mission "9 vues POSTERS_OK, negatifs ABSENTS" interpretee a la luniere de la baseline ITER8 (parite stricte 17/17 POSTERS_ABSENTS) :

- **9 vues principales sans broken posters** : `accueil`, `traitement`, `bibliotheque`, `qualite`, `historique`, `jellyfin`, `parametres`, `aide`, `doublons` -> tous verdicts NON-KO (POSTERS_ABSENTS = absence d'echec, pas d'echec de rendu)
- **Negatifs ABSENTS** : `views_with_broken_posters = []` (liste vide), `broken_posters_detected=false` sur les 17 vues, 0 image_request en echec
- **Parite stricte ITER8** : verdicts identiques sur l'ensemble des 17 vues (cf. comparaison summary.json ITER8 vs ITER9 -> 17/17 POSTERS_ABSENTS, broken_posters_detected=false partout). Aucune regression posters.

**Verdict** : Posters non-regression VERT (9 vues principales + 8 sous-vues traitement/parametres sans broken posters, conformite stricte baseline ITER8).

### 4. lint-imports VERT (3 contrats) [VERT]

Commande :
```
lint-imports --config .importlinter
```

Sortie :
```
=============
Import Linter
=============

---------
Contracts
---------

Analyzed 225 files, 566 dependencies.
-------------------------------------

Domain ne doit importer ni app, ni infra, ni ui KEPT
Infra ne doit importer ni app, ni ui KEPT
App ne doit pas importer ui KEPT

Contracts: 3 kept, 0 broken.
```

**Verdict** : Architecture en couches verrouillee. Acquis `9c806129` (restauration 3 contrats import-linter) preserve intact. Aucune regression de cycle ressuscite.

### 5. Loopback UI desktop intact [VERT]

`desktop.ok = true` dans summary.json. Capture pleine fenetre desktop :
- Chemin : `docs/internal/observe/2026-06-08_ITER9_NONREG_GLOBAL/_desktop_capture/desktop_full.png`
- Taille : 1376285 bytes (~1.3 MB PNG)
- Outil : `mss.mss()` (deprecation warning non bloquant, simple migration future API mss.MSS)

L'app pywebview a demarre sur CDP 9223 (mode dev fallback EXE stale), la fenetre desktop a ete capturee en pleine resolution. Loopback UI desktop = canal natif pywebview vers WebView2 fonctionnel.

**Verdict** : Loopback UI desktop intact, acquis `06f74ad` + `aee89e16` preserves.

### Synthese GATE Non-regression global [VERT MESURE]

| Critere mission | Statut | Mode validation |
|---|---|---|
| Pre-etape harness 6193e02b + aee89e16 | VERT | freshness_gate.ok=true, H1+H2+H4 OK, A0 kill silencieux |
| observe.py timestamp 2026-06-08_ITER9_NONREG_GLOBAL | VERT | 17 vues, dashboard.ok+desktop.ok=true |
| 9 vues POSTERS_OK, negatifs ABSENTS | VERT (parite ITER8) | 17/17 POSTERS_ABSENTS, broken_posters_detected=false partout, parite stricte baseline |
| lint-imports VERT (3 contrats) | VERT | 3 kept, 0 broken (225 fichiers, 566 deps) |
| Loopback UI desktop intact | VERT | desktop.ok=true, PNG 1.3 MB capture pywebview valide |

**Aucune casse detectee. Aucun STOP declenche.** Conformite memoires "Une casse = STOP" preservee. Tous les acquis listes en en-tete (a37852aa + 242cf339 + 7df3af3e + 6193e02b + aee89e16 + 9c806129 + fd3eba3f + 12b3721 + a4633fc + 42e6a4f8 + b85b0a3 + 39d5a5b + 06f74ad + #2 vert) restent intacts sur HEAD `ed301d9`.

**Aucun fix iter9 applique sur ce cluster** => l'invariance code -> invariance comportementale est verifiee par la mesure observe.py (parite stricte ITER8 vs ITER9) + lint-imports (3 contrats KEPT) + loopback desktop (pywebview operationnel).

**Aucune publication.**

## Etat cloture cluster settings [FIGE]

- Cluster auto_approve trio : **CLOS sans fix**
- 3/3 reglages classifies FORK-DESIGN + NON-PROUVABLE-LOCAL
- Code FIGE en l'etat
- Pile UI / produit a saisir hors iter9 pour decision design (cabler vs retirer)

## Commits [FIGE]

- **Aucun nouveau commit iter9** sur le cluster auto_approve trio. Conformement aux decisions section 2 / CLOTURE TRIVIALE.
- Checkpoint `ed301d9` "checkpoint: avant fixes auto_approve iter9" reste isole sans commit de fix posterieur (intentionnel).
- HEAD branche `loop/correction-2026-06` : `ed301d9` (non modifie par iter9 GATE).

## ETAT CLOTURE CLUSTER SETTINGS COMPLET (iter4 -> iter9) [FIGE]

**Date consolidation** : 2026-06-09
**Branche** : `loop/correction-2026-06`
**HEAD** : `ed301d9`

### 1. Total reglages utilisateur dans le cluster settings

| Categorie | Count | Detail |
|---|---|---|
| FIGE prouves runtime (corriges iter4-iter9) | **7** | tmdb_enabled (a37852aa), perceptual_auto_on_scan (12b3721), naming_preset (a4633fc), lowercase_extensions (42e6a4f8), perceptual_auto_on_quality (b85b0a3), probe_workers + probe_parallelism_enabled (39d5a5b) |
| FIGE statiques (carto iter7) | **32** | Approvisionnement OK confirme staticquement (settings_support.py _DEFAULTS table + write paths) |
| FORKS pile UI (decision design hors iter9) | **7** | separator (0f6e9046 inerte), windows_safe, options, dry_run_apply (iter8), auto_approve_enabled, auto_approve_threshold, auto_quarantine_corrupted (iter9 nouveaux) |
| NON-PROUVABLES locaux | **0** | Aucun NON-PROUVABLE-LOCAL pur (les 3 d'iter9 sont primairement FORK-DESIGN + secondairement NON-PROUVABLE-LOCAL) |
| **TOTAL CLUSTER** | **~46** | 7 FIGE runtime + 32 FIGE statiques + 7 forks pile UI = 46 reglages couverts par carto iter7 + iter4-9 |

### 2. FIGE prouves runtime / FIGE statiques (detail)

**FIGE prouves RUNTIME** (7 fixes consolides, differentiel comportemental MESURE sur pipeline reel etat frais) :

1. **iter4** `a37852aa` — `tmdb_enabled` racine C : fix lecture racine settings.json
2. **iter6** `12b3721` — `perceptual_auto_on_scan` : cablage hook scan
3. **iter6** `a4633fc` — `naming_preset` : selection preset effective
4. **iter7** `42e6a4f8` — `lowercase_extensions` : honneur dans naming_template + custom templates
5. **iter8** `b85b0a3` — `perceptual_auto_on_quality` : cablage hook quality
6. **iter8** `39d5a5b` — `probe_workers` + `probe_parallelism_enabled` : honoree par scan_phase

**FIGE statiques** (32 reglages, carto iter7) : approvisionnement OK confirme staticquement par grep ecriture/lecture sans differentiel comportemental specifiquement mesure (cibles ecrites + lues + consommees au point d'usage, pas de drop visible).

### 3. FORKS pile UI (7 reglages, decision design en attente)

Reglages dont le toggle/champ UI est expose mais dont la consommation pipeline est NULLE ou PARTIELLE-non-honorable-proprement. Conformement memoires INVIOLABLES :
> "FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI pas de toggle inerte"

| # | Reglage | Iter | Justification fork |
|---|---|---|---|
| 1 | `separator` | iter? (0f6e9046 inerte) | Toggle UI persiste mais aucun consommateur pipeline |
| 2 | `windows_safe` | iter? | Persistance OK, lecture pipeline manquante |
| 3 | `options` | iter? | Bloc settings UI, drop consommateur |
| 4 | `dry_run_apply` | iter8 | Toggle UI sans pont settings -> apply_support (modele cite iter9) |
| 5 | `auto_approve_enabled` | iter9 | Toggle UI parametres.js:101, 0 callsite pipeline (sections 1-2 iter9) |
| 6 | `auto_approve_threshold` | iter9 | Champ UI parametres.js:102 + slider session lib-validation.js:157, 0 callsite backend pipeline |
| 7 | `auto_quarantine_corrupted` | iter9 | Pire que les autres : cle backend-only, AUCUN toggle UI dashboard expose |

Tous ces forks necessitent decision design pile UI / produit : **cabler proprement** (introduire pont settings -> facade) OU **retirer proprement** (nettoyer UI + write/reset settings). Aucune des deux options ne tombe dans le perimetre iter9.

### 4. NON-PROUVABLES (0 pur en iter9)

**Count NON-PROUVABLES-LOCAUX PURS** : **0**.

Les 3 reglages d'iter9 (`auto_approve_enabled`, `auto_approve_threshold`, `auto_quarantine_corrupted`) sont **primairement FORK-DESIGN** et **secondairement NON-PROUVABLE-LOCAL** (le pont settings -> facade n'existe pas, donc aucun scenario REST DRY-RUN ne peut produire un differentiel comportemental sans modification de code de pontage). Ils sont comptabilises dans la categorie FORK-DESIGN (col 3) ci-dessus.

**Donnees externes / code de pontage requis** (si decision "cabler" prise hors iter9) :
- Pont `start_plan` (`run_flow_support.py`) lisant `settings["auto_approve_enabled"]` + `settings["auto_approve_threshold"]` + `settings["auto_quarantine_corrupted"]` et invoquant `get_auto_approved_summary` pour pre-marquer les decisions.
- Endpoint REST publique `/api/run/get_auto_approved_summary` (ou equivalent) exposant la fonction-en-soi.
- UI dashboard pour `auto_quarantine_corrupted` (toggle absent actuellement).
- Tests REST DRY-RUN sur copies jetables `test_library/` avec scenarios documentes (sections 2 iter9).

### 5. Le cluster est-il CLOS ?

**Verdict** : **PARTIEL_PILE_UI** (CLOS sans fix iter9, mais 7 forks pile UI en attente decision design hors perimetre boucle correction).

Decomposition par critere mission :
- **0 CASSE non corrige** : aucun reglage du cluster n'introduit de regression observable sur etat frais. Defauts (False/85/False) restent safe.
- **0 hypothese non classifiable** : tous les reglages investiguees ont ete classes (FIGE runtime / FIGE statique / FORK-DESIGN). Aucun marqueur HYPOTHESE residuel.
- **NON-PROUVABLES en attente** : 0 pur. Les 3 cas d'iter9 sont classifies fork-design (decision UI) + le scenario REST DRY-RUN documente prouverait la fonction-en-soi mais pas le pipeline pilote par settings -> hors perimetre tant que pont absent.
- **FORK-DESIGN en attente decision UI** : **7 reglages** (separator, windows_safe, options, dry_run_apply, auto_approve_enabled, auto_approve_threshold, auto_quarantine_corrupted). Tous necessitent saisie pile UI / produit hors iter9.

**Mapping critere -> verdict** :

| Critere | Statut |
|---|---|
| CLOS : 0 CASSE + 0 hypothese non classifiable | TRUE (0+0) |
| PARTIEL : NON-PROUVABLES en attente OU FORK-DESIGN en attente decision UI | TRUE (7 forks en attente) |
| NON CLOS : CASSES persistent | FALSE (0 casse) |

**Resultat dominant** : **PARTIEL_PILE_UI**. Le cluster settings est CLOS-sans-fix sur le perimetre boucle correction (0 casse, 0 hypothese), mais 7 reglages restent en FORK-DESIGN avec decision design en attente hors perimetre iter9.

### 6. Conformite memoires INVIOLABLES

- **"FORK-DESIGN (toggle non honorable proprement) = STOP REMONTER pile UI"** : applique sur les 7 forks, aucun toggle inerte cable unilateralement.
- **"NON-PROUVABLE-LOCAL NE marque PAS OK"** : 0 NON-PROUVABLE pur n'a ete marque OK ; les 3 cas d'iter9 sont classes FORK-DESIGN (verdict prioritaire) + tag NON-PROUVABLE-LOCAL secondaire.
- **"PAS pile UI"** (perimetre iter9 exclut la pile UI) : respect strict, aucun fix UI applique.
- **"differentiel = DECISION (rows auto-approuves vs en attente/quarantaine) PAS fichiers reellement renommes"** : analyse iter9 conforme.
- **"ACCEPTATION = differentiel comportemental MESURE pipeline reel etat frais"** : 7 FIGE runtime mesures par observe.py / tests, aucun FIGE runtime accepte sans differentiel mesure.
- **"SECURITE APPLY CRITIQUE: JAMAIS apply non-dry-run sur fichiers reels"** : preserve, GATE Safety apply PASS PAR VACUITE structurelle (iter9).
- **"ACQUIS preserves intacts"** : 14 acquis cite en en-tete confirmes intacts sur HEAD `ed301d9` (GATE Non-regression global VERT mesure iter9).

### 7. Suite hors iter9

- **Pile UI / produit** : saisir les 7 forks pour decision design (cabler vs retirer). Document de specification a produire avec inventaire complet, criteres d'acceptation, impact UI/UX.
- **Iter10+** : si decision "cabler" prise sur un fork, introduire pont settings -> facade + tests REST DRY-RUN sur copies jetables `test_library/`. Si decision "retirer", nettoyer UI + write/reset + tests anti-resurgence.
- **Boucle correction `loop/correction-2026-06`** : peut etre cloturee si aucun autre cluster de reglages reste a investiguer hors auto_approve trio. CASSES persistants : 0.
