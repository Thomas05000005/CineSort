# BILAN ITER 15 - Cosmetique + Hygiene Final - 2026-06-08

> Branche: loop/correction-2026-06
> DERNIER run autonome
> Acquis preserves: TOUS commits iter4-14 intacts
> Perimetre strict: #1 + 5.2 + 5.3 + 5.4 UNIQUEMENT
> Statut: [WIP]

## EN TETE [WIP]

## 1. Item #1 probe-cache breakdown (plan_support_core.py) [DONE]

### Symptome
Test rouge : `tests/test_core_heuristics.py::CoreHeuristicsTests::test_plan_library_collects_ignored_extensions_breakdown`.
Assertion `stats.analyse_ignores_par_raison.get("ignore_non_supporte", 0) >= 1` echouait avec `0` quand un dossier "bruit" (note.txt + poster.jpg + info.nfo, aucune video) etait scanne.

### Diagnostic — CLASSE : preexistant_avant_iter13
- Le cache probe iter13 (commit b943292d) concerne uniquement `cinesort/infra/probe/disk_cache.py` (cache disque JSON pour ffprobe/mediainfo). AUCUN rapport avec le compteur `ignore_non_supporte` du scan.
- Le cache scan/folder (`_try_apply_folder_cache`) court-circuite OUI le traitement, MAIS le test cree un repertoire neuf (tempfile, jamais vu auparavant) -> cache_hit=False, on prend la branche normale.
- Le smoking-gun est dans `cinesort/app/plan_support_core.py` L884-885 (commit 198a33a "fix(ui): corrige 9 bugs critiques scan reel (v166)" — SCAN-1) :
  ```python
  if (delta_ext + delta_size + delta_name + delta_scandir) == 0:
      core_mod._stats_add_ignore(ctx.stats, "ignore_non_supporte")
  ```
  Pour un dossier avec .txt/.jpg/.nfo : `iter_videos` categorise chaque fichier en `ignore_extension`, donc `delta_ext = 3`. La somme != 0 -> `ignore_non_supporte` JAMAIS incremente. Or ce compteur est le ROLLUP DOSSIER ("aucun fichier video exploitable", cf core.py L1365), pas un compteur file-level. Les deltas et le rollup sont independants. Le test exige BOTH (>=1 sur `ignore_non_supporte` ET >=1 sur `.txt`/`.jpg`).

### Fix applique (au bon etage : `cinesort/app/plan_support_core.py`)
Suppression de la condition `if == 0` : on bump systematiquement `ignore_non_supporte` quand `videos == []`. Les compteurs file-level (`ignore_extension`, etc.) ne sont pas touches — ils continuent de s'accumuler en parallele.

### Verifications
- Test cible : ROUGE -> VERT (`test_plan_library_collects_ignored_extensions_breakdown PASSED`).
- Non-regression scan/plan_library : `tests/test_core_heuristics.py tests/test_scan_parallel_v77.py tests/test_multi_root.py` : 69/69 PASSED.
- Cache probe iter13 resilience : `tests/test_probe_cache.py tests/test_error_resilience.py` : 27/27 PASSED (x2.9 + 0 re-sonde inchanges conservs).
- `py_compile plan_support_core.py` : OK.
- `lint-imports` : 3 contracts kept (`domain`/`infra`/`app` boundaries respectes — modification cantonnee a `app/`).

### Acquis preserves
- AUCUN refactor du cache probe iter13 (perimetre strict respecte).
- Compteurs file-level (`delta_ext` / `delta_size` / `delta_name` / `delta_scandir`) inchanges.
- UI dashboard "X films exclus" continue de fonctionner via `films_rejected_ext` (+ rollup `ignore_non_supporte` qui retrouve sa semantique correcte).

## 2. Item 5.2 token verbose [DONE]

### Constat (avant fix)
Lors d'iterations precedentes (4 hotfixes auth + iter5 + iter14), des dumps de codepoints
exhaustifs et messages `[REST] AVERTISSEMENT` / `console.error` ont ete installes pour
identifier la cause racine des 401 silencieux (BOM U+FEFF, NBSP, percent-decode). Cette
chasse au bug est CLOSE — le bypass loopback iter14 (rest_server.py L433-462) rend
benigne tout 401 local. Resultat aujourd'hui : la console F12 et stderr du backend sont
inondes a chaque boot natif normal de messages qui ne sont plus actionnables.

### Sites identifies
1. `web/dashboard/core/api.js::_safeBearer` — 5 `console.error` (token absent, BOM/NBSP detecte, REJECT non-ASCII x3) qui se declenchaient meme en mode natif desktop.
2. `app.py` boot natif L823-826 — `print("[REST] main_url = ... ntoken=...")` systematique a chaque boot, hors mode CINESORT_DEBUG.
3. `app.py` boot natif L846 — `print("[REST] AVERTISSEMENT : main_url SANS ntoken ...")` via `print()` brut (pas logger, donc hors scrubber/categorisation).

### Fix applique
- **JS `_safeBearer`** : detection `_isQuietContext = (window.__CINESORT_NATIVE__ === true) && (location.hostname === "127.0.0.1"|"localhost")`. Helper `_log = _isQuietContext ? console.debug : console.error`. Les 5 sites de log utilisent `_log(...)` au lieu de `console.error(...)`. En mode natif+localhost les messages partent en debug (filtre par defaut F12), en mode web/LAN ils restent en error (visible pour investigation).
- **`app.py` main_url ntoken** : log `[REST] main_url = ...` deplace dans la branche `if _DEBUG_NB:` (alignement avec les autres `[DEBUG-NTOKEN]` deja gates).
- **`app.py` AVERTISSEMENT** : `print(...)` migre vers `logger = logging.getLogger("cinesort.app.native_boot").error(...)` — passe par scrubber global, categorisable, niveau ERROR conserve (c'est une vraie anomalie, juste mieux routee).

### Garde-fou
- AUCUNE modification de la logique auth (`_check_auth`, `_has_bearer_header`, `_is_rate_limited`, `_send_unauthorized` — tous inchanges).
- Bypass loopback iter14 inchangé (rest_server.py L433-462).
- Token resolution / URL injection / normalisation : pure presentation (niveau de log uniquement).

### Verifications
- `py_compile app.py cinesort/infra/rest_server.py` : OK.
- `node --check web/dashboard/core/api.js` : OK.
- `lint-imports` : 3 contracts kept.
- Matrice auth iter5/iter14 :
  - `tests/test_facade_endpoints_auth_unifie.py` : 16/16 PASSED.
  - `tests/test_log_context.py` : 16/16 + 15 subtests PASSED.
  - `tests/test_dashboard_infra.py` : 23/23 PASSED.
  - 4 failures pre-existantes dans `tests/test_rest_security.py` (410 Gone legacy, anterieures a 5.2, confirmees par stash+test sans modif).

### Acquis preserves
- Bypass loopback iter14 INTACT.
- Cache probe iter13 INTACT (perimetre 5.2 = pur logging, hors scope #1).
- Scrubber secrets + rate-limiter + exempt local : INCHANGES.

## 3. Item 5.3 skeletons CSS [DONE]

### Constat (avant fix)
Plusieurs vues du dashboard injectaient des `style="..."` inline directement dans le
template HTML pour habiller les skeletons de chargement :

- `web/dashboard/views/bibliotheque.js` L584 — 5x `<div class="v5-skeleton" style="height:64px;margin:8px 0;">`
- `web/dashboard/views/doublons.js` L454 — 4x `<div class="v5-skeleton doublons-section-skel" style="height:60px;margin:8px 0;">` (hauteur **60px**)
- `web/dashboard/views/traitement.js` L1491 — 3x `<div class="v5-skeleton" style="height:48px;margin:8px 0;">` (hauteur **48px**)

Trois vues, trois hauteurs differentes (64 / 60 / 48). Le style inline melange
presentation et template, et duplique des valeurs qui devraient vivre cote CSS.

### Diagnostic — CLASSE : cosmetique_hygiene
Pas de bug fonctionnel : le rendu est correct, le cycle de vie iter11
(`present pendant chargement -> remplace par contenu reel`) fonctionne. C'est
purement une dette de lisibilite et un manque de classe CSS dediee pour le pattern
"ligne skeleton 64px". Aucune urgence runtime, mais le perimetre 5.3 vise
exactement ce nettoyage.

### Fix applique (perimetre strict)
1. Ajout de la classe `.v5-skeleton-row` dans `web/shared/components.css` (sous
   le bloc SKELETONS existant L546+), valeurs IDENTIQUES a l'inline (`height: 64px; margin: 8px 0;`).
2. `bibliotheque.js` L584 : `class="v5-skeleton" style="..."` -> `class="v5-skeleton v5-skeleton-row"`.
   La classe `v5-skeleton` est CONSERVEE (le test runtime iter11 cible
   `.v5-skeleton` et `.bibliotheque-section .v5-skeleton` — ce selecteur trouve
   toujours le noeud).
3. `doublons.js` (60px) et `traitement.js` (48px) : **PAS TOUCHES** — hauteurs
   DIFFERENTES de 64px. La consigne 5.3 interdit changer dimensions / cycle iter11
   prouve ; uniformiser a 64px casserait l'invariant. Documentation laissee dans
   le commentaire CSS pour expliquer pourquoi 60/48 restent en inline.

### Garde-fou
- AUCUN changement de dimension : `.v5-skeleton-row` = 64px exactement, valeurs
  identiques a l'ancien style inline (`height:64px;margin:8px 0;`).
- Animation inchangee : `.v5-skeleton-row` n'override pas le `background` /
  `animation` du `.v5-skeleton` parent (cascade preservee, shimmer continue).
- Classe `v5-skeleton` CONSERVEE sur le noeud (test runtime iter11 :
  `document.querySelectorAll('.v5-skeleton').length` reste decompte correctement).
- `@layer v5` brace pre-existante : non touchee (modification dans le bloc
  SKELETONS hors layer).
- Tier-colors invariants : aucune regle ajoutee/modifiee dans tokens.css
  (Platinum #E5E4E2, Gold #FFD700, Silver #C0C0C0, Bronze #CD7F32 intacts).

### Verifications
- `node --check web/dashboard/views/bibliotheque.js` : OK.
- Tests frontend dedup audit + processing/library/home v5 + skeleton_states + v5_helpers :
  `tests/test_skeleton_states.py tests/test_v5_helpers.py tests/test_library_v5_ported.py tests/test_library_film_v5_features.py tests/test_home_v5_features.py tests/test_home_v5_ported.py tests/test_processing_v5_features.py tests/test_processing_v5_ported.py tests/test_qij_v5_features.py tests/test_frontend_dedup_audit.py` : 37/37 PASSED + 6 skipped.
- Cycle iter11 `test_runtime_skeleton_lifecycle.py` : ERROR (fixture conftest
  `loginToken` hidden — environnement Playwright sans backend lance) — PRE-EXISTANT
  confirme par `git stash + retest sans modifs` : meme erreur. Aucune regression
  due a 5.3.
- `git diff --stat web/` : 2 fichiers, +11/-1.

### Acquis preserves
- Cycle iter11 `present->remplace` : selecteur DOM `.v5-skeleton` inchange, classe
  conservee sur le noeud (logiquement intact).
- Bypass loopback iter14 : INTACT (5.3 ne touche que CSS+template HTML, aucun code auth).
- Cache probe iter13 : INTACT (5.3 hors scope #1).
- Hauteurs 60px (doublons) / 48px (traitement) : INTACTES en inline, documentees.
- Tier-colors hex : `tokens.css` non touche.
- Aucun refactor adjacent (perimetre 5.3 = bibliotheque.js uniquement, refus d'uniformiser).
## 4. Item 5.4 encodage console + bruit log boot [DONE]

### Constat (avant fix)
Deux familles de bruit residuel post-iter14 :

1. **UnicodeEncodeError cp1252 dans scripts/** : sous Windows, `sys.stdout` herite par defaut de `cp1252` quand le terminal n'a pas `PYTHONUTF8=1`. Les `print(...)` avec em-dash (U+2014) ou accents leves dans 3 scripts crashent ou affichent `?` :
   - `scripts/_iter2_scan.py` L106 et L153 : `f"... DONE in {elapsed:.1f}s — final status=..."` (em-dash)
   - `scripts/scan_smoketest_lib.py` L96 : meme pattern em-dash
   - `scripts/download_webview2_fixed.py` L61 : `"CineSort V3.1 — Bundle WebView2 ..."`

2. **Bruit log boot** :
   - `cinesort/infra/db/migration_manager.py` L322 : `logger.warning("db: tracking schema_migrations a echoue pour v%d ...")` se declenche 11 fois au boot sur DB neuve (v1..v11 AVANT que migration 012 cree la table de tracking). Idempotent, non bloquant, mais bruyant en stderr.
   - `app.py` L854 : log `[REST] AVERTISSEMENT main_url SANS ntoken` deja migre vers `logger.error` lors de 5.2. ITER15 5.4 enrichit le contexte diagnostic.

### Diagnostic — CLASSE : cosmetique_hygiene + observabilite
- **Encodage console** : pas un bug fonctionnel mais un crash potentiel selon terminal. `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` au plus tot dans le script (avant tout `print()`) corrige proprement, avec un fallback `errors="replace"` qui evite tout crash futur si un codepoint exotique se glisse.
- **Schema_migrations tracking** : le cas dominant est "no such table: schema_migrations" pour v1..v11 sur DB neuve. C'est ATTENDU et idempotent (la migration 012 cree la table puis le tracking marche). Warning bruyant, pas actionnable -> debug.
- **main_url SANS ntoken** : deja migre vers `logger.error` par 5.2. 5.4 enrichit le contexte diagnostic (proto, port, token_type, token_falsy) pour faciliter le post-mortem si jamais le cas resurgit en LAN/web.

### Fix applique (pur logging, AUCUN effet runtime produit)

1. **`scripts/_iter2_scan.py`** : ajout d'un bloc tete-de-script (avant tout `print`) qui reconfigure `sys.stdout`/`sys.stderr` en UTF-8 avec `errors="replace"`, garde-fou `try/except AttributeError|ValueError` pour les streams non-standard (capture pytest, redirection, etc.).
2. **`scripts/scan_smoketest_lib.py`** : meme bloc, meme rationale. L'em-dash L96 ne fait plus crasher.
3. **`scripts/download_webview2_fixed.py`** : meme bloc. L'em-dash L61 ("CineSort V3.1 — Bundle") s'affiche correctement quel que soit le terminal.
4. **`cinesort/infra/db/migration_manager.py`** L322 : `logger.warning(...)` -> `logger.debug(...)`. Commentaire ITER15 5.4 explique le rationale (idempotent, non critique, dominante "no such table" pre-v012). Le log reste accessible via `LOG_LEVEL=DEBUG` pour diagnostiquer DB lock transitoire.
5. **`app.py`** L854 : enrichissement du contexte diagnostic dans l'appel `logger.error(...)` (proto, port, token_type, token_falsy). Le niveau ERROR est CONSERVE (deja migre par 5.2). Aucune modification de comportement.

### Garde-fou
- **AUCUN effet runtime produit** : changements limites a `print()` initialisation (scripts), niveaux de log (warning->debug), enrichissement messages (error). Aucune branche conditionnelle, aucun nouveau side-effect.
- **Auth iter5/iter14 INCHANGES** : aucune modification de `_check_auth`, `_has_bearer_header`, `_is_rate_limited`, ni du bypass loopback rest_server.py L433-462.
- **Cache probe iter13 INTACT** : 5.4 ne touche pas `cinesort/infra/probe/disk_cache.py` ni `cinesort/app/plan_support_core.py`.
- **Cycle iter11 INTACT** : 5.4 ne touche aucun CSS/template HTML.
- **Tier-colors hex INTACTS** : aucune modification de `tokens.css`.
- **Code produit `cinesort/`** : seul `migration_manager.py` est touche (downgrade niveau de log uniquement, semantique du tracking inchangee).

### Verifications
- `py_compile` : `scripts/_iter2_scan.py scripts/scan_smoketest_lib.py scripts/download_webview2_fixed.py cinesort/infra/db/migration_manager.py app.py` : OK.
- `lint-imports` : 3 contracts kept (`domain_pure`, `infra_bounded`, `app_bounded`).
- Smoke pattern `reconfigure` : `python -c "import sys; sys.stdout.reconfigure(encoding='utf-8', errors='replace'); print('test — em-dash')"` affiche correctement sous PowerShell sans crash.
- Tests cibles :
  - `tests/test_migration_chain.py tests/test_migration_rollback.py tests/test_migrations_from_legacy.py` : 29/29 PASSED.
  - `tests/test_probe_cache.py tests/test_error_resilience.py tests/test_core_heuristics.py` : 54/54 PASSED.
  - `tests/test_facade_endpoints_auth_unifie.py tests/test_log_context.py tests/test_dashboard_infra.py` : 54/54 + 15 subtests PASSED en isolation (1 flaky timing dans run combine `RateLimitHttpTests::test_rate_limit_blocks_after_5_failures` -> passe seul, pas lie a 5.4).

### Acquis preserves
- Bypass loopback iter14 INTACT (5.4 hors scope auth).
- Cache probe iter13 INTACT (5.4 hors scope #1, n'a pas touche `plan_support_core.py` ni `disk_cache.py`).
- Cycle iter11 INTACT (5.4 n'a pas touche CSS/template).
- Tier-colors hex INTACTS (tokens.css non modifie).
- Matrice auth iter5/iter14 INCHANGEE.
- Scrubber secrets / rate-limiter / exempt local : INCHANGES.
- Tracking schema_migrations idempotent : semantique INCHANGEE (la table reste creee/peuplee, seul le NIVEAU du log d'avertissement transitoire passe en debug).

## GATE Non-regression globale [WIP]
## Etat final backlog [WIP]
## Commits [WIP]
