# BILAN ITER 5 - Frontiere Auth - CineSort - 2026-06-08

> Branche: loop/correction-2026-06
> Acquis preserves: a37852aa + 242cf339 + 7df3af3e + 6193e02b + 9c806129 + 06f74ad + #2 vert
> Statut: [WIP]

## EN TETE [WIP]

## 1. Confirmation exposition (X breche reelle vs Y artefact loopback) [WIP]

### 1a. Trace dispatcher test #3 `test_login_invalid_token_returns_401` [OPERATIONNEL]

**Test cible** : `tests/test_dashboard_shell.py::DashboardShellHttpTests::test_login_invalid_token_returns_401` (L193-197)

```python
def test_login_invalid_token_returns_401(self) -> None:
    """Un mauvais token retourne 401."""
    status, data = self._post("/api/settings/get_settings", body={}, token="wrong-token")
    self.assertEqual(status, 401)
    self.assertFalse(data["ok"])
```

**Repro** : POST `http://127.0.0.1:<port>/api/settings/get_settings` avec header `Authorization: Bearer wrong-token`. Le serveur est instancie via `RestApiServer(cls.api, port=cls.port, token=cls.token)` (L49 du test), donc **`host` defaut = `"127.0.0.1"`** (cf `RestApiServer.__init__` L1062).

**Route table** : `_get_api_methods()` (rest_server.py L190-237) construit le mapping en 2 passes :
- Pass 1 (legacy direct) : **DESACTIVEE par defaut** (P0 #233, kill switch `_legacy_pass1_disabled()` L91-113, defaut `True`).
- Pass 2 (facades) : pour chaque facade dans `_FACADE_ATTR_NAMES = ("run", "settings", "quality", "integrations", "library", "runtime")` (L69), enregistre `{facade}/{method} -> facade.method`.

Resultat : la cle `"settings/get_settings"` est mappee vers `api.settings.get_settings` (cf `settings_facade.py` L30-35 : `def get_settings(self): return self._api._get_settings_impl()`).

**Chemin de dispatch** : **facade pure** (Pass 1 desactivee). `_handle_post()` (rest_server.py L904-1028) execute strictement :
1. L908-911 : verif prefix `/api/`
2. L914-916 : `_is_rate_limited()` (exempt loopback L553-555)
3. **L918-921 : `if not self._check_auth(): self._send_unauthorized()` <- SEUL POINT D'AUTH**
4. L931 : `method_name = path[5:]` = `"settings/get_settings"`
5. L932 : `method = self.api_methods.get("settings/get_settings")` = wrapper facade
6. L987 : `result = method(**params)` = `api.settings.get_settings()`

**La facade appelle-t-elle `_check_auth` ?** **NON.** Grep `_check_auth` dans `cinesort/ui/api/facades/` retourne 0 fichier. La seule definition et le seul appel de `_check_auth` sont dans `rest_server.py:433` (def) et `rest_server.py:918` (appel dans `_handle_post`).

**Code `_check_auth` (rest_server.py L433-508) — BYPASS LOOPBACK INCONDITIONNEL** :

```python
def _check_auth(self) -> bool:
    client_ip = self.client_address[0] if self.client_address else ""
    bypass_disabled = os.environ.get("CINESORT_DISABLE_LOCAL_AUTH", "0").strip() == "1"
    if (
        not bypass_disabled
        and client_ip in _LOCAL_CLIENT_IPS                # {"127.0.0.1","::1","::ffff:127.0.0.1"}
        and self.bind_host == "127.0.0.1"
    ):
        logger.info("Auth bypass localhost ...")
        return True                                         # <- L462 RENVOIE TRUE SANS LIRE LE TOKEN
    if not self.auth_token:
        return False
    auth = self.headers.get("Authorization", "")
    if not auth.startswith("Bearer "):
        return False
    bearer = auth[7:].strip()
    match = hmac.compare_digest(bearer.encode(), self.auth_token.encode())
    ...
    return match
```

**Faille code confirmee (cote dispatch)** : OUI.

Le test #3 frappe le dispatcher depuis `127.0.0.1` (loopback) sur un serveur bind `127.0.0.1`. Les 3 conditions du bypass L452-456 sont remplies :
1. `bypass_disabled = False` (env var non positionnee)
2. `client_ip = "127.0.0.1"` ∈ `_LOCAL_CLIENT_IPS`
3. `self.bind_host = "127.0.0.1"` (defaut RestApiServer)

`_check_auth()` retourne `True` **sans jamais lire `Authorization`**. Le wrong-token n'est pas compare. Le dispatcher appelle `api.settings.get_settings()` et renvoie **200**, alors que le test attend **401**.

**Conclusion #1a** :
- Chemin dispatch reel = **FACADE** (Pass 1 legacy 410 Gone par defaut).
- Aucune facade n'appelle `_check_auth`. L'auth est exclusivement portee par `_handle_post()` -> `_check_auth()` (1 seul site, rest_server.py L918).
- Le `_check_auth` actuel **court-circuite l'auth pour TOUT client loopback quand bind=127.0.0.1**, indistinctement legitime/illegitime, et indistinctement token-valide/token-invalide/token-absent.
- En contexte test (HTTPConnection 127.0.0.1 -> serveur bind 127.0.0.1), le test attend 401 mais doit recevoir 200 -> **echec OBSERVE attendu** (a confirmer par run reel en etape suivante).
- La memoire BYPASS LOOPBACK CONSERVE (UI desktop dependante) explique le choix design, mais il **rend impossible le test "rouge -> vert" depuis loopback** : tout test d'auth doit donc passer par **vraie entree publique non-loopback** ou positionner `CINESORT_DISABLE_LOCAL_AUTH=1` dans `setUpClass`. Sinon faux vert systematique.

**Marqueur** : OPERATIONNEL pour la trace de code. HYPOTHESE pour le verdict comportemental tant que le run reel du test n'a pas ete execute en etape 1b.

_Section 1a terminee — la section globale 1 reste WIP._

### 1b. Reponses runtime sur loopback vs non-loopback [OPERATIONNEL]

**Question** : un client non-loopback (LAN ou bypass desactive par env) recoit-il bien 401 quand le token est absent ou faux ? Si oui -> Y artefact loopback, si non -> X breche reseau.

**Methodologie** : script autonome `C:/Users/blanc/test_iter5_1bc_loopback_vs_lan.py` (non commite, supprimable) qui instancie 3 fois `RestApiServer` :
- Reference : bind 127.0.0.1, bypass actif (default).
- Strategy B : bind 127.0.0.1 + `CINESORT_DISABLE_LOCAL_AUTH=1` -> force evaluation token meme en loopback.
- Strategy A : bind 0.0.0.0 + token 40 chars (>= MIN_LAN_TOKEN_LENGTH=32, pas de demotion) + requete depuis IP LAN reelle Wi-Fi `192.168.27.172`.

Chaque cas frappe `POST /api/settings/get_settings` (route facade reelle, evite le 410 Gone legacy P0 #233). 3 sondes par cas : `valid-token` (40 chars `A`), `wrong-token` (40 chars `Z`), `no-token` (header absent).

Le mock `_MockApi` expose `settings.get_settings()` retournant `{"library_path": "C:/tmp"}` (sans dependance SQLite/DPAPI). Le dispatcher `_handle_post` est strictement exerce comme en prod : `_is_rate_limited` -> `_check_auth` -> `method = api_methods.get("settings/get_settings")` -> `method(**params)`.

**Configuration confirmee** :
- `settings.json` reel (`%LOCALAPPDATA%\CineSort\settings.json`, lu utf-8-sig) : `rest_api_enabled=True`, `port=8642`, `token` 32 chars. -> bind voulu en prod = `0.0.0.0`.
- IP LAN active : `192.168.27.172` (Wi-Fi). `127.0.0.1` reste loopback.

**Trace brute (sortie script, codes HTTP)** :

```text
========== REFERENCE : LOOPBACK + bypass actif (default) ==========
  bind effectif: 127.0.0.1:37051
  loopback valid_token  -> 200  (attendu 200)
  loopback wrong_token  -> 200  (attendu 200 car bypass)
  loopback no_token     -> 200  (attendu 200 car bypass)

========== STRATEGY B : CINESORT_DISABLE_LOCAL_AUTH=1 + loopback ==========
  bind effectif: 127.0.0.1:37055
  loopback+bypass-off valid_token  -> 200  (attendu 200)
  loopback+bypass-off wrong_token  -> 401  (attendu 401)
  loopback+bypass-off no_token     -> 401  (attendu 401)

========== STRATEGY A : bind 0.0.0.0 + requete IP LAN 192.168.27.172 ==========
  bind effectif: 0.0.0.0:37059  lan_demoted=False
  LAN-IP valid_token  -> 200  (attendu 200)
  LAN-IP wrong_token  -> 401  (attendu 401)
  LAN-IP no_token     -> 401  (attendu 401)

  Meme serveur bind 0.0.0.0, requete loopback wrong_token -> 401  (attendu 401, bypass desactive sur 0.0.0.0)
  Meme serveur bind 0.0.0.0, requete loopback no_token    -> 401  (attendu 401)

========== SYNTHESE ==========
{
  "loopback_valid": 200,
  "loopback_wrong": 200,
  "loopback_none": 200,
  "B_loopback_disabled_valid": 200,
  "B_loopback_disabled_wrong": 401,
  "B_loopback_disabled_none": 401,
  "A_lan_valid": 200,
  "A_lan_wrong": 401,
  "A_lan_none": 401,
  "A_loopback_on_0000_wrong": 401,
  "A_loopback_on_0000_none": 401
}
```

Logs serveur correspondants : `REST auth failure from 127.0.0.1 ...` (strategy B), `REST auth failure from 192.168.27.172 ...` (strategy A), `REST: serveur expose sur 0.0.0.0:37059 (acces LAN)`. Le code passe bien dans `_send_unauthorized` -> `record_failure` (non-loopback uniquement).

### 1c. Classification : Y = ARTEFACT LOOPBACK [OPERATIONNEL]

**Tableau de decision** (memoire du prompt) :
- X = BRECHE RESEAU REELLE : non-loopback wrong/no-token -> 200.
- Y = ARTEFACT LOOPBACK : non-loopback wrong/no-token -> 401.

**Lecture** :

| Strategy | bind | client | wrong-token | no-token | valid-token | Verdict |
|----------|------|--------|------|------|------|------|
| Reference | 127.0.0.1 | 127.0.0.1 | 200 | 200 | 200 | Bypass loopback (design) |
| B | 127.0.0.1 | 127.0.0.1 + `CINESORT_DISABLE_LOCAL_AUTH=1` | **401** | **401** | 200 | Auth nominale |
| A | 0.0.0.0 | LAN `192.168.27.172` | **401** | **401** | 200 | Auth nominale |
| A bis | 0.0.0.0 | 127.0.0.1 (meme serveur LAN) | **401** | **401** | (non teste) | Auth nominale (bypass off car bind != 127.0.0.1) |

**Verdict** : **Y = ARTEFACT LOOPBACK CONFIRME**.

Des qu'on sort du seul couple `(bind=127.0.0.1, client=loopback, env var non set)`, l'auth Bearer s'execute correctement :
- token absent -> 401
- token mauvais -> 401
- token bon -> 200

La trace `facade_appelle_check_auth=false` de 1a est exacte mais sans consequence : la facade n'a pas besoin d'appeler `_check_auth` car le seul point d'entree HTTP (`_handle_post` L918 de `rest_server.py`) le fait avant de dispatcher. Le bypass est strictement gate par `client_ip ∈ {127.0.0.1, ::1, ::ffff:127.0.0.1}` ET `bind_host == "127.0.0.1"` ET `CINESORT_DISABLE_LOCAL_AUTH != "1"`. Les 3 conditions sont conjointes (AND) — toute deviation reactive l'auth complete.

**Aucune breche reseau identifiee. Le BYPASS LOOPBACK CONSERVE (memoire user) est non-mutilant pour la securite reelle** : un attaquant LAN ne peut pas usurper 127.0.0.1 sans deja avoir compromis le poste, et en prod (`rest_api_enabled=True` -> bind 0.0.0.0) le bypass est de toute facon desactive.

**Consequences pour la GATE Auth** :
1. La memoire "ACCEPTATION AUTH = test rouge->vert vraie entree publique ET check runtime non-loopback" reste valide. Le test `test_login_invalid_token_returns_401` actuel (Strategy reference loopback) **doit etre re-ancre** sur l'un des 3 modes :
   - `setUpClass` positionne `CINESORT_DISABLE_LOCAL_AUTH=1` (le plus simple, isole le test du bypass design).
   - Le test fabrique un serveur `host="0.0.0.0"` avec token >= 32 chars + requete via IP non-loopback (lourd, requiert interface).
   - Le test mocke `client_address = ("192.168.x.x", port)` au niveau handler (artificiel mais deterministe).
   Option 1 (env var) est la plus alignee avec la design intent : "le bypass est volontairement contournable pour les tests" — c'est exactement le role du kill-switch `CINESORT_DISABLE_LOCAL_AUTH`.
2. L'etape 1a "echec OBSERVE attendu" est confirmee : `test_login_invalid_token_returns_401` echoue **par design** sur loopback default. Il faut le re-ancrer, pas le supprimer.
3. Pas de fix code requis sur `rest_server.py` cote auth. Le code est correct, c'est le test qui n'a jamais ete adapte au bypass introduit le 2026-06-08.

**Restauration** : aucun changement persiste. Le script `C:/Users/blanc/test_iter5_1bc_loopback_vs_lan.py` est en dehors du repo CineSort (sandbox utilisateur), aucune modif a `cinesort/`, aucun changement `settings.json`, aucun port reste ecoutant (chaque serveur shutdown apres test).

**Marqueur** : OPERATIONNEL.

_Section 1c terminee — la section globale 1 passe a [OPERATIONNEL] (1a + 1b + 1c convergent)._

## 2. Perimetre facade endpoints [OPERATIONNEL]

### 2.0 Rappel architectural (issu de 1a)

Le chemin de dispatch reel est **toujours** :

```
do_POST -> _handle_post (rest_server.py L904-1028)
  -> _is_rate_limited           (L914)  exempt loopback (FIX 2026-06-07)
  -> _check_auth                (L918)  bypass loopback + bind=127.0.0.1
  -> method = api_methods.get("<facade>/<method>")  (L932)
  -> method(**params)           (L987)  delegue _BaseFacade -> self._api._X_impl(...)
```

**Aucune facade n'appelle `_check_auth`, ni `_is_rate_limited`, ni `_send_request_id_header`.**
Toutes les protections sont portees par **un seul site** : `_handle_post()`.
Verifie : grep `_check_auth` dans `cinesort/ui/api/facades/` = **0 hit**.

Par consequent, pour TOUS les endpoints qui passent par le dispatcher facade :
- `auth_check` = OUI (mais bypass loopback sur bind=127.0.0.1 sans `CINESORT_DISABLE_LOCAL_AUTH=1`)
- `rate_limit` = OUI (mais exempt loopback `_LOCAL_CLIENT_IPS`)
- `x_request_id` = OUI (positionne via `set_request_id` au debut de `do_POST` L894, emis via `_send_request_id_header` dans `_respond_json` L607)

Le rappel `Y = artefact loopback` (1c) confirme que l'auth Bearer est nominale **des qu'on sort de (bind=127.0.0.1, client=loopback, env var non set)**. Tableau ci-dessous documente cette uniformite.

### 2.1 Endpoints exposes par les 6 facades (166 routes)

Convention URL : `POST /api/<facade>/<methode>`. Body JSON = kwargs. Backward compat : 410 Gone sur `/api/<methode>` direct (P0 #233).

| facade | methode | type | auth_check | rate_limit | x_request_id |
|--------|---------|------|------|------|------|
| **run** | start_plan | ecriture (lance scan+plan, payload {settings:{library_path}}) | YES (handle_post L918, bypass loopback) | YES (L914, exempt loopback) | YES (L894 + L607) |
| run | get_status | lecture (progress logs) | YES | YES | YES |
| run | get_plan | lecture (plan.jsonl) | YES | YES | YES |
| run | export_run_report | lecture/ecriture (fichier export) | YES | YES | YES |
| run | cancel_run | ecriture (cancel_requested=1) | YES | YES | YES |
| run | build_apply_preview | lecture (preview FS dry) | YES | YES | YES |
| run | list_apply_history | lecture (batches) | YES | YES | YES |
| run | pause_run | ecriture (signal + DB) | YES | YES | YES |
| run | resume_run | ecriture (signal + DB) | YES | YES | YES |
| run | save_for_later | ecriture (signal + DB) | YES | YES | YES |
| run | list_pending_runs | lecture | YES | YES | YES |
| run | rescan_rows_bulk | ecriture (JobRunner) | YES | YES | YES |
| run | get_history_stats | lecture | YES | YES | YES |
| run | **delete_run** | **ecriture DESTRUCTIVE** (purge DB run) | YES | YES | YES |
| run | **cleanup_old_runs** | **ecriture DESTRUCTIVE** (purge > N jours) | YES | YES | YES |
| run | **purge_quarantine_bucket** | **ecriture DESTRUCTIVE** (purge FS _review) | YES | YES | YES |
| run | **purge_quarantine_bucket_all** | **ecriture DESTRUCTIVE** (vide _review) | YES | YES | YES |
| run | list_quarantine_bucket | lecture | YES | YES | YES |
| run | rescan_row | ecriture (probe + persiste) | YES | YES | YES |
| run | mark_duplicate_winner | ecriture (decision user) | YES | YES | YES |
| run | get_dashboard | lecture | YES | YES | YES |
| run | get_global_stats | lecture | YES | YES | YES |
| run | get_sidebar_counters | lecture | YES | YES | YES |
| run | **apply** | **ecriture CRITIQUE** (deplacements FS reels ou dry, payload {run_id, decisions:{}, dry_run, ...}) | YES | YES | YES |
| run | save_validation | ecriture (validation.json + DB decisions) | YES | YES | YES |
| run | load_validation | lecture | YES | YES | YES |
| run | check_duplicates | lecture (calcul collisions, payload {run_id, decisions:{}}) | YES | YES | YES |
| run | check_duplicates_fusion | lecture (calcul fusion) | YES | YES | YES |
| run | get_cleanup_residual_preview | lecture | YES | YES | YES |
| run | **undo_last_apply** | **ecriture CRITIQUE** (rollback FS) | YES | YES | YES |
| run | export_run_nfo | ecriture (genere .nfo) | YES | YES | YES |
| run | export_apply_audit | lecture | YES | YES | YES |
| run | import_watchlist | ecriture (persiste watchlist) | YES | YES | YES |
| run | get_auto_approved_summary | lecture | YES | YES | YES |
| run | undo_last_apply_preview | lecture | YES | YES | YES |
| run | undo_by_row_preview | lecture | YES | YES | YES |
| run | **undo_selected_rows** | **ecriture CRITIQUE** (rollback selectif) | YES | YES | YES |
| **settings** | get_settings | lecture (settings.json utf-8-sig) | YES | YES | YES |
| settings | **save_settings** | **ecriture CRITIQUE** (re-bind serveur potentiel) | YES | YES | YES |
| settings | set_locale | ecriture | YES | YES | YES |
| settings | **restart_api_server** | **ecriture CRITIQUE** (relance le REST) | YES | YES | YES |
| settings | **reset_all_user_data** | **ecriture DESTRUCTIVE** (wipe + backup) | YES | YES | YES |
| settings | get_user_data_size | lecture | YES | YES | YES |
| settings | reset_settings | ecriture (reset par scope) | YES | YES | YES |
| settings | **reset_database** | **ecriture DESTRUCTIVE** (wipe DB) | YES | YES | YES |
| settings | get_profiles | lecture | YES | YES | YES |
| settings | save_profile | ecriture | YES | YES | YES |
| settings | set_active_profile | ecriture | YES | YES | YES |
| settings | get_server_info | lecture | YES | YES | YES |
| settings | get_dashboard_qr | lecture | YES | YES | YES |
| settings | get_naming_presets | lecture | YES | YES | YES |
| settings | preview_naming_template | lecture | YES | YES | YES |
| settings | get_advanced_pragma_settings | lecture | YES | YES | YES |
| settings | **set_advanced_pragma_settings** | **ecriture DESTRUCTIVE** (locking_mode=EXCLUSIVE) | YES | YES | YES |
| settings | get_scan_max_workers | lecture | YES | YES | YES |
| settings | set_scan_max_workers | ecriture | YES | YES | YES |
| settings | get_confidence_thresholds | lecture | YES | YES | YES |
| **quality** | get_quality_profile | lecture | YES | YES | YES |
| quality | save_quality_profile | ecriture | YES | YES | YES |
| quality | reset_quality_profile | ecriture | YES | YES | YES |
| quality | export_quality_profile | lecture | YES | YES | YES |
| quality | import_quality_profile | ecriture | YES | YES | YES |
| quality | get_quality_presets | lecture | YES | YES | YES |
| quality | apply_quality_preset | ecriture | YES | YES | YES |
| quality | simulate_quality_preset | lecture (no persist) | YES | YES | YES |
| quality | get_quality_report | lecture | YES | YES | YES |
| quality | analyze_quality_batch | ecriture (probe + persiste) | YES | YES | YES |
| quality | save_custom_quality_preset | ecriture | YES | YES | YES |
| quality | get_custom_rules_templates | lecture | YES | YES | YES |
| quality | get_custom_rules_catalog | lecture | YES | YES | YES |
| quality | validate_custom_rules | lecture (no persist) | YES | YES | YES |
| quality | get_perceptual_report | lecture/ecriture (a la demande) | YES | YES | YES |
| quality | get_perceptual_details | lecture | YES | YES | YES |
| quality | analyze_perceptual_batch | ecriture (persiste perceptual_reports) | YES | YES | YES |
| quality | compare_perceptual | lecture (calcul) | YES | YES | YES |
| quality | get_perceptual_compare_frames | lecture (PNG b64) | YES | YES | YES |
| quality | get_perceptual_compare_audio | lecture | YES | YES | YES |
| quality | queue_perceptual_analyses | ecriture (queue job) | YES | YES | YES |
| quality | get_perceptual_job_status | lecture | YES | YES | YES |
| quality | submit_score_feedback | ecriture | YES | YES | YES |
| quality | delete_score_feedback | ecriture | YES | YES | YES |
| quality | get_calibration_report | lecture | YES | YES | YES |
| quality | get_profiles (alias) | lecture | YES | YES | YES |
| quality | save_profile (alias) | ecriture | YES | YES | YES |
| quality | set_active_profile (alias) | ecriture | YES | YES | YES |
| quality | get_films_by_tier | lecture | YES | YES | YES |
| quality | get_history | lecture | YES | YES | YES |
| quality | recompute_all_scores | ecriture (job background) | YES | YES | YES |
| quality | get_recompute_job_status | lecture | YES | YES | YES |
| quality | export_shareable_profile | lecture | YES | YES | YES |
| quality | import_shareable_profile | ecriture | YES | YES | YES |
| quality | export_recyclarr_yaml | lecture | YES | YES | YES |
| quality | import_recyclarr_yaml | ecriture | YES | YES | YES |
| quality | get_embedded_presets | lecture | YES | YES | YES |
| quality | get_upgrade_until_score | lecture | YES | YES | YES |
| quality | set_upgrade_until_score | ecriture | YES | YES | YES |
| quality | get_breakdown_5_axes | lecture | YES | YES | YES |
| **integrations** | test_tmdb_key | lecture (HTTP externe) | YES | YES | YES |
| integrations | get_tmdb_posters | lecture (HTTP externe, cache) | YES | YES | YES |
| integrations | test_jellyfin_connection | lecture (HTTP externe) | YES | YES | YES |
| integrations | get_jellyfin_libraries | lecture | YES | YES | YES |
| integrations | get_jellyfin_sync_report | lecture | YES | YES | YES |
| integrations | refresh_jellyfin_library_now | ecriture (declenche scan Jellyfin) | YES | YES | YES |
| integrations | test_plex_connection | lecture | YES | YES | YES |
| integrations | get_plex_libraries | lecture | YES | YES | YES |
| integrations | get_plex_sync_report | lecture | YES | YES | YES |
| integrations | refresh_plex_library_now | ecriture (declenche scan Plex) | YES | YES | YES |
| integrations | test_radarr_connection | lecture | YES | YES | YES |
| integrations | get_radarr_status | lecture | YES | YES | YES |
| integrations | request_radarr_upgrade | ecriture (declenche upgrade) | YES | YES | YES |
| integrations | test_omdb_connection | lecture | YES | YES | YES |
| integrations | test_email_report | ecriture (envoi SMTP) | YES | YES | YES |
| **library** | get_library_filtered | lecture | YES | YES | YES |
| library | get_smart_playlists | lecture | YES | YES | YES |
| library | save_smart_playlist | ecriture | YES | YES | YES |
| library | delete_smart_playlist | ecriture | YES | YES | YES |
| library | get_scoring_rollup | lecture | YES | YES | YES |
| library | set_film_tmdb_candidate | ecriture | YES | YES | YES |
| library | search_tmdb | lecture (HTTP externe) | YES | YES | YES |
| library | **mark_for_deletion** | **ecriture DESTRUCTIVE** (bucket suppression) | YES | YES | YES |
| library | mark_alert_ignored | ecriture | YES | YES | YES |
| library | get_film_full | lecture | YES | YES | YES |
| library | get_film_history | lecture | YES | YES | YES |
| library | list_films_with_history | lecture | YES | YES | YES |
| library | export_full_library | lecture (RGPD) | YES | YES | YES |
| library | get_library_podiums | lecture | YES | YES | YES |
| library | get_library_timeline | lecture | YES | YES | YES |
| library | get_library_counters_by_chip | lecture | YES | YES | YES |
| library | **mark_for_deletion_bulk** | **ecriture DESTRUCTIVE bulk** | YES | YES | YES |
| library | export_films | lecture (export multi-format) | YES | YES | YES |
| library | get_films_by_decade | lecture | YES | YES | YES |
| library | get_incomplete_sagas | lecture | YES | YES | YES |
| library | set_field_lock | ecriture | YES | YES | YES |
| library | clear_field_lock | ecriture | YES | YES | YES |
| library | list_field_locks | lecture | YES | YES | YES |
| **runtime** | get_diagnostic | lecture | YES | YES | YES |
| runtime | get_recent_logs | lecture | YES | YES | YES |
| runtime | get_doc | lecture (whitelist) | YES | YES | YES |
| runtime | search_docs | lecture | YES | YES | YES |
| runtime | get_app_version | lecture | YES | YES | YES |
| runtime | get_update_info | lecture | YES | YES | YES |
| runtime | check_for_updates | lecture (HTTP externe) | YES | YES | YES |
| runtime | get_log_paths | lecture | YES | YES | YES |
| runtime | open_logs_folder | ecriture (action OS, refuse remote) | YES | YES | YES |
| runtime | open_external_url | ecriture (action OS, refuse remote) | YES | YES | YES |
| runtime | is_demo_mode_active | lecture | YES | YES | YES |
| runtime | start_demo_mode | ecriture (15 films fictifs) | YES | YES | YES |
| runtime | stop_demo_mode | ecriture (purge demo) | YES | YES | YES |
| runtime | get_notifications | lecture | YES | YES | YES |
| runtime | dismiss_notification | ecriture | YES | YES | YES |
| runtime | mark_notification_read | ecriture | YES | YES | YES |
| runtime | mark_all_notifications_read | ecriture | YES | YES | YES |
| runtime | clear_notifications | ecriture | YES | YES | YES |
| runtime | get_notifications_unread_count | lecture | YES | YES | YES |
| runtime | validate_dropped_path | lecture | YES | YES | YES |
| runtime | get_probe_tools_status | lecture | YES | YES | YES |
| runtime | get_tools_status | lecture (alias) | YES | YES | YES |
| runtime | recheck_probe_tools | ecriture (re-detection) | YES | YES | YES |
| runtime | set_probe_tool_paths | ecriture | YES | YES | YES |
| runtime | **install_probe_tools** | **ecriture CRITIQUE** (winget) | YES | YES | YES |
| runtime | **update_probe_tools** | **ecriture CRITIQUE** (winget) | YES | YES | YES |
| runtime | **auto_install_probe_tools** | **ecriture CRITIQUE** (DL + install) | YES | YES | YES |
| runtime | **purge_probe_cache** | **ecriture DESTRUCTIVE** (wipe cache) | YES | YES | YES |
| runtime | get_probe | lecture | YES | YES | YES |
| runtime | get_event_ts | lecture | YES | YES | YES |
| runtime | **reset_incremental_cache** | **ecriture DESTRUCTIVE** (wipe 3 tables) | YES | YES | YES |
| runtime | run_nas_benchmark | ecriture (benchmark perf, non destructif) | YES | YES | YES |

### 2.2 Endpoints non-dispatcher (hors facade)

| route | methode HTTP | type | auth_check | rate_limit | x_request_id |
|-------|-----|------|------|------|------|
| `/api/health` | GET | lecture (active_run_id, last_event_ts) | **NON** (do_GET ne passe pas par `_check_auth` cf rest_server.py L809-879) | NON (idem) | YES (L869) |
| `/api/spec` | GET | lecture (OpenAPI 3.0.3) | **NON** | NON | YES |
| `/dashboard/*`, `/shared/*`, `/locales/*` | GET | lecture (static assets) | **NON** | NON | YES (L744, L771, L800) |

**Note importante** : `do_GET` (L809-888) ne fait jamais d'`_check_auth`, contrairement a `do_POST`/`_handle_post`. Tous les GET sont **publics**. Le rate-limit ne s'applique qu'au POST. C'est une **decision de design** assumee (health endpoints + statics doivent etre joignables sans token, sinon le dashboard distant ne charge pas).

`open_path` est volontairement hors dispatcher REST (vector path-traversal) ; uniquement accessible via le bridge pywebview (cf docstring runtime_facade L23-26).

### 2.3 Distinction lectures / ecritures / actions critiques

**Lectures pures (GET semantique, idempotentes, pas d'I/O persiste)** : ~95 endpoints sur 166.
- Tous les `get_*`, `list_*`, `load_*`, `search_*`, `preview_*`, `simulate_*`, `validate_*`, `check_*`, `export_*` (sans creation fichier), `test_*` (sans persist), `compare_*`, `get_diagnostic`, `get_recent_logs`.

**Ecritures (mutation DB ou settings.json)** : ~50 endpoints.
- `save_*`, `set_*`, `apply_*`, `import_*`, `submit_*`, `mark_*`, `dismiss_*`, `pause_run`, `resume_run`, `cancel_run`, `rescan_*`, `start_*`, `stop_*`, `analyze_*` (persistance), `recompute_*`, `queue_*`, `request_radarr_upgrade`, `refresh_*_library_now`.

**Actions DESTRUCTIVES ou CRITIQUES (memoire user "Actions dangereuses")** : ~20 endpoints. Demandent confirmation UI + dangerConfirmModal + countdown 3s si > 50 items :
- `run/apply` (dry=False) — deplacements FS reels
- `run/undo_last_apply`, `run/undo_selected_rows` — rollback FS
- `run/delete_run`, `run/cleanup_old_runs` — purge DB run
- `run/purge_quarantine_bucket`, `run/purge_quarantine_bucket_all` — purge FS _review
- `settings/save_settings` — peut re-binder le serveur, changer le profile actif
- `settings/restart_api_server` — relance REST
- `settings/reset_all_user_data` — wipe complet + backup ZIP
- `settings/reset_database` — wipe DB
- `settings/set_advanced_pragma_settings` (locking_mode=True) — bloque lectures concurrentes
- `library/mark_for_deletion`, `library/mark_for_deletion_bulk` — bucket suppression
- `runtime/install_probe_tools`, `runtime/update_probe_tools`, `runtime/auto_install_probe_tools` — installation winget/DL
- `runtime/purge_probe_cache` — wipe cache
- `runtime/reset_incremental_cache` — wipe 3 tables incremental

### 2.4 Conclusions section 2

1. **Uniformite totale du gating** : les 166 endpoints facade passent par le meme tunnel `_handle_post -> rate_limit -> auth_check -> dispatch`. Pas un seul endpoint facade ne court-circuite l'auth. Aucune asymetrie a corriger.
2. **Le seul point d'audit auth/rate/request_id reste `rest_server.py:_handle_post` L904-1028**. Toute correction (par exemple, requalification du bypass loopback pour qu'il refuse les ecritures destructives meme en loopback) doit etre faite la, pas dans les facades.
3. **GET = pas d'auth, pas de rate-limit**. Les 3 routes `/api/health`, `/api/spec`, `/dashboard/*` sont publiques par design. Aucune n'expose d'ecriture, donc pas de surface d'attaque par mutation. Risques limites : enumeration de l'API via `/api/spec` (deja public dans le code source du dashboard) + info disclosure via `/api/health` (`active_run_id` revele qu'un run tourne).
4. **Frontiere coherente avec la decision de design `BYPASS LOOPBACK CONSERVE`** : aucun endpoint d'ecriture n'a de protection supplementaire interne (pas de re-check auth dans `_apply_impl`, `_reset_all_user_data_impl`, etc.). En contexte desktop (bind=127.0.0.1, client loopback), TOUT est accessible sans token. Ce n'est pas une faille car l'utilisateur loopback "a deja le shell" (cf memoire 1c), mais c'est un point d'attention si jamais on voulait durcir les actions destructives meme en local (option future : exiger un re-prompt UI cote pywebview pour `reset_*`/`apply` avec `dry=False`).
5. **`open_path` reste hors facade** (volontairement non expose REST) — confirme par la docstring de `runtime_facade.py:23-26`.

**Marqueur** : OPERATIONNEL. Inventaire complet, croise avec rest_server.py L904-1028 (POST) et L809-888 (GET).

_Section 2 terminee — passe a section 3 (racine commune #2/#4)._

## 3. Racine commune #2/#4 [OPERATIONNEL]

### 3.1 Reperage exact des deux tests

**Test #2** : `tests/test_dashboard_infra.py::RateLimitHttpTests::test_rate_limit_blocks_after_5_failures` (L363-380).
- URL frappee : `POST /api/get_settings` (L375, `self._post("/api/get_settings", token="bad-token")`).
- Path = **legacy direct** (pas de prefixe facade). Token = `"bad-token"`.
- Serveur : `RestApiServer(api, port, token)` (L336) -> bind defaut `127.0.0.1`.
- Client : `HTTPConnection("127.0.0.1", ...)` (L351) -> ip client = `127.0.0.1`.
- Assertion : `self.assertEqual(status, 401, ...)` pour 7 iterations (L376-380).

**Test #4** : `tests/test_log_context.py::RestRequestIdHeaderTests::test_x_request_id_on_unauthorized_post` (L366-370).
- URL frappee : `POST /api/get_settings` (L367), body `b"{}"`, **headers vides** (pas de `Authorization`).
- Serveur : `RestApiServer(cls.api, port=cls.port, token=cls.token)` (L333) -> bind defaut `127.0.0.1`.
- Client : `http.client.HTTPConnection("127.0.0.1", ...)` (L347) -> ip = `127.0.0.1`.
- Assertions : `self.assertEqual(status, 401)` puis `X-Request-ID` present sur la 401 (L369-370).

**Test #3** (rappel section 1a) : `tests/test_dashboard_shell.py::DashboardShellHttpTests::test_login_invalid_token_returns_401` (L193-197).
- URL : `POST /api/settings/get_settings` (path **facade**), token=`"wrong-token"`.
- Bind defaut `127.0.0.1`, client `127.0.0.1`.
- Assertion : `self.assertEqual(status, 401)`.

### 3.2 Trace dispatch comparée (rest_server.py L904-959, code lu en RUNTIME)

Le dispatcher POST execute strictement dans cet ordre :

```python
# L904 _handle_post()
path = self.path.split("?")[0].rstrip("/")            # L906
if not path.startswith("/api/"): return 404            # L908-911
if self._is_rate_limited(): return 429                 # L914-916  (EXEMPT loopback)
if not self._check_auth():                             # L918  <- POINT CLE
    self._send_unauthorized() ; return                 # L920 -> 401
# (rate-limit success reset)                            # L928-929
method_name = path[5:]                                  # L931  -> "get_settings" OU "settings/get_settings"
method = self.api_methods.get(method_name)              # L932
if not method:
    if _legacy_pass1_disabled() and "/" not in method_name and method_name not in _FACADE_ATTR_NAMES:
        return 410                                      # L938-955  <- legacy guard
    return 404                                          # L957
result = method(**params); return 200                   # L987-1002
```

**Trace #2** (`POST /api/get_settings`, bad-token, client+bind 127.0.0.1) :
1. L908 prefix OK
2. L914 rate-limit : loopback exempt (FIX 2026-06-07, cf section 1a) -> faux
3. **L918 `_check_auth()` -> bypass loopback (L443-462, conditions `bypass_disabled=False && client_ip ∈ {127.0.0.1, ::1, ::ffff:127.0.0.1} && bind_host == "127.0.0.1"`) -> RETOURNE TRUE SANS LIRE LE TOKEN**
4. L931 `method_name = "get_settings"` (Pass 1 desactivee -> non enregistre)
5. L932 `method = api_methods.get("get_settings")` = `None`
6. L938-950 legacy guard : `_legacy_pass1_disabled()=True && "/" not in "get_settings" && "get_settings" not in _FACADE_ATTR_NAMES` -> tous vrais -> **renvoie 410**

Le test attend 401, observe 410. Status code obtenu **uniquement parce que le bypass loopback a court-circuite l'auth** ; sinon le `_send_unauthorized()` L920 aurait emis 401 avant que le legacy guard ne soit atteint.

**Trace #4** (`POST /api/get_settings`, headers vides, client+bind 127.0.0.1) :
1. L908 prefix OK
2. L914 rate-limit : idem -> faux
3. **L918 `_check_auth()` -> bypass loopback identique -> RETOURNE TRUE** (le `not self.auth_token / not auth.startswith("Bearer ")` L475-478 n'est jamais evalue)
4. L931-955 identique -> **renvoie 410**

Test attend 401, observe 410. Meme cause exacte que #2.

**Trace #3** (`POST /api/settings/get_settings`, wrong-token, client+bind 127.0.0.1) :
1-3. Idem #2/#4 : **bypass loopback -> True**
4. L931 `method_name = "settings/get_settings"`
5. L932 `method = api_methods.get("settings/get_settings")` = wrapper facade (Pass 2 enregistree)
6. L987 `result = method(**params)` = `api.settings.get_settings()` -> dict valide -> **renvoie 200**

Test attend 401, observe 200. Encore une fois, le **bypass loopback** a court-circuite l'auth.

### 3.3 Verdict racine commune : OUI

Les trois traces partagent EXACTEMENT la meme branche bloquante :

| Test | URL | post-L918 (bypass) | post-L932 (lookup) | Status obtenu |
|------|-----|--------------------|--------------------|---------------|
| #2 | `/api/get_settings` (legacy) | bypass -> True | None (Pass 1 KO) | **410** (legacy guard) |
| #3 | `/api/settings/get_settings` (facade) | bypass -> True | facade method | **200** (dispatch) |
| #4 | `/api/get_settings` (legacy) | bypass -> True | None (Pass 1 KO) | **410** (legacy guard) |

**Racine unique** : `_check_auth()` L918 retourne `True` sans lire `Authorization` quand
`(bypass_disabled=False) && (client_ip ∈ _LOCAL_CLIENT_IPS) && (bind_host == "127.0.0.1")`,
conditions toutes remplies dans les 3 fixtures de test.

La difference de status (410 vs 200) est un **artefact aval** du path d'URL : ce n'est PAS deux bugs distincts, c'est un seul bug (bypass loopback) suivi de deux trajets de dispatch (legacy guard vs facade). La memoire "BACKLOG P0 #233" (migration vers facade) reste hors-perimetre : meme si tous les paths legacy etaient migres, #2/#4 seraient passes par la facade et auraient renvoye **200** comme #3, donc toujours echec.

### 3.4 Preuve "fix unifie resout #2 + #3 + #4"

**Hypothese de fix minimal** : forcer `CINESORT_DISABLE_LOCAL_AUTH=1` au `setUpClass` des 3 tests (ou re-ancrer les fixtures sur bind `0.0.0.0` + IP non-loopback). Le code de prod (`rest_server.py:_check_auth`) reste inchange -> respect de la memoire **BYPASS LOOPBACK CONSERVE (UI desktop dependante)**.

Trace attendue apres fix #2 / #4 (env var positionnee) :
1-2. Identique
3. **L918 `_check_auth()` -> bypass_disabled=True -> tombe sur L475 `if not self.auth_token: return False`** (test #4, no header) ou **L482 `hmac.compare_digest("bad-token", "rl-test-token")` -> False** (test #2). Retour `False`.
4. **L920 `_send_unauthorized()` -> 401** + `X-Request-ID` emis par `_respond_json` (L607).
5. Le legacy guard L938 N'EST JAMAIS ATTEINT -> 410 disparait, 401 conforme.

Trace attendue apres fix #3 (env var ou bind 0.0.0.0) :
1-2. Identique
3. **L918 -> False** (token wrong)
4. **L920 -> 401**, dispatcher s'arrete avant L932.

**Demonstration runtime deja effectuee en section 1b** : le script `C:/Users/blanc/test_iter5_1bc_loopback_vs_lan.py` a deja prouve en RUNTIME que **`CINESORT_DISABLE_LOCAL_AUTH=1` + `wrong-token` sur loopback -> 401** et que **bind `0.0.0.0` + LAN + `wrong-token` -> 401** (synthese L127-139 de cette meme bilan). Le meme env var fixe donc simultanement #2, #3 et #4 sans toucher le code de production.

### 3.5 Conclusion

- **Racine commune confirmee** : les 3 tests echouent pour **un seul defaut design** (bypass loopback inconditionnel) cote test, pas cote code de prod. Cote prod le bypass est volontaire (memoire BYPASS LOOPBACK CONSERVE).
- **Fix unifie possible** : `CINESORT_DISABLE_LOCAL_AUTH=1` dans `setUpClass` des 3 fixtures HTTP -> #2 vire de 410 a 401, #3 vire de 200 a 401, #4 vire de 410 a 401.
- **#2/#4 backlog P0 #233 complet ?** : **NON**. La migration legacy -> facade ne resout PAS #2/#4 : si on les re-pointait sur `/api/settings/get_settings`, ils echoueraient comme #3 (200 au lieu de 401) tant que le bypass loopback est actif. La migration est une amelioration orthogonale (et deja largement faite, cf section 2.0), mais elle n'est ni necessaire ni suffisante pour rendre #2/#4 verts.
- **Le 410 Gone observe est un effet de bord, pas la cause** : il signifie juste que Pass 1 est correctement desactivee (P0 #233 ABOUTI cote architecture) ; le vrai bug est en amont au L918.

**Marqueur** : OPERATIONNEL. Lecture de code croisee `rest_server.py:_handle_post` (L904-1028) + 3 fixtures de test (`test_dashboard_infra.py:RateLimitHttpTests`, `test_log_context.py:RestRequestIdHeaderTests`, `test_dashboard_shell.py:DashboardShellHttpTests`). Demonstration runtime via section 1b (script externe).

_Section 3 terminee — passe a section 4 (cluster settings 3 sites)._

## 4. Racine cluster settings (3 sites) [OPERATIONNEL]

### 4.1 Identification exacte des 3 sites

| Site | Fichier | Lignes | Role |
|------|---------|--------|------|
| S1 | `cinesort/ui/api/facades/run_facade.py` | L113-133 | Appel `_validate_passive(StartPlanRequest, request_payload, endpoint="start_plan.request")` apres re-wrapping `{settings: dict_plat}` |
| S2 | `cinesort/ui/api/schemas/run.py` | L41-59 | `class StartPlanRequest(BaseModel)` : Field `settings: PlanSettings` obligatoire, `extra="allow"` top-level |
| S3 | `cinesort/ui/api/schemas/run.py` | L15-38 | `class PlanSettings(BaseModel)` : Field `library_path: str` obligatoire (min_length=1), `options: Optional[Dict]`, `extra="allow"` |

### 4.2 Question prompt : "le cluster settings (toggle requete vs store, pattern silencieux Pydantic validation echec -> fallback dict) partage-t-il la racine facade ?"

**Decomposition de la "racine facade"** (etablie en sections 1c + 3.3) : bypass loopback inconditionnel dans `_check_auth()` (rest_server.py L443-462) lorsque `(bypass_disabled=False) && (client_ip ∈ _LOCAL_CLIENT_IPS) && (bind_host == "127.0.0.1")`. C'est une frontiere reseau/auth dans `rest_server.py`, totalement en amont du dispatcher facade.

**Decomposition du "cluster settings" tel que designe par le prompt** : pattern `_validate_passive` (run_facade.py L47-89) qui :
1. Tente `model_cls.model_validate(payload)` (L64)
2. En cas d'echec, lit le toggle `_pydantic_strict_enabled()` (env var `CINESORT_PYDANTIC_STRICT`, L42-44)
3. Si flag = 0 (defaut) -> **fallback silencieux dict** + log ERROR (L76-81) — BACKWARD COMPAT ABSOLUE
4. Si flag = 1 -> raise ValueError (L68-69)

Le "toggle requete vs store" referencerait ici la divergence entre le dict envoye par le caller REST (`settings={"library_path": "..."}`) et la representation typee Pydantic (`StartPlanRequest{settings: PlanSettings{library_path, options}}`). La re-wrapping L124-132 detecte si le caller a deja envoye `{settings: {...}}` (cas conforme) ou s'il a envoye le dict plat (cas nominal), et reconstruit le wrapper a la volee.

### 4.3 Verdict : **DISTINCT, ne partage PAS la racine facade**

**Argumentaire** :

1. **Echelle architecturale differente** :
   - La "racine facade" (cluster auth #2/#3/#4) est une frontiere **HTTP/reseau** (rest_server.py:_handle_post L918, AVANT toute facade). Elle gate 166 endpoints uniformement (cf section 2.0).
   - Le cluster settings ici est un detail **intra-facade**, strictement limite a `RunFacade.start_plan` (S1+S2+S3 ne touchent QUE le bounded context Run, pas SettingsFacade). Aucune autre methode REST ne passe par `_validate_passive(StartPlanRequest, ...)`.

2. **Bounded context different** :
   - Le mot "settings" dans `PlanSettings` est polysemique avec le bounded context Settings (`SettingsFacade.get_settings/save_settings` qui lit/ecrit `%LOCALAPPDATA%/CineSort/settings.json` utf-8-sig). Ici, `PlanSettings` modelise le **dict d'options de scan** (library_path + ~50 cles legacy comme `tmdb_api_key`, `tmdb_enabled`) passe en kwarg a `start_plan(settings=...)`. C'est l'argument de la methode, pas la table de configuration utilisateur.
   - Aucune liaison code entre `schemas/run.py:PlanSettings` et `SettingsFacade`. Grep `PlanSettings` ne retournerait des hits que dans `run.py` (definition) et `run_facade.py` (import). Pas de chevauchement avec `settings_facade.py` ni avec le toggle de re-bind du serveur.

3. **Toggle et fallback different** :
   - Cote auth (racine facade) : toggle = `CINESORT_DISABLE_LOCAL_AUTH` (env var) ; fallback = bypass inconditionnel renvoyant `True` sans lire le token. C'est un **bypass securite**.
   - Cote Pydantic (cluster settings) : toggle = `CINESORT_PYDANTIC_STRICT` (env var) ; fallback = retourner le payload dict legacy. C'est un **bypass validation de schema**, non securitaire (le run continue avec les memes donnees brutes que pre-Pydantic).
   - Les deux env vars sont independantes, leurs domaines disjoints, leurs criteres d'evaluation disjoints (pas de partage de code path).

4. **Site d'appel different** :
   - L'auth est gatee par `_handle_post()` (rest_server.py L918) en amont du dispatcher, avant que le wrapper facade ne soit invoque.
   - Le `_validate_passive` est appele a l'INTERIEUR de `RunFacade.start_plan` (L133, L140), donc strictement APRES que `_check_auth()` a deja repondu OK. Si l'auth refuse en amont, le pattern Pydantic n'est jamais atteint.
   - Ce sont deux etages distincts du pipeline POST : `auth -> dispatch -> facade.start_plan -> validate_passive(in) -> impl -> validate_passive(out)`.

5. **Impact des tests #2/#3/#4 sur ce cluster** : **NUL**. Aucun des 3 tests cibles (test_dashboard_infra/test_log_context/test_dashboard_shell) ne frappe `start_plan` ni n'instancie un `StartPlanRequest`/`PlanSettings`. Tous frappent `get_settings` (lecture facade Settings ou route legacy). Le `_validate_passive` de S1 n'est jamais execute pendant ces tests. Inversement, fixer le bypass loopback (positionner `CINESORT_DISABLE_LOCAL_AUTH=1` dans `setUpClass`) n'a aucun effet sur le pattern Pydantic silencieux : meme env var positionnee, un `start_plan` reel avec un `settings` mal-forme retombera toujours sur le fallback dict (flag strict = 0).

6. **Approvisionnement settings** (interpretation alternative du prompt : "classe approvisionnement settings, toggles qui ne font rien") : Le pattern S1-S3 est effectivement une **chaine d'approvisionnement de schemas Pydantic pour la requete `start_plan`**, parallele au pipeline d'auth, qui :
   - **S1** orchestre : re-wrap dict_plat -> `{settings: dict_plat}`, valide passif input, appelle `_start_plan_impl(settings)` (notons : on passe le `settings` ORIGINAL, pas le `request_payload` valide ; cf L136), valide passif output.
   - **S2** declare le schema d'entree REST (`StartPlanRequest`).
   - **S3** declare le sous-schema des options de scan (`PlanSettings`).
   - Les toggles (`CINESORT_PYDANTIC_STRICT`, fallback silencieux) sont des **safeguards backward compat** specifiques a la migration Pydantic V2.1 — ils ne font partie d'aucun cluster auth ni d'aucun cluster Settings persistant.

### 4.4 Observations annexes (HYPOTHESE, hors-perimetre fix)

- **L136 `result = self._api._start_plan_impl(settings)`** : on passe l'argument `settings` brut (le kwarg d'entree de la facade), PAS le `request_payload` valide. La validation Pydantic est donc strictement **observation passive**, sans normalisation effective des donnees. C'est coherent avec "BACKWARD COMPAT ABSOLUE" (le run consomme le meme dict que pre-Pydantic), mais ca signifie qu'un caller qui enverrait `{library_path: ""}` (echec min_length=1) ne sera ni bloque (flag passif) ni nettoye — le `_start_plan_impl` recoit le dict invalide et doit le tolerer ou crasher en interne. Le warning log ERROR L76-81 reste la seule trace.
- **Tolerance re-wrapping L125** : si le caller envoie deja `{settings: {...}}` (cas conforme StartPlanRequest), on ne re-wrap pas. Mais on transmet quand meme `settings` (entier, donc `{settings: {...}}` complet) a `_start_plan_impl`, ce qui ne matche PAS la signature `_start_plan_impl(settings={"library_path": ...})`. Risque : un caller "moderne" conforme au schema casserait le `_start_plan_impl` legacy. C'est theorique tant qu'aucun caller ne fait cette migration (UI desktop envoie le dict plat).
- Ni l'un ni l'autre point n'est lie a la frontiere auth — ils sont strictement intra-cluster Pydantic/Run.

### 4.5 Conclusion section 4

**Cluster settings (S1+S2+S3) DISTINCT de la racine facade auth**.

| Critere | Racine facade (auth) | Cluster S1+S2+S3 (Pydantic Run) |
|---------|----------------------|----------------------------------|
| Etage pipeline | HTTP gate (`_handle_post:_check_auth`) | Intra-facade (`RunFacade.start_plan`) |
| Site | `rest_server.py:443-462` (1 site) | `run_facade.py:47-89,113-141` + `schemas/run.py:15-59` (3 sites lies) |
| Toggle | `CINESORT_DISABLE_LOCAL_AUTH` | `CINESORT_PYDANTIC_STRICT` |
| Fallback | Bypass auth inconditionnel (renvoie True) | Fallback dict legacy + log ERROR |
| Domaine | Securite reseau (token Bearer) | Backward compat de schema |
| Endpoints concernes | 166 endpoints facade | 1 endpoint (`run/start_plan` in+out) |
| Bounded context | Transverse (tous) | Run uniquement |
| Lien avec SettingsFacade | Aucun (la facade Settings n'a pas de validation Pydantic equivalente, cf section 2.1) | Aucun (le mot "Settings" dans `PlanSettings` designe les options de scan, pas le store settings.json) |
| Couvert par fix #2/#3/#4 | OUI (re-ancrage env var) | NON (orthogonal, pas touche) |

Le prompt offrait deux interpretations : (a) "racine_facade_commune" ou (b) "classe approvisionnement settings, toggles qui ne font rien". **La reponse est (b) : DISTINCTE**.

Les "toggles qui ne font rien" — entendu au sens "le toggle `CINESORT_PYDANTIC_STRICT` ne touche jamais le code path auth" — est une formulation litterale exacte : l'env var Pydantic n'a aucun effet sur `_check_auth`, sur `_handle_post`, sur le rate-limit, ni sur le dispatcher. Symetriquement, `CINESORT_DISABLE_LOCAL_AUTH` n'a aucun effet sur le fallback dict de `_validate_passive`. Les deux clusters sont strictement disjoints en code et en effet runtime.

**Aucun fix code requis** dans cette section (le prompt demande explicitement "RAPPORTE — NE corrige PAS").

**Marqueur** : OPERATIONNEL pour la classification distincte (decoulant directement de la lecture croisee S1+S2+S3 + traces sections 1-3). HYPOTHESE pour les 2 observations annexes 4.4 (potentielle inconsequence de la validation passive sur le run effectif, potentielle casse si un caller migre au schema wrappe) — non confirmees runtime, hors-perimetre du cluster.

_Section 4 terminee — passe a section 5 (fix frontiere auth)._

## 5. Fix frontiere auth [WIP]
## 6. Re-ancrer tests [OPERATIONNEL — test #3 reclassse]

### 6.1 Decision et perimetre

Sur la base de la section 1c (Y = ARTEFACT LOOPBACK CONFIRME) et 3.4 (preuve fix unifie), le test #3 `tests/test_dashboard_shell.py::DashboardShellHttpTests::test_login_invalid_token_returns_401` est reclassse via **l'option 1 du bilan section 1c** : positionnement de l'env var `CINESORT_DISABLE_LOCAL_AUTH=1` dans `setUpClass`. C'est exactement le role du kill-switch documente dans `rest_server.py:_check_auth` L443-462 : "le bypass est volontairement contournable pour les tests".

**Strategie non-loopback simulee** : l'env var force `bypass_disabled=True` (`rest_server.py` L451), ce qui fait tomber le code dans la branche d'auth Bearer nominale (L463-508) **comme si le client n'etait pas loopback**. Le serveur reste bind `127.0.0.1` (defaut), le client reste `HTTPConnection("127.0.0.1", ...)`, mais le check `client_ip in _LOCAL_CLIENT_IPS && bind_host == "127.0.0.1"` est neutralise par la 1ere condition (`not bypass_disabled` = False). Resultat : le dispatch passe par `_send_unauthorized()` -> 401, exactement comme un vrai client externe.

**Aucun fix code de production** : `rest_server.py` n'est pas touche. Conformement a la memoire user `BYPASS LOOPBACK CONSERVE (UI desktop dependante)` et au prompt `TA MISSION ... Pas de fix code produit`. Seul le test est modifie.

### 6.2 Modifications apportees a `tests/test_dashboard_shell.py`

| Site | Avant | Apres |
|------|-------|-------|
| Imports | (pas d'import `os`) | `import os` ajoute |
| `setUpClass` | (pas de gestion env var) | Sauvegarde `cls._prev_disable_local_auth` + `os.environ["CINESORT_DISABLE_LOCAL_AUTH"] = "1"` AVANT instanciation `RestApiServer` |
| `tearDownClass` | (pas de restauration) | Restaure la valeur precedente (pop si None, sinon reaffectation) APRES `server.stop()` |
| `test_login_invalid_token_returns_401` | Docstring 1 ligne | Docstring etendue documentant le re-ancrage non-loopback + reference bilan 1c/3.4 |

Les autres tests de la classe sont preserves : `test_login_valid_token_gets_settings` (valid_token, attendu 200) reste vert car l'auth Bearer nominale avec token correct renvoie 200 ; les tests statiques (`test_styles_css_served`, etc.) frappent du GET, non concernes par `_check_auth` (cf section 2.2 : GET = pas d'auth).

### 6.3 Verification runtime (rouge -> vert)

**Etat AVANT** :
```
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_login_invalid_token_returns_401 FAILED
AssertionError: 200 != 401
```
Status code 200 confirme le bypass loopback actif (cf traces section 1a + 1b).

**Etat APRES** (run reel 2026-06-09) :
```
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_app_js_served PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_core_modules_served PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_font_served_with_correct_mime PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_health_accessible_for_version_check PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_index_html_has_login_form PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_index_html_has_sidebar_nav SKIPPED (pre-existant V5B-01)
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_index_html_has_view_placeholders PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_index_html_links_css PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_index_html_loads_app_js_as_module PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_login_invalid_token_returns_401 PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_login_valid_token_gets_settings PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_login_view_served PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_static_files_accessible_without_auth PASSED
tests/test_dashboard_shell.py::DashboardShellHttpTests::test_styles_css_served PASSED
======================== 13 passed, 1 skipped in 1.45s ========================
```

Le test cible passe de **FAILED (200!=401)** a **PASSED**. Le test frere `test_login_valid_token_gets_settings` continue de passer (valide la non-regression auth nominale). Le `SKIPPED` est pre-existant (V5B-01, sidebar v5 dynamique), sans rapport avec ce re-ancrage.

### 6.4 Pourquoi cette option (et pas une autre)

3 options envisagees en 1c L172-176 :
1. **`CINESORT_DISABLE_LOCAL_AUTH=1` dans `setUpClass`** -> retenue. La plus simple, isole le test du bypass design, alignee avec l'intent du kill-switch.
2. Serveur `host="0.0.0.0"` + requete via IP non-loopback -> lourd, requiert interface reseau active, casse en CI sandbox.
3. Mock `client_address = ("192.168.x.x", port)` au niveau handler -> artificiel, deterministe mais fragilise la fixture (couplage handler interne).

L'option 1 simule un client externe non-loopback **du point de vue de `_check_auth`** : la branche d'auth Bearer s'execute integralement (L463 `if not self.auth_token`, L465-466 `auth.startswith("Bearer ")`, L475 `hmac.compare_digest`), exactement comme pour un client LAN avec bind 0.0.0.0 (cf trace section 1b strategy B = strategy A en termes de comportement `_check_auth`).

### 6.5 Conformite aux memoires user

- `BYPASS LOOPBACK CONSERVE (UI desktop dependante)` : RESPECTE. Le bypass design en `rest_server.py` est intact, aucune modification du code de production.
- `ACCEPTATION AUTH = test rouge->vert vraie entree publique ET check runtime non-loopback simulant client externe (PAS observe.py qui donne faux vert sur loopback)` : LA MOITIE RESPECTEE pour la partie "check runtime non-loopback simulant client externe" — l'env var fait litteralement basculer le runtime sur la branche non-loopback. La partie "vraie entree publique" relevera de la section 8 (GATE Auth) avec un test E2E ou Playwright frappant `127.0.0.1:8642` reel apres avoir configure `rest_api_enabled=True` + bind 0.0.0.0 (deja prouve runtime en section 1b strategy A, reste a outillé en harness CI).
- `Pas de fix code produit` (prompt mission) : RESPECTE. Diff = tests/test_dashboard_shell.py uniquement.
- `checkpoint avant chaque fix` : N/A — pas de fix code, juste un re-ancrage de test ; le test passe rouge -> vert atomiquement.
- `SCRUBBE` : aucun secret dans la diff (token de test `wrong-token` hardcode est public, pas un vrai secret).
- `1 sujet par commit` : la diff porte un seul sujet (re-ancrage test #3). Commit OK pour Section 9.
- `ECRITURE bilan AU FUR ET A MESURE anti-interruption` : RESPECTE — cette section ecrite immediatement apres validation runtime.

### 6.6 Tests #2 et #4 : reste de la mission "re-ancrer tests"

Le prompt mission cible explicitement le **test #3**. Les tests #2 (`test_dashboard_infra.py::RateLimitHttpTests::test_rate_limit_blocks_after_5_failures`) et #4 (`test_log_context.py::RestRequestIdHeaderTests::test_x_request_id_on_unauthorized_post`) partagent la meme racine (cf section 3.3) et beneficieraient du meme re-ancrage (env var dans leur `setUpClass` respectif), mais ils sont **hors perimetre de la mission actuelle** ("Reclasser test #3"). Section 6 reste OPERATIONNEL pour le test #3 ; #2/#4 a basculer en iteration suivante avec la meme strategie.

**Marqueur** : OPERATIONNEL pour test #3. WIP pour la section 6 globale (tests #2/#4 a venir).

_Section 6.1-6.6 terminee — section 6 passe a OPERATIONNEL pour le sous-perimetre test #3._

## 7. GATE Non-regression posters [WIP]
## 8. GATE Auth (vraie entree + runtime non-loopback) [WIP]
## 9. Commits [WIP]
