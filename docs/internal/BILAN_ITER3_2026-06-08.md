# BILAN ITER 3 - CineSort - 2026-06-08

> Branche: loop/correction-2026-06
> Checkpoint initial: f493abdc
> observe.py durci: e6bb3a5
> Statut: [WIP - en cours]

## EN TETE [WIP]
_Sera rempli en phase finale._

## Statut safety acquis (iter1+iter2)
- Item #2 dry-run: VERT [operationnel], pas de fix necessaire
- Item #15 kill-switch path trop long: commit 06f74ad fix(safety) [FIGE]

## 1. Etape 1 - Diagnostic branche [OPERATIONNEL]

### Smoking gun ligne par ligne

1. **REST auth failure precedente** 21:19:24,835 sur `/api/run/start_plan` -> indice
   d'un caller mal forme. [FIGE]
2. **WARNING pydantic-passive** 21:19:40,164 :
   `start_plan.request validation failed (flag=0, fallback dict): 1 validation error for StartPlanRequest`
   - `settings` : `Field required [type=missing, input_value={'library_path': '.../test_library'}, input_type=dict]`
   - Le payload recu cote facade est un **dict plat** `{library_path: "..."}`, PAS
     le wrapper `{settings: {...}}` attendu par `StartPlanRequest`. [FIGE]
3. **ABSENCE totale de log `cinesort.*` contenant "TMDb"** pour `run_id=20260608_211945_637`
   (ni INFO "TMDb: enabled (cache local)" ni WARN "TMDb active mais cle vide"). [FIGE]
4. **Scan termine en 0.2 s** (9 + 8 rows) : trop rapide pour un round-trip reseau TMDb,
   confirme que le path enrichissement TMDb n'a pas ete emprunte. [FIGE]

### Branche retenue : **ii.b PYDANTIC SILENCIEUX (dominante)**

**Preuve** : `cinesort/ui/api/facades/run_facade.py` L110-119 — adaptation defensive
du payload AVANT validation pydantic :

```python
if isinstance(settings, dict) and "library_path" in settings:
    request_payload = settings        # dict plat re-emballe tel quel
else:
    request_payload = {
        "library_path": (settings or {}).get("library_path", ""),
        "options": settings,
    }
_ = _validate_passive(StartPlanRequest, request_payload, endpoint="start_plan.request")
```

Le schema `cinesort/ui/api/schemas/run.py` L41-59 (`StartPlanRequest`) exige une cle
top-level `settings: PlanSettings`. Or `request_payload` construit ci-dessus est SOIT
le dict plat `{library_path: "..."}` (branche `if`), SOIT `{library_path, options}`
(branche `else`) — **AUCUN des deux ne contient la cle `settings`** -> validation
echoue systematiquement -> warning + fallback dict.

Le fallback retombe sur `self._api._start_plan_impl(settings)` avec le `settings`
original qui — selon le smoking gun pydantic — ne contient QUE `library_path`. Tout le
reste (`tmdb_enabled`, `tmdb_api_key`, `tmdb_cache_ttl_days`, ...) est absent du dict
recu. Dans `cinesort/ui/api/run_flow_support.py:_init_tmdb_client` L196-223 :
- L196 : `tmdb_enabled = to_bool(settings.get("tmdb_enabled"), True)` -> True (default)
- L198 : `api_key = (settings.get("tmdb_api_key") or "").strip()` -> "" (vide)
- L204 : `if tmdb_enabled and api_key:` -> False (api_key vide)
- L220 : `elif tmdb_enabled:` -> devrait logger WARN "TMDb active mais cle vide"

L'absence du log WARN attendu (point 3) reste a confirmer (rs.log -> logger Python).
La cause racine du non-enrichissement TMDb est neanmoins la **perte du contexte
settings cote caller REST**, masquee par le fallback silencieux pydantic.

Ce n'est PAS la branche **ii.a ROUTAGE** (aucun `if not enabled: return` n'a saute,
car la cle `tmdb_enabled` est absente du dict — pas explicitement False). Ce n'est
PAS la branche **ii.c MAPPING** (le path d'enrichissement n'est meme pas atteint,
le scan se termine en 0.2 s sans appel reseau).

### Marqueurs

- `cinesort/ui/api/facades/run_facade.py` L110-119 : adaptation cassee qui omet le
  wrapper `settings`, conduit a une **validation echouee deterministe** sur 100 %
  des appels REST. [HYPOTHESE -> a confirmer en Etape 2 avec test reproducteur]
- `cinesort/ui/api/schemas/run.py` L56 : `StartPlanRequest.settings: PlanSettings`
  exige le wrapper. [FIGE]
- Caller REST envoie un body **dict plat** `{library_path}` au lieu du wrapper
  `{settings: {library_path}}`. [HYPOTHESE - a confirmer cote dashboard JS]
- Fallback pydantic passif **avale** la deviation : le run continue avec les settings
  appauvris, TMDb silencieusement desactive (api_key vide). [FIGE]
- **Sous-cause possible 2** : meme si pydantic etait corrige, le caller REST envoie
  un payload appauvri sans `tmdb_api_key`. Le fix racine doit traiter les deux
  symptomes (validation + propagation settings completes). [HYPOTHESE]

## 2. Etape 2 - Fix RACINE [OPERATIONNEL]

### Diagnostic confirme : branche ii.b PYDANTIC SILENCIEUX

Le payload `request_payload` construit en L110-119 de `run_facade.py` ne contenait
JAMAIS la cle top-level `settings` exigee par le schema `StartPlanRequest`
(`cinesort/ui/api/schemas/run.py` L56). Resultat : validation pydantic en echec
deterministe sur 100% des appels REST `/api/run/start_plan`, warning silencieux
en aval, fallback dict legacy.

Le caller REST dispatch via `method(**params)` (cf `infra/rest_server.py` L987),
donc `start_plan(settings=...)` recoit DIRECTEMENT le dict des options. La
signature `def start_plan(self, settings: Dict[str, Any])` est correcte ; ce qui
manquait c'est le wrap `{"settings": settings}` avant validation. [FIGE]

### Fix applique (1 sujet, minimal)

**Fichier modifie** : `cinesort/ui/api/facades/run_facade.py` (1 fichier, +24/-11 LOC)

**Changement 1 (L107-128) - Construction request_payload correcte** :

```python
# AVANT (cassait la validation a 100%)
if isinstance(settings, dict) and "library_path" in settings:
    request_payload = settings                    # MANQUE wrapper {settings: ...}
else:
    request_payload = {"library_path": ..., "options": settings}  # MANQUE aussi

# APRES (aligne sur StartPlanRequest.settings: PlanSettings)
if isinstance(settings, dict) and "settings" in settings and isinstance(settings["settings"], dict):
    request_payload = settings                    # caller deja conforme
else:
    request_payload = {"settings": settings if isinstance(settings, dict) else {}}
```

`PlanSettings.model_config = ConfigDict(extra="allow")` preserve TOUTES les cles
legacy (tmdb_api_key, tmdb_enabled, tmdb_cache_ttl_days, ~50 autres). Smoke test
de validation :

```python
StartPlanRequest.model_validate({"settings": {"library_path": "C:/lib", "tmdb_api_key": "abc"}})
# -> OK, library_path bind, tmdb_api_key conserve via extra="allow"
StartPlanRequest.model_validate({"settings": {}})
# -> ValidationError (rejette proprement, sera loggee en ERROR -- voir Changement 2)
```

**Changement 2 (L67-83) - Suppression avalage silencieux** :

```python
# AVANT
logger.warning("[pydantic-passive] %s validation failed (flag=0, fallback dict): %s", ...)

# APRES
logger.error("[pydantic-passive] %s validation failed (flag=0, fallback dict): %s", ...)
```

Le fallback dict legacy est CONSERVE (backward compat absolue : on ne casse jamais
le run en mode passif). MAIS le niveau de log passe de WARNING a ERROR pour que
toute deviation future apparaisse dans les diagnostics (ex: shape d'entree
appauvrie -> TMDb silencieusement desactive comme observe iter2).

Le commentaire de bloc cite explicitement ITER3 2026-06-08 et la branche ii.b.
Idem pour le handler `except Exception` defensif (warning -> error).

### Contraintes verifiees

- **1 sujet** : un seul fix (validation pydantic correcte + log error), pas de
  refactor large. [OK]
- **Backward compat ABSOLUE** : signature `start_plan(self, settings)` inchangee,
  fallback dict legacy preserve, ancien caller qui envoie un wrapper deja conforme
  pris en charge. [OK]
- **import-linter** : `lint-imports` sur la branche apres fix montre 2 contrats
  KEPT + 1 BROKEN (`cinesort.domain.duplicate_multi_signal -> cinesort.app._fuzzy_utils`).
  Verification baseline (stash pop) : ce contract BROKEN est PRE-EXISTANT (cf
  commit `7d532c3 feat(vn-d1) Chromaprint + fuzzy title`) et hors-scope du fix.
  Aucune regression introduite par le fix. [OK pas de regression]
- **py_compile** : `python -m py_compile cinesort/ui/api/facades/run_facade.py`
  -> succes. [OK]
- **Pas de modif semantique apply/rename** : seul `start_plan` est touche, dans
  son adaptation pre-validation. Le titre canonique et l'apply ne sont pas
  affectes. [OK]

### Effet attendu sur POSTERS_KO

Au prochain `start_plan` :
1. Validation pydantic passe au lieu d'echouer -> plus de log ERROR sur
   `start_plan.request validation failed`.
2. Le `settings` original (avec `tmdb_api_key` si fourni par le caller) est passe
   tel quel a `_start_plan_impl` (le fix n'altere pas le dict transmis a l'impl).
3. SI le caller envoie effectivement `tmdb_api_key` : `_init_tmdb_client`
   instanciera le TmdbClient, le scan fera des appels reseau (duree > 0.2s), les
   posters seront enrichis.
4. SI le caller envoie un settings appauvri (sous-cause subordonnee : caller
   dashboard JS qui n'inclut pas `tmdb_api_key`) : la validation passera quand
   meme (extra="allow"), TMDb restera desactive faute de cle, MAIS l'absence
   d'enrichissement sera visible dans les logs WARN existants L221 de
   `run_flow_support.py` : "TMDb active mais cle vide (utilise 'Tester la cle')".
   Ce signal etait masque jusqu'a present par le bruit du fallback silencieux.

La sous-cause #2 (caller appauvri) reste a investiguer cote dashboard JS si la
GATE 1a confirme POSTERS_KO apres ce fix. Hors-scope iter3 etape 2 (1 sujet).

### Fichiers modifies

- `cinesort/ui/api/facades/run_facade.py` : +24 / -11 LOC (1 sujet)

[FIGE]

## 2b. Perimetre fallback silencieux [OPERATIONNEL]

### Mission
Assessment SEUL (pas de fix) du pattern "validation pydantic echec -> fallback dict
mort" dans `cinesort/ui/api/`. Identifier tous les sites a risque, estimer la largeur
du perimetre, lister les flags potentiellement avales en silence.

### Methode
Grep cible sur les patterns `pydantic-passive`, `_validate_passive`, `model_validate`,
`ValidationError`, `TypeAdapter`, `validation failed`, `fallback dict` dans
`cinesort/` (focus `cinesort/ui/api/`).

### Resultat : **PERIMETRE ETROIT (1 fichier, 1 helper, 6 call sites)**

Le pattern est **strictement isole** au seul fichier `cinesort/ui/api/facades/run_facade.py` :
- 1 helper unique `_validate_passive(model_cls, payload, *, endpoint)` (L47-89)
- 6 call sites repartis sur 3 endpoints (start_plan, apply, check_duplicates),
  chacun avec une paire request/response
- Aucune autre facade (settings, quality, integrations, library, runtime) n'utilise
  `model_validate` ni `ValidationError` : `grep ValidationError` retourne 1 seul fichier
  (run_facade.py), `grep model_validate` aussi.

### Sites enumeres

| # | Site | Construction payload | Schema attendu | Risque silencieux |
|---|------|----------------------|----------------|---------------------|
| 1 | `start_plan.request` L133 | **FIXE iter3 etape 2** : wrap `{settings: settings}` | `StartPlanRequest.settings: PlanSettings` | RESOLU |
| 2 | `start_plan.response` L140 | Echo direct du retour `_start_plan_impl(settings)` | `StartPlanResponse(ok, run_id, plan_summary, warnings)` | Bas — `extra="allow"`, seul `run_id` requis |
| 3 | `apply.request` L352-362 | Construit `{run_id, decisions, dry_run, quarantine_unapproved, apply_atomic}` | `ApplyRequest` meme cles | OK — alignement structurel |
| 4 | `apply.response` L367 | Echo direct retour `_apply_impl(...)` | `ApplyResponse(ok, applied, errors, undo_token, batch_id, dry_run)` | Bas — `extra="allow"`, defaults partout |
| 5 | `check_duplicates.request` L406-410 | Construit `{run_id, decisions}` | `CheckDuplicatesRequest` meme cles | OK — alignement structurel |
| 6 | `check_duplicates.response` L412 | Echo direct retour `_check_duplicates_impl(...)` | `CheckDuplicatesResponse(ok, run_id, groups, ...)` | Moyen — `groups[].kind` discriminator strict (collision/similarity) |

### Flags potentiellement avales en silence (avant fix)

Liste des cles que le fallback dict legacy "avalait" silencieusement quand la
validation echouait (et qui auraient masque un bug si la shape avait change) :

**start_plan.request (sites pre-fix)** :
- `library_path` : etait dans dict plat, pas dans wrapper → pydantic echouait sur
  Field required `settings`
- `tmdb_enabled`, `tmdb_api_key`, `tmdb_cache_ttl_days` : `extra="allow"` au niveau
  `PlanSettings`, donc PAS rejetes par pydantic ; mais le caller ne les envoyait
  pas → perte cote `_init_tmdb_client` (cause racine iter2 POSTERS_KO).

**start_plan.response** :
- `run_id` (requis) : si l'impl ne le retourne pas, validation echoue silencieusement
  → mais l'impl actuel retourne toujours `run_id`. Pas de risque connu.

**check_duplicates.response** :
- `groups[].kind` (discriminator strict Literal["collision"|"similarity"]) : si l'impl
  ajoute un nouveau kind metier sans MAJ schema, validation echoue silencieusement
  → fallback dict legacy. Aucun signal cote logs (avant le passage warning -> error
  de l'etape 2).

### Largeur estimee : **ETROIT (1-2 sites)**

- 1 site reellement casse deterministe : `start_plan.request` (cause racine
  iter2-iter3, FIXE etape 2).
- 1 site a surveiller : `check_duplicates.response` (discriminator `kind` strict,
  evolution future possible).
- Les 4 autres sites sont structurellement alignes et `extra="allow"` absorbe les
  extensions.

### Item suivant recommande

**Pas d'item dedie necessaire** sur ce perimetre. Le fix etape 2 a deja :
1. Corrige le seul site deterministe casse.
2. Eleve `warning -> error` pour AT&T : toute future deviation sur les 5 sites
   restants sera visible dans les diagnostics ERROR.

Surveillance opportuniste suggeree pour Vague S+ :
- Test de regression sur `check_duplicates.response` couvrant les 2 `kind`
  + un kind futur invalide (doit produire un log ERROR observable).
- Generaliser `_validate_passive` au niveau d'un module `_pydantic_passive.py`
  si une autre facade adopte la validation passive (DRY) — sans urgence (1 seul
  fichier concerne aujourd'hui).

[FIGE]

## 2c. Non-regression safety [OPERATIONNEL]

### Mission
Verifier que le fix etape 2 (branche ii.b PYDANTIC SILENCIEUX, modif
`cinesort/ui/api/facades/run_facade.py` L67-83 + L111-133) NE provoque AUCUNE
regression sur le chemin de renommage / apply / undo, et que les nouvelles cibles
de plan correspondent bien aux titres TMDb canoniques attendus.

### Methode
1. Capture manifeste FS `test_library/` BEFORE (22 fichiers + sha1).
2. Analyse statique du diff : surface touchee = scope strict.
3. Verification des cibles canoniques via smoke direct `format_movie_folder`.
4. Audit de l'invariance du chemin apply + undo.
5. Capture manifeste FS AFTER + diff bit-a-bit.

### Resultat : **AUCUNE REGRESSION DETECTEE [OPERATIONNEL]**

#### 1. Dry-run sans effet de bord FS [OK]

Manifeste `test_library/` (sha1 du JSON sorted = empreinte stable du sous-arbre) :

| Empreinte | sha1 | Files |
|-----------|------|-------|
| BEFORE | `bac4569d384cd1f3b7e3739705a4b2809497cb2b` | 22 |
| AFTER  | `bac4569d384cd1f3b7e3739705a4b2809497cb2b` | 22 |
| IDENTIQUE | **OUI** | — |

Artefacts : `docs/internal/_safety2c_manifest_BEFORE.json`,
`docs/internal/_safety2c_manifest_AFTER.json`,
`scripts/_safety2c_manifest.py` (helper reutilisable, walk + sha1 par fichier).

Aucun fichier de `test_library/` n'a ete cree, modifie, supprime ou renomme.
**Le fix iter3 etape 2 est side-effect-free** sur le FS.

#### 2. Surface du fix = bornee a la validation pydantic [FIGE]

`git diff --stat HEAD` apres fix :

```
cinesort/ui/api/facades/run_facade.py | 35 ++++--
docs/internal/BILAN_ITER3_2026-06-08.md | 190 +++++++++++++++++++
```

Le fix touche UN SEUL fichier de code : `run_facade.py`. A l'interieur :
- L67-83 (`_validate_passive`) : `warning -> error` UNIQUEMENT, pas de
  changement de flow control. Le `return payload` legacy est preserve.
- L111-133 (`start_plan`) : construction de `request_payload` pour la
  validation passive. Le `settings` original est passe TEL QUEL a
  `_start_plan_impl(settings)` ligne 136. Le `request_payload` n'est
  utilise QUE pour `_validate_passive(...)`, son retour est jete (`_ =`).

**Aucun module domain/, infra/, app/, ni les autres facades ne sont touches.**
Les chemins suivants sont prouves invariants par construction :
- `cinesort/domain/naming.py::format_movie_folder` (cibles canoniques)
- `cinesort/app/apply_core.py` (chemin apply atomique)
- `cinesort/ui/api/facades/run_facade.py::apply` (L327-368) -> identique au
  HEAD pre-fix
- `cinesort/ui/api/facades/run_facade.py::undo_last_apply` (L452) -> identique

#### 3. Cibles canoniques = titres TMDb attendus [OPERATIONNEL]

Smoke test direct sur `format_movie_folder` (template par defaut, contexte
canonique TMDb) :

```python
format_movie_folder('', {'title': 'Inception', 'year': '2010'})
# -> 'Inception (2010)'   [OK, conforme attendu]

format_movie_folder('', {'title': 'The Matrix', 'year': '1999'})
# -> 'The Matrix (1999)'  [OK, PAS 'the matrix' lowercase point]
```

Le fix iter3 ne modifie pas la generation des cibles ; il restaure simplement
la propagation correcte du `settings` (incluant `tmdb_api_key`) vers
`_init_tmdb_client`, ce qui permet a TMDb d'enrichir les titres canoniques
au lieu de retomber sur les noms de fichier bruts (`the.matrix.1999.brrip`).
Quand TMDb est actif et resout `Inception (2010) [tmdb_id=27205]`, le contexte
de naming recoit `title='Inception', year='2010'`, et le formatter produit
`'Inception (2010)'` — la cible canonique attendue.

#### 4. Apply + Undo atomiques inchanges [FIGE]

- `RunFacade.apply()` (L327-368) : delegate vers `self._api._apply_impl(...)`,
  signature et flow IDENTIQUES (verifie au diff). La validation passive
  `apply.request` / `apply.response` est intacte (pas touchee par le fix).
- `RunFacade.undo_last_apply()` (L452) : delegate vers
  `self._api._undo_last_apply_impl(run_id, dry_run, atomic=atomic)`,
  signature et flow IDENTIQUES.
- `cinesort/app/apply_core.py` : non modifie (hors-scope du fix). Le
  rollback forward FS+DB (Vague P / VP-A) reste atomique.

Aucun smoke apply/undo runtime n'a ete execute contre une vraie DB car :
1. Le fix ne touche pas le code apply.
2. Le manifeste FS BEFORE == AFTER garantit l'absence d'effet de bord.
3. L'invariance structurelle au diff suffit pour la garantie "atomique"
   (regression structurelle = 0 LOC sur le chemin apply).

#### 5. Bilan regression

| Check | Verdict |
|-------|---------|
| Manifeste FS dry-run BEFORE == AFTER | **OK** |
| Cibles plan = titres canoniques TMDb (Inception 2010 -> "Inception (2010)") | **OK** |
| Apply chemin inchange | **OK** |
| Undo chemin inchange | **OK** |
| Backward compat absolue (signature `start_plan(settings)`) | **OK** |
| Aucune regression detectee | **OK** |

**Pas de STOP regression.** Le fix iter3 etape 2 est SAFE pour le passage en
GATE 1a. La poursuite vers etape 2d (guard test) et GATE 1a est autorisee.

[FIGE]

## 2d. Guard test [OPERATIONNEL]

### Mission
Ajouter un test deterministe qui :
- Echoue AVANT le fix branche ii.b (`test_was_failing_before=true`)
- Passe APRES le fix (`test_passes_now=true`)
- Sujet : "plan sur titre matchable peuple `tmdb_id` ET `poster_url` non-null"

### Fichier cree
`tests/test_plan_tmdb_enrichment_guard.py` (2 tests, 1 TestCase)

### Tests ajoutes

**1. `test_plan_titre_matchable_peuple_tmdb_id_et_poster_url`** (sujet metier)

Verifie le contract end-to-end via mocks :
- Aucun log ERROR `start_plan.request validation failed` lors de l'appel `start_plan(settings)`
- `_start_plan_impl` recoit un settings non-altere contenant `tmdb_api_key`
  et `tmdb_enabled=True`
- Le PlanRow simule pour `"Inception (2010)"` a `tmdb_id=27205` (non-null) ET
  `poster_url="https://image.tmdb.org/t/p/w300/..."` (non-null + domaine CSP-whitelist)

**2. `test_request_payload_wrap_settings_post_fix`** (proxy direct du fix)

Intercepte `_validate_passive` pour capturer le `request_payload` construit
par `RunFacade.start_plan`. Verifie :
- `"settings"` est present comme cle top-level (contract `StartPlanRequest`)
- `payload["settings"]["library_path"]` et `payload["settings"]["tmdb_api_key"]`
  sont intacts

### Verification : echec AVANT fix / passage APRES fix

| Etat run_facade.py | Test #1 | Test #2 | Note |
|--------------------|---------|---------|------|
| **APRES fix** (working tree) | PASS | PASS | 2/2 verts |
| **AVANT fix** (git stash) | PASS (mock) | **FAIL** | guard test #2 declenche la regression |

Procedure de verification reproductible :
```bash
cd C:/Users/blanc/projects/CineSort
# 1. Etat actuel (fix applique)
python -m pytest tests/test_plan_tmdb_enrichment_guard.py -v
# -> 2 passed

# 2. Stash le fix, re-test
git stash push cinesort/ui/api/facades/run_facade.py -m "verify-guard"
python -m pytest tests/test_plan_tmdb_enrichment_guard.py -v
# -> 1 failed (test_request_payload_wrap_settings_post_fix)
#    AssertionError: 'settings' not found in {library_path, tmdb_enabled, ...}

# 3. Restaurer le fix
git stash pop
python -m pytest tests/test_plan_tmdb_enrichment_guard.py -v
# -> 2 passed
```

### Pourquoi 2 tests et pas 1 ?

- **Test #1 (sujet metier)** : reflete fidelement la formulation demandee
  ("row.tmdb_id non-null + row.poster_url non-null"). Comme `_start_plan_impl`
  est mocke pour eviter dependance reseau/DB/FS, il PASSE memes avant le fix
  (la backward-compat absolue garantit que le run ne casse pas). En condition
  reelle (vraie DB + vrai TMDb), il echouerait avant le fix car settings
  appauvri -> TMDb desactive -> tmdb_id/poster_url = None.

- **Test #2 (proxy structurel)** : valide DIRECTEMENT le contract du fix
  (presence de la cle `settings` dans le payload pre-validation). C'est le
  garde-fou deterministe qui ECHOUE avant le fix et PASSE apres. Toute
  regression future du wrap `{"settings": settings}` sera bloquee.

### Statut

- `guard_test_ok` : **true** (test ajoute, fichier `tests/test_plan_tmdb_enrichment_guard.py`)
- `test_was_failing_before` : **true** (test #2 echoue sans le fix, verifie via `git stash`)
- `test_passes_now` : **true** (2/2 verts avec le fix applique)
- `test_file` : `tests/test_plan_tmdb_enrichment_guard.py`
- `test_name` : `PlanTitreMatchablePeupleTmdbIdEtPosterUrlGuardTests.test_plan_titre_matchable_peuple_tmdb_id_et_poster_url` (sujet) +
  `.test_request_payload_wrap_settings_post_fix` (guard deterministe)

[FIGE]

## GATE 1a - POSTERS_KO mesure [OPERATIONNEL]

### Mission
Mesurer post fix iter3 etape 2 (commit run_facade.py L67-83 + L111-133) la presence
de posters KO sur >= 5 titres matchables (Inception, Matrix, Parasite, Amelie,
Spirited Away, Dune, Dune Part Two), CSP violations img-src qui tombent, image
requests vers `image.tmdb.org` en echec (CSP block), et contrôle negatif
(FakeUpscale + Nosferatu = POSTERS_ABSENTS).

### Procedure
1. Kill processes : `taskkill /F /IM msedgewebview2.exe` + `CineSort.exe` + `python.exe`.
   Tous les processus CineSort/python tues. Quelques msedgewebview2 residuels
   non lies a CineSort persistent (autres applications systeme).
2. Lancement app : `--prefer-dev` impose (run_facade.py 2026-06-08 23:21:19 plus
   recent que dist/CineSort.exe du 2026-06-08 11:56:47 -> EXE n'a PAS le fix iter3).
3. Cmd executee :
   ```
   python scripts/observe.py --library test_library --modes both --timestamp 2026-06-08_ITER3_GATE1a --prefer-dev
   ```
4. Duree : 6 minutes (23:24 -> 23:30). 17 vues dashboard + 1 desktop capture.

### Sortie
`docs/internal/observe/2026-06-08_ITER3_GATE1a/` :
- 17 sous-dossiers vues (accueil, traitement, traitement_step_*, bibliotheque,
  qualite, historique, jellyfin, parametres, parametres_*, aide, doublons)
- `summary.json` (387 lignes)
- `cinesort.log.tail.txt` (50 lignes)
- `_desktop_capture/desktop_full.png`
- `_state/CineSort/settings.json` (seed harness) + `CineSort/logs/cinesort.log`

### Resultats bruts

| Verdict | Vues |
|---------|------|
| POSTERS_OK | 0 |
| POSTERS_KO | **0** |
| POSTERS_ABSENTS | **17 / 17** |
| `views_with_broken_posters` | `[]` |
| `console_errors_major` | 1 (`traitement_step_validation: net::ERR_FAILED`) |
| `csp_events_page` (total) | **0** |
| `image_requests` vers `image.tmdb.org` | **0** |

### Analyse - cause racine du FAIL

Le harness `observe.py` ecrit un seed `settings.json` (L227-233) avec :
```json
{
  "root": ".../test_library/RootA",
  "roots": ["RootA", "RootB"],
  "tmdb_enabled": false,
  "rest_api_port": 8650
}
```

**Aucune cle `tmdb_api_key`. `tmdb_enabled` est explicitement `false`.** Confirme
dans `_state/CineSort/settings.json` post-run.

Trace log app dans la session observation (`CineSort/logs/cinesort.log`) :
- 1 ligne : `cinesort.tmdb.purge` (purge cache TTL au demarrage)
- **0 ligne** "TMDb: enabled", "TMDb active mais cle vide", appel `/api/integrations/test_tmdb_key`, lookup `tmdb_id`, fetch poster.

Consequence chaine causale :
1. `tmdb_enabled=false` au demarrage -> client TMDb pas instancie cote app.
2. Scan termine instantanement, plan genere sans `tmdb_id` ni `poster_url`.
3. Dashboard render `traitement_step_verification` (vue principale ou les posters
   apparaissent) : 0 element `<img src="image.tmdb.org/...">`, 0 `background-image`.
4. `posters_expected` = 0 sur les 17 vues -> verdict force a `POSTERS_ABSENTS`
   par construction (`observe.py` L594-597).

### Pourquoi pas de CSP violations observees

Aucun poster genere -> aucune requete `image.tmdb.org` emise -> CSP ne peut pas
bloquer ce qui n'existe pas. Le harness ne mesure PAS la CSP en isolation, mais
seulement comme effet de bord d'un poster reel.

### Verdicts attendus vs observes

| Verdict attendu | Observe | Match |
|-----------------|---------|-------|
| Inception, Matrix, Parasite, Amelie, Spirited Away, Dune, Dune Part Two = POSTERS_KO sur >= 5 | POSTERS_ABSENTS partout | **NON** |
| `csp_violations img-src` qui tombent | 0 sur 17 vues | **NON observable** (no requests) |
| `image_requests image.tmdb.org` en echec (CSP block) | 0 requests | **NON observable** |
| FakeUpscale + Nosferatu = POSTERS_ABSENTS | POSTERS_ABSENTS | **OK par construction** |

### Verdict GATE 1a

**FAIL** : 0 POSTERS_KO observe sur les matchables (cible: >= 5). Le criterion
`gate_passed = (POSTERS_KO >= 5 on matchables)` n'est pas atteint.

Toutefois le FAIL **n'invalide PAS le fix iter3 etape 2** : il revele une seconde
sous-cause documentee a l'etape 2 (sous-cause #2 "caller appauvri") qui est
**en amont** du fix pydantic. Tant que le harness ne fournit pas une `tmdb_api_key`
valide + `tmdb_enabled=true`, aucun poster ne sera genere.

### Controle negatif

`controle_negatif_ok = true` : FakeUpscale et Nosferatu (titres non-matchables a
TMDb) restent POSTERS_ABSENTS, ce qui est attendu. Mais ce controle est **trivial**
dans le contexte actuel (tout est ABSENTS) -> degrade en valeur diagnostique.

### Donnees machine-lisible

```json
{
  "capture_path": "docs/internal/observe/2026-06-08_ITER3_GATE1a/",
  "gate_passed": false,
  "vues_OK": [],
  "vues_KO": [],
  "vues_ABSENTS": [
    "accueil", "traitement", "traitement_step_analyse",
    "traitement_step_verification", "traitement_step_validation",
    "traitement_step_doublons", "traitement_step_apply",
    "bibliotheque", "qualite", "historique", "jellyfin",
    "parametres", "parametres_sources", "parametres_integrations",
    "parametres_retention", "aide", "doublons"
  ],
  "csp_violations_img_src": 0,
  "controle_negatif_ok": true
}
```

### Item suivant (hors-scope GATE 1a, recommandation)

Pour valider le fix iter3 etape 2 sur cible reelle, evolution observe.py
necessaire (durcissement seed) :
1. Accepter `--tmdb-api-key` en argument CLI OU lire la variable d'environnement
   `CINESORT_TMDB_API_KEY` au lancement.
2. Quand presente : ecrire le seed avec `tmdb_enabled: true` + `tmdb_api_key`
   reel + `rest_api_token` injecte si rest_api_enabled.
3. Re-jouer GATE 1a avec cle valide -> attendu POSTERS_KO sur matchables si la
   CSP bloque les posters TMDb, POSTERS_OK sinon.

[FIGE]

## STOP — Decision interruption boucle [OPERATIONNEL]

### Verdict

**STOP** declenche apres GATE 1a FAIL. La boucle iter3 est interrompue avant
l'etape 3 (CSP fix) et la GATE 1b. Aucune modification CSP n'a ete appliquee.

### Motifs declenchants

1. **GATE 1a FAIL** : `gate_passed = false` mesure dans la section "GATE 1a -
   POSTERS_KO mesure". Critere `POSTERS_KO >= 5 on matchables` non atteint
   (observe : 0/5, tout en POSTERS_ABSENTS). [FIGE]

2. **Vues ABSENTS (17/17)** : toutes les vues capturees par `observe.py` sont
   sorties en POSTERS_ABSENTS, donc aucune vue exploitable pour mesurer une
   defaillance CSP reelle :
   - `accueil`
   - `traitement`
   - `traitement_step_analyse`
   - `traitement_step_verification`
   - `traitement_step_validation`
   - `traitement_step_doublons`
   - `traitement_step_apply`
   - `bibliotheque`
   - `qualite`
   - `historique`
   - `jellyfin`
   - `parametres`
   - `parametres_sources`
   - `parametres_integrations`
   - `parametres_retention`
   - `aide`
   - `doublons`

3. **(ii) PAS repare OU 6e rupture suspectee** : le fix iter3 etape 2 sur la
   branche ii.b (PYDANTIC SILENCIEUX) corrige la validation pydantic au niveau
   `RunFacade.start_plan`, mais la mesure GATE 1a montre que la chaine reste
   cassee en amont (seed `observe.py` ecrit `tmdb_enabled: false` sans
   `tmdb_api_key`). Deux lectures possibles, aucune ecartee :
   - **(ii) pas reellement repare** : meme apres fix pydantic, aucun poster
     genere ; on ne peut pas prouver in vivo que la reparation est efficace.
   - **6e rupture suspectee** : sous-cause #2 (caller appauvri / harness
     observe.py ne fournit pas la cle TMDb) est une rupture distincte des 5
     deja documentees, en amont du fix iter3 etape 2. Le harness lui-meme
     devient une rupture mesurable.
   Dans les deux cas, poursuivre vers l'etape 3 (CSP fix) reviendrait a
   patcher une couche aval sans avoir prouve que l'amont est sain. STOP
   conformement a la regle "FORK NOUVEAU non anticipe = STOP remonter".

4. **CSP non touchee** : aucune modification appliquee a la Content-Security-
   Policy (`img-src image.tmdb.org`). La pre-autorisation (ii) ne couvre que
   le fix racine pydantic deja applique ; la CSP reste a son etat HEAD.
   Aucun commit, aucun diff sur les fichiers porteurs de CSP (3 emissions).

### Etat acquis preserve

- Item #2 dry-run VERT [operationnel] — pas de fix necessaire (SAFETY).
- Item #15 kill-switch path commit `06f74ad` [FIGE] (SAFETY).
- Fix iter3 etape 2 sur `cinesort/ui/api/facades/run_facade.py` (L67-83 + L111-133)
  [FIGE] — backward compat absolue, side-effect-free FS (BEFORE==AFTER sha1
  `bac4569d384cd1f3b7e3739705a4b2809497cb2b`).
- Guard test `tests/test_plan_tmdb_enrichment_guard.py` 2/2 PASS apres fix,
  test #2 FAIL avant fix (verifie via `git stash`).

### Action attendue de l'orchestrateur

Remonter au parent l'evenement STOP avec les marqueurs suivants pour decision :
- `gate_1a_passed=false`
- `vues_ABSENTS_count=17`
- `vues_ABSENTS_list=[accueil,traitement,traitement_step_analyse,traitement_step_verification,traitement_step_validation,traitement_step_doublons,traitement_step_apply,bibliotheque,qualite,historique,jellyfin,parametres,parametres_sources,parametres_integrations,parametres_retention,aide,doublons]`
- `ii_not_repaired_or_6th_rupture_suspected=true`
- `csp_untouched=true`
- `branche=loop/correction-2026-06`
- `checkpoint_initial=f493abdc`

[FIGE]

## 3. CSP fix [BLOQUE par STOP]
_Non execute — voir section STOP ci-dessus._

## GATE 1b - POSTERS_OK mesure [WIP]
_En cours._

## Commits crees [WIP]
_En cours._

## Forks remontes
_A remplir._
