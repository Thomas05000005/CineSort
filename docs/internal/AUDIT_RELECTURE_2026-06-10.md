# AUDIT RELECTURE INTEGRALE DU CODE — 2026-06-10/11

> Demande : relecture de TOUT le code de l'app + rapport de tout ce qui ne va pas.
> Methode : workflow multi-agents ultracode `wf_984bef0d-63a` — 36 lecteurs (~120k lignes), 3 lentilles
> transverses, verification adversariale de 1er niveau, puis PANEL de double-refutation sur chaque
> CRITICAL/HIGH unique (2 juges independants : logique du code + atteignabilite runtime) et critique
> de completude. Acheve en 4 reprises sur coupures de quota (journal de resume, zero perte).
> Volume cumule : ~27M tokens, ~9300 tool calls, ~510 agents.

## Suivi des corrections

**Vague 1 — perte de donnees (3/3 corrigees, 2026-06-11)** :

| Finding | Commit | Statut |
|---|---|---|
| `quarantine_ttl.py:195` TTL sur st_mtime (CRITICAL) | `7089cab` | corrige + 2 GATE |
| `apply_core.py:1368/988` journal write-ahead perdu (HIGH) | `9d89200` | corrige + GATE |
| `apply_rollback.py:113` QUARANTINE_* non revertes (HIGH) | `e7346ee` | corrige + GATE |

Non-regression Vague 1 : 78/78 sur les fichiers touches, 359 passed sur la suite
apply/quarantine/rollback/journal. 2 echecs PRE-EXISTANTS hors perimetre
(`test_apply_op_labels`, `RecordApplyOpTests::test_returns_false_on_failure_and_logs`
— a traiter separement, ils echouaient deja avant ces fixes).

**Vague 2 — securite (6/6 corrigees, 2026-06-11)** :

| Finding | Commit | Statut |
|---|---|---|
| `rest_server.py` CORS * + bypass loopback = CSRF (REAL 2/2) | `b1dd226` | corrige + 7 GATE (garde Origin same-site + CORS sans wildcard) |
| `omdb_client.py:43` OMDb en HTTP clair | `5dc70ea` | corrige (HTTPS) |
| `log_scrubber.py:113` args non-str non scrubbes + `:150` install avant basicConfig | `e20d2c0` | corrige + 3 GATE |
| `tooling.py:122` whitelist binaire contournable | `8a387f2` | corrige + GATE (fail-closed, source unique) |
| `auto_install.py:53` archives sans SHA256 | `9ef5457` | mitige : pin via env var (rolling release, pas de hash fabrique) ; hash reel = tache release |

Non-regression Vague 2 : 120+ passed sur la suite REST/scrubber/probe. Echecs hors perimetre :
4 PRE-EXISTANTS (legacy 410 sur `/api/get_settings`) + 1 fuite d'etat rate-limiter inter-fichiers
(`test_x_request_id_on_unauthorized_post` passe en isolation).

**Vagues suivantes (a faire)** : (3) secrets masques (~12 chemins, fix centralise) ->
(4) contrats JS vivants (doublons, accueil, traitement).

---

## Chiffres finaux

| Severite | Findings uniques |
|---|---|
| CRITICAL | 6 |
| HIGH | 139 |
| MEDIUM | 133 |
| LOW | 35 |
| INFO | 1 |
| **Total** | **314** |

**Verdicts du panel de double-refutation** (144 CRITICAL/HIGH uniques juges, 2 votes chacun) :

- ✅ **106 double-confirmes REAL (2/2)** — fiabilite maximale, a corriger.
- ⚠️ **37 contestes (1 juge sur 2 a refute)** — reels mais portee/atteignabilite discutee, lire la raison de refutation avant de prioriser.
- ❌ **1 tue (2/2 refute)** — retire du backlog (marque TUE ci-dessous).

Notes de fiabilite : 14 findings de la 1re passe n'ont pas ete re-confirmes au re-run
(marques `[NON RE-CONFIRME]`, non-determinisme des re-runs partiels — a re-verifier a la main avant fix) ;
9 findings sont apparus au re-run (marques `[NOUVEAU RE-RUN]`, verifies une fois).
Les MEDIUM/LOW n'ont qu'une verification simple (pas de panel).

## Themes transverses (synthese)

1. **Secrets masques consommes en interne** — `_get_settings_impl()` masque les secrets (SEC-H2/H3) mais ~12 consommateurs internes relisent le payload masque et envoient litteralement `••••••••` comme credential : rapports Jellyfin/Plex/Radarr, emails SMTP, re-match TMDb (rescan), scans auto du watcher, scan lance depuis l'Accueil (JS), redemarrage du serveur REST (token + host + cors), mode `--api`. Toutes ces integrations echouent en 401 des qu'une vraie cle est configuree.
2. **Analyse perceptuelle morte de bout en bout** — crashes ndarray (3 CRITICAL) + mauvais index de piste audio (`-map 0:a:{index_global}`) + `-v quiet` qui supprime le JSON loudnorm + `signalstats` sans `metadata=print` + bloc astats Overall sans crest/dynamic range + mpdecimate qui logge en debug + composite v2 qui lit des cles de probe inexistantes (`audio` vs `audio_tracks`, `has_hdr10` vs `hdr10`) + `api._tmdb_client()` inexistant. Quasi aucune metrique perceptuelle reelle n'est mesuree ; les scores affiches sont des valeurs neutres/parfaites par defaut.
3. **Endpoints morts par drift interne** — au moins 10 features appellent des methodes/cles/chemins qui n'existent plus : quarantaine (viewer + purge + cron TTL), simulateur qualite, sagas incompletes, timeline Jellyfin, historique par film (x2), reset DB (mauvais chemin), preview naming sur vrai film, recherche TMDb du modal, analyze_quality_batch scope validated.
4. **Contrats UI<->backend casses dans le JS** — processing.js (4 etapes mortes), traitement.js (resume pre-apply faux, options inertes, auto-save auto-aborte), accueil.js (progression jamais affichee, options drawer ignorees dont dry_run), doublons.js (group_key mismatch : les decisions Garder A/B ne deplacent JAMAIS les perdants), historique.js (filtres morts), filtres avances bibliotheque (source/codec/480p/langues/aucun-st : 0 resultat systematique).
5. **Integrite apply/undo/rollback** — journal write-ahead silencieusement desactive pour les ops par-row, rollback atomique qui ne revert pas les QUARANTINE_*, reconciliation boot qui classe `completed` les batches zombies, undo selectif sans deadline 24h ni slot guard (course avec apply), UNDONE_PARTIAL qui bloque l'undo global, exceptions post-apply qui declenchent un rollback forward d'un apply reussi.
6. **TTL quarantaine = perte de donnees** (CRITICAL, double-confirme panel) — TTL base sur st_mtime preserve par move : tout fichier plus vieux que ttl_days est purge ~60 s apres le boot suivant sa mise en quarantaine, au lieu de 30 jours apres.
7. **Securite** — CORS `*` + bypass loopback = n'importe quel site web visite peut piloter l'API locale (start_plan/apply) ; OMDb en HTTP clair ; archives ffmpeg/MediaInfo telechargees sans verification SHA256 ; whitelist binaire contournable via le save generique ; scrubber qui laisse fuiter les exceptions (`%s` non-str) et les handlers console crees apres install ; token REST jamais persiste (auto-gen fantome) et serveur demarrable avec Bearer `••••••••` (constante publique).
8. **Scan/plan : films perdus et caches menteurs** — fast-path dossier `(YYYY)` jamais descendu (film imbrique absent du plan sans recours) ; scene_parser mutile des titres reels (`21 Jump Street` -> `Jump Street`, `Thor - Ragnarok` -> `Thor`) en violation de l'invariant titres ; `.ts` legitime marque CAM et cape Bronze ; caches incrementaux qui perdent 10 champs TV/sous-titres (renommage `S00E00` !), ignorent le kind, et cfg_sig incomplet.
9. **DB/infra** — pragma_history en INSERT+COMMIT a chaque connexion sans purge ; rotation backups qui detruit les backups manuels/post-apply en 5 boots ; migration 032 jamais appliquee (nom avec tirets) ; ignored_alerts cle sur hash() randomise par session ; circuit breaker probe jamais alimente en mode auto (le scenario NAS vise par iter13) ; circuit breaker TMDb inerte sur les 5xx.
10. **Invariants tier violes en 4 endroits** — depth-effects.css (gold = var(--accent) theme-dependant), export HTML (#f59e0b...), chart simulateur qualite, badge platinum historique (#b9f2ff). Et themes.css V6 supprime mais encore reference : variables glass fantomes, notification center sans pastille non-lu.

## Angles NON couverts par cet audit (verdict du critique de completude)

- CI/.github/workflows (19 fichiers : ci, release, codeql, gitleaks, bandit, smoke-exe, dependabot-auto-merge...)|Aucun lot ne couvre la supply chain : pinning des actions, permissions GITHUB_TOKEN, auto-merge Dependabot et release.yml signent/publient les binaires — un défaut ici compromet tout ce que l'audit du code a validé.
- PACKAGING (CineSort.spec, nuitka.config.cfg, build_windows.bat, scripts/build_nuitka.ps1, package_zip.py, sign_windows_release.ps1, runtime_hooks/splash_hook.py, version_info.txt, CineSort.exe.manifest)|La chaîne build→zip→sha256→signature n'est dans aucun lot ; c'est elle qui produit l'artefact livré aux utilisateurs (manifests dans packages/), une erreur d'inclusion/exclusion ou de signature invalide le binaire final.
- LOCALES (locales/en.json, locales/fr.json + scripts/i18n_full_sync.py)|Aucun lot i18n : désynchronisation de clés en/fr, encodage BOM (problème déjà documenté sur settings.json), placeholders incohérents = clés brutes ou crashs d'interpolation à l'écran ; les tests test_i18n_* existent mais leur validité n'a pas été auditée.
- HYGIENE REPO / ARTEFACTS TRACKÉS (docs/internal/observe = 771 Mo dont 1609 fichiers git-trackés avec profils EBWebView, logs _iter*/boot_*.err trackés à la racine, dist_backup_*, packages/*.zip)|Risque de fuite PII/token REST dans les logs et captures webview commités + bloat massif du repo ; le lot xcut-secrets a audité le code, pas l'historique git ni les artefacts.
- TESTS COMME OBJET D'AUDIT (tests/ ~400 fichiers, conftest.py, _helpers.py, tests/live, tests/stress, tests/manual, tests/e2e, cinesort/tests/ dupliqué)|Les 313 findings reposent sur du code prod, mais personne n'a vérifié que les tests d'invariants testent vraiment (sur-mock, tests live frappant de vraies API avec clés, fixtures conftest partagées) ; un test menteur masque les régressions des fixes à venir.
- SCRIPTS DEV (~50 fichiers dans scripts/ : migrate_*_84.py, privatize_cinesort_api_84.py, fix_*, dedup_*, fake_probes/, observe.py)|Scripts de migration/refactor automatisés qui réécrivent le code prod et scripts de scan touchant la bibliothèque réelle : non audités, potentiellement destructifs ou divergents des façades qu'ils sont censés maintenir.
- BINAIRES EMBARQUÉS (tools/ffmpeg.exe, ffprobe.exe, MediaInfo.exe — non trackés git)|Provenance, version et CVE non vérifiées (ffmpeg a un historique de CVE d'exécution via fichiers média malveillants — exactement le flux d'entrée de CineSort) ; pas de pin par hash visible donc build non reproductible.
- DEPENDANCES MULTI-SOURCES (requirements.txt, requirements.lock, uv.lock, requirements-build.txt, requirements-dev.txt, pyproject.toml)|Quatre sources de vérité de versions qui peuvent diverger silencieusement : le binaire livré peut embarquer d'autres versions que celles testées en CI ; vérifier quel manifest pip-audit.yml scanne réellement.
- WEB/SHARED + SPLASH (web/shared/tokens.css, themes.css, components.css, fonts/, web/splash.html, web/dashboard/bootstrap-debug.js, bootstrap-bisect.js, inline-overrides.css, depth-effects.css)|Si les lots css/js-* ne couvraient que web/dashboard/styles.css et les vues, les tokens/thèmes partagés (source des invariantes tier-colors déjà fragiles, cf. :root dupliqué L1998) et le splash chargé au boot WebView2 sont hors radar, plus des fichiers debug livrés en prod.
- GARDE-FOUS LOCAUX + DONNÉES LIVRÉES (.pre-commit-config.yaml, check_project.bat, settings.json.example, cinesort/data/presets/tier_preset_trash_2026.json)|Cohérence pre-commit vs CI non vérifiée (gitleaks/bandit locaux ?), settings.json.example peut normaliser un token en clair, et les presets JSON livrés pilotent le comportement domaine (tiers/naming) sans être passés dans les lots dom-*.

---

## CRITICAL (6)

### Domaine — scoring, parsing, perceptuel

- **`cinesort/domain/perceptual/video_analysis.py:504`** — ✅ PANEL 2/2
  `if pixels:` sur un np.ndarray (frame_extraction.parse_raw_frame retourne un ndarray depuis la migration B3, cf commentaire upscale_detection.py:138) leve ValueError « The truth value of an array with more than one element is ambiguous » des la 1re frame reelle — verifie empiriquement avec le venv du projet. Toute l'analyse video perceptuelle (_video_task) crashe ; l'exception est avalee par run_parallel_tasks (simple warning log dans perceptual_support.py:432) → video_result=None silencieux pour chaque film. Meme bug latent ligne 192 (`if not pixels:` dans luminance_histogram).

- **`cinesort/domain/perceptual/grain_analysis.py:202`** — ✅ PANEL 2/2
  Meme famille de crash ndarray : analyze_grain fait `if not pixels: continue` sur des pixels np.ndarray -> ValueError. analyze_grain_v2 (L478 `if not pixels or w <= 0`) et estimate_grain (L55) ont le meme defaut. Appele par perceptual_support._video_task avec les frames de extract_representative_frames (pixels=ndarray) : l'analyse grain v1 ET v2 plantent sur tout fichier reel.

- **`cinesort/domain/perceptual/comparison.py:202`** — ✅ PANEL 2/2
  Le deep-compare plante : compare_per_frame passe pixels_a/pixels_b (np.ndarray issus de parse_raw_frame via extract_aligned_frames) a luminance_histogram et block_variance_stats dont les gardes `if not pixels:` levent ValueError sur ndarray multi-elements (verifie empiriquement). La comparaison perceptuelle profonde entre 2 fichiers crashe systematiquement des la premiere paire de frames valide.

### App — apply, plan, jobs

- **`cinesort/app/quarantine_ttl.py:195`** — ✅ PANEL 2/2
  Le TTL quarantaine est base sur st_mtime, mais les fichiers entrent dans _review via atomic_move/shutil.move (apply_core.py:598, 2125-2169) qui PRESERVE le mtime d'origine. Un film mis en quarantaine aujourd'hui dont le fichier a un mtime > ttl_days (cas quasi systematique pour des videos telechargees/encodees il y a des mois) est supprime des le premier cycle du cron (60 s apres le boot suivant, app.py:509/1011, defaut 30 j), au lieu de 30 jours apres sa mise en quarantaine. Perte de donnees sur le chemin normal : les rows non approuvees et conflits sont detruits avant que l'utilisateur ait pu les examiner. Le viewer (age_days, ligne 159) affiche le meme age errone. Il faudrait dater l'entree en quarantaine (timestamp d'arrivee, ex. mtime du dossier cree ou journal DB), pas le mtime du fichier.

### Backend UI/API — facades + modules support

- **`cinesort/ui/api/tmdb_support.py:51`** — ✅ PANEL 2/2
  `api._normalize_user_path` n'existe pas sur CineSortApi (verifie par hasattr a l'import : c'est un import module-level dans cinesort_api.py:116, jamais une methode/attribut d'instance). Des qu'une cle TMDb est configuree, get_tmdb_posters (L51) et search_tmdb (L143) levent AttributeError, NON rattrapee par leurs `except (OSError, KeyError, TypeError, ValueError)` (L70/L199) -> HTTP 500 sur les endpoints integrations/get_tmdb_posters et library/search_tmdb (recherche manuelle TMDb du Modal Film cassee), et poster_url silencieusement None via film_support._fetch_poster_url qui avale l'AttributeError.

- **`cinesort/ui/api/cinesort_api.py:1007`** — ⚠️ CONTESTE 1/2
  _restart_api_server_impl lit `rest_api_token` depuis `_get_settings_impl()` qui renvoie le secret MASQUE (8 puces • via _SECRET_FIELDS/SEC-H3, settings_support.py:1199). Le serveur REST est donc redemarre avec le token masque ("••••••••", 8 car. invalide) au lieu du vrai token disque. De plus host n'est PAS passe (defaut 127.0.0.1) alors que app._start_rest_server bind 0.0.0.0 si rest_api_enabled. Resultat : apres clic « Redemarrer le service API », tout client LAN distant recoit 401 et l'exposition LAN est perdue. Il faut relire via _read_settings/_unmask_or_stored comme les tests de connexion.
  > Refutation (1 juge) : Le mecanisme de lecture est exact (cinesort_api.py:1002→get_settings_payload→_mask_secrets masque bien rest_api_token, le serveur relance avec token="••••••••") MAIS les consequences CRITICAL annoncees sont impossibles dans le code tel qu'ecrit : (1) _restart_api_server_impl ne passe PAS de host, donc RestApiServer bind sur son defaut 127.0.0.1 (rest_server.py:1126) — seul le boot app.py:351 passe

---

## HIGH (139)

### Domaine — scoring, parsing, perceptuel

- **`cinesort/domain/scene_parser.py:179`** — ✅ PANEL 2/2
  _AUDIO_RESIDUE_RE (\b(?:ma|hra|[257][\s.]?[01]|2[\s.]?0|atmos)\b, appliquee ligne 364 sur tout le nom) mutile des titres reels : verifie en execution, '21 Jump Street (2012)...' -> 'Jump Street', '50 First Dates' -> 'First Dates', 'Ma Vie de Courgette' -> 'Vie de Courgette', '71 (2014)' -> '2014', '20 000 Leagues...' -> '000 Leagues...'. Le titre mutile alimente la query TMDb ET le Candidate source=name qui peut devenir proposed_title : risque de renommage errone du film (violation 'titres jamais alteres').

- **`cinesort/domain/scene_parser.py:381`** — ✅ PANEL 2/2
  parse_scene_title applique _RELEASE_GROUP_RE (' -GROUP$') SANS la validation _SCENE_MARKER_RE qu'utilise extract_release_group (L266) : tout sous-titre d'un seul mot apres un tiret est traite comme release group. Verifie en execution : 'Thor - Ragnarok (2017)' -> 'Thor', 'Blade - Trinity (2004)' -> 'Blade', 'Cloverfield - Paradox' -> 'Cloverfield'. Sans TMDb (offline), le plan propose 'Thor (2017)' au lieu de 'Thor - Ragnarok (2017)'.

- **`cinesort/domain/scan_helpers.py:280`** — ✅ PANEL 2/2
  Fast-path BUG 5 : un dossier dont le nom matche '(YYYY)' devient candidat direct et n'est JAMAIS descendu (_walk non appele, pas ajoute a subdirs). Si le film est dans un sous-dossier release imbrique (layout courant 'Avatar (2009)/Avatar.2009.1080p-GRP/film.mkv'), iter_videos (non recursif, plan_support_core.py:854) ne trouve aucune video directe -> dossier classe 'ignore_non_supporte' et le film est silencieusement absent du plan, sans aucun recours.

- **`cinesort/domain/naming.py:93`** — ✅ PANEL 2/2
  Le pattern de nettoyage `\s+-\s+(?=[\[\(])` dans _CLEANUP_PATTERNS supprime TOUJOURS le tiret configure avant une parenthese/crochet NON vide : comme les groupes vides sont deja retires par les patterns precedents (lignes 88-90), ce pattern ne frappe que des groupes pleins. Verifie par execution : template "{title} - ({year})" rend "Inception (2010)" au lieu de "Inception - (2010)", "{title} - [{resolution}]" rend "Inception [1080p]". Le separateur configure par l'utilisateur est silencieusement perdu dans tous les renommages reels (apply_core.py:1708, library_support.py:1199) — violation du rendu fidele du template configure.

- **`cinesort/domain/perceptual/audio_perceptual.py:389`** — ✅ PANEL 2/2
  `idx = int(best.get("index", 0))` est l'index GLOBAL du conteneur (probe ffprobe : video=0, audio=1, cf _normalize_ffprobe.py:212) mais il est utilise comme index audio-RELATIF dans `-map 0:a:{idx}` (lignes 136/217/279) et transmis a compute_audio_fingerprint et analyze_spectral. Pour un film standard a 1 piste audio, `-map 0:a:1` ne matche aucun flux → loudnorm/astats/clipping/fingerprint echouent TOUS (rc=-22 verifie avec tools/ffmpeg.exe) et l'echec est totalement silencieux. Seul le fallback mediainfo (index 0-based audio-relatif) fonctionnerait par accident.

- **`cinesort/domain/perceptual/audio_perceptual.py:141`** — ✅ PANEL 2/2
  analyze_loudnorm passe `-v quiet` a ffmpeg : le resume JSON de loudnorm est emis au niveau INFO et donc entierement supprime — verifie empiriquement (rc=0 et stderr VIDE avec `-v quiet`, JSON `input_i` present avec `-v info`). La fonction retourne toujours None meme avec un index de piste correct → integrated_loudness/loudness_range/true_peak toujours None, s_lra fige a 50 neutre, classify_drc prive de LRA.

- **`cinesort/domain/perceptual/video_analysis.py:90`** — ✅ PANEL 2/2
  run_filter_graph construit `signalstats=stat=tout+vrep,blockdetect,blurdetect` SANS `metadata=mode=print` : signalstats n'imprime jamais ses metriques par frame (0 ligne `YAVG=` verifie sur ffmpeg reel), et blockdetect/blurdetect n'emettent qu'un resume final (`[Parsed_blockdetect] block mean: 103.69`) que _RE_BLOCK/_RE_BLUR ne matchent pas — et que _parse_filter_output ignorerait de toute facon faute de `_has_signal` → filter_results toujours []. Consequence : blockiness_mean/blur_mean restent 0.0 et _score_blockiness/_score_blur retournent 95/95 (qualite « parfaite » jamais mesuree), visual_score systematiquement gonfle.

- **`cinesort/domain/perceptual/audio_perceptual.py:281`** — ✅ PANEL 2/2
  analyze_clipping_segments suppose qu'astats avec `reset=N` imprime un bloc de stats par segment dans stderr ; en realite les stats par segment ne sont que des metadonnees de frame (il faudrait `ametadata=print`), seul le resume final est loggue — verifie empiriquement : audio 5s en segments d'1s = 5 segments attendus, 1 seule ligne « Overall Peak level » obtenue. total_segments vaut donc toujours 1 et clipping_pct ne peut etre que 0.0 ou 100.0 : l'analyse « par segments » affichee a l'utilisateur (clipping_total_segments, clipping_pct) est fictive.

- **`cinesort/domain/perceptual/audio_perceptual.py:234`** — ✅ PANEL 2/2
  Regression du MEGA-HOTFIX bug(1) : le bloc « Overall » reel d'astats ne contient NI « Crest factor » NI « Dynamic range » (presents uniquement dans les blocs par canal — verifie sur sortie ffmpeg 8.x reelle). Apres restriction au bloc Overall par _extract_overall_block, crest_factor et dynamic_range sont toujours None → s_dyn/s_crest figes a 50, crest_verdict/dynamics_verdict toujours « unknown », et classify_drc (prive aussi de LRA, cf finding loudnorm) retourne toujours (« unknown », 0.0) : la classification DRC §14 est morte.

- **`cinesort/domain/perceptual/composite_score_v2.py:593`** — ✅ PANEL 2/2
  Regle 6 (fake lossless) lit `(normalized_probe or {}).get("audio")` mais la cle reelle du probe normalise est `audio_tracks` (cf _normalize_ffprobe.py:185 ; perceptual_support.py:217 et :465 passent le MEME dict `normalized`) → audio_tracks vaut toujours [], has_lossless_codec toujours False : le malus ADJUSTMENT_FAKE_LOSSLESS_MALUS et le warning « Codec audio annonce lossless mais… » ne se declenchent jamais.

- **`cinesort/domain/perceptual/composite_score_v2.py:163`** — ✅ PANEL 2/2
  _score_hdr lit `has_hdr10`/`has_dv`/`has_hdr10_plus`/`max_cll`/`max_fall` dans normalized_probe["video"], or ces cles n'existent pas dans le probe normalise (les vraies : `hdr10`, `hdr_dolby_vision`, `hdr10_plus`, `hdr_type`, `dv_present`, cf _normalize_ffprobe.py:95-118 ; library_support.py:79-91 documente explicitement que les « anciennes cles has_dv/has_hdr10 » ne sont PAS produites) → tout contenu HDR10/Dolby Vision est score comme SDR (60.0, confiance 0.3), les flags dv_profile_5/hdr_metadata_missing ne sont jamais leves, donc regles d'ajustement 3-4 et warnings UI « Dolby Vision Profile 5 » / « HDR10 sans MaxCLL » morts. Seul has_hdr10_plus_detected (Pass 2) survit.

- **`cinesort/domain/perceptual/audio_perceptual.py:142`** — ✅ PANEL 2/2
  analyze_loudnorm lance ffmpeg avec `-v quiet`, or le bloc JSON loudnorm est emis via av_log niveau INFO : verifie avec ffmpeg 8.1.1, stderr est totalement vide (0 octet) -> la fonction retourne toujours None. integrated_loudness/LRA/true_peak ne sont JAMAIS mesures : score LRA fige au neutre 50, classification DRC privee de LRA, verdict croise 'audio_crushed' inatteignable. Il faut `-v info` (ou -nostats) comme pour astats.

- **`cinesort/domain/perceptual/metadata_analysis.py:410`** — ⚠️ CONTESTE 1/2
  detect_judder lance mpdecimate avec `-v info`, mais mpdecimate logge ses lignes 'drop pts'/'keep pts' au niveau DEBUG uniquement (verifie ffmpeg 8.1.1 : 0 ligne en info, 73 lignes en debug) -> drop=keep=0 -> verdict 'judder_none' pour 100% des fichiers. La detection judder/pulldown 3:2 est un faux positif permanent de bonne sante, silencieux pour l'utilisateur.
  > Refutation (1 juge) : Bug technique confirme en source ET empiriquement (ffmpeg 8.1.1 local : 0 ligne drop/keep pts en -v info, lignes presentes uniquement en -v debug -> drop=keep=0 -> toujours judder_none), MAIS le chemin est inatteignable au runtime : detect_judder n'est appele que si p_settings["judder_detection_enabled"] (perceptual_support.py:356), cle perceptual_judder_detection_enabled par defaut False (setting

- **`cinesort/domain/release_name_parser.py:121`** — ✅ PANEL 2/2
  Le pattern CAM `\bHDTS\b|\bTS\b` matche l'extension de fichier .ts : parse_release_name recoit le nom AVEC extension (quality_report_support.py:116 passe release_name=str(row.video)), donc tout fichier MPEG-TS sain (.ts, extension supportee au scan via _UNCOMMON_VIDEO_EXTS) est marque is_cam=True + source_hint force a 'cam' (L190-195). Consequence dans quality_score.py:1988/2206 : subscores video/audio plafonnes a 14, facteur -30 'Captation degradee (TS)' et tier cape a Bronze, inconditionnellement. Tout .ts legitime est detruit au scoring avec un message faux visible par l'utilisateur.

### App — apply, plan, jobs

- **`cinesort/app/apply_core.py:1368`** — ✅ PANEL 2/2
  Le wrapper `row_record_op` (aussi L988 dans move_duplicate_losers_to_user_decided) est une fonction nue qui perd les attributs `journal_store`/`journal_batch_id` portes par RecordOpWithJournal (cree dans apply_support.py:1392 pour l'apply reel). `atomic_move` (move_journal.py:178) fait `getattr(record_op, 'journal_store', None)` -> None -> retombe sur `shutil.move` brut SANS journal write-ahead apply_pending_moves. Resultat : la protection CR-1 contre crash mi-move (copy+delete cross-volume) est silencieusement desactivee pour quasi toutes les ops par-row (apply_single via merge, apply_collection_item, apply_tv_episode L2068/2083, quarantine_row L2125/2155/2169, losers L1057) — aucune reconciliation au boot possible apres un crash pendant ces moves (risque fichier duplique/demi-copie non detecte).

- **`cinesort/app/apply_rollback.py:113`** — ✅ PANEL 2/2
  `_revert_one_op` ne revert que MOVE_FILE/MOVE_DIR alors qu'apply_core journalise QUARANTINE_FILE/QUARANTINE_DIR avec reversible=True (apply_core.py:599-605, 2126-2132, 2156-2162, 2170-2176). Lors d'un rollback atomique d'un batch echoue, tous les fichiers envoyes en _review/_conflicts, _duplicates_identical, _duplicates_user_decided et quarantaine restent deplaces (SKIPPED 'op_type non revert-able'). Pire : les SKIPPED ne degradent pas le statut — avec failed==0 le rollback retourne ok=True + ROLLED_BACK_BY_ATOMIC alors que le FS n'est PAS restaure. Contrat de reversibilite casse entre les deux modules.

- **`cinesort/app/apply_batches_reconciliation.py:129`** — ✅ PANEL 2/2
  `_classify_batch` classe 'completed' des que les ops existantes n'ont pas d'error_message — or un apply tue a mi-parcours (le scenario zombi vise par le module) laisse N/2 ops journalisees toutes sans erreur (le forward apply n'ecrit jamais error_message). Le batch demi-applique est donc marque COMPLETED_BY_BOOT_CLEANUP. La comparaison documentee avec summary_json.expected_ops (docstring L31-33 : 'en l'absence d'info on tombe sur ROLLED_BACK par defaut, plus prudent') n'est PAS implementee : summary_json est lu (L69) mais jamais parse. Consequence user-visible : statut 'complete' mensonger + le batch sort de la chaine undo (plus PENDING, pas DONE) sans aucun revert FS.

- **`plan_support_core.py:62`** — ⚠️ CONTESTE 1/2
  plan_row_from_jsonable ne restaure pas 10 champs declares de PlanRow pourtant serialises par plan_row_to_jsonable (asdict) : tv_series_name, tv_season, tv_episode, tv_episode_title, tv_tmdb_series_id, subtitle_count, subtitle_languages, subtitle_formats, subtitle_missing_langs, subtitle_orphans. Consequence : sur tout hit du cache incremental (dossier ou row), les rows TV reviennent avec tv_season/tv_episode=None — apply_tv_episode (apply_core.py:2020-2021) calcule alors season=0/episode=0 et renomme/range l'episode en S00E00 ; les infos sous-titres affichees en UI sont vides pour toutes les rows cachees.
  > Refutation (1 juge) : Le defaut existe dans le source (plan_row_from_jsonable omet bien les 10 champs tv_*/subtitle_*), mais aucun chemin runtime ne l'atteint avec les settings reels : les deux seuls points de desérialisation du pipeline scan (cache dossier _try_apply_folder_cache, plan_support_core.py:569/514, et cache row v2 _try_lookup_row_cache, plan_support_replan.py:52) sont gates par cfg.incremental_scan_enabled

- **`plan_support_replan.py:67`** — ✅ PANEL 2/2
  Le lookup du row cache v2 ignore le `kind` demande : get_incremental_row_cache (infra/db/repositories/scan.py:238-256) filtre uniquement sur (root_path, video_path, cfg_sig) et _try_lookup_row_cache ne compare jamais cached['kind'] au kind courant. Si un dossier change de classification (ex: suppression d'une 2e video -> collection devient single, ou l'inverse), folder_sig change (miss dossier) mais le row cache hit retourne la row perimee avec l'ancien kind et collection_name -> l'apply execute la mauvaise action (deplacement en sous-dossier au lieu de renommage du dossier, ou l'inverse).

- **`plan_support_replan.py:73`** — ⚠️ CONTESTE 1/2
  La cle de validation du row cache (video size/mtime/quick_hash + nfo_sig) n'inclut pas les fichiers sous-titres externes alors que les warning_flags subtitle_missing_* sont stockes dans row_json. Ajouter le .srt FR manquant change folder_sig (miss du cache dossier) mais le row cache hit ressort les vieux flags subtitle_missing_fr, puis persist_folder_cache (plan_support_core.py:467-498) re-fige ces flags perimes sous la NOUVELLE folder_sig : le badge 'sous-titre manquant' reste affiche definitivement tant que la video ou le NFO ne change pas.
  > Refutation (1 juge) : Logique interne correcte mais chemin non atteignable aujourd'hui : tout le cache incremental (row cache v2 ET folder cache) est garde par incremental_scan_enabled. _try_lookup_row_cache exige cfg_sig+scan_index (plan_support_replan.py:52), persist_folder_cache exige incremental_enabled (plan_support_core.py:477,514), et l'unique appelant prod n'injecte scan_index que si cfg.incremental_scan_enable

- **`plan_support_core.py:117`** — ⚠️ CONTESTE 1/2
  cfg_signature_for_incremental omet plusieurs parametres qui changent le resultat du plan : enable_tv_detection (utilise L633 pour basculer pipeline TV vs skip), min_video_bytes (L844-845, seuil iter_videos), naming_movie_template (boost confiance 'deja conforme' dans _build_resolved_row, plan_support_replan.py:307-317) et la liste subtitle_expected_languages (passee hors cfg). Modifier ces reglages ne change pas cfg_sig -> les caches dossier/row restent valides et le rescan renvoie des rows calculees avec les ANCIENS reglages (ex: episodes TV toujours planifies apres desactivation de la detection TV).
  > Refutation (1 juge) : Le defaut d'omission dans cfg_signature_for_incremental existe bien dans le code, mais tout le chemin cache est verrouille derriere incremental_scan_enabled : run_flow_support.py:413 ne passe scan_index que si ce flag est vrai, sinon plan_support_core.py:514 met incremental_enabled=False et aucun cache n'est lu ni ecrit (_try_apply_folder_cache retourne immediatement L569-570). Or ce flag est Fals

- **`plan_support_replan.py:114`** — ✅ PANEL 2/2
  Replan non idempotent : _rematch_tmdb_and_update_plan (ui/api/library_actions_support.py:320-328) construit cfg avec root=dossier du film, donc dans _resolve_folder_context folder_resolved == cfg_root_resolved et folder_name est remplace par le stem du FICHIER video au lieu du nom du dossier. Le rescan d'une row 'single' score alors sur le nom de fichier (souvent un release-name cryptique) au lieu du nom de dossier propre -> proposition differente/degradee par rapport au scan initial alors que rien n'a change sur le disque.

- **`cinesort/app/export_support.py:17`** — ✅ PANEL 2/2
  Violation de l'invariant projet couleurs tiers : _TIER_COLORS code gold=#f59e0b, silver=#94a3b8, bronze=#ca8a04, platinum=#e2e8f0 alors que l'invariant (documente dans web/shared/tokens.css:24-57) impose Gold #FFD700, Silver #C0C0C0, Bronze #CD7F32, Platinum #E5E4E2. Les rapports HTML exportes (pills tier + graphique SVG) affichent des couleurs de tiers differentes de celles de l'app.

- **`cinesort/app/job_runner.py:149`** — ✅ PANEL 2/2
  _invoke_job_fn : le `except TypeError` autour de `job_fn(should_cancel, should_pause=...)` re-execute le job ENTIER via `job_fn(should_cancel)`. Or cette branche n'est atteinte que si inspect.signature a deja confirme que le job accepte should_pause — un TypeError ici provient donc du CORPS du job (donnees invalides), pas de la signature. Consequence : un scan/apply partiellement execute qui leve TypeError est silencieusement rejoue depuis le debut (double effets de bord : deplacements de fichiers, journal, notifications), et le job_fn de production accepte bien should_pause (run_flow_support.py:381-383), donc le chemin est actif. Le fallback ne devrait s'appliquer qu'aux erreurs de binding, ex. via functools/inspect.bind, pas a tout TypeError.

- **`cinesort/app/watchlist.py:121`** — ✅ PANEL 2/2
  compare_watchlist pass 1 : le fallback commente "titre seul si la watchlist a une annee mais le local non" teste `norm in local_title_only`, or ce set contient TOUS les titres locaux y compris ceux qui ONT une annee. Un film watchlist avec annee (ex. Dune 2021) est marque "owned" si la bibliotheque contient un film de meme titre d'une AUTRE annee (Dune 1984). Le pass 2 fuzzy fait correctement `local_by_year[w_year] + local_by_year[0]`. Faux positifs sur les remakes -> couverture watchlist gonflee et films manquants jamais signales.

### Infra — REST, clients API, SQLite, probe

- **`cinesort/infra/rest_server.py:408`** — ✅ PANEL 2/2
  CORS par defaut `Access-Control-Allow-Origin: *` (ligne 1135) combine au bypass d'auth loopback (lignes 452-462) rend l'API pilotable en cross-origin depuis n'importe quel site web visite par l'utilisateur. Une page malveillante peut faire `fetch('http://127.0.0.1:8642/api/run/start_plan',{method:'POST',headers:{'Content-Type':'application/json'},...})` : le preflight OPTIONS renvoie ACAO:* + POST + Content-Type autorises, donc le navigateur execute le POST ; cote serveur client_ip=127.0.0.1 + bind=127.0.0.1 declenche le bypass et la requete est autorisee SANS token. C'est une CSRF/SSRF vers l'API locale permettant de declencher des endpoints a effet de bord (runs, modification settings, operations fichiers) -> risque de perte de donnees. L'invariant impose de garder le bypass loopback, donc le defaut CORS `*` permissif est l'element fautif a restreindre.

- **`cinesort/infra/tmdb_client.py:176`** — ✅ PANEL 2/2
  Circuit breaker TMDb inerte sur les statuts HTTP : la session est creee avec raise_on_status=False (_http_utils.py:47) et raise_for_status() est appele par les callers APRES _breaker.call(), donc une reponse 503/429 sort du lambda comme un succes — le breaker remet _failures a 0 au lieu de compter l'echec et ne s'ouvre JAMAIS quand TMDb repond 5xx. C'est exactement le bug 'omdb-1 circuit breaker inerte' deja corrige dans omdb_client.py:375-378 (raise_for_status deplace DANS le lambda) mais jamais reporte sur TmdbClient : le scenario que #76 devait empecher (~3,5s de retries x 5000 films = ~5h de scan bloque sur panne TMDb 5xx) reste entier.

- **`cinesort/infra/omdb_client.py:43`** — ✅ PANEL 2/2
  OMDB_API_BASE est en HTTP clair ('http://www.omdbapi.com/') alors qu'OMDb supporte HTTPS : la cle API (param apikey ajoute ligne 361) et toutes les reponses transitent non chiffrees — fuite de la cle vers tout observateur reseau et reponses falsifiables par un on-path (qui seraient ensuite persistees dans le cache local 7 jours). Le scrubber de logs protege les fichiers de log mais pas le fil reseau.

- **`cinesort/infra/db/pragma_profile.py:219`** — ✅ PANEL 2/2
  _record_pragma_history fait un INSERT+COMMIT dans pragma_history a CHAQUE ouverture de connexion (connect_sqlite -> apply_pragmas, record_history=True par defaut), or chaque methode repository ouvre 1 a 3 connexions courte-vie (_ensure_schema_group fait get_user_version + _existing_tables avant l'operation). Aucune purge/retention de pragma_history n'existe nulle part dans le codebase (grep verifie). Consequences : croissance non bornee de la table (~des dizaines de milliers de lignes JSON par scan de bibliotheque), et chaque operation de LECTURE devient une transaction d'ECRITURE -> contention sur le write-lock WAL entre thread de scan et thread UI, et ecriture durable synchronous=FULL a chaque connexion sur les profils nas_smb.

- **`cinesort/infra/db/sqlite_store.py:613`** — ⚠️ CONTESTE 1/2
  _backup_before_migrations est appele a CHAQUE boot par initialize() (ligne 374) meme quand AUCUNE migration n'est pending, et rotate_backups (backup.py:106) applique un max_count unique (5) sur TOUS les triggers confondus (stem_filter='cinesort' matche pre_migration, post_apply ET manual). Resultat : 5 demarrages de l'app suffisent a supprimer silencieusement le backup manual cree par l'utilisateur via Settings (backup_now) et le backup post_apply de securite cree apres un apply reel (apply_support.py:2109). Risque de perte du seul point de restauration utile apres une operation destructive.
  > Refutation (1 juge) : Le scenario central n'est pas atteignable : backup_now(trigger="manual") n'a AUCUN appelant production. Le seul appel runtime est apply_support.py:2109 (post_apply) ; le trigger "manual" n'apparait que dans tests/test_db_backup.py. Aucun endpoint REST, aucune methode facade, aucun bouton Settings n'existe — dans web/, parametres.js ne mentionne que les backups de reset/wipe, nommes wipe_<ts>.bak (

- **`cinesort/infra/probe/service.py:189,542`** — ✅ PANEL 2/2
  En backend "auto" (le defaut), un echec de probe du a un timeout NAS (ffprobe ET mediainfo expirent => raw_mediainfo=None et raw_ffprobe=None alors que les deux binaires sont disponibles) est mal classe par _is_tool_unavailable_failure() qui retourne True a la ligne 189 (`return raw_mediainfo is None and raw_ffprobe is None`). Du coup le bloc circuit breaker ligne 542 (`if backend != 'none' and not tool_unavailable:`) est saute et record_failure() n'est JAMAIS appele. Le circuit breaker par source UNC ne se degrade donc jamais sur une biblio reseau en panne en mode auto : exactement le scenario de hang 41h (30s+ x retries par fichier) que le breaker etait cense empecher. Le bug ne touche QUE le mode auto ; en mode 'ffprobe'/'mediainfo' seul, tool_unavailable reste False et le breaker fonctionne.

- **`cinesort/infra/probe/tooling.py:122-133`** — ✅ PANEL 2/2
  _resolve_tool_path() execute le chemin explicite (settings ffprobe_path/mediainfo_path) des que `Path(explicit).is_file()` est vrai, SANS aucune verification du nom du binaire. Le whitelist anti-injection de l'issue #71 (_EXPECTED_BINARY_NAMES) n'est applique que dans validate_tool_path(), invoque uniquement par set_probe_tool_paths(). Un enregistrement via le save_settings generique (_save_section_probe dans settings_support.py:1357 ne fait qu'un strip()) contourne le whitelist : get_tools_status() executera alors n'importe quel .exe configure (`[path,'-version']` puis `[path, ...args probe..., media]`) => execution de binaire arbitraire via la config (atteignable par un client REST authentifie).

- **`cinesort/infra/probe/auto_install.py:53-54,104-110`** — [NON RE-CONFIRME]
  EXPECTED_SHA256_FFMPEG et EXPECTED_SHA256_MEDIAINFO valent None : _verify_archive() avec expected=None se contente de logger un warning 'integrity UNVERIFIED' et retourne sans verifier. Les archives ffmpeg/MediaInfo telechargees sont donc extraites et leurs .exe ensuite executes via subprocess SANS aucun controle d'integrite. La defense fail-closed documentee ne s'active jamais en configuration par defaut. Un MITM / miroir compromis peut servir un ffprobe.exe verole execute avec les droits utilisateur. HTTPS est l'unique protection effective.

- **`cinesort/infra/log_scrubber.py:113`** — ✅ PANEL 2/2
  SecretsScrubFilter ne scrub que record.msg, exc_text et les args de type str (lignes 112-119). Or l'idiome ubiquitaire logger.x("... : %s", exc) passe un objet exception non-str : son __str__ (ex. requests.HTTPError = '401 ... for url: https://www.omdbapi.com/?apikey=SECRET') n'est PAS scrubbe et fuit en clair, y compris dans le fichier de log pourtant protege par le filtre. Verifie empiriquement (le secret traverse meme un handler portant le filtre). Sites reels : omdb_client.py:387, tmdb_client.py:548.

- **`cinesort/infra/log_scrubber.py:150`** — ✅ PANEL 2/2
  install_global_scrubber n'attache le filtre qu'aux .filters du root logger et aux handlers DEJA existants. En semantique logging stdlib, un filtre pose sur un LOGGER ne s'applique pas aux records propages depuis un logger enfant (seuls les filtres de HANDLER s'appliquent). Comme app.py appelle install_global_scrubber() AVANT logging.basicConfig() (app.py:409 puis 419 ; 648 puis 652), le StreamHandler console/stderr cree ensuite n'a aucun filtre de scrub. Reproduit : une apikey OMDb logguee par un logger enfant s'affiche en clair sur la console (le fichier rotatif, lui, est couvert car install_rotating_log pose le filtre directement sur le handler).

### Backend UI/API — facades + modules support

- **`cinesort/ui/api/cinesort_api.py:2639`** — ✅ PANEL 2/2
  Les 3 endpoints quarantaine appellent _build_cfg_from_settings_payload(settings) avec un seul argument alors que build_cfg_from_settings exige 4 kwargs keyword-only obligatoires (root, default_collection_folder_name, default_empty_folders_folder_name, default_residual_cleanup_folder_name) — verifie a l'execution : TypeError systematique, avale par le except et converti en {ok:False, error:"build_cfg_failed"}. Consequence : _purge_quarantine_bucket_impl (L2639), _purge_quarantine_bucket_all_impl (L2657) et _list_quarantine_bucket_impl (L2671) echouent TOUJOURS -> le cron TTL quarantaine (quarantine_ttl.start_quarantine_ttl_cron) ne purge jamais rien (warning silencieux), le bouton "Vider maintenant" et le viewer _review sont morts, le bucket grossit indefiniment.

- **`cinesort/ui/api/cinesort_api.py:1286`** — ⚠️ CONTESTE 1/2
  Famille "secrets masques consommes en interne" : depuis SEC-H2, _get_settings_impl() masque tmdb_api_key/jellyfin_api_key/plex_token/radarr_api_key/email_smtp_password en "••••••••" (settings_support._mask_secrets), mais _get_jellyfin_sync_report_impl (L1286), _get_plex_sync_report_impl (L1425-1427), _get_radarr_status_impl (L1505), _request_radarr_upgrade_impl (L1563), _get_plex_libraries_impl (fallback L1407), _test_email_report_impl (L1262) et _dispatch_email (L271-276) lisent ces cles via _get_settings_impl() sans _unmask_or_stored : les clients Jellyfin/Plex/Radarr recoivent litteralement le masque comme cle (401 systematique) et les rapports email echouent a l'auth SMTP des qu'un secret est configure.
  > Refutation (1 juge) : Le drift de masquage existe bien dans le source (cinesort_api.py:1286 lit le payload masque par _mask_secrets, settings_support.py:1203-1210), MAIS le chemin n'est pas atteignable par l'utilisateur aujourd'hui : les deux seuls appelants de integrations/get_jellyfin_sync_report sont qij.js:398 (vue morte confirmee) et jellyfin.js:227, elle aussi morte — initJellyfin (jellyfin.js:283) n'est importe 

- **`cinesort/ui/api/cinesort_api.py:1426`** — ✅ PANEL 2/2
  _get_plex_sync_report_impl lit plex_token depuis _get_settings_impl() (masque) -> token "••••••••" envoye a PlexClient -> echec d'auth systematique. Meme bug dans _get_plex_libraries_impl (L1407) quand url/token ne sont pas fournis par le caller : le fallback settings renvoie le masque au lieu du vrai token (devrait passer par _unmask_or_stored ou _read_settings).

- **`cinesort/ui/api/cinesort_api.py:1506`** — ⚠️ CONTESTE 1/2
  _get_radarr_status_impl (L1506/L1539) et _request_radarr_upgrade_impl (L1563-1570) lisent radarr_api_key depuis _get_settings_impl() (masque) -> RadarrClient instancie avec "••••••••" -> 401 systematique des qu'une cle est stockee. Le rapport Radarr et la demande d'upgrade sont casses, contrairement a _test_radarr_connection_impl (L1492) qui passe correctement par _unmask_or_stored.
  > Refutation (1 juge) : Bug reel au niveau code (cinesort_api.py L1506/L1563-1564 lisent radarr_api_key depuis _get_settings_impl() qui retourne _mask_secrets, settings_support.py L1188-1210 : radarr_api_key dans _SECRET_FIELDS, masque truthy donc le guard passe et RadarrClient recoit les bullets) MAIS non user-reachable aujourd'hui : les seuls appelants frontend de integrations/get_radarr_status et integrations/request_

- **`cinesort/ui/api/cinesort_api.py:274`** — ✅ PANEL 2/2
  _dispatch_email passe `_get_settings_impl()` (secrets masques) a email_report.dispatch_email → send_email_report (app/email_report.py:124) lit `email_smtp_password` = "••••••••" et fait smtp.login(user, masque) → echec d'auth SMTP systematique, rapports email jamais envoyes quand le serveur exige une authentification. Les autres consommateurs de secrets utilisent _unmask_or_stored (cinesort_api.py:1190) ; oubli ici.

- **`cinesort/ui/api/cinesort_api.py:1638`** — ⚠️ CONTESTE 1/2
  _preview_naming_template_impl : self._get_or_create_infra(state_dir, settings) passe 2 arguments positionnels alors que la methode n'en accepte qu'un (def L585) -> TypeError a CHAQUE appel, avale par le except (TypeError) L1648 -> context=None. Le chargement du vrai film via sample_row_id ne fonctionne donc JAMAIS : la preview affiche toujours le mock Inception au lieu des donnees du film selectionne. En aval, store.quality.get_quality_report(rid) en positionnel violerait aussi la signature keyword-only (*, run_id, row_id) et le hasattr teste le mauvais objet (store au lieu de store.quality).
  > Refutation (1 juge) : Bug source reel (L1638 passe 2 args vs signature L585 a 1 arg) mais NON atteignable : le bloc bugue est garde par `if rid:` (L1634) et ne s'execute que si sample_row_id est non vide. Or AUCUN code frontend vivant n'appelle preview_naming_template — grep repo-complet sur *.js/*.html ne trouve que tests/manual/pywebview_api_mock.js. Le seul appelant UI historique (web/views/settings.js, _fetchNaming

- **`cinesort/ui/api/apply_support.py:672`** — ✅ PANEL 2/2
  undo_selected_rows n'applique jamais le delai d'annulation 24h (_UNDO_DEADLINE_SECONDS) alors que undo_last_apply le refuse avec 410 (l.1018-1027). Un client REST peut contourner l'enforcement backend v1.5.2 (Spec 08 §3.5) en annulant TOUS les films via l'undo selectif (dry_run=false) bien apres 24h — la promesse de securite redevient cosmetique sur ce chemin.

- **`cinesort/ui/api/apply_support.py:987`** — ✅ PANEL 2/2
  Aucun chemin d'undo (undo_last_apply l.987, undo_selected_rows l.672, _execute_undo_ops l.338) n'acquiert api._apply_slot_guard, contrairement a apply_changes (l.1922) et build_apply_preview (l.2318). Verifie dans run_facade.py:450-523 et cinesort_api.py:2261-2295 : passthrough sans garde. Un undo reel peut donc s'executer EN PARALLELE d'un apply en cours sur le meme run (ex: undo depuis l'ecran historique pendant un apply depuis traitement), avec shutil.move concurrents sur les memes fichiers — course filesystem et etat journal incoherent.

- **`cinesort/ui/api/apply_support.py:778`** — ⚠️ CONTESTE 1/2
  Apres un undo selectif d'UN seul film, mark_apply_batch_undo_status passe le batch a UNDONE_PARTIAL (l.774-786). Or get_last_reversible_apply_batch (infra/db/repositories/apply.py:244) filtre status='DONE' : le batch disparait de undo_last_apply (qui n'accepte aucun batch_id), de build_undo_preview_payload et des previews sans batch_id explicite. Resultat : annuler 1 film rend l'annulation globale des N-1 films restants impossible dans la fenetre 24h (ops encore PENDING/reversibles en base mais inaccessibles via l'UI).
  > Refutation (1 juge) : Le mecanisme est exact (UNDONE_PARTIAL sort bien du filtre status='DONE' de get_last_reversible_apply_batch, apply.py:244) mais le scenario n'est pas atteignable par l'utilisateur : aucune vue vivante du frontend n'appelle undo_selected_rows ni undo_by_row_preview (grep web/ : seuls run/undo_last_apply et undo_last_apply_preview dans traitement.js:1237/1296, historique.js:1170, lib-apply.js:290/31

- **`cinesort/ui/api/apply_support.py:2065`** — ✅ PANEL 2/2
  Les etapes post-apply (_trigger_jellyfin_refresh l.2065, _trigger_plex_refresh l.2076, notify l.2083, hooks/email l.2103-2104) sont dans le MEME try que l'apply. Une exception non prevue la (ex: ValueError de float(plex_timeout_s) l.1767, non couverte par l'except IntegrationError/OSError/RequestException l.1773) apres un apply 100% reussi : (1) l'utilisateur recoit cannot_apply_changes alors que tout est applique ; (2) close_apply_batch FAILED est tente sur un batch deja DONE (transition refusee, WARN) ; (3) avec apply_atomic=True, _atomic_rollback_forward est declenche — rollback_forward (apply_rollback.py:338) n'a AUCUNE garde de statut batch et revert donc l'integralite d'un apply reussi, le batch restant DONE en base (journal undo incoherent : toutes les destinations 'missing').

- **`cinesort/ui/api/settings_support.py:1199`** — ⚠️ CONTESTE 1/2
  rest_api_token figure dans _SECRET_FIELDS, donc get_settings_payload retourne le masque '••••••••' a la place du token. Or cinesort_api._restart_api_server_impl (cinesort_api.py:1002-1016) lit le token via _get_settings_impl() : apres un restart du serveur REST depuis l'UI, le serveur demarre avec le Bearer token litteral '••••••••' (constante publique de 8 bullets). Tous les clients legitimes recoivent 401 et le token attendu devient une constante connue ; le check 'Aucun token configure' (L1008) ne declenche jamais car le masque est non vide.
  > Refutation (1 juge) : Le masque arrive bien dans auth_token, mais l'impact annonce (401 pour tous les clients legitimes) est inatteignable : _restart_api_server_impl omet le kwarg host, donc le serveur rebinde 127.0.0.1 (rest_server.py:1126, bind_host L1248), et _check_auth (rest_server.py:452-462, bypass 'desktop trusted' du 2026-06-08) retourne True pour tout client loopback AVANT toute comparaison Bearer (CINESORT_D

- **`cinesort/ui/api/settings_support.py:939`** — ✅ PANEL 2/2
  Le fix 'BUG 1' (generation d'un rest_api_token aleatoire si vide) est inoperant : apply_settings_defaults n'est appele que dans le chemin GET (get_settings_payload), le token genere est immediatement masque par _mask_secrets et n'est JAMAIS persiste (au save, le masque est remplace par la valeur existante du fichier, soit ''). Consequences : chaque GET genere un token different et ephemere, l'UI affiche _has_rest_api_token=true alors que settings.json n'en contient aucun, et reset_support.py:139 ('sera regenere par apply_settings_defaults') repose sur une regeneration qui n'atterrit jamais sur disque.

- **`cinesort/ui/api/settings_support.py:1671`** — ✅ PANEL 2/2
  Le mapping alias UI perceptual_workers_count -> perceptual_workers de _save_section_advanced (clamp [1..32]) est mort : le dispatcher applique _save_section_advanced (L1911) PUIS _save_section_perceptual (L1918) qui ecrit inconditionnellement perceptual_workers = _coerce_workers_int(payload.get('perceptual_workers')) (clamp [0..16]). L'UI reelle (web/dashboard/views/parametres.js:87) n'envoie QUE perceptual_workers_count, donc la valeur saisie est toujours ecrasee par 0 (auto) ; au rechargement l'UI relit perceptual_workers_count qui n'existe jamais et reaffiche le default 2. Round-trip du setting completement casse + clamps contradictoires entre les deux sections.

- **`cinesort/ui/api/settings_support.py:1890`** — ✅ PANEL 2/2
  Contrat de save destructif sur payload partiel : les sections non conditionnelles (_save_section_tmdb/jellyfin/plex/radarr/omdb/notifications/rest_api/watch/plugins/email/subtitles/perceptual/appearance, L1890-1919) reecrivent leurs cles avec les defaults quand elles sont absentes du payload. Via POST /api/save_settings (dispatcher REST expose), un payload partiel met plex_token/radarr_api_key/email_smtp_password/omdb_api_key a '' -> write_settings droppe les blobs DPAPI (preserves uniquement via _orig_* si le dechiffrement avait echoue) = secrets perdus ; rest_api_token devient '' -> hot-swap kill-switch et lockout des clients REST. _unmask_secrets_for_save (L1213-1219) ne couvre que les champs presents avec le masque, pas les champs omis. Attenue par les 5 backups mais perte silencieuse.

- **`cinesort/ui/api/run_flow_support.py:488`** — ✅ PANEL 2/2
  Annulation en cours de scan : plan_multi_roots retourne des rows PARTIELLES sans lever (break silencieux sur should_cancel, cf plan_support_dedup.py:811-813) et job_fn ne re-teste jamais should_cancel() apres le retour. Resultat : un run ANNULE envoie quand meme la notification succes 'scan_done' (N films detectes), declenche les hooks plugin + email post_scan, lance l'auto-recompute qualite et le batch perceptuel sur les rows partielles, et logge '=== PLAN READY ===', alors que le runner marque ensuite le run CANCELLED. Etat machine incoherent et message utilisateur faux sur un chemin normal (cancel).

- **`cinesort/ui/api/run_flow_support.py:1685`** — ⚠️ CONTESTE 1/2
  check_duplicates_fusion casse : _build_fusion_inputs_from_groups lit grp.get('members') alors que les groupes produits par find_duplicate_targets exposent la cle 'rows' (duplicate_support.py:377). Tous les groupes sont skippes -> avec CINESORT_FUSION_DOUBLONS=ON l'endpoint retourne TOUJOURS pairs=[] (stub silencieux). Doublement casse : meme avec 'rows', les items n'ont ni 'src' ni 'source_path' ni 'path' (cles reelles : 'target'/'source_folder') donc video_path serait introuvable.
  > Refutation (1 juge) : Bug de cles reel dans la source (grp.get('members') vs 'rows', items avec 'target'/'source_folder') mais chemin inatteignable en runtime : CINESORT_FUSION_DOUBLONS est OFF par defaut et AUCUN code de l'app ne le positionne (le toggle settings n'est qu'un commentaire futur, duplicate_pipeline.py:50-52) ; surtout, AUCUN appelant frontend n'existe (zero occurrence de check_duplicates_fusion dans web/

- **`cinesort/ui/api/run_flow_support.py:1300`** — ✅ PANEL 2/2
  _filename_from_row lit les cles 'video'/'folder' mais les rows des groupes doublons contiennent uniquement row_id/kind/title/year/target/source_folder (duplicate_support.py:293-302) -> file_a_name et file_b_name valent TOUJOURS '?' dans le payload comparison. L'UI doublons affiche '?' au lieu des noms de fichiers (doublons.js:275-276 et duplicate-comparator-modal.js:266-267 : la valeur '?' est truthy donc le fallback 'Fichier A/B' ne s'applique meme pas).

- **`cinesort/ui/api/dashboard_support.py:1408`** — ✅ PANEL 2/2
  _compute_active_insights compare r.get('status') == 'running' en minuscules alors que les statuts sont persistes en MAJUSCULES (RunStatus.RUNNING.value == 'RUNNING', cf run.py:103 SET status='RUNNING' et list_runs qui retourne les rows DB brutes). La condition n'est jamais vraie -> l'insight 'Analyse en cours' de la Home ne s'affiche JAMAIS pendant un run actif.

- **`cinesort/ui/api/library_support.py:408`** — ✅ PANEL 2/2
  `score_v2 = perc.global_score_v2 or qual.score` melange 2 echelles incomparables : le commentaire VN-B.2 juste au-dessus (L396-401) documente que V1 (seuils 70/66/55/40) et V2 (90/80/65/50) ne sont pas comparables et corrige le tier via reconcile_display_tier, mais le SCORE garde le fallback brut sans marqueur de source. Consequence : tri score_desc/score_asc (_SORT_KEY L587-588) et avg_score de get_scoring_rollup (L932) melangent des scores V1 et V2 ; de plus l'operateur `or` ecrase un score V2 legitime de 0.0 (reject) par le score qualite V1.

- **`cinesort/ui/api/library_support.py:502`** — ✅ PANEL 2/2
  Le filtre `source` compare filters.source aux valeurs de row.proposed_source, or PlanRow.proposed_source = "nfo"|"name"|"tmdb" (source d'IDENTIFICATION, cf cinesort/domain/core.py:399), pas la source media. Le drawer UI (library-advanced-drawer.js L29) envoie ["bluray","web","dvd","other"] : le filtre Source ne matche JAMAIS aucun film (0 resultat systematique) et filtre de toute facon le mauvais concept.

- **`cinesort/ui/api/library_support.py:391`** — ✅ PANEL 2/2
  Mismatch valeurs codec/resolution backend vs drawer UI : _normalize_codec mappe h265/hevc -> "hevc" (jamais "h265") et _classify_resolution (L62-73) retourne "sd"/"unknown" (jamais "480p" ni "other"), alors que library-advanced-drawer.js L27-28 propose "h265", "480p" et "other". Selectionner "H265" (codec moderne le plus courant), "480p" ou "autre" retourne toujours 0 resultat via _in_list (comparaison stricte L471-474).

- **`cinesort/ui/api/library_support.py:428`** — ✅ PANEL 2/2
  `audio_languages` expose les codes ISO639-2/3 BRUTS de detected.languages (ex "eng","fra", cf commentaire L306-309) sans normalisation ISO639-1, contrairement aux sous-titres embarques qui passent par _normalize_iso639 (L329-339). Le drawer envoie "fr"/"en"/"multi" (library-advanced-drawer.js L30-34) : le filtre langue audio ne matche pas les fichiers tagges "fra"/"fre"/"eng" (majorite des MKV) et l'option "multi" ne matche jamais rien.

- **`cinesort/ui/api/library_support.py:481`** — ✅ PANEL 2/2
  _any_in_list retourne False quand row_vals est vide : l'option sous-titres "Aucun" (value "none", library-advanced-drawer.js L38) exclut precisement les films SANS sous-titres qu'elle devrait selectionner, et "none" n'est jamais present dans subtitle_languages des autres films -> filtrer sur "Aucun" retourne toujours 0 resultat. Aucun traitement special de "none" cote backend (L506-507).

- **`cinesort/ui/api/perceptual_support.py:494`** — ⚠️ CONTESTE 1/2
  `result_dict["global_score_v2"] = gv2_payload` est ajoute APRES l'appel upsert_perceptual_report (L476-493) qui serialise metrics en JSON immediatement (repositories/perceptual.py:61 json.dumps) : le payload V2 n'est donc jamais persiste dans metrics_json. Le chemin cache-hit (L163-184) reconstruit la reponse depuis ces metrics DB et ne recopie pas la cle -> compare_perceptual (L739-744) n'expose global_score_v2_a/b QUE lors d'une premiere analyse fraiche ; toute comparaison suivante (cache) fait disparaitre silencieusement le bloc scores V2 cote-a-cote du modal doublons (lib-duplicates.js L121-124).
  > Refutation (1 juge) : Le defaut backend existe (metrics_json serialise avant l'ajout de global_score_v2, et le chemin cache L163-184 ne recopie pas le payload), mais il est non atteignable par l'utilisateur : l'endpoint compare_perceptual et les cles global_score_v2_a/b ne sont consommes QUE par lib-duplicates.js (L111, L121-124), importe uniquement par views/library/library.js, lequel n'est importe nulle part (app.js 

- **`cinesort/ui/api/reset_support.py:405`** — ✅ PANEL 2/2
  reset_database() cherche <state_dir>/cinesort.db mais la vraie DB est <state_dir>/db/cinesort.sqlite (db_path_for_state_dir, sqlite_store.py L135-136 + DEFAULT_DB_FILENAME='cinesort.sqlite' L29). db_path.exists() est donc toujours False et l'endpoint retourne ok=True 'Aucune DB existante a supprimer' : le wipe DB promis par l'UI Parametres ne se produit jamais, sans aucune erreur visible.

- **`cinesort/ui/api/quality_simulator_support.py:206`** — ⚠️ CONTESTE 1/2
  _load_reports_for_scope fait getattr(api, '_store', None) et _resolve_latest_run_id lit api._store (L231), mais CineSortApi ne possede aucun attribut _store (les stores vivent dans _infra_by_state_dir ; grep repo : seule definition self._store est dans app/job_runner.py). run_simulation retourne donc TOUJOURS 'Aucun rapport qualite disponible dans ce scope' : le simulateur de preset (ecran G5) est entierement mort en production, il ne fonctionne qu'avec les fakes de tests.
  > Refutation (1 juge) : Le defaut code est exact (api._store n'existe pas sur CineSortApi, seul job_runner.py:65 definit self._store ; pas de property ni __getattr__) mais il n'est PAS atteignable en production : le seul point d'entree UI est openQualitySimulator (web/dashboard/views/quality-simulator.js:155), appele uniquement par quality.js:271 et qij.js:307 — deux vues MORTES (routes redirigees, app.js:289-293) — et p

- **`cinesort/ui/api/quality_simulator_support.py:131`** — ⚠️ CONTESTE 1/2
  save_custom_preset persiste via api._save_active_quality_profile qui appelle store.quality.save_quality_profile(..., is_active=True) (quality_internal_support.py L36-43) : sauvegarder un preset custom depuis le simulateur ACTIVE silencieusement ce profil en DB, sans mettre a jour settings.active_quality_profile_id ni settings.custom_quality_profiles. Le scoring de toute la bibliotheque change a l'insu de l'utilisateur et on cree exactement la divergence settings/DB que set_active_profile (profiles_support_crud.py L389-416) s'efforce d'eviter avec son rollback.
  > Refutation (1 juge) : Chemin non atteignable aujourd'hui : le seul appelant UI de quality/save_custom_quality_preset est web/dashboard/views/quality-simulator.js:459, dans le modal ouvert par openQualitySimulator (L155). Or openQualitySimulator n'est importe que par quality.js (vue morte), qij.js (vue morte) et custom-rules-editor.js — lequel n'est lui-meme importe que par quality.js et qij.js, toutes deux mortes (rout

- **`cinesort/ui/api/library_actions_support.py:283`** — ✅ PANEL 2/2
  _rematch_tmdb_and_update_plan lit les settings via api.settings.get_settings(), qui retourne le payload MASQUE (_mask_secrets, settings_support.py L1203-1210 : tmdb_api_key remplace par 8 bullets). _build_tmdb_client_optional (L411-431) construit alors un TmdbClient avec '••••••••' comme cle API -> tout re-match TMDb de rescan_row/rescan_rows_bulk echoue en 401, avale par le catch best-effort L221 : la fonctionnalite 'Re-scanner ce fichier' ne re-matche jamais TMDb des qu'une vraie cle est configuree. Il faut lire la cle via read_settings (brut) et non via la facade masquee.

- **`cinesort/ui/api/quality_audit_support.py:353`** — ✅ PANEL 2/2
  _recompute_worker lit api.settings.get_settings().get('settings') or {} mais get_settings retourne un dict PLAT (get_settings_payload -> apply_settings_defaults, aucune cle imbriquee 'settings' ; tous les autres call sites font settings.get('state_dir') directement). Resultat : settings={} toujours, perceptual_enabled lu False -> l'analyse perceptuelle auto post-recompute (fix ITER8) ne se lance JAMAIS meme avec les deux toggles actives, et le WARN 'perceptual_auto_on_quality active mais perceptual_enabled=False' est loggue a chaque recompute. Le no-op silencieux que ce code pretend corriger persiste.

- **`cinesort/ui/api/library_actions_support.py:73`** — ✅ PANEL 2/2
  Feature 'marquer pour suppression' write-only : mark_for_deletion_bulk/mark_single_for_deletion persistent dans runs/<run>/deletion_marks.json que RIEN ne lit (grep repo complet : aucun consommateur ; apply_support.py et cinesort/app ne referencent ni deletion_marks ni film_marked_for_deletion), alors que l'UI promet 'deplaces vers _user_marked_for_deletion/ au prochain apply (reversible via Undo)' (web/dashboard/views/bibliotheque.js L1619). En plus, le marquage single du Modal Film (library_support.py L1369 -> table DB film_marked_for_deletion) et le bulk (fichier JSON) utilisent deux stores divergents qui ne se voient pas mutuellement.

- **`cinesort/ui/api/reset_support.py:226`** — ✅ PANEL 2/2
  reset_all_user_data supprime db/, runs/, settings.json mais n'invalide ni api._infra_by_state_dir, ni api._runs, ni _RECONCILED_STATE_DIRS (la methode _close_infra testee par reset_database n'existe nulle part dans le code). Le SQLiteStore cache pointe vers db/cinesort.sqlite dont le dossier parent vient d'etre supprime, et _managed_conn ne fait aucun mkdir (sqlite_store.py L212-222, le mkdir n'est que dans initialize) -> toute operation DB suivante echoue 'unable to open database file' jusqu'au redemarrage, alors que l'endpoint a repondu ok=True. Accessoirement removed.append(item.name) (L243) compte un dossier comme supprime meme si shutil.rmtree(ignore_errors=True) a echoue.

- **`cinesort/ui/api/quality_support.py:78`** — ⚠️ CONTESTE 1/2
  `api.load_validation` n'existe plus sur CineSortApi (methode legacy supprimee, verifie hasattr=False ; seuls _load_validation_impl / api.run.load_validation existent). L'AttributeError n'est pas rattrapee par `except (OSError, TypeError, ValueError)` L79 et _resolve_ids_from_scope est appele AVANT le try global (L215 vs try L232) -> HTTP 500 systematique sur analyze_quality_batch avec row_ids=[] et options.scope='validated', chemin reellement appele par web/dashboard/views/qij.js:320.
  > Refutation (1 juge) : Le defaut statique existe (api.load_validation absent de CineSortApi, AttributeError non rattrapee L79, hors du try L232) MAIS il n'est declenchable qu'avec row_ids=[] + scope='validated', et les deux seuls call sites de ce payload sont qij.js:320 et quality.js:301 (via _analyzeBatch('validated') L276) — deux vues MORTES : importees nulle part dans app.js (imports L196-230) et routes /qij /quality

- **`cinesort/ui/api/library_audit_support.py:242`** — ✅ PANEL 2/2
  `TmdbClient(api_key=api_key)` omet le parametre positionnel REQUIS `cache_path` (cf tmdb_client.py:130-133) -> TypeError a chaque appel. Comme `getattr(api, "_tmdb_client", None)` (L233) est toujours None (attribut jamais defini sur CineSortApi), ce fallback est l'unique chemin ; le TypeError est avale par le `except Exception` L267 -> _fetch_collection_parts retourne toujours None -> get_incomplete_sagas retourne TOUJOURS `sagas: []` : la feature 'sagas incompletes' (spec 10) est 100% morte silencieusement.

- **`cinesort/ui/api/library_timeline_support.py:125`** — ⚠️ CONTESTE 1/2
  Appel `client.get_movies(user_id=..., library_id=...)` : cette methode n'existe pas sur JellyfinClient (la methode reelle est `get_all_movies`, cf jellyfin_client.py:339 ; seule autre proche : get_movies_count). L'AttributeError est avalee par l'except L126 (AttributeError dans le tuple) avec un simple log info -> la source prioritaire 'Jellyfin DateCreated' documentee en tete de module ne fonctionne JAMAIS ; la timeline retombe silencieusement sur le mtime filesystem et affiche source='filesystem' meme avec Jellyfin configure.
  > Refutation (1 juge) : Le defaut existe bien dans le code (JellyfinClient n'a pas de methode get_movies, seulement get_all_movies jellyfin_client.py:339 et get_movies_count L303, pas de __getattr__) mais le chemin n'est PAS atteignable par l'utilisateur aujourd'hui : le seul appelant frontend de library/get_library_timeline est web/dashboard/views/status.js:82, et le seul importeur du composant de rendu web/dashboard/co

- **`cinesort/ui/api/run_data_support.py:113`** — ✅ PANEL 2/2
  row_from_json ne reparse PAS les champs TV de PlanRow (tv_series_name, tv_season, tv_episode, tv_episode_title, tv_tmdb_series_id -> tous remis a None par defaut). Apres redemarrage de l'app (RunState plus en memoire), apply recharge les rows via _load_rows_from_plan_jsonl (apply_support.py:191/200) puis apply_tv_episode (app/apply_core.py:2019-2029) renomme avec season=0, episode=0, titre episode vide -> fichiers TV renommes faux type 'S00E00 - .ext' : violation de l'invariant de renommage + perte d'information sur fichiers utilisateur.

- **`cinesort/ui/api/film_support.py:273`** — ✅ PANEL 2/2
  `history = h_res.get("history") or []` : film_history_support.get_film_history retourne `{ok, film_id, title, year, events, current_score, ...}` (cle 'events', cf domain/film_history.py:228-236) — il n'y a JAMAIS de cle 'history' -> le champ `history` de get_film_full est toujours [] et la timeline du Modal Film est systematiquement vide.

- **`cinesort/ui/api/film_history_support.py:27`** — ✅ PANEL 2/2
  get_film_history / list_films_with_history delèguent a get_film_timeline / list_films_overview (domain/film_history.py:143,265-266) qui construisent `state_dir/runs/<run_id>/plan.jsonl` alors que les dossiers run reels sont `runs/tri_films_<run_id>` (infra/state.py:57, run_id genere sans prefixe — cf fallback explicite export_support.py:145) -> plan.jsonl jamais trouve -> get_film_history retourne toujours 0 evenement et list_films_with_history toujours 0 film : les 2 endpoints historiques par film sont morts.

- **`cinesort/ui/api/settings_support.py:2063`** — ⚠️ CONTESTE 1/2 [NOUVEAU RE-RUN]
  restore_settings_backup execute _backup_settings_before_write puis _rotate_settings_backups AVANT shutil.copy2 : avec 5 backups existants (etat nominal apres rotation), la creation du 6e backup fait supprimer le plus ancien par la rotation ; si c'est precisement le backup que l'utilisateur restaure (il est liste par list_settings_backups), il est unlink avant la copie -> copy2 leve FileNotFoundError (catche, return False) : la restauration echoue ET le backup choisi est definitivement detruit (perte de donnees).
  > Refutation (1 juge) : Bug logiquement exact dans la fonction (avec 5 backups, _backup_settings_before_write cree un 6e puis _rotate_settings_backups unlink le plus ancien avant copy2 -> FileNotFoundError catchee par OSError, backup detruit) MAIS non atteignable en production : restore_settings_backup et list_settings_backups n'ont AUCUN appelant hors tests (grep repo entier : seuls settings_support.py:2034/2047 (defini

### Boot — app.py, packaging, CSP

- **`app.py:446`** — ✅ PANEL 2/2
  main_api (mode --api) lit le token via api.settings.get_settings() qui passe TOUJOURS par _mask_secrets (settings_support.py:1199/1209) apres auto-generation systematique (settings_support.py:939-940) : token = masque constant "••••••••" (8 bullets). Le serveur REST standalone demarre donc avec ce Bearer connu publiquement (present dans le source) ; tout client presentant le VRAI token recoit 401, et --public est systematiquement retrograde localhost (8 < MIN_LAN_TOKEN_LENGTH=32, rest_server.py:1148) -> le mode --api --public ne fonctionne jamais. Le garde 'if not token: SystemExit(1)' (L448-450) est du code mort (le token masque n'est jamais vide). Contrairement au chemin desktop (_start_rest_server L302-344 qui relit settings.json brut), main_api n'a jamais recu le fix.

- **`app.py:334-344`** — ✅ PANEL 2/2
  _start_rest_server avec rest_api_token vide sur disque (premier lancement ou settings.json absent, branche L343-344 incluse) : in_memory_token = masque 8 bullets (auto-gen + _mask_secrets), donc le serveur REST demarre avec Bearer "••••••••" ; le save L338 est reverti a "" par _unmask_secrets_for_save (settings_support.py:1213-1219, incoming==_SECRET_MASK -> valeur disque existante) et le pipeline de save ne regenere jamais de token (_save_section_rest_api L1473 strip seulement) -> AUCUN token n'est jamais persiste et le message '[REST] Token persiste dans settings.json (auto-gen)' (L340) est faux. Consequence : ntoken=%E2%80%A2x8 dans main_url rejete par _URLSAFE_TOKEN_RE (web/dashboard/core/state.js:43-50) -> le bug 'CAUSE RACINE 2026-06-07' que le commentaire L784-794 pretend corrige se reproduit a chaque session tant que l'utilisateur n'a pas clique 'Regenerer' manuellement ; acces distant/LAN inconfigurables.

- **`cinesort/ui/api/cinesort_api.py:1007-1020`** — ✅ PANEL 2/2
  (adjacent au scope, decouvert en tracant la chaine token) _restart_api_server_impl (bouton 'Redemarrer le service API' de Parametres, parametres.js:240) reconstruit le serveur avec token = settings masques ("••••••••") ET omet le parametre host -> defaut 127.0.0.1 (rest_server.py:1126) alors que rest_api_enabled=True est verifie L1003. Apres un restart via l'UI : l'exposition LAN disparait silencieusement (bind localhost au lieu de 0.0.0.0) et l'auth par vrai token casse (Bearer = masque). Le bypass loopback masque la regression en local, l'utilisateur ne decouvre la panne que depuis un autre appareil.

### Dashboard JS

- **`web/dashboard/core/journal-polling.js:102`** — ⚠️ CONTESTE 1/2
  `const isDone = d.done || (!d.running && !d.error)` : toute reponse d'erreur HTTP renvoyee par apiPost SANS throw (429 rate-limit, 409 conflit, 5xx apres retries, 401 natif) produit data={ok:false, message} sans champs running/done/error -> isDone=true -> onDone({error:undefined}) declenche et polling stoppe definitivement alors que le run tourne encore. Une seule erreur transitoire (les avalanches 429 au boot sont documentees dans api.js) fait afficher un run "termine" a tort dans le Journal/QIJ. Seul le wrap backend run_status_unavailable (qui inclut un champ `error`) est protege.
  > Refutation (1 juge) : Le defaut logique de journal-polling.js:102 est exact en theorie, mais ce chemin est inatteignable aujourd'hui : l'unique consommateur de journalPoller est views/qij.js (import L32, start() L783), une vue MORTE — les routes /qij et /quality redirigent immediatement vers /accueil et /qualite (app.js:291,293), donc le code de montage de qij.js ne s'execute jamais. bootstrap-bisect.js (index.html:143

- **`web/dashboard/app.js:759`** — ⚠️ CONTESTE 1/2
  _checkProbeToolsBoot ne verifie pas payload.ok : sur reponse d'erreur non-throw d'apiPost (429 d'avalanche boot, 401 natif pendant la grace period token de 800ms — deux scenarios documentes dans le code lui-meme), payload={ok:false} sans `tools` -> `ffprobe.available` falsy -> fausse banniere persistante "ffprobe introuvable. Le scoring qualite ne fonctionnera pas" affichee alors que ffprobe est installe. Il faut tester payload.ok===true avant de conclure a l'absence de ffprobe.
  > Refutation (1 juge) : Le defaut de code existe (payload.ok jamais teste, apiPost retourne {ok:false} sans throw sur 401/429 — core/api.js:411-422), mais les deux scenarios declencheurs cites sont morts cote serveur pour l'app desktop : (1) 429 impossible depuis le client pywebview : _is_rate_limited (cinesort/infra/rest_server.py:553-555, FIX 2026-06-07) exempte totalement les IP loopback du rate-limiter, verifie en ru

- **`web/dashboard/core/drop.js:64`** — ⚠️ CONTESTE 1/2
  _processDrop ne verifie ni sr.status ni s.ok apres settings/get_settings : si la lecture echoue (429/5xx -> data={ok:false} sans roots), `roots` devient `[dossier depose]` seul puis save_settings est envoye. Cote backend, resolve_roots_from_payload lit les roots DU PAYLOAD (verifie dans settings_support.py:1849) : toutes les racines de bibliotheque configurees de l'utilisateur sont remplacees par le seul dossier droppe. Perte de configuration silencieuse sur erreur transitoire.
  > Refutation (1 juge) : Le chemin d'echec cite n'est pas atteignable en pratique. (1) Le drop n'opere qu'en mode natif desktop (drop.js:88, gate __CINESORT_NATIVE__ ; un navigateur distant sort a drop.js:51-54 faute de file.path), donc le client est toujours loopback 127.0.0.1. (2) 429 impossible : _is_rate_limited() exempte totalement les IPs locales (rest_server.py:553-555, _LOCAL_CLIENT_IPS ligne 37, FIX DEFINITIF 202

- **`web/dashboard/components/film-detail.js:1357`** — ✅ PANEL 2/2
  Cascade Échap sur modales empilées : le handler keydown du mode C est posé sur document et stopPropagation() est inopérant entre listeners d'un même target. Quand dangerConfirmModal (mark-delete, modal.js:321) ou la modale commentaire feedback (modal.js:113) est ouverte PAR-DESSUS la fiche film, presser Échap pour annuler la confirmation ferme AUSSI la fiche film en dessous — perte de contexte sur un chemin normal (annulation d'un marquage suppression).

- **`web/dashboard/components/right-panel.js:245`** — ✅ PANEL 2/2
  isExpandedWidth() utilise offsetWidth > MAX_WIDTH avec MAX_WIDTH=600 alors que la largeur élargie vaut exactement 600px (box-sizing:border-box global, styles.css:135 ; .v5-right-panel max-width 600) : 600 > 600 = false, donc le mode élargi n'est jamais détecté. Conséquence dans perceptual-modal.js:129+773 : le bouton toggle largeur ◀/▶ appelle toujours rpSetExpandedWidth(true) — impossible de revenir à la largeur normale, le bouton est cassé en permanence.

- **`web/dashboard/components/film-detail.js:727`** — ✅ PANEL 2/2
  Depuis la fiche film en mode C (overlay plein écran z-index 10001, ouvert par double-clic sur /bibliotheque, /doublons ou /traitement), les actions « Analyser perceptuel » / « Voir l'analyse complète » appellent openPerceptualModal() sans mode ; perceptual-modal._resolveMode (perceptual-modal.js:77-83) lit le hash (/bibliotheque ∈ _VIEWS_INSPECTOR) et choisit le mode A : l'analyse est injectée dans l'inspecteur droit DERRIÈRE l'overlay opaque. Pour l'utilisateur, le clic ne produit aucun effet visible.

- **`web/dashboard/views/parametres.js:54`** — ✅ PANEL 2/2
  Separateur ';' annonce mais backend ne parse que ','. Les champs text file_extensions (L54-56, placeholder '.mkv;.mp4;...', hint 'Séparées par ;'), subtitle_lang_priority (L96-97) et subtitle_expected_languages (L98) sont envoyes en string brute (_readFieldValue default L1777, aucun split ';'), or le backend splitte uniquement sur la virgule (settings_support.py L1637-1642, L1685-1690, L262-268). En suivant le hint, file_extensions devient ['mkv;.mp4;...'] (entree unique corrompue -> le scan ne matche plus AUCUN fichier video) et subtitle_lang_priority devient ['fr;'] (tronque a 3 chars par [:3]).

- **`web/dashboard/views/parametres.js:87`** — ✅ PANEL 2/2
  Champ 'Workers parallèles (scan)' (perceptual_workers_count) inoperant : la cle n'existe pas dans le payload get_settings (le backend n'expose que perceptual_workers, settings_support.py L859) donc l'input affiche toujours le default 2 quelle que soit la vraie valeur ; et a la sauvegarde, le mapping compat _save_section_advanced (L1671-1674) est immediatement ecrase par l'echo du vieux perceptual_workers (L1675-1676) puis par _save_section_perceptual (L1541, execute apres dans _save_settings_payload_locked L1911 vs L1918). La valeur saisie par l'utilisateur n'est JAMAIS persistee.

- **`web/dashboard/views/parametres.js:2249`** — ✅ PANEL 2/2
  Bouton 'Copier' du token REST copie le masque, pas le token : le backend masque rest_api_token en 8 bullets (settings_support.py _SECRET_FIELDS L1199 + _mask_secrets L1203-1210, SEC-H3) et parametres.js n'appelle jamais settings/reveal_rest_token (aucune occurrence). copyBtn copie tokenInput.value = '••••••••' en affichant '✓ Copié' (L2250-2253), et l'oeil (L1129) revele le masque. Impossible d'obtenir le vrai token depuis cet ecran (sauf juste apres une regeneration) -> onboarding dashboard distant casse.

- **`web/dashboard/views/parametres.js:2407`** — ✅ PANEL 2/2
  Modale de purge quarantaine illisible : la consequence passee a dangerConfirmModal (L2409) contient du HTML (<strong>, sampleHtml = <ul class=parametres-quarantine-sample>...) alors que modal.js echappe la consequence via escapeHtml (modal.js L256-258). L'utilisateur voit le markup brut en texte dans une confirmation destructive et la liste des fichiers n'est jamais rendue en liste — violation de l'invariant 'modale avec liste elements' (le param items, supporte par dangerConfirmModal, n'est pas utilise ici).

- **`web/dashboard/views/parametres.js:2860`** — ⚠️ CONTESTE 1/2
  Confirmations probe tools via window.confirm() natif (L2860-2868 install ~50 Mo, L2889-2896 update winget, L2939-2946 reinstall) alors que ce meme fichier documente les dialogs natifs comme incompatibles WebView2 et interdits (commentaires L1605-1607 et L1637-1639, fix A11Y-03 L31-33 qui a remplace window.confirm partout ailleurs). Selon l'hote, confirm() retourne false silencieusement (boutons morts : 'Installation annulée' systematique) ; et le fallback ': true' si confirm absent execute l'action SANS aucune confirmation — violation de l'invariant actions dangereuses (dangerConfirmModal obligatoire).
  > Refutation (1 juge) : Le chemin est bien atteignable (vue /parametres live, section Outils externes, boutons bindés L2537), mais le coeur factuel du finding est faux : l'hote pywebview active explicitement les dialogs natifs WebView2 — settings.AreDefaultScriptDialogsEnabled = True dans .venv313\Lib\site-packages\webview\platforms\edgechromium.py:289 (idem .venv:289), et aucun override dans CineSort (0 occurrence de Ar

- **`web/dashboard/views/processing.js:471`** — ✅ PANEL 2/2
  Le polling compare _state.status.status a "running" en minuscules alors que le backend renvoie "RUNNING"/"PENDING" en majuscules (run_flow_support.py:942-959) : la condition d'arret est toujours vraie, le polling s'arrete apres le 1er tick reussi, et isRunning (l.366) est toujours false donc la barre de progression et le bouton Annuler ne s'affichent jamais pendant un scan en cours.

- **`web/dashboard/views/processing.js:367`** — ✅ PANEL 2/2
  Mapping de progression casse : le code lit _state.status?.progress mais le payload run/get_status n'a aucun champ progress (il expose idx/total, cf run_flow_support.py:999-1014) ; meme avec la casse corrigee, la barre et le pourcentage resteraient figes a 0%.

- **`web/dashboard/views/processing.js:766`** — ✅ PANEL 2/2
  Contrat casse sur la route reachable /processing : _runApply appelle apiPost("run/apply", {run_id, dry_run, quarantine}). La facade apply(self, run_id, decisions, dry_run, quarantine_unapproved, ...) exige decisions (dict) et quarantine_unapproved, tous deux sans valeur par defaut. Le parametre envoye s'appelle "quarantine" (inexistant) et decisions est absent -> TypeError -> HTTP 400. L'etape Apply de ce stepper echoue systematiquement.

- **`web/dashboard/views/processing.js:548`** — ✅ PANEL 2/2
  L'etape Review est non fonctionnelle : run/load_validation renvoie uniquement {ok, decisions}, jamais de cle rows (history_support.py:77-119) ; payload.rows || [] est donc toujours vide et l'utilisateur voit systematiquement l'empty-state "Aucun film a valider" meme apres un scan reussi.

- **`web/dashboard/views/processing.js:758`** — ⚠️ CONTESTE 1/2
  Violation de l'invariant actions dangereuses : decocher dry-run puis cliquer "Lancer Apply" declenche un apply reel sans aucune modale de confirmation (pas de liste, pas de consequence, pas de delai 3s) ; idem _cancelRun (l.449) qui annule le run au simple clic alors que traitement.js exige dangerConfirmModal pour ces deux actions.
  > Refutation (1 juge) : L'apply reel est impossible depuis processing.js : _runApply (l.766) envoie {run_id, dry_run, quarantine} alors que RunFacade.apply exige decisions et quarantine_unapproved sans defaut (run_facade.py l.327-334) ; le dispatcher REST appelle method(**params) sans filtrage (rest_server.py l.1051) -> TypeError capture l.1068 -> HTTP 400 'Parametres invalides' AVANT toute operation filesystem. Decocher

- **`web/dashboard/views/traitement.js:455`** — ✅ PANEL 2/2
  Sur echec get_dashboard, _loadRunInfo met _runInfo=null (l.211, l.256) : pendant une simple panne reseau le header affiche "Aucun run actif detecte" + CTA "Lancer un scan" (risque de double scan) et le badge backoff "Reconnexion dans Xs" du fix iter13 (l.502-519) est inatteignable car il n'est rendu que dans la branche _runInfo non-null — la degradation est donc silencieuse dans le cas d'echec le plus frequent.

- **`web/dashboard/views/traitement.js:1367`** — ✅ PANEL 2/2
  Resume pre-apply faux : _renderApplyStep filtre r.decision === "ok" || "approved" en minuscules alors que le module ecrit "APPROVED"/"REJECTED" (l.1941, l.2564) et que _initDecisionsState compare en majuscules ; de plus le fallback || confidence >= seuil compte comme "renommages" des films explicitement REJETES a confiance haute — le compteur et l'apercu divergent du opCount reel de la modale danger.

- **`web/dashboard/views/traitement.js:2653`** — ✅ PANEL 2/2
  Auto-save de unmountTraitement auto-detruit : _handleSaveValidation() est lance (fetch avec _signal()) puis _abortController.abort() (l.2662) est appele quelques lignes plus bas dans la meme pile -> la requete save_validation est annulee quasi systematiquement -> les decisions non enregistrees sont perdues a la navigation (toast d'erreur en prime), exactement la perte que ce fix pretendait empecher.

- **`web/dashboard/views/traitement.js:2027`** — ✅ PANEL 2/2
  Options apply inertes : les cases "Exporter le rapport en CSV" (l.1448) et "Synchroniser Jellyfin" (l.1452) ne sont jamais transmises dans le payload run/apply (l.2027-2036 dry-run, l.2106-2113 reel : seuls run_id/decisions/dry_run/quarantine_unapproved/apply_atomic) alors que la modale de confirmation affirme "CSV : exporte" — promesse UI jamais tenue.

- **`web/dashboard/views/traitement.js:1649`** — ✅ PANEL 2/2
  Panneau "Options de scan" entierement inerte : _handleScanStart envoie settings={perceptual, subtitles, omdb, nfo, parallelism}, clés qu'aucun code backend ne lit (clés reelles : perceptual_enabled/perceptual_auto_on_scan, subtitle_detection_enabled, omdb_enabled+omdb_api_key...) ; via _hydrate_settings_from_store le scan utilise les valeurs on-disk quoi que l'utilisateur coche (ex. decocher OMDb ne le desactive pas).

- **`web/dashboard/views/library/lib-apply.js:182`** — ⚠️ CONTESTE 1/2
  Deadlock UI sur le flux apply reel : la modale de confirmation est attendue via `await new Promise(resolve => showModal({...}))` mais resolve n'est appele QUE par les boutons Annuler/Confirmer. Si l'utilisateur ferme par Esc, clic backdrop ou la croix (chemins supportes par components/modal.js closeModal, qui n'invoque aucun callback), la Promise ne se resout jamais : le finally ne s'execute pas, le bouton Appliquer reste disabled definitivement et le message 'Application en cours...' reste fige jusqu'a re-navigation.
  > Refutation (1 juge) : Le bug est techniquement exact dans le code cite (closeModal via Esc/backdrop/croix n'appelle aucun callback, la Promise reste pendante), mais le chemin est mort : _onApply n'est branche que par initApply(), appele uniquement par initLibraryWorkflow() (views/library/library.js:48), et initLibraryWorkflow n'est importe NULLE PART. La route live /bibliotheque (alias legacy /library, app.js:292/321) 

- **`web/dashboard/views/library/lib-validation.js:337`** — ⚠️ CONTESTE 1/2
  buildDecisionsPayload emet `decision:"rejected"` + `ok:false` pour TOUTES les lignes jamais decidees (dec null), confondant 'pending' et 'rejete'. Consequences verifiees : (1) apres Sauvegarder puis rechargement de la vue, _loadRows (L87-97) restaure ces lignes comme rejetees (rouges) et les compteurs affichent des centaines de 'rejete(s)' que l'utilisateur n'a jamais rejetes ; (2) avec 'Quarantaine non-OK' cochee dans lib-apply, un apply reel deplace physiquement vers la quarantaine des films jamais examines (le backend projette decision->ok et quarantine les ok:false).
  > Refutation (1 juge) : Le defaut existe dans le source (lib-validation.js:333-336 mappe bien dec=null vers decision:"rejected"/ok:false), mais il est inatteignable au runtime : initValidation n'est appele que par initLibraryWorkflow (library/library.js:46), qui n'est importe NULLE PART (grep exhaustif web/ : seule occurrence hors definition = commentaire perime index.html:92). La route /processing monte en realite initP

- **`web/dashboard/views/library/lib-apply.js:334`** — ⚠️ CONTESTE 1/2
  Ouvrir le panneau '+ Diagnostic de prevision' (toggle <details>, L140-142) declenche run/apply dry_run:true avec buildDecisionsPayload(). Or le backend _validate_apply (apply_support.py:1141) persiste INCONDITIONNELLEMENT les decisions mergees dans validation.json, meme en dry-run, et _merge_decisions donne priorite au payload entrant. Une simple consultation du diagnostic (action percue comme read-only) ecrase donc la validation sauvegardee sur disque en convertissant toutes les lignes non decidees en 'rejected'. Idem pour le bouton dry-run (L176/196). Perte silencieuse de l'etat de validation utilisateur.
  > Refutation (1 juge) : Le chemin UI cite est du code mort : initApply (lib-apply.js:13) exige l'element #libApplyContent, cree uniquement par views/library/library.js:91, dont l'unique export initLibraryWorkflow n'est importe nulle part (seuls vestiges : commentaires index.html:92 et keyboard.js:129). Les routes reelles n'y menent pas : /library est un _legacyRedirect vers /bibliotheque (app.js:292) qui monte views/bibl

- **`web/dashboard/views/library/lib-verification.js:180`** — ⚠️ CONTESTE 1/2
  Filtre 'Priorite' totalement inoperant : `parseInt(tr.children[0].textContent)` parse le libelle du badge ('Haute'/'Moyenne'/'Basse') et retourne toujours NaN ; toutes les comparaisons NaN>=40 / NaN<40 / NaN>=70 (L184-186) valent false, donc aucune ligne n'est jamais masquee quel que soit le choix high/med/low. Le filtre semble fonctionner mais n'a strictement aucun effet.
  > Refutation (1 juge) : Bug exact dans le source (parseInt('Haute')=NaN, comparaisons L184-186 toutes false) mais chemin non atteignable : le filtre n'existe qu'apres _render(), qui exige l'element #libVerificationContent cree uniquement par initLibraryWorkflow (library/library.js:79). Or initLibraryWorkflow n'est importe/appele NULLE PART : /processing route vers initProcessing (processing.js, app.js:281) qui n'importe 

- **`web/dashboard/views/library/lib-verification.js:96`** — ⚠️ CONTESTE 1/2
  Filtre 'Raison' casse pour 3 options sur 5 : les values du <select> ('year', 'notmovie', 'integrity') sont comparees par includes() aux libelles francais lowercases de _reasonLabel ('annee incoherente', 'non-film', 'integrite') qui ne les contiennent jamais (L179+183). Selectionner 'Annee incoherente', 'Non-film' ou 'Integrite' masque donc TOUTES les lignes (tableau vide), seuls 'nfo' et 'conf' matchent par coincidence.
  > Refutation (1 juge) : Bug reel dans le source (values 'year'/'notmovie'/'integrity' jamais contenues dans les libelles francais compares par includes() L183) MAIS code mort non atteignable : lib-verification.js n'est importe que par views/library/library.js (initVerification) et views/library/lib-validation.js (refreshVerification), or l'unique point d'entree de ce sous-arbre, initLibraryWorkflow (library/library.js:33

- **`web/dashboard/views/bibliotheque.js:419`** — ✅ PANEL 2/2
  Action bulk 'Posters TMDb' sans aucun effet visible : _bulkRefreshPosters (L1465-1476) patche row.poster_url avec les URLs retournees par integrations/get_tmdb_posters, mais _renderFilmCard priorise systematiquement posterProxyUrl(row.tmdb_id) (`proxiedPoster || row.poster_url`) — et seules les rows AVEC tmdb_id sont patchees (L1431). Le src de l'<img> reste donc identique (et en cache navigateur) : le toast 'N posters TMDb recharges' annonce un succes alors que rien ne change a l'ecran.

- **`web/dashboard/views/bibliotheque.js:421`** — ✅ PANEL 2/2
  Fallback poster inatteignable : pour toute row avec tmdb_id valide, posterSrc est toujours non vide (posterProxyUrl retourne /api/poster?id=...), donc la branche placeholder 'tmdb_id sans poster' (L423-424) est du code mort, et l'<img> n'a aucun handler onerror. Le proxy renvoie 404 si TMDb n'a pas de poster et 503 si la cle API TMDb n'est pas configuree (poster_proxy.py:634-638/684-688) : dans ce dernier cas toute la grille affiche des icones d'image cassee au lieu des placeholders 🎬.

- **`web/dashboard/views/library/lib-validation.js:226`** — ⚠️ CONTESTE 1/2
  Tri descendant inatteignable dans la table Validation : attachSort (components/table.js) stocke currentKey/currentDir dans une closure, mais son callback rerender est _renderTable qui regenere tout le DOM ET rappelle attachSort avec une closure neuve (currentKey=null). Chaque clic sur un header repart donc en tri ascendant (jamais desc, jamais retour a l'etat initial), et l'indicateur visuel/aria-sort pose avant le rerender est immediatement efface.
  > Refutation (1 juge) : Le bug de closure attachSort est reel dans le code (rerender=_renderTable regenere la table et rappelle attachSort avec currentKey=null, donc desc inatteignable), mais ce chemin n'est pas atteignable en runtime : _renderTable n'est appele que via initValidation, lui-meme appele uniquement par initLibraryWorkflow (views/library/library.js:46), qui n'est importe NULLE PART (seul vestige : commentair

- **`web/dashboard/views/doublons.js:97`** — ✅ PANEL 2/2
  _groupKey fallback genere "Title::Year" (titre non lowercase, separateur "::") alors que le backend _group_key_for (run_flow_support.py:1412) genere "title|year" (lowercase, separateur "|"). check_duplicates ne renvoie jamais group_key/id/signature (duplicate_support.py:373-381), donc le fallback est TOUJOURS utilise et ne matche JAMAIS cote backend : mark_duplicate_winner (run_flow_support.py:1462) ne retrouve pas le groupe, persiste la decision avec losers=[] et a l'apply (apply_support.py:1317) aucun fichier perdant n'est deplace vers _review/_duplicates_user_decided/. Toutes les decisions Garder A/B (carte, comparateur, auto-decider) sont des no-op silencieux avec toast de succes.

- **`web/dashboard/views/doublons.js:1101`** — ✅ PANEL 2/2
  check_duplicates ne renvoie JAMAIS winner_decided/winner_side (zero occurrence dans cinesort/, les groupes n'ont que title/year/rows/existing_paths/plan_conflict/comparison). Consequence : apres chaque clic Garder A/B, le resync _loadGroups() (appele ligne 716) remplace les groupes et efface l'etat decide — le badge "Decide" disparait, decidedCount retombe a 0, le filtre "Decides" est toujours vide, le CTA "Passer a Apply" n'est jamais debloque et _goToApply affiche systematiquement la modale "N groupes non decides".

- **`web/dashboard/views/accueil.js:985`** — ✅ PANEL 2/2
  _triggerStartPlan envoie a run/start_plan le payload complet de settings/get_settings dont les secrets sont MASQUES ("••••••••" pour tmdb_api_key/omdb_api_key/jellyfin_api_key/plex_token/radarr_api_key, cf settings_support.py:1203). _hydrate_settings_from_store (run_flow_support.py:693-697) donne priorite au caller sur le settings.json on-disk et start_plan ne demasque jamais (_SECRET_MASK absent de run_flow_support) : la vraie cle TMDb est ecrasee par les bullets et tout scan lance depuis l'Accueil tourne avec une cle TMDb invalide (identification cassee).

- **`web/dashboard/views/accueil.js:763`** — ✅ PANEL 2/2
  _extractScanProgress lit payload.active_run_id et payload.run_info que get_dashboard ne renvoie JAMAIS (aucune occurrence dans dashboard_support.py ; active_run_id n'existe que sur GET /api/health ; test_phase3_1_fix_payload_mapping.py confirme "payload.run_info inexistant"). Resultat : la carte "Scan en cours" et le hero scan n'apparaissent jamais, le polling lance apres start_plan conclut "run termine" des le 1er tick (100ms) et re-init l'Accueil sans aucun feedback de progression, et _updateSidebarForActiveRun(false) grise en permanence l'entree Traitement ("Aucun run actif") meme pendant un scan actif.

- **`web/dashboard/views/accueil.js:1323`** — ✅ PANEL 2/2
  initAccueil teste dashRes.ok === false alors que l'enveloppe apiPost est {status, data} SANS propriete ok (core/api.js:466) — le ok metier est dans dashRes.data.ok (pattern corrige partout ailleurs, cf _triggerStartPlan L990). Une erreur metier de get_dashboard (DB inaccessible, 401 natif, 429) n'est donc jamais detectee : l'Accueil se rend avec le payload d'erreur comme donnees et affiche "Bienvenue. Lance ton premier scan" au lieu de l'ecran d'erreur. Meme defaut lignes 1331-1333 : settings peut devenir l'objet d'erreur {ok:false,...} -> banniere "TMDb non configure" a tort et _currentSettings pollue.

- **`web/dashboard/views/accueil.js:1127`** — ✅ PANEL 2/2
  Les 3 options du drawer "Options…" (dry_run, skip_duplicates, apply_after) ne correspondent a AUCUNE cle reconnue par le backend : zero occurrence de skip_duplicates/apply_after dans cinesort/, et start_plan ne lit pas settings.dry_run (seul apply_dry_run du run state existe, run_flow_support.py:936). Cocher "Dry-run (simulation seulement)" n'a strictement aucun effet — l'utilisateur croit lancer une simulation alors que le flag est silencieusement ignore (invariant dry_run sensible). De plus les cases ne sont pas pre-remplies depuis les settings courants.

- **`web/dashboard/views/historique.js:868`** — ✅ PANEL 2/2
  La recherche par nom de film (placeholder L425 "Rechercher run_id ou nom de film...") ne peut jamais fonctionner : _filmsCacheByRun n'est rempli que si get_history_stats renvoie un tableau films, or le payload backend (history_support.py:306-325) ne contient pas de cle films (seulement films_by_tier). Faux negatifs systematiques sur toute recherche par titre, et le prefetch "fix audit" (L261-282) declenche 2 requetes inutiles par run non cache (get_history_stats + get_status) sans jamais peupler le cache.

- **`web/dashboard/views/historique.js:176`** — ✅ PANEL 2/2
  runs_history (dashboard_support.py:87-105) n'expose que 9 cles (run_id, run_dir, started_ts, ended_ts, duration_s, total_rows, applied_rows, errors_count, anomalies_count) — ni status, ni undone, ni is_undo/type. Consequences : les options de filtre Statut "Cancelled"/"Undone"/"En validation" et Type "Undo" ne matchent jamais (liste toujours vide alors que CANCELLED/AWAITING_VALIDATION sont de vrais statuts backend, run_models.py:8-18), le compteur header "X undo" vaut toujours 0, et apres un undo reussi le statut UNDONE n'est jamais derivable -> le bouton "Annuler l'apply" reste propose au lieu de "Deja annule" (L621).

- **`web/dashboard/views/accueil.js:136`** — ✅ PANEL 2/2
  showResume teste latestRun.status === "AWAITING_VALIDATION" mais les items runs_history n'ont pas de champ status (dashboard_support.py:87-105) alors que AWAITING_VALIDATION est un statut backend reel (run_models.py:18) : le CTA "Reprendre la validation" de la carte Dernier run n'apparait JAMAIS, le rappel operateur "Pense a valider la run de ce matin" (L918) non plus, et avg_confidence_pct (L132, absent du payload) affiche toujours "—".

- **`web/dashboard/views/qualite.js:777`** — ✅ PANEL 2/2
  Bouton periode "Tout" casse : _loadHistory envoie `Number(periodDays) || 30` qui transforme 0 en 30, alors que le backend supporte explicitement period_days=0 (=tout l'historique, quality_audit_support.py:183/199). Cliquer "Tout" charge 30 jours et, comme la reponse echo period_days=30, le bouton actif rebascule visuellement sur "30j" (_renderEvolutionSection:393) — la fonctionnalite est inatteignable.

- **`web/dashboard/views/about.js:58`** — ✅ PANEL 2/2
  _readAppInfo lit res.ok/res.version/res.build_date sur le wrapper {status,data} retourne par apiPost (core/api.js:466) au lieu de res.data.* ; runtime/get_app_version (runtime_facade.py:81-89) renvoie bien {ok,version,...} mais sous data -> la modale 'A propos' (vue LIVE, chargee via index.html:146) affiche TOUJOURS 'version indisponible', jamais build/sha/python.

- **`web/dashboard/views/film-detail.js:466`** — ✅ PANEL 2/2
  Bouton retour 'Bibliotheque' : window.navigateTo n'est assigne nulle part dans web/ (grep exhaustif) -> fallback window.location.hash='library' SANS slash ; 'library' ne matche aucune route (_routes contient '/library') -> resolve() notFound (router.js:124-129, setNotFound jamais appele) -> redirection /accueil en natif et ECRAN /login en dashboard distant. Le retour vers la bibliotheque ne fonctionne jamais.

- **`web/dashboard/views/film-detail.js:385`** — ✅ PANEL 2/2
  Le bouton 'Lancer l'analyse perceptuelle' (etat vide onglet Analyse V2, data-v5-film-rescan) n'a aucun listener : _bindEvents() (l.475) s'execute dans _loadAndRender AVANT _renderTabPanel() (l.576-577) et n'est jamais re-execute au changement d'onglet -> clic sans effet pour tout film sans analyse V2 (chemin normal d'usage).

- **`web/dashboard/views/aide.js:546`** — ✅ PANEL 2/2
  Bouton '⤤ Ouvrir en grand' du drawer doc : navigue vers #/aide/doc/<id> puis ferme le drawer, mais la route n'existe pas (aucun registerRoute, commentaire l.547-548 l'admet) -> notFound du router -> ejection vers /accueil (natif) ou /login (web) avec perte du document. Vue Aide = vue LIVE (/aide).

- **`web/dashboard/views/aide.js:782`** — ✅ PANEL 2/2
  Bouton '🐛 Signaler un bug' : window.open() direct, silencieusement bloque par WebView2 sans handler new-window (cf commentaire app.js:566-568 et runtime_facade.open_external_url) ; l'intercepteur natif app.js:571-584 ne couvre que les <a href>, pas window.open, et open() ne throw pas donc le fallback location.href (l.786) ne s'execute jamais -> bouton inoperant dans l'app desktop (environnement principal). Doit passer par runtime/open_external_url.

- **`web/dashboard/views/quality-simulator.js:430`** — ⚠️ CONTESTE 1/2
  '✓ Appliquer ce preset' n'envoie que preset_id alors que la simulation affichee incluait simState.overrides (sliders ponderations/seuils onglet Avance) et custom_rules ; le backend apply_quality_preset(preset_id) (quality_facade.py:82-87) ne sait pas appliquer d'overrides -> ce qui est applique ne correspond PAS a l'apercu 'Apres' que l'utilisateur vient de valider.
  > Refutation (1 juge) : Le code est bien incoherent (la simulation envoie overrides a simulate_quality_preset L273-278 mais _applyPreset L433 n'envoie que preset_id), MAIS la modale n'est pas atteignable en runtime : openQualitySimulator n'est importe que par qij.js et quality.js (vues MORTES, routes /qij et /quality = redirections app.js:291,293) et par custom-rules-editor.js, lui-meme importe uniquement par ces deux me

- **`web/dashboard/views/quality-simulator.js:259`** — ⚠️ CONTESTE 1/2
  'Simuler l'impact' des regles custom (custom-rules-editor.js:414 passe {custom_rules}) est casse : pendingOverrides.custom_rules ne sont injectees que par _gatherOverrides(), appele uniquement sur input slider (l.223-231) ; or _selectPreset() (l.212-214) remet overrides=null avant _runSimulation -> la simulation lancee au choix du preset n'inclut JAMAIS les regles sauf si l'utilisateur bouge aussi un slider. De plus _saveCustom (l.442-463) ne reporte pas custom_rules dans le profil sauvegarde.
  > Refutation (1 juge) : Bug latent, pas atteignable en runtime : le seul appel passant {custom_rules} est custom-rules-editor.js:414 (_previewImpact), et openCustomRulesEditor n'est invoque que depuis qij.js:311 et quality.js:273 via le bouton #btnCustomRulesEditor rendu uniquement dans ces deux vues — or qij.js et quality.js sont les vues MORTES confirmees (app.js:291 /qij redirige vers /accueil, app.js:293 /quality red

- **`web/dashboard/views/quality-simulator.js:330`** — ⚠️ CONTESTE 1/2
  Violation invariante couleurs tiers : le chart 'Distribution par tier' colore Platinum=var(--success), Gold=var(--accent), Silver=var(--info), Bronze=var(--warning), au lieu des couleurs tier invariantes --tier-platinum #E5E4E2 / --tier-gold #FFD700 / --tier-silver #C0C0C0 / --tier-bronze #CD7F32 (styles.css:2003-2006). Les tiers changent de couleur selon le theme semantique.
  > Refutation (1 juge) : Code mort non atteignable : _renderTiersChart (quality-simulator.js:330) n'est rendu qu'apres openQualitySimulator(), dont les 3 seuls appelants sont quality.js:271 et qij.js:307 (vues MORTES, routes /quality et /qij = redirections app.js:291/293) et custom-rules-editor.js:414, lui-meme importe uniquement par ces deux memes vues mortes. La vue live qualite.js n'importe ni le simulateur ni l'editeu

- **`web/dashboard/app.js:289`** — ✅ PANEL 2/2
  Regression refonte : le simulateur qualite G5 (quality-simulator.js) et l'editeur de regles custom G6 (custom-rules-editor.js) n'ont plus AUCUN point d'entree dans le dashboard. Seuls qij.js et quality.js les importent, or les routes /qij et /quality sont des redirections (app.js:291,293) et la vue live qualite.js ne les importe pas (grep vide) -> simulate/apply preset et regles custom invisibles pour l'utilisateur (backend fonctionnel sans UI).

- **`web/dashboard/views/traitement.js:191`** — ❌ TUE PAR LE PANEL [NOUVEAU RE-RUN]
  _writeStep reecrit le hash en "#/traitement#step-X" sans preserver le fragment #run-XXX ; _onHashChange (l.2594-2606) voit alors nextRun=null != _targetRunId, reset le run cible et recharge get_dashboard sur "latest" SANS recharger _validationPlan/_decisionsState : les decisions du run cible peuvent ensuite etre postees via save_validation sous le run_id d'un autre run (ecriture croisee inter-runs).
  > Refutation (1 juge) : La chaine decrite ne peut pas s'executer : le routeur (core/router.js l.273-276) ecoute hashchange AVANT la vue (resolve enregistre au boot, _onHashChange seulement au mount) et resolve() remonte integralement la vue a CHAQUE hashchange sans dedupe intra-route. unmountTraitement (appele via _currentCleanup, app.js:319) retire le listener _onHashChange (l.2677) pendant le dispatch meme — un listene

### CSS / themes

- **`web/dashboard/depth-effects.css:393`** — ⚠️ CONTESTE 1/2
  Violation des invariants tier sur la home V6 (rendue par status.js:414-415) : .tier-label.tier-gold et .quality-fill.tier-gold (L393-394, L426) colorent le tier Gold avec var(--accent) — donc bleu en Studio, rouge en Cinema, violet en Neon — au lieu du hex invariant #FFD700 ; Platinum est #E4E4E7 (L387) au lieu de #E5E4E2 et Silver #C0C0CC (L396) au lieu de #C0C0C0.
  > Refutation (1 juge) : Les regles incriminees (depth-effects.css L387-429) existent bien, mais le seul code qui genere le markup .tier-label.tier-gold / .quality-fill.tier-gold est status.js:414-415 — une vue MORTE : status.js n'est importe nulle part (ni dans app.js L183-250, ni dans bootstrap-bisect.js dont la liste MODULES ne le contient pas), et les routes /home et /status redirigent immediatement vers /accueil (app

- **`web/dashboard/depth-effects.css:186`** — ⚠️ CONTESTE 1/2
  Les variables --glass-inset/--glass-border/--glass-border-hover/--glass-bg-strong sont consommees 17 fois SANS fallback mais ne sont definies nulle part (le themes.css V6 du dashboard a ete retire — cf index.html:30 "fichier inexistant (404 garanti)") : declarations invalides at computed-value time → track du ring SVG invisible (L186), bordures des workflow-step/service-tile/topbar-avatar absentes (L246/290/651), fonds du champ recherche topbar et des quality-track transparents (L586/407) ; --r-pill (L408) est dans le meme cas.
  > Refutation (1 juge) : Les variables --glass-*/--r-pill sont bien indefinies, mais aucun selecteur consommateur n'est atteignable aujourd'hui : .ring-track/.workflow-step/.quality-track/.suggestion-card ne sont rendus que par status.js (vue morte) et par la branche heroLayout de kpi-card.js (L94) que personne n'active — plex/radarr/jellyfin passent des configs simples sans heroLayout/ring/workflowSteps ET leurs routes r

- **`web/dashboard/styles.css:274`** — ✅ PANEL 2/2
  .card (L274), .kpi-card (L423), .sidebar (L158) et .topbar (L1916) deleguent explicitement par commentaire leurs background/border/box-shadow a "themes.css V6" qui n'existe plus (index.html:30) : ces composants legacy n'ont plus aucun fond ni bordure glass ; effet visible aggrave par .card:hover (L975) qui applique encore var(--shadow) → la card n'a une ombre qu'au survol et "clignote".

- **`web/shared/components.css:3576`** — ✅ PANEL 2/2
  --accent-1 n'est defini nulle part dans le projet : tous les usages sans fallback du Notification Center sont morts → la pastille "non lu" est invisible (background/box-shadow L3576-3577), le filtre actif n'a aucune surbrillance (L3516-3518), le hover des items est sans effet (L3565), le fond de l'icone info (L3591) et le badge cat--event (L3646-3648) sont casses. Composant rendu par notification-center.js:93/110/158.

- **`web/dashboard/styles.css:1371`** — ⚠️ CONTESTE 1/2
  .kpi-card--hero declare `background: var(--bg-glass), linear-gradient(...)` : une couleur n'est autorisee que dans la DERNIERE couche d'un background multiple, la declaration (parse-valide a cause de var()) devient invalide at computed-value time → background force a unset/transparent ; la card hero du F-pattern (status.js:525, kpi-card.js:91/112) perd a la fois le fond glass ET le degrade accent, sans retomber sur un fond de cascade inferieure.
  > Refutation (1 juge) : Latent, non atteignable : la classe .kpi-card--hero n'est generee que par (1) status.js:525, vue MORTE (/status redirige vers /accueil, app.js:290), et (2) kpi-card.js:91/112 uniquement si cfg.hero ou cfg.heroLayout est truthy — or aucun appelant vivant ne passe ces flags (grep exhaustif : seuls radarr.js, plex.js, jellyfin.js consomment kpiGridHtml, sans aucun 'hero', et accueil.js n'importe pas 

- **`web/shared/components.css:10011`** — ✅ PANEL 2/2
  .historique-films-history-tier--platinum { background:#b9f2ff; color:#003a4a } : bleu clair "diamant" au lieu de l'invariant Platinum #E5E4E2 — les lignes voisines gold/silver/bronze (L10012-10014) respectent la charte, seul platinum diverge dans la modale historique des films.

### Migrations SQL

- **`cinesort/infra/db/migrations/032-vector-search-tables.sql:38`** — ⚠️ CONTESTE 1/2
  Migration jamais appliquee : le nom de fichier utilise des tirets (032-vector-search-tables.sql) alors que _MIGRATION_FILE_RE dans migration_manager.py:15 exige un underscore apres le numero (^\d+_.*\.sql$). La migration est silencieusement ignoree par apply() ET par build_bootstrap_script() : la table vec_films_hash n'est creee nulle part (SqliteVecAdapter.create_vec_table leve NotImplementedError, REQUIRED_SCHEMA_TABLES de sqlite_store.py ne la contient pas), user_version plafonne a 31, et docs/internal/notes/sqlite_vec_setup.md + vector_search/__init__.py affirment que la table existe. Toute implementation V3.3 qui s'appuiera dessus crashera en 'no such table'. Renommer en 032_vector_search_tables.sql.
  > Refutation (1 juge) : Le fait brut est exact (032-vector-search-tables.sql ne matche pas _MIGRATION_FILE_RE migration_manager.py:15, migration ignoree par apply() et build_bootstrap_script()), mais AUCUN chemin runtime n'est atteignable aujourd'hui : SqliteVecAdapter est un scaffold dont create_vec_table/add_embedding/knn_search levent NotImplementedError AVANT tout SQL sur vec_films_hash (et load_extension echouerait 

- **`cinesort/infra/db/migrations/023_film_modal_state.sql:18`** — ✅ PANEL 2/2
  ignored_alerts est cle UNIQUE(row_id, alert_code) SANS run_id pour un etat cense persister par film, mais row_id est derive du hash() Python randomise par processus (plan_support_replan.py:542 : hash((folder, video.name)) & 0xFFFFFFFF ; PYTHONHASHSEED=0 n'est pose qu'au build dans build_windows.bat, pas au runtime). Des qu'une row est recalculee hors cache (changement de config/cfg_sig, fichier modifie, purge cache) dans une nouvelle session, son row_id change : les alertes explicitement ignorees par l'utilisateur reapparaissent et les rows orphelines s'accumulent sans purge possible (film_modal.py:51-62 requete strictement par row_id courant). Collisions 32 bits inter-films egalement possibles sur grosses bibliotheques (un ignore masquant l'alerte d'un autre film).

### Transverse — contrats, concurrence, secrets

- **`web/dashboard/views/traitement.js:2376`** — ✅ PANEL 2/2
  Mauvais prefixe de facade : l'action "Ignorer l'alerte" de l'etape Verification appelle apiPost("run/mark_alert_ignored", {run_id, row_id}). La methode n'existe que sur la facade Library (library/mark_alert_ignored(row_id, alert_code)). Le dispatcher REST ne trouve pas "run/mark_alert_ignored" (contient un '/', donc pas de 410) et renvoie 404 "Methode inconnue". De plus alert_code n'est jamais transmis. L'action echoue systematiquement (toast "Echec de l'ignorance") dans le workflow principal Traitement.

- **`cinesort/app/watcher.py:188`** — ✅ PANEL 2/2
  FolderWatcher._trigger_scan recupere les settings via api.settings.get_settings() (payload MASQUE) puis les passe a run.start_plan ; run_flow_support._hydrate_settings_from_store (L693-696) donne priorite a la requete sur le settings.json on-disk, donc tmdb_api_key="••••••••" ecrase la vraie cle dechiffree : _init_tmdb_client cree un TmdbClient avec le masque comme cle API -> tous les scans automatiques (watch_enabled) tournent avec TMDb en 401, identification silencieusement degradee vs un scan manuel (idem omdb_api_key).

- **`cinesort/ui/api/perceptual_support.py:1479`** — ✅ PANEL 2/2
  _load_tmdb_metadata appelle api._tmdb_client() alors qu'aucune methode/attribut _tmdb_client n'existe sur CineSortApi (grep projet : zero definition) ; l'AttributeError n'est PAS dans le tuple except (ImportError, KeyError, OSError, TypeError, ValueError) L1482 et remonte dans _video_task (L289) ou run_parallel_tasks la capture comme echec de la tache : pour TOUT film avec un candidat TMDb (tmdb_id>0, cas majoritaire), l'analyse video perceptuelle entiere (grain, fake-4K FFT/SSIM, HDR10+, interlacing, crop, judder) echoue silencieusement et le rapport ne contient que l'audio. quality_report_support.py:102 se protege avec hasattr, pas ce module.

- **`cinesort/ui/api/quality_simulator_support.py:204`** — ⚠️ CONTESTE 1/2
  _load_reports_for_scope fait store = getattr(api, "_store", None) et _resolve_latest_run_id (L231) lit api._store, mais CineSortApi n'a JAMAIS d'attribut _store (les stores vivent dans _infra_by_state_dir, cf commentaire BUG dans cinesort_api) : store=None -> reports=[] -> run_simulation retourne systematiquement "Aucun rapport qualite disponible dans ce scope." — le simulateur de preset qualite (endpoint simulate_quality_preset, feature G5) est entierement non fonctionnel.
  > Refutation (1 juge) : Le defaut de code existe (api._store n'est jamais defini sur CineSortApi, seul job_runner.py:65 pose self._store), mais le chemin est non atteignable aujourd'hui : l'unique appelant UI de quality/simulate_quality_preset est openQualitySimulator (quality-simulator.js:273), invoque seulement par qij.js:307 et quality.js:271 (vues MORTES confirmees) et par custom-rules-editor.js:414, lui-meme importe

- **`cinesort/ui/api/cinesort_api.py:1013`** — ✅ PANEL 2/2
  _restart_api_server_impl recree RestApiServer sans les kwargs host ni cors_origin alors que le boot (app.py L352-361) passe host="0.0.0.0" si rest_api_enabled et cors_origin des settings : apres un "redemarrage du serveur REST" via l'UI, le serveur rebind silencieusement sur 127.0.0.1 (la supervision LAN/dashboard distant cesse de fonctionner malgre rest_api_enabled=true, jusqu'au redemarrage complet de l'app) et le rest_api_cors_origin configure retombe sur "*".

- **`web/dashboard/views/status.js:937`** — ✅ PANEL 2/2
  Le bloc « Acces distant » lit `settings.rest_api_token` issu de settings/get_settings, lequel masque le token (SEC-H3). L'affichage 👁 (ligne 985) et la copie 📋 (ligne 991, navigator.clipboard.writeText(dataset.full)) renvoient le masque "••••••••" et non le vrai Bearer → le token copie/scanne vers un appareil distant ne s'authentifie pas. Meme defaut dans parametres.js (champ api-key-rest input value masquee + data-rest-token-copy:2249). L'endpoint reveal_rest_token mentionne dans settings_support.py:1198 n'est jamais implemente, donc aucun moyen UI d'obtenir le vrai token hors regeneration.

---

## MEDIUM (133)

### Domaine — scoring, parsing, perceptuel

- **`cinesort/domain/quality_score.py:1759`**
  _merge_probe_with_name_hints : si le probe dict legacy contient "sources": None (setdefault L1753 ne remplace pas une valeur None existante), le garde isinstance L1756-1758 met sources_video={} mais la ligne 1759 fait enriched["sources"]["video"]=... sur le None d'origine -> TypeError et crash de compute_quality_score. Le champ "video" beneficie du remplacement correct (L1754-1755) mais pas "sources" : le garde est contredit par le subscript inconditionnel.

- **`cinesort/domain/quality_score.py:1825`**
  _merge_probe_with_name_hints : probe dict avec "audio_tracks": None (setdefault L1752 inoperant sur None) combine a un hint audio dans le nom de release -> audio_tracks.append(synth_track) sur None -> AttributeError, crash du scoring. Aucune normalisation None->[] avant l'append.

- **`cinesort/domain/quality_score.py:1888`** — [NON RE-CONFIRME]
  probe_quality present avec valeur None est classe UNKNOWN (permissif, aucun cap) au lieu de FAILED : probe.get("probe_quality") retourne None aussi bien pour cle-presente-a-null que pour cle-absente, contrairement au commentaire L1883-1887 ("present mais vide -> warning + fallback FAILED"). Un probe echoue serialise en JSON avec null contourne silencieusement le cap tier Silver (L2191-2201) et peut afficher Gold/Platinum non verifie.

- **`cinesort/domain/quality_score.py:849`**
  _score_video, branche bitrate_kbps None + hint 4K Light : add_reason(-4, "4K Light probable (tag release) sans debit mesure") enregistre un facteur -4 jamais soustrait de video_sub (seul le -8 est applique L848). Delta factor != increment reel, exactement la classe de bug corrigee par le hotfix coherence 2026-06-04 (cf L855-858) ; fausse weighted_delta et top_negative de explain_score.

- **`cinesort/domain/quality_score.py:788`**
  _score_video, bloc resolution (L788-805) non aligne sur le principe du hotfix coherence 2026-06-04 : increments reels sur video_sub +34/+24/+14/+4 vs deltas reportes dans factors +16/+10/+5/-6. Cas extreme : "Resolution faible" reporte -6 alors que video_sub recoit +4 (signes opposes). L'explication enrichie sous-pondere la resolution sur tous les films et affiche une penalite inexistante pour les SD.

- **`cinesort/domain/quality_score.py:2078`**
  compute_quality_score : video_sub/audio_sub/extras_sub ne sont jamais reclampes [0,100] apres les ajustements contextuels (ere L2078, encode warnings L2086, genre L2100) avant _apply_weights (L2113) et les metrics. Exemple concret : CAM (plancher 14, L1993) puis upscale -8 + reencode -6 + 4k_light -3 -> video_sub = -3 ; metrics.subscores et score_explanation exposent une valeur negative (ou >100 avec bonus ere + genre cumulables) et la ponderation depasse les bornes de conception par axe.

- **`cinesort/domain/scan_helpers.py:106`**
  iter_videos teste extension (L110) et nom suspect (L113) AVANT entry.is_file() (L117) : les SOUS-DOSSIERS contenant un point (ex 'Avatar.2009.1080p-GRP') sont comptes dans analyse_ignores_par_raison['ignore_extension']. Quand videos==[] ce delta est reverse dans stats.films_rejected_ext (plan_support_core.py:866-871) : la carte dashboard 'Diagnostic scan' compte des repertoires comme 'fichiers video rejetes par extension' (semantique compteur fausse).

- **`cinesort/domain/scan_helpers.py:19`**
  IGNORE_VIDEO_NAME_RE rejette tout fichier dont le nom contient sample/trailer/teaser comme mot delimite, y compris des vrais films : 'Trailer.Park.Boys.The.Movie.2006.mkv' matche ('trailer' en debut + separateur) -> exclu du scan via iter_videos L113, jamais planifie. Le commentaire SCAN-1 a retire 'demo' pour ce meme motif de faux positif mais 'trailer' en tete de titre reste touche.

- **`cinesort/domain/core.py:978`**
  Filtre _TMDB_BONUS_KEYWORDS par SUBSTRING ('kw in combined_title_lower') et non par mot entier : 'promo' matche 'The Promotion' (film 2008), 'trailer' matche 'Trailer Park Boys: The Movie', 'bonus' matche tout titre le contenant -> candidats TMDb legitimes silencieusement elimines de build_candidates_from_tmdb, le film ne peut jamais matcher sa fiche TMDb.

- **`cinesort/domain/core.py:573`**
  _parse_nfo_runtime concatene TOUS les chiffres du tag <runtime> : un NFO '2h 28min' donne digits='228' -> 228 minutes au lieu de 148 (passe le garde 1..1200). Le nfo_runtime errone est persiste dans PlanRow et sert au cross-check probe P1.1.d -> faux ecarts de duree signales (ou vrais ecarts masques).

- **`cinesort/domain/core.py:597`**
  parse_movie_nfo essaie utf-8 puis latin-1 puis cp1252 : latin-1 ne leve JAMAIS UnicodeDecodeError (accepte tous les octets), donc la branche cp1252 est code mort. Un NFO encode cp1252 (frequent sous Windows) voit ses apostrophes typographiques 0x92 decodees en controles C1 (U+0092) dans le titre ; windows_safe (path_utils.py:106) ne strip que 0x00-0x1F/0x7F -> le caractere de controle peut finir dans le nom du dossier renomme (mojibake user-visible).

- **`cinesort/domain/core.py:110`**
  VIDEO_EXTS_ALL est documente comme 'union maximale des extensions video reconnues dans toute la codebase' mais n'est PAS un sur-ensemble de VIDEO_EXTS_DEFAULT : .m4v/.mpg/.mpeg/.vob/.flv/.ogv manquent (verifie par difference de sets). Consequence concrete : ui/api/apply_support.py:75 l'utilise SEUL (sans union cfg.video_exts) dans _resolve_hashed_target -> la pre-verification d'integrite de l'undo (hash du video principal MOVE_DIR) est silencieusement sautee pour les films dans ces formats.

- **`cinesort/domain/core.py:517`**
  is_sidecar_for_video accepte tout stem prefixe du stem video suivi d'un delimiteur incluant l'espace : pour video 'Rocky.mkv', le sidecar 'Rocky 2.srt' (appartenant a 'Rocky 2.mkv' dans le meme dossier collection) matche 'rocky 2'.startswith('rocky '). Double attribution du meme sidecar a deux rows -> le premier apply le deplace/renomme avec le MAUVAIS film, le second le trouve manquant.

- **`cinesort/domain/duplicate_multi_signal.py:259`**
  Perte silencieuse de doublons fuzzy dans augment_groups_with_multi_signal : la Phase B (Pass 1, ligne 259) ajoute les matches fuzzy aux groupes Phase A par mutation (group_match.members.append) sans changer leur phase (reste strict_metadata). Or augment_groups_with_multi_signal saute ensuite tous les groupes phase==PHASE_STRICT_METADATA (ligne 627-628, "deja gere par base_groups"). Resultat : tout candidat detecte par fuzzy contre un groupe strict existant disparait completement de la sortie advisory (il est consomme en Pass 1 donc absent aussi des groupes Phase B). Fonction non encore cablee en prod (tests seulement) mais contrat API casse pour le branchement futur.

- **`cinesort/domain/duplicate_compare.py:105`**
  Asymetrie de la division entiere dans le critere perceptuel : delta = (pa - pb) // 5 avec floor division Python. Verifie par execution : un deficit de 1 a 4 points (pa-pb=-1..-4) donne delta=-1 (B gagne le critere), alors qu'un avantage de 1 a 4 points (pa-pb=+1..+4) donne delta=0 (tie). Le fichier B est systematiquement favorise sur les ecarts perceptuels faibles, ce qui peut faire basculer le verdict global pres du seuil _TIE_THRESHOLD=5 et inverser la recommandation garder/archiver.

- **`cinesort/domain/duplicate_compare.py:40`**
  _VIDEO_CODEC_RANK omet vp9 (pourtant reconnu par naming._codec_label ligne 494) : un fichier VP9 recoit le rang fallback 0, INFERIEUR a mpeg4/xvid/divx (rang 1). Consequence : dans une comparaison de doublons VP9 vs Xvid, le Xvid gagne le critere codec (15 points, > seuil tie de 5) et la recommandation devient "Garder Xvid, archiver VP9" — recommandation d'archivage inversee pour un codec moderne.

- **`cinesort/domain/custom_rules.py:227`**
  _act_cap_max avec une value non numerique : _clamp() retourne 0 pour _MISSING, donc cap=0 et le score est plafonne a 0 → le film tombe silencieusement en Reject. C'est exactement la classe de bug corrigee pour force_score (lignes 196-203, avec warning et early-return) mais PAS pour cap_max, et _validate_action (ligne 378) ne valide pas la numericite de value pour cap_max/cap_min/score_delta/score_multiplier — une regle cap_max value="abc" passe la validation puis detruit le score a l'application.

- **`cinesort/domain/perceptual/audio_perceptual.py:336`**
  Quand ffmpeg echoue (stderr non vide mais zero ligne Peak — cas reel avec le bug d'index `-map`), total=0 → pct=0.0 → verdict « acceptable » au lieu de « unknown » (incoherent avec la branche stderr-vide ligne 290 qui retourne bien « unknown »). _compute_audio_score applique alors s_clip=90 (meilleur que le defaut 80 sans analyse) : un echec subprocess AMELIORE le score clipping au lieu de le neutraliser.

- **`cinesort/domain/perceptual/scene_detection.py:88`**
  Le downsampling temporel annonce (2 fps) est inoperant : `-r` est place en option de SORTIE (apres -i, applique apres -vf), donc select/scene+showinfo tournent sur TOUTES les frames decodees a pleine cadence. Sur un vrai film (2h, 1080p/4K) le decodage complet depasse largement le timeout de 30s (SCENE_DETECTION_TIMEOUT_S) -> TimeoutExpired -> retour [] -> l'echantillonnage hybride par keyframes ne fonctionne jamais et chaque film gaspille 30s. Il faudrait `fps=2` en tete de chaine -vf.

- **`cinesort/domain/perceptual/ssim_self_ref.py:89`**
  Le filter_complex hardcode l'upscale a 3840:2160 puis fait ssim contre l'original [b] : pour tout 4K dont la resolution n'est pas exactement 3840x2160 (DCI 4096x2160, 3996x2160, etc., height>=1800 donc non skippes), le filtre ssim exige des dimensions identiques et ffmpeg echoue -> returncode!=0 -> verdict 'error' systematique. La detection fake-4K SSIM est inoperante sur les sources DCI.

- **`cinesort/domain/perceptual/spectral_analysis.py:236`**
  Le garde-fou HE-AAC/SBR de classify_cutoff teste uniquement la chaine codec ('he-aac', 'heaac'...), or ffprobe normalise reporte codec_name='aac' avec le SBR dans le champ 'profile' (HE-AAC), champ jamais transmis par analyze_audio_perceptual (passe best['codec']). La branche 'lossy_ambiguous_sbr' est du code mort : les pistes HE-AAC dont le SBR reconstruit les hautes frequences peuvent etre classees lossless/lossy_high a tort.

- **`cinesort/domain/perceptual/lpips_compare.py:157`**
  Chargement modele non protege : _get_session ne catch pas les exceptions de ort.InferenceSession (modele ONNX corrompu/tronque -> InvalidProtobuf/Fail d'onnxruntime, qui heritent d'Exception et PAS de RuntimeError), et compute_lpips_distance_pair ne catch que FileNotFoundError et RuntimeError autour de _get_session -> un modele corrompu fait remonter l'exception jusqu'au pipeline deep-compare a chaque paire, contredisant la garantie de la docstring ('jamais de plantage'). Seule l'ABSENCE du modele est geree.

- **`cinesort/domain/perceptual/constants.py:604`** — [NON RE-CONFIRME]
  TIER_V2_COLORS contredit l'invariant projet des couleurs tiers : platinum='#FFD700' (qui est l'hex reserve a Gold), gold='#22C55E', silver='#3B82F6', bronze='#F59E0B' au lieu de Gold #FFD700 / Silver #C0C0C0 / Bronze #CD7F32 / Platinum #E5E4E2. Le dict n'est actuellement reference nulle part dans le code (seulement dans docs/internal/plans/PLAN_CODE_v7_5_0.md), donc pas de violation runtime, mais c'est une mine : tout futur consommateur UI casserait l'invariante des couleurs de tiers.

- **`cinesort/domain/release_name_parser.py:76`**
  Le lookahead `(?!\.?\s*[Dd])` apres `\bDV\b` est inutile contre 'DVD' (deja exclu par \b et (?![A-Za-z0-9])) mais supprime la detection Dolby Vision des que DV est suivi d'un mot commencant par D : noms tres courants type '...WEB-DL.DV.DDP5.1...' ou '...DV.DTS-HD...' perdent le hint hdr='dv' → bonus HDR manquant au scoring fallback.

- **`cinesort/domain/mkv_title_check.py:53`** — [NON RE-CONFIRME]
  check_container_title retourne 'mkv_title_mismatch' pour TOUT ecart case-insensitive entre titre conteneur et titre propose (ex. 'Inception (2010)' vs 'Inception', titre + tags release), alors que _is_scene_title/_SCENE_PATTERNS/_SCENE_THRESHOLD (L17-32) ne sont JAMAIS appeles : la detection 'titre scene' documentee (docstring, CHANGELOG 9.23) n'est pas cablee. Resultat : warning encode_warnings (quality_report_support.py:313) bruyant sur quasi tout fichier ayant un tag title.

- **`cinesort/domain/duplicate_support.py:291`**
  Asymetrie des cles movie_key : la cle planifiee inclut l'edition (movie_key(title, year, edition=row_edition)) mais existing_movie_folder_index (L156/167) construit ses cles SANS edition. Pour toute row avec edition detectee, existing_idx.get(key) est toujours vide → les dossiers 'Titre (Annee)' deja presents ailleurs sur disque ne sont jamais signales en existing_paths, et un conflit plan-vs-plan entre la version editee et la version standard visant le meme dossier cible n'est pas detecte (groupes separes, has_plan_dupe par groupe).

- **`cinesort/domain/subtitle_helpers.py:159`**
  detect_language_from_suffix n'inspecte que le DERNIER token du stem (rsplit('.', 1)) : les conventions multi-tags Plex/Jellyfin 'Film.fr.forced.srt', 'Film.fre.sdh.srt', 'Film.en.cc.srt' retombent sur le tag special ('forced'/'sdh'/'cc' → '') et la langue precedente est perdue → language='' et la langue attendue est flaggee manquante a tort dans missing_languages (build_subtitle_report).

- **`cinesort/domain/video_hash.py:135`**
  Contrat docstring viole : 'Liste vide si ... duree invalide' mais quand duration_s <= 0 le code prend la branche useful_duration < 10 avec start=0, end=max(1.0, duration_s)=1.0 et extrait les 10 thumbnails dans la premiere seconde du fichier (frames noires/logo). Le caller (app/duplicate_pipeline.py:126) passe duration_s sans garde : deux films differents avec duree inconnue produisent des dHash quasi identiques (frames noires) → faux positifs 'confirmed' dans la fusion doublons.

- **`cinesort/domain/tv_helpers.py:15`**
  _SEASON_EPISODE_TEXT_RE exige des espaces litteraux (\s+) entre 'Saison/Season', le numero et 'Episode', alors que les noms de fichiers utilisent des points : 'Serie.Saison.2.Episode.5.mkv' ne matche jamais ce pattern et retombe sur _EP_RE → season=None et series_name pollue ('Serie Saison 2'), contrairement aux autres regex du module qui tolerent [ ._-]. Episode TV mal regroupe/renomme.

### App — apply, plan, jobs

- **`cinesort/app/apply_core.py:157`**
  `record_apply_op` ne capture que (TypeError, ValueError, OSError) alors que le callback reel ecrit en SQLite (apply_support.py:1238 append_apply_operation, dont la closure ne capture aussi que OSError/TypeError/ValueError). Un sqlite3.OperationalError transitoire (DB locked — frequent sur Windows avec antivirus/backup) n'est attrape ni ici ni par les handlers par-row de apply_rows (PermissionError/OSError/ValueError/TypeError, L1478-1560) : l'exception remonte hors de apply_rows et avorte TOUT le batch a mi-parcours apres que les moves physiques aient eu lieu, en violation du contrat documente 'Retourne False si l'enregistrement echoue, erreur non-fatale'.

- **`cinesort/app/apply_core.py:2066`**
  `apply_tv_episode` n'appelle record_apply_op que dans le bloc `if not dry_run` (L2069, L2084), contrairement au contrat P1.3 applique dans le meme fichier ('record l'op meme en dry_run pour la preview UI', L801 et L1851) et exploite par apply_support.py:1384 (preview_ops_out collecte en dry_run). Meme probleme pour move_to_review_bucket (L594-605 : QUARANTINE_* non record en dry_run) et migrate_legacy_collection_root (L529-536). Resultat : la preview dry_run omet tous les moves TV et les mises en quarantaine alors que les compteurs (res.moves L2094, res.quarantined) les comptent — incoherence compteurs/liste d'ops cote UI.

- **`plan_support_core.py:812`**
  Compteurs de cache auto-pollues : stats_before est capture AVANT _try_apply_folder_cache, donc lors d'un miss le delta persiste par persist_folder_cache contient incremental_cache_misses=+1. A chaque hit ulterieur, stats_apply_cached_delta rejoue ce delta PUIS incremental_cache_hits est incremente (L601-603) : un rescan entierement cache de N dossiers affiche hits=N ET misses=N au lieu de misses=0. Les stats de cache exposees a l'utilisateur sont systematiquement fausses en mode incremental.

- **`plan_support_core.py:150`**
  stats_snapshot_for_cache omet les compteurs du dashboard 'Diagnostic scan' mutes pendant la classification : tv_episodes_seen, root_level_films_seen, folders_rejected_tv_like_count_videos, films_rejected_ext/size/name, folders_rejected_scandir_error (champs reels de Stats, core.py:338-359). Sur cache hit dossier, ces compteurs ne sont pas rejoues -> un rescan incremental affiche ~0 partout dans le diagnostic alors qu'un scan a froid affiche les vraies valeurs.

- **`plan_support_core.py:944`**
  Purge incoherente du row cache v2 : ctx.video_paths_seen n'est rempli que dans _classify_and_plan_folder (chemin MISS), jamais sur hit du cache dossier (L814-816 continue avant). prune_incremental_row_cache supprime donc les entrees row de tous les dossiers ayant hit, et si TOUS les dossiers hittent, keep=[] declenche 'DELETE FROM incremental_row_cache WHERE root_path=?' (scan.py:343-348) : le row cache est integralement vide a chaque scan sans changement, forcant un re-scoring TMDb complet du dossier des qu'il change ensuite.

- **`plan_support_core.py:550`**
  discover_candidate_folders(ctx.cfg) est appele sans stats=ctx.stats alors que le parametre existe (scan_helpers.py:186-190) et que _walk comptabilise ignore_prefix_underscore/ignore_profondeur_max/ignore_scandir_error dedans. Ces raisons n'atteignent donc jamais ctx.stats et le report QW07 de _dedup_and_finalize_phase (L957-965) lit toujours 0 : folders_rejected_underscore et folders_rejected_depth affichent systematiquement 0 au dashboard — exactement le bug que le commentaire QW07 pretend avoir corrige.

- **`plan_support_replan.py:542`**
  row_id derive de hash((str(folder), video.name)) & 0xFFFFFFFF : le hash de str est randomise par processus (PYTHONHASHSEED=0 n'est pose que dans build_windows.bat au moment du BUILD PyInstaller, pas au runtime). Les rows restaurees du cache incremental conservent des row_id frappes sous une ancienne seed et cohabitent avec des rows fraiches sous la seed courante dans le meme plan : collisions 32-bit possibles entre deux films differents, et decisions etant un dict keye par row_id, une collision routerait une decision utilisateur vers la mauvaise row a l'apply.

- **`plan_support_dedup.py:670`**
  Le runtime HARD filter lit sa configuration via settings=getattr(cfg, '_runtime_filter_settings', None) (plan_support_replan.py:628) mais cet attribut n'est assigne NULLE PART dans le codebase (grep exhaustif : seule occurrence = la lecture). Les reglages runtime_hard_filter_enabled / runtime_hard_threshold_min documentes comme mecanisme de rollback (domain/runtime_hard_filter.py:23-24) sont donc morts : le filtre tourne toujours active avec le seuil par defaut et ne peut etre ni desactive ni ajuste par l'utilisateur.

- **`cinesort/app/jellyfin_sync.py:205`**
  restore_watched : la boucle de retry catch (ConnectionError, OSError, TimeoutError, ValueError) mais PAS JellyfinError (herite d'IntegrationError(Exception), pas d'OSError — cf infra/jellyfin_client.py:25 et infra/integration_errors.py:27), alors que get_all_movies_from_all_libraries leve precisement JellyfinError (client lignes 139-186, 380). Une erreur transitoire pendant la re-indexation Jellyfin — le scenario exact que les 5 retries H-11 devaient absorber — fait sortir l'exception de restore_watched et avorte toute la restauration (catchee en simple WARN dans apply_support.py:1894) : statuts vus perdus au lieu d'etre retentes. snapshot_watched (ligne 117) catch broad Exception pour cette raison exacte.

- **`cinesort/app/watcher.py:147`**
  FolderWatcher.run : le snapshot baseline est remplace AVANT l'appel _trigger_scan. Si start_plan retourne ok=False (course : un scan manuel demarre entre _is_scan_running() et le trigger, root devenu invalide, settings KO), le changement detecte est definitivement perdu — au poll suivant _has_changed(B,B)=False et aucun scan ne sera jamais declenche pour ce changement. Le commentaire lignes 137-144 protege ce risque pour le cas scan-running mais pas pour l'echec du trigger : il faudrait ne graver le baseline qu'apres result.ok.

- **`cinesort/app/quarantine_ttl.py:488`**
  start_quarantine_ttl_cron capture ttl_days une fois au boot (app.py:508-509/1010-1011) et _purge_quarantine_bucket_impl (cinesort_api.py:2642) utilise la valeur passee sans relire les settings. Mettre quarantaine_ttl_days=0 en cours de session — l'UI affiche "0 = desactive" sans mention de redemarrage (web/dashboard/views/parametres.js:343) — ne stoppe PAS le cron qui continue de purger toutes les 24 h avec l'ancienne valeur jusqu'au redemarrage ; inversement passer de 0 a N ne demarre aucun cron. Mismatch UI-backend sur une action destructrice.

- **`cinesort/app/merge_metadata.py:110`** — [NON RE-CONFIRME]
  _is_empty(False) retourne True car bool est une sous-classe d'int (`isinstance(False, (int, float)) and False == 0`). En mode "completer les manques uniquement" (replace_data=False), un champ booleen volontairement a False dans target est ecrase par la valeur source, en contradiction avec la garantie anti-ecrasement de la barriere (seuls None/""/collections vides/0 numerique sont documentes comme "manquants"). Ajouter un garde `isinstance(value, bool)` avant le test numerique.

### Infra — REST, clients API, SQLite, probe

- **`cinesort/infra/rest_server.py:874`** — [NON RE-CONFIRME]
  L'endpoint GET /api/poster n'appelle JAMAIS _check_auth (commentaire lignes 868-873 le justifie par le bypass loopback). Mais le bypass de _check_auth est conditionne a `bind_host == '127.0.0.1'` : quand le serveur est expose en 0.0.0.0 (LAN, autorise si token >= 32 chars), /api/poster reste totalement non authentifie alors que tout le reste exige le Bearer. Un client LAN non authentifie peut donc invoquer le proxy poster (relai sortant vers TMDb) ; risque limite par la validation anti-SSRF de poster_proxy mais c'est une surface d'auth manquante incoherente avec le bind LAN.

- **`cinesort/infra/rest_server.py:490`**
  En mode CINESORT_DEBUG, sur tout mismatch de token le handler logge les codepoints du token SERVEUR reel : `token_cps = [f'U+{ord(c):04X}' for c in self.auth_token]` puis `logger.warning('[DEBUG-AUTH] server token codepoints=%s', token_cps)` (lignes 481, 490). Cela ecrit le secret Bearer en clair (trivialement reconstructible) dans les logs a chaque tentative ratee. Les logs sont souvent joints aux rapports de bug -> fuite de secret. Seul le bearer fourni par le client devrait etre logge, jamais le token serveur.

- **`cinesort/infra/network_utils.py:55-63`**
  La protection anti-SSRF #70 (utilisee par jellyfin_client.py:48, plex_client.py:53, ollama_client.py:103) est contournable de 3 facons : (a) un FQDN controle par l'attaquant resolvant vers 169.254.169.254 passe (le DNS n'est pas resolu, le commentaire l.63 l'admet), (b) ::ffff:169.254.169.254 (IPv4-mapped IPv6) echappe au test isinstance(IPv4Address) l.60, (c) seule l'URL initiale est validee alors que les sessions suivent les redirects (allow_redirects=True par defaut dans _get/_post) — un serveur 'valide' peut repondre 302 vers l'endpoint metadata.

- **`cinesort/infra/_http_utils.py:46`**
  respect_retry_after_header=True sans plafond pour toutes les sessions clients : urllib3 dort la duree COMPLETE du header Retry-After entre chaque tentative (jusqu'a 3), et timeout_s ne borne que chaque tentative HTTP, pas les sleeps inter-retries. Un serveur Jellyfin/Plex/reverse-proxy mal configure ou hostile renvoyant 'Retry-After: 3600' sur 429/503 bloque le thread appelant (handler REST ou pipeline scan) pendant des heures sans interruption possible.

- **`cinesort/infra/integrations/poster_proxy.py:119-157`**
  _build_or_get_tmdb_client est appele depuis les threads du ThreadingHTTPServer (rest_server.py:901) sans aucun lock sur le cache module-level _tmdb_client_cache : (a) plusieurs TmdbClient peuvent etre construits en parallele et l'ancien client remplace n'est jamais close() (sockets + ecritures throttlees perdues) ; (b) ce client long-lived partage tmdb_cache.json avec les clients de l'app (run_flow_support.py:209, film_support.py:95...) et chaque instance reecrit le fichier ENTIER depuis son snapshot memoire — le proxy peut ecraser avec un snapshot perime des milliers d'entrees fraichement ecrites par un scan (re-fetch API massif derriere) ; (c) settings.json + dechiffrement DPAPI sont relus a CHAQUE requete poster (grille = dizaines de lectures/s).

- **`cinesort/infra/tmdb_client.py:262-267`**
  _cache_get_stale relit et reparse le fichier cache COMPLET depuis le disque (20-100 MB d'apres les commentaires PERF-6 l.303-305) a chaque echec API dont la cle est absente de la memoire ; pendant une panne TMDb avec circuit ouvert, chaque film non cache d'un scan declenche ce parse complet (search_movie:464, _get_movie_detail_cached:551, get_alternative_titles:783) — le fast-fail du breaker (#76) est annule par des minutes/heures de parse JSON et d'IO disque.

- **`cinesort/infra/jellyfin_client.py:222`**
  validate_connection ne verifie jamais que les payloads JSON sont des dicts : si l'URL pointe vers un service non-Jellyfin renvoyant 200 + JSON liste, server_info.get() (l.222) ou u.get() sur un element non-dict de users (l.259) levent AttributeError au lieu de retourner {ok:False,...} — le bouton 'Tester la connexion' crashe au lieu d'afficher l'erreur. PlexClient.validate_connection a recu exactement ce guard (fix Vague H v1.5.3, plex_client.py:144-154) mais pas Jellyfin ; meme trou dans get_libraries (l.293) et get_all_movies (l.382).

- **`cinesort/infra/jellyfin_client.py:137-146`**
  _get/_post/_delete (et plex_client.py:115-124, identique) ne convertissent que ConnectionError/Timeout/HTTPError en JellyfinError/PlexError : les autres requests.RequestException (ChunkedEncodingError sur reponse tronquee, TooManyRedirects, ContentDecodingError, RetryError...) s'echappent en exceptions brutes, cassant le contrat IntegrationError (BUG-1 v7.8.0) — les callers qui catchent IntegrationError laissent remonter un crash non gere (500 REST / plantage du sync watched) au lieu d'un message d'erreur propre.

- **`cinesort/infra/tmdb_client.py:860`** — [NON RE-CONFIRME]
  find_by_imdb_id appelle data.get('movie_results') HORS du try (l.847-858) et sans le guard isinstance(data, dict) pourtant present dans search_movie:471 et _get_movie_detail_cached:556 : un payload JSON non-dict provoque une AttributeError non rattrapee qui remonte dans le pipeline plan (cross-check Phase 6.2) ; meme absence de validation dans search_tv:936-948 ou les conversions int()/float() sur les champs des items sont aussi hors du try/except.

- **`cinesort/infra/db/sqlite_store.py:75`**
  REQUIRED_SCHEMA_TABLES omet user_quality_feedback (migration 014) et incremental_row_cache (migration 008), et SCHEMA_GROUPS['incremental'] (ligne 110) n'inclut pas incremental_row_cache alors que ScanRepository.get/upsert_incremental_row_cache passent par _with_schema_group('incremental'). De plus _ensure_tables (ligne 693) ne re-verifie PAS la presence des tables apres initialize(). Sur une DB partielle (le scenario exact que le filet self-healing Vague L/BUG-002 est cense couvrir), ces deux tables manquantes ne declenchent jamais le bootstrap : chaque appel feedback qualite ou row-cache scan v2 crashe en boucle avec 'no such table' sans auto-reparation.

- **`cinesort/infra/db/repositories/run.py:390`**
  delete_run ne purge que perceptual_reports et apply_batches (+ cascades errors/quality_reports/anomalies/apply_operations via migration 021). Verifie dans les migrations : duplicate_decisions, film_marked_for_deletion, film_tmdb_overrides, film_decisions_v2, user_quality_feedback, apply_batch_modes et apply_pending_moves n'ont AUCUNE FK CASCADE vers runs/apply_batches et ne sont pas supprimes -> lignes orphelines accumulees a chaque suppression de run (bloat + apply_batch_modes/pending_moves referencant des batch_id inexistants), et le compteur retourne sous-estime les enregistrements reellement lies au run.

- **`cinesort/infra/db/sqlite_store.py:63`**
  _detect_cloud_sync_folder fait un match SOUS-CHAINE sur tout le chemin (marker.lower() in path_str) alors que sa docstring (lignes 54-58) affirme explicitement comparer 'segment par segment pour eviter les faux positifs'. Les markers courts 'Box' et 'Mega' matchent n'importe quel chemin contenant ces sous-chaines (ex. D:\MediaToolbox\..., D:\Omega\...) -> fausse notification warning persistante 'Base SQLite dans un dossier cloud' publiee a chaque boot via runtime_support._publish_cloud_sync_warning_if_any (runtime_support.py:108).

- **`cinesort/infra/db/repositories/scan.py:350`**
  prune_incremental_row_cache construit un placeholder '?' PAR video conservee (keep_video_paths = toute la bibliotheque) ; au-dela de SQLITE_MAX_VARIABLE_NUMBER (32766) la requete leve sqlite3.OperationalError 'too many SQL variables' et le prune crashe systematiquement sur les tres grandes bibliotheques. Meme pattern dans prune_incremental_scan_cache (ligne 221) avec la liste des dossiers.

- **`cinesort/infra/db/sqlite_store.py:693`**
  _ensure_tables avale toute sqlite3.Error du check de schema (except: pass) puis appelle initialize() : une erreur transitoire 'database is locked' (busy_timeout depasse) pendant un simple check declenche un cycle complet integrity_check + PRAGMA wal_checkpoint(RESTART) + backup pre_migration + rotation + migrations, en plein milieu d'un scan, depuis n'importe quel thread et potentiellement en concurrence depuis plusieurs threads (aucun verrou autour d'initialize) ; chaque occurrence consomme en plus un slot de la rotation backup (aggrave le defect backup ci-dessus).

- **`cinesort/infra/probe/_retry_backoff.py:118-119`**
  _is_transient() considere subprocess.TimeoutExpired comme transitoire et le retente (default_runner => max_retries=3, soit 4 tentatives). Combine au timeout adaptatif (jusqu'a 300s/fichier), un NAS mort/tres lent coute jusqu'a ~4x300s+backoff (~20 min) PAR fichier avant que ffprobe_backend ne renvoie un echec, et le circuit breaker exige 5 echecs consecutifs : la degradation reseau n'est observee qu'apres plusieurs dizaines de minutes, ce qui annule en grande partie l'objectif du breaker (court-circuiter le timeout par fichier). Le retry ne consulte jamais l'etat du breaker entre les tentatives.

- **`cinesort/infra/probe/auto_install.py:174-175,204,257`**
  install_ffprobe()/install_mediainfo() court-circuitent sur `path.exists()` et ecrivent le binaire via write_bytes() NON atomique (pas de tmp+os.replace contrairement a disk_cache.py). Si le process est tue pendant l'extraction, un ffprobe.exe/MediaInfo.exe tronque subsiste ; au lancement suivant `exists()` renvoie True et le binaire corrompu est retourne puis execute indefiniment, sans nouvelle verification ni re-telechargement.

- **`cinesort/infra/security/dpapi_ng.py:188`**
  protect_dpapi_ng(b"") echoue : NCryptProtectSecret renvoie NTSTATUS=0x80090027 (NTE_INVALID_PARAMETER), leve DpapiNgOperationError. Le code construit pourtant explicitement un chemin 'plaintext vide' (lignes 178-183 et 237-242) qui ne fonctionne pas : Windows refuse une longueur nulle. Consequence : save_secret(name, "") (secret_storage.py:103) et la re-chiffrement d'un blob legacy vide remontent une SecretStorageError au lieu d'un aller-retour propre. Verifie empiriquement.

### Backend UI/API — facades + modules support

- **`cinesort/ui/api/cinesort_api.py:876`**
  _sync_watcher : si le watcher est deja vivant et watch_enabled reste True, les nouveaux roots et watch_interval_minutes sauvegardes sont ignores (la branche start n'est prise que si watcher mort). L'utilisateur qui modifie le dossier surveille ou l'intervalle pense que c'est applique alors que le watcher continue avec les anciens parametres jusqu'a un toggle off/on ou un redemarrage.

- **`cinesort/ui/api/cinesort_api.py:969`**
  _check_for_updates_impl : sequence read-modify-write des settings (lecture L956 -> appel reseau force_check potentiellement long -> save L970) realisee HORS du _settings_write_lock. Un save_settings concurrent pendant le check GitHub est ecrase par le snapshot perime relu en debut de fonction (perte des modifications utilisateur). C'est exactement le TOCTOU corrige pour _set_locale_impl (L839) mais pas ici.

- **`cinesort/ui/api/cinesort_api.py:1021`**
  _restart_api_server_impl : old_server.stop() est execute (L999) AVANT toute validation, puis server.start() (L1021) est hors de tout try/except. Si le port est occupe ou le certificat HTTPS invalide, l'exception remonte brute a pywebview/REST alors que l'ancien serveur est deja detruit -> API REST down sans reponse d'erreur structuree ni rollback.

- **`cinesort/ui/api/apply_support.py:1998`**
  _snapshot_jellyfin_watched est appele HORS du try principal (l.1996-2000). float(data.get('jellyfin_timeout_s')) dans _make_jellyfin_client (l.1725) leve ValueError si le setting est une chaine malformee, non couverte par l'except (IntegrationError, OSError, RequestException) de l.1859 : l'exception remonte non geree jusqu'a la couche HTTP (500) et l'apply reel ne demarre jamais — crash evitable pour une simple valeur de timeout invalide dans settings.json.

- **`cinesort/ui/api/apply_support.py:1141`**
  build_apply_preview promet 'Ne touche NI le filesystem NI la BDD' (l.2276) mais _validate_apply (appele avec dry_run=True l.2350) ecrit validation.json via atomic_write_json (l.1141) avec les decisions mergees. merge_decisions (run_data_support.py:266) donne priorite a l'entrant : un appel preview avec un dict decisions partiel/perime ECRASE sur disque les decisions sauvegardees de l'utilisateur — effet de bord d'ecriture sur un endpoint contractuellement read-only, y compris en pur dry_run.

- **`cinesort/ui/api/apply_support.py:2039`**
  _summarize_apply est appele aussi en dry_run et tronque summary.txt au marqueur '=== RESUME APPLICATION ===' (l.1693-1695) avant de reecrire le bloc : un apply dry-run lance APRES un apply reel detruit le resume du vrai apply et le remplace par des stats simulees ('Appliquees : N/M') sans aucune mention dry-run dans le bloc — trace utilisateur trompeuse et perte du resume de l'apply reel.

- **`cinesort/ui/api/apply_support.py:1418`**
  Multi-root : le merge des resultats partiels (l.1412-1426) ne fusionne que les champs int et le dict skip_reasons. Tous les autres champs des roots suivants (notamment cleanup_residual_diagnostic, dict utilise par _cleanup_apply et _summarize_apply) sont silencieusement perdus : sur un apply multi-root, le diagnostic nettoyage residuel et le resume n'affichent que les donnees du PREMIER root.

- **`cinesort/ui/api/settings_support.py:2338`**
  set_scan_max_workers_payload (L2338-2342) et set_advanced_pragma_settings_payload (L2433-2437) font un read_settings -> mutate -> write_settings SANS acquerir _get_settings_write_lock(state_dir), et restore_settings_backup (L2047-2069) ecrit aussi hors verrou : lost-update race avec save_settings_payload (un save parallele est silencieusement ecrase), exactement le scenario que le verrou par state_dir (L44-51) a ete cree pour fermer. La rotation concurrente peut aussi faire lever OSError dans le tri par stat() de _rotate_settings_backups.

- **`cinesort/ui/api/settings_support.py:136`**
  _backup_settings_before_write catch (json.JSONDecodeError, OSError) mais read_text(encoding='utf-8-sig') peut lever UnicodeDecodeError (sous-classe de ValueError, ni OSError ni JSONDecodeError) sur un settings.json aux octets non-UTF8 (ex: re-encode latin-1). L'exception traverse write_settings (L704) : toute sauvegarde des parametres devient impossible (crash) tant que le fichier corrompu existe, alors que read_settings, lui, catch ValueError et degrade proprement vers {}.

- **`cinesort/ui/api/settings_support.py:1595`**
  _coerce_appearance_int fait int(payload[key]) sans garde : une string non numerique ('abc', '50.5', '50%') dans effect_speed/glow_intensity/light_intensity leve ValueError qui remonte de _save_section_appearance a travers save_settings_payload (aucun try dans le dispatcher ni dans cinesort_api._save_settings_impl) -> crash du save complet. C'est le seul point du pipeline de save sans coercion tolerante (tout le reste passe par to_int/_coerce_int_with_default qui ne levent jamais).

- **`cinesort/ui/api/settings_support.py:1621`**
  Defaults incoherents pour separator : _save_section_naming retombe sur '.' (L1621-1622) alors que build_cfg_from_settings retombe sur ' ' (L1071-1075) et que separator est absent de _LITERAL_DEFAULTS. Un utilisateur qui vide le champ obtient des noms de fichiers avec separateur '.' alors que le defaut effectif d'une config vierge est ' '. Meme pattern d'incoherence save/lecture que le bug theme studio/luxe deja corrige (cf commentaire L872-876).

- **`cinesort/ui/api/run_flow_support.py:969`** — [NON RE-CONFIRME]
  Incoherence d'unites dans get_status apres le recount Vague J : une fois le run done, total est recompte depuis plan.jsonl (ex. 853 PlanRow) mais idx reste le compteur de dossiers explores (ex. 855, discover). Le frontend affiche 'idx / total' (traitement.js:597) -> peut montrer '855 / 853' soit idx > total et une barre >100%. Meme probleme dans la branche DB-only (idx DB en dossiers vs total recompte L890).

- **`cinesort/ui/api/run_flow_support.py:1523`**
  rescan_row execute des DELETE bruts via store._managed_conn() mais le except (L1532) ne couvre pas sqlite3.Error (contrairement a _mirror_decisions_to_sql L1256 qui le catche). Une sqlite3.OperationalError (DB locked pendant un recompute/perceptual background, scenario realiste juste apres un scan auto) remonte non geree -> crash de l'endpoint (500) au lieu d'un warning best-effort.

- **`cinesort/ui/api/dashboard_support.py:522`**
  _build_library_rows lit detected.get('resolution_label') mais le dict detected des quality_reports expose la cle 'resolution' (quality_score.py:1633 construit 'resolution': vr['resolution_label'] sans cle 'resolution_label'). Le champ resolution des rows du payload get_dashboard est donc TOUJOURS vide, meme pour les films analyses — incoherent avec _build_row_payload (L913) qui lit correctement detected.get('resolution').

- **`cinesort/ui/api/dashboard_support.py:1547`**
  get_global_stats calcule total_films (et unscored L1567-1569) depuis runs_summary.total_rows qui privilegie runs.total = discover_total (run.py:365), contrairement a l'harmonisation count-2 documentee (compute_total_fallback / count_plan_rows) appliquee a runs_history dans le MEME module (L81-84). Consequence : KPI Home 'total films' gonfle (855 vs 853) et 'unscored_films' fantome (ex. 2 films non scores affiches alors que tout est score).

- **`cinesort/ui/api/run_flow_support.py:1459`**
  mark_duplicate_winner persiste la decision meme quand la liste losers est restee vide : si check_duplicates echoue (warning L1468) ou si group_key ne matche aucun groupe (aucune garde apres la boucle), upsert_duplicate_decision enregistre winner + losers=[] et retourne ok=True. L'utilisateur croit le doublon tranche mais l'apply ne deplacera aucun loser vers _review/_duplicates_user_decided — decision silencieusement inoperante. winner_row_id n'est pas non plus valide comme appartenant au groupe.

- **`cinesort/ui/api/run_flow_support.py:281`**
  _validate_and_init_plan_context produit des effets de bord AVANT le controle de concurrence (qui n'a lieu que dans runner.start_job) : mkdir du nouveau state_dir, mutation de api._state_dir (L283-284), clean_old_runs, creation du run_dir (ensure_exists=True L311). Sur un start_plan rejete en 409 (run deja actif), seul api._runs est nettoye (L749-750) : api._state_dir pointe deja sur le nouveau state_dir (faussant les resolutions normalize_user_path du run en cours s'il utilise un autre state_dir) et un run_dir orphelin sans row DB subsiste.

- **`cinesort/ui/api/library_support.py:586`**
  Cle de tri title_desc cassee : `tuple(-ord(c) for c in titre[:50])` inverse mal les paires prefixe ("Alien" trie AVANT "Aliens" en Z->A car le tuple plus court est plus petit), tronque a 50 caracteres (egalite arbitraire au-dela) et n'a pas le tie-break annee contrairement au tri asc (L585). Option "Titre Z->A" visible dans l'UI (bibliotheque.js L72). Un simple sorted(reverse=True) serait correct.

- **`cinesort/ui/api/library_support.py:1081`**
  get_library_counters_by_chip et get_scoring_rollup (L899) appellent _build_library_rows (dont api.run.get_plan L190 hors try) et _row_matches (casts int() L520/529/538 sur valeurs de filtres) SANS le wrap top-level Vague F dont beneficie get_library_filtered (L654-676) : un run obsolete/plan illisible ou un filtre malforme (year_min="abc") leve une exception non geree -> HTTP 500, exactement le bug que la Vague F corrigeait sur l'endpoint voisin.

- **`cinesort/ui/api/perceptual_support.py:114`**
  get_perceptual_details lit uniquement le store de `api._state_dir` (lecture directe non synchronisee, alors que cinesort_api.py:325-333 documente que les lectures directes de _state_dir racent avec _save_settings_impl et impose _get_state_dir()), tandis que get_perceptual_report ECRIT via le store retourne par _find_run_row (runtime_support.py:463-475) qui resout le state_dir propre au run. Pour un run vivant dans un state_dir different du courant, details retourne `missing: True` alors que l'analyse existe -> UI "Aucune analyse perceptuelle persistee" a tort.

- **`cinesort/ui/api/perceptual_support.py:85`**
  get_perceptual_report ne catche que (ImportError, OSError) : un ValueError de _build_settings_dict (casts int()/float() L1434-1441 sur settings malformes), un KeyError, ou une sqlite3.Error (qui n'est PAS un OSError) levee par upsert_perceptual_report apres plusieurs minutes d'analyse ffmpeg s'echappent -> reponse 500 et resultat d'analyse perdu (non persiste). En batch l'exception est absorbee par run_batch_parallel, mais l'appel single-film (inspecteur UI) crashe.

- **`cinesort/ui/api/demo_support.py:353`**
  start_demo_mode ecrit dans quality_reports les tiers LEGACY pre-migration 011 ('Premium'/'Bon'/'Moyen'/'Mauvais', cf DEMO_FILMS L35-216) et history_support.get_history_stats (L255-267) ne normalise pas via to_canonical_v2_tier contrairement a library_support : l'inspecteur Historique expose des buckets films_by_tier 'premium/bon/moyen/mauvais' inconnus de l'UI v2 (platinum/gold/silver/bronze/reject) et les films 'Mauvais' sont comptes validated_count car seul tier=='reject' incremente rejected_count.

- **`cinesort/ui/api/profiles_support_import_export.py:144`**
  _yaml_parse_scalar strippe le commentaire inline (' #') AVANT de detecter les quotes, alors que _yaml_format_scalar (L67-93) quote justement les chaines contenant ' #' : un label exporte 'Top #1' redevient la chaine corrompue "'Top" au re-import (split sur ' #' puis echec du test de quotes). De plus un label contenant un \n reel est emis tel quel entre quotes simples -> YAML multi-ligne invalide. Le round-trip Recyclarr annonce 'lossless' corrompt donc les valeurs contenant ' #' ou des retours ligne.

- **`cinesort/ui/api/profiles_support_import_export.py:366`**
  import_recyclarr_yaml accepte jusqu'a MAX_PROFILES_PER_IMPORT=16 profils (L501) mais _recyclarr_dict_to_profile n'extrait que profiles[0] : les profils 2..N d'un YAML Recyclarr multi-profils sont silencieusement jetes, sans aucun warning dans la reponse {ok, profile_id, lossy} - perte de donnees invisible a l'import.

- **`cinesort/ui/api/profiles_support_import_export.py:749`**
  get_breakdown_5_axes max-merge systematiquement le trash_scoring du preset TRaSH 2026 embarque avec le profil ACTIF (l'axe 'source' L750-751 vient meme EXCLUSIVEMENT du preset), alors que ce preset est desactive par defaut (AC-3) et peut ne pas etre active par l'utilisateur : l'UI affiche des poids 'effectifs' qui ne s'appliquent pas au scoring reel du profil courant.

- **`cinesort/ui/api/runtime_support.py:619`**
  _library_counts execute SELECT COUNT(*) FROM library_items et COUNT(DISTINCT library_item_id) FROM quality_reports alors que la table library_items et la colonne library_item_id n'existent nulle part dans le schema (absentes de REQUIRED_SCHEMA_TABLES et de toutes les migrations, grep projet vide) : l'OperationalError est avalee en debug et le diagnostic "Copier diagnostic" (ecran Aide) affiche toujours lib_total=0 / lib_scored=0 quelle que soit la bibliotheque.

- **`cinesort/ui/api/run_data_support.py:92`**
  row_from_json ne reparse pas non plus `nfo_runtime` (champ PlanRow core.py:432, pourtant reparse par l'autre loader app/plan_support_core.py:110) -> pour toute row rechargee depuis plan.jsonl (run plus en memoire), quality_report_support.py:300 recoit getattr(row,'nfo_runtime',None)=None -> le cross-check P1.1.d 'nfo_runtime_mismatch' ne se declenche jamais apres redemarrage (warning silencieusement inoperant).

- **`cinesort/ui/api/quality_report_support.py:102`**
  `hasattr(api, "_tmdb_client")` est toujours False (aucune definition/assignation de _tmdb_client sur CineSortApi dans tout le projet, verifie a l'import) -> `tmdb` vaut toujours None -> tmdb_genres toujours [] -> le scoring genre-aware P4.2 (compute_quality_score(tmdb_genres=...)) n'est JAMAIS applique : feature de scoring silencieusement morte.

- **`cinesort/ui/api/library_audit_support.py:217`**
  _load_plan_rows_with_collection lit `r.get("tmdb_id")` qui n'existe pas au top-level d'un PlanRow serialise (PlanRow n'a pas de champ tmdb_id, il n'existe que dans candidates[]) -> owned_tmdb_ids (L318) toujours vide -> le matching possede/manquant de get_incomplete_sagas repose uniquement sur le fallback (titre lower, annee) L330 -> des films possedes seraient listes 'manquants' des que le titre fr-FR TMDb ou l'annee differe (bug latent derriere le defaut L242).

- **`cinesort/ui/api/run_control_support.py:115`**
  resume_run et save_for_later/_pause_or_save (L238) n'ont pas le wrap try/except global ajoute a pause_run (fix Vague G L69-79) : une sqlite3.OperationalError (DB locked/corrompue) levee par store.run.mark_run_resumed / mark_run_paused n'est rattrapee nulle part (sqlite3.Error n'herite pas de OSError) -> HTTP 500 sur ces 2 endpoints de pilotage du run ; meme trou dans list_pending_runs (L171/L196 : seuls KeyError/OSError/TypeError/ValueError sont couverts).

- **`cinesort/ui/api/library_support.py:768`** — [NOUVEAU RE-RUN]
  save_smart_playlist/delete_smart_playlist font un read-modify-write non verrouille sur settings.json complet : _get_playlists_from_settings lit, puis _write_playlists_to_settings relit get_settings, mute et reecrit TOUT le dict settings. Deux operations concurrentes (2 sauvegardes de playlist, ou playlist + save_settings depuis l'ecran Reglages) se font perdre mutuellement leurs ecritures (lost update du fichier de config entier).

### Boot — app.py, packaging, CSP

- **`app.py:513`**
  main_api : server.start() n'est pas protege, contrairement au chemin desktop (L362-378 qui catch OSError EADDRINUSE/10048 et RuntimeError HTTPS M1 avec message explicite). En mode --api, un port deja occupe ou un cert/cle HTTPS invalide produit un crash traceback brut sans message utilisateur ni _startup_error.

- **`app.py:867-869`**
  Fallback si le serveur REST ne demarre pas : index.html du dashboard est charge en file:// (as_uri), mais ce fichier reference les CSS du design system en chemins ABSOLUS /shared/*.css (index.html L17-27, resolus vers la racine du disque -> 404) et des scripts type="module" (L143-146, bloques par CORS sur origine file:// dans WebView2/Chromium) -> la fenetre de secours est structurellement cassee (page sans style ni JS), le fallback documente dans resolve_ui_entrypoint L77-87 ne peut jamais fonctionner.

- **`web/dashboard/index.html:50`**
  L'ecran de login indique 'Entrez la cle d'acces fournie par CineSort (Parametres -> API REST -> Cle d'acces...)' mais ce champ ne peut afficher/copier que le masque "••••••••" (get_settings masque rest_api_token, parametres.js:2245-2247 copie tokenInput.value = masque), et l'endpoint de revelation promis POST /api/settings/reveal_rest_token (settings_support.py:1181/1198) N'EXISTE NULLE PART dans le code (seulement en commentaires). Suivre l'instruction affichee fait coller 8 bullets -> echec de connexion sans cause comprehensible ; la seule voie fonctionnelle est 'Regenerer' qui remplace le token existant (invalide les autres appareils).

### Dashboard JS

- **`web/dashboard/core/api.js:325`**
  Race dedup/abort : apiPost dedup TOUJOURS (fix 2026-06-05) mais seuls les opts du PREMIER appelant sont composes dans le signal du fetch partage. (1) Si le 1er appelant abort (nav-abort au changement de route, abort synchrone AVANT l'init de la vue suivante dans router.resolve), tous les appelants dedupliques recoivent AbortError alors qu'ils n'ont rien annule -> vue suivante sans donnees, silencieusement (isAbortError avale). Le commentaire pretend "abort quand TOUS les callers ont abort" mais le code abort des qu'UN signal abort. (2) Le timeoutMs d'un appelant deduplique (ex: 5000ms du journalPoller) n'est jamais applique. De nombreuses vues passent getNavSignal() (accueil, qualite, doublons, historique...), le scenario est reel en navigation rapide.

- **`web/dashboard/app.js:281`** — [NON RE-CONFIRME]
  Router leak : la route /processing est enregistree avec `init: initProcessing` (fonction async -> Promise<undefined>) sans jamais cabler unmountProcessing (export jamais appele nulle part dans le code, verifie par grep). La boucle _pollStatus (processing.js:460-487, setTimeout auto-replanifie 2s) continue apres navigation hors de la vue tant que le run est "running" : fetchs run/get_status + re-render dans un container cache. stopAllPolling() du router ne la voit pas (timer brut, pas startPolling de state.js). Un retour sur /processing pendant qu'un tick est en await cree une 2e chaine de polling parallele dont la reference timer est perdue (non annulable).

- **`web/dashboard/core/i18n.js:211`**
  setLocale : _fetchLocale ne throw jamais (retourne {} apres 3 tentatives). Un echec transitoire du fetch de en.json cache donc `{}` pour toute la session (le check `== null` empeche tout re-fetch ; reloadLocale n'est appele que par les tests, verifie par grep) ET la locale "en" est quand meme activee + persistee en localStorage. L'utilisateur anglophone voit silencieusement toute l'UI en francais jusqu'au reload complet de la page, sans aucun message ni retry.

- **`web/dashboard/components/right-panel.js:218`**
  Cap « 50% de window.innerWidth » mort dans setWidth() : upper = Math.min(EXPANDED_MAX_WIDTH, Math.max(MAX_WIDTH, winLimit)) avec EXPANDED_MAX_WIDTH === MAX_WIDTH === 600 vaut toujours 600 quel que soit winLimit. Sur petite fenêtre (ex. 800px), le panneau Mode A occupe 600px (75% de l'écran) au lieu d'être borné à 400px comme documenté.

- **`web/dashboard/components/home-charts.js:43`**
  renderDonut : quand un seul tier représente 100% des films (count === total), sweep = 2π donne des points de départ/arrivée identiques ; par spec SVG, un arc A avec endpoints identiques est omis → le donut s'affiche vide (cercle de fond seul) alors que le total au centre est correct.

- **`web/dashboard/components/library-advanced-drawer.js:364`**
  closeLibraryAdvancedDrawer() (API publique, aussi appelée en garde au début de openLibraryAdvancedDrawer) supprime le host du DOM sans retirer le keydown _onKey posé sur document par l'instance ouverte : le listener fuit, et la prochaine pression d'Échap subit un preventDefault() global + exécute un close() fantôme. Seul le close() interne (closure) nettoie correctement.

- **`web/dashboard/components/film-detail.js:1033`**
  _pickTmdbManualResult : _chooseCandidate attrape toutes ses erreurs en interne (l.933-937, jamais re-throw) donc le try/catch de ré-activation du bouton est du code mort et _closeTmdbManualSearchModal() s'exécute même en cas d'échec du choix : la modale de recherche TMDb se ferme sur erreur, l'utilisateur perd ses résultats de recherche et doit tout recommencer.

- **`web/dashboard/views/parametres.js:1774`**
  Aucune validation des champs number malgre min/max declares dans le schema : _readFieldValue fait Number(value)||0 sans clamp ni message. Vider un champ sauvegarde 0 (rest_api_port efface -> 0, clampe silencieusement a 1024 cote backend L1472), taper hors bornes persiste tel quel cote UI (auto_approve_threshold=50 affiche 50 mais le backend stocke 70, L1399). L'UI continue d'afficher la valeur saisie, divergente de settings.json, sans aucun message de validation.

- **`web/dashboard/views/parametres.js:2018`** — [NON RE-CONFIRME]
  Race autosave vs reset : le handler Confirmer de la modale reset (L2018-2052) ne fait ni clearTimeout(_state.saveTimer) ni attente du save debounce en vol. Un autosave parti ~500ms avant peut atteindre le backend APRES settings/reset_settings et reecrire les anciens parametres, annulant silencieusement le reset (le _loadSettings qui suit L2034 peut aussi relire un etat intermediaire).

- **`web/dashboard/views/parametres.js:3407`**
  Perte silencieuse de modifications a la navigation : unmountParametres fait clearTimeout(_state.saveTimer) sans flush. Toute modification effectuee dans la fenetre de 500ms du debounce avant de quitter la vue n'est jamais envoyee au backend, alors que l'UI l'affichait comme prise en compte (pattern autosave sans bouton Enregistrer).

- **`web/dashboard/views/parametres.js:1665`**
  Validation du nom de profil invisible et saisie perdue : modal.js ferme TOUJOURS la modale apres onClick (modal.js L136-139 onClick() puis closeModal()). En cas de nom invalide, le message d'erreur ecrit dans errEl (L1666-1669) est detruit immediatement avec la modale, et la re-ouverture via setTimeout (L1671) repart avec la valeur par defaut 'MonProfil_v1' (L1647) — l'utilisateur perd sa saisie et ne voit jamais pourquoi elle a ete refusee.

- **`web/dashboard/views/parametres.js:633`** — [NON RE-CONFIRME]
  Message OMDb faux quand la cle est configuree mais non testee : _computeOmdbState retourne 'config-pending' des que lastTest est null (L635), meme si une cle existe (le masque 8 bullets est truthy), et _renderOmdbStatus affiche alors 'Cle OMDb requise. Renseignez votre cle OMDb' (L668-672) a un utilisateur dont la cle est deja enregistree et fonctionnelle — l'etat 'cle presente non testee' est conflate avec 'cle absente'.

- **`web/dashboard/views/traitement.js:413`**
  Race unmount/polling : un _pollTick en vol au moment de unmountTraitement survit (abort -> catch -> continue) et se re-programme via _scheduleNextPoll() avec _signal() devenu undefined -> polling zombie sans abort possible ; pire, _currentStep ayant ete resete a "analyse", la branche done (l.380-388) execute _writeStep("verification") qui reecrit location.hash et re-navigue l'utilisateur vers /traitement depuis n'importe quelle autre vue.

- **`web/dashboard/views/traitement.js:380`**
  Apres une annulation (CANCELLED) ou un echec (FAILED), le backend renvoie done=true (run_flow_support.py:876) : _pollTick auto-avance alors Analyse -> Verification + _loadPlan() comme si le scan avait reussi, poussant l'utilisateur a derouler le workflow (jusqu'a Apply) sur un plan partiel issu d'un run qu'il vient d'annuler.

- **`web/dashboard/views/traitement.js:411`**
  _renderInPlace() inconditionnel a chaque tick de polling reecrit tout le innerHTML ; pour un run non-done en memoire (AWAITING_VALIDATION/PAUSED, statuts user-held ou done=false) le polling tourne a 5s pendant que l'utilisateur edite l'etape Validation -> perte du focus et de la saisie en cours dans les inputs annee (l'event change ne fire jamais si l'input est detruit avant le blur ; _persistValidationDomState n'est appele que sur filtre/tri/expand, pas au tick).

- **`web/dashboard/views/bibliotheque.js:588`**
  Echec d'un append du scroll infini (page>=2, erreur reseau ou payload ok:false, L801/860) : _state.error est pose et _renderBody remplace TOUTE la liste deja chargee par une simple banniere d'erreur, sans bouton de relance — le handler [data-bibliotheque-retry] (L1106) est du code mort, cet attribut n'est rendu nulle part. L'utilisateur perd sa position et ne peut recuperer qu'en changeant un filtre/tri.

- **`web/dashboard/views/library/lib-analyse.js:221`**
  La condition de fin de polling `d.done || d.error || (!d.running && !d.error)` traite comme terminal le payload d'erreur transitoire de run/get_status ({ok:false, error:'run_status_unavailable'} renvoye par le wrap backend sur TOUTE exception, run_flow_support.py:825-835) : un seul echec ponctuel stoppe definitivement le polling, affiche 'Erreur' + le bouton 'Charger les resultats' et reactive 'Lancer l'analyse' alors que le scan continue en arriere-plan ; la progression n'est jamais reprise.

- **`web/dashboard/views/bibliotheque.js:1748`**
  _promptSavePlaylist : quand le nom est vide, le handler affiche le toast 'Le nom est requis' et retourne SANS closeModal pour laisser l'utilisateur corriger — mais components/modal.js (L136-139) appelle TOUJOURS closeModal() apres onClick. La modale se ferme donc quand meme, la saisie est perdue et l'utilisateur doit rouvrir le panneau playlists et tout retaper.

- **`web/dashboard/views/doublons.js:789`**
  Race dans _autoDecideAll : pendant la boucle sequentielle, les boutons "Garder A/B" des cartes restent actifs (seul decisionInFlightByGroup les desactive, pas bulkInFlight) et la boucle ne reverifie pas g.winner_decided avant chaque post (snapshot candidates fige avant la modale de confirmation). Une decision manuelle posee pendant le bulk est silencieusement ecrasee par la decision auto (upsert_duplicate_decision, PK run+group, dernier ecrit gagne).

- **`web/dashboard/views/doublons.js:462`**
  Empty state trompeur : quand le FILTRE actif ne matche aucun groupe (ex: "Decides (0)" — toujours 0 vu l'absence de winner_decided, ou "Conflits seulement"), _renderBody affiche _renderEmpty() "✨ Aucun doublon detecte — Lance un scan..." alors que des groupes existent bel et bien dans les autres filtres. Le message devrait distinguer "aucun resultat pour ce filtre" de "aucun doublon detecte".

- **`web/dashboard/views/historique.js:1408`**
  _retentionDays lit data.history_retention_days depuis le payload run/get_dashboard qui ne contient JAMAIS cette cle (zero occurrence de retention dans dashboard_support.py ; c'est une cle settings, settings_support.py:1651) : la valeur retombe toujours sur 90. La banniere retention (L349-364) affiche donc une duree et une date de coupure fausses des que l'utilisateur a modifie la retention — via le CTA "Modifier" que la banniere propose elle-meme. Idem L1220 dans _refreshRuns.

- **`web/dashboard/views/help.js:447`**
  6 vues mortes jamais importees par app.js : help.js, logs.js, jellyfin.js, plex.js, radarr.js, enrichment.js (les routes /help /logs /jellyfin /plex /radarr redirigent, app.js:294-308) -> duplication legacy vs v5 de aide.js/parametres avec defauts figes : help.js:20 URL 'github.com/PLACEHOLDER/cinesort' cliquable -> 404 ; help.js:411 ajoute un click-listener sur le container persistant a chaque init (cumul) ; jellyfin.js:284 le bouton Sync perd son listener apres re-render via btnRefresh (setTimeout _load l.201 ne rappelle pas _hookSyncButton).

- **`web/dashboard/views/enrichment.js:78`**
  Scaffold enrichment (vue non routee mais exportee comme entree runtime V4.1) : fetchEnrichmentStatus retourne le wrapper {status,data} d'apiPost et renderStatus/le gating du bouton lisent status.feature_flag/ollama_available au niveau wrapper -> le bouton 'Appliquer' ne s'activera JAMAIS meme flag ON ; de plus showToast('Suggestions IA appliquees','success') (l.123,125) viole l'API objet de toast.js:83-85 ({type,text}) -> text vide, toast jamais affiche ; et apply_bulk est appele avec film_ids:[] en dur (l.122).

- **`web/dashboard/views/custom-rules-editor.js:410`**
  _previewImpact ferme la modale editeur (closeModal) avant d'ouvrir le simulateur : les regles editees mais non enregistrees sont perdues silencieusement, car la reouverture via openCustomRulesEditor() (l.457-471) recharge l'etat depuis le backend en ecrasant state.rules, sans avertissement.

- **`web/dashboard/views/film-detail.js:528`**
  _renderTabs() ne rebinde que le click des onglets (l.533-539), pas le keydown : la navigation clavier ArrowLeft/ArrowRight posee par _bindEvents (l.512-524) est perdue des le premier changement d'onglet (les boutons d'origine sont detruits par tabsWrap.innerHTML) -> regression a11y/clavier persistante. Bouton 'Choisir un fichier a comparer' (l.448, data-v5-film-pick-compare) : aucun handler nulle part dans web/ -> bouton mort.

- **`web/dashboard/core/api.js:629`** — [NOUVEAU RE-RUN]
  testConnection traite tout statut hors {401, 429, >=500} comme un succes : un 404 (endpoint deplace/renomme — exactement le bug #233 documente 5 lignes au-dessus) ou un 400/403 retourne {ok:true} et le flow de login accepte un token jamais valide. Aucun garde-fou n'a ete ajoute contre la recidive du #233 alors que le commentaire decrit precisement ce mode de panne ("404 cassait silencieusement le flow de login").

- **`web/dashboard/app.js:891`** — [NOUVEAU RE-RUN]
  _loadDashTheme (et le fetch theme de _mountV5Shell ligne 423) traite une reponse d'erreur comme des settings : sur 429/5xx transitoire, res.data={ok:false} est truthy -> s.theme undefined -> theme force a "luxe", animation/effects reinitialises, ecrasant visuellement le theme configure de l'utilisateur jusqu'au prochain fetch reussi. Meme probleme dans _applyTheme ligne 919 : le merge {...{ok:false,message}, theme} part en save_settings sans roots -> echec backend silencieux (root_required), le changement de theme semble applique mais n'est jamais persiste.

- **`web/dashboard/views/library/lib-duplicates.js:34`** — [NOUVEAU RE-RUN]
  Race au mount + gating affaibli : initDuplicates lance _loadDuplicates() immediatement alors que initValidation._loadRows (async, non awaite par library.js) n'a pas encore peuple _allRows -> buildDecisionsPayload() retourne {} (ou l'etat residuel d'une session precedente) et check_duplicates est evalue sans les vraies decisions. De plus markDuplicatesChecked() (L83) coche automatiquement 'Doublons verifies' dans la checklist Securite des le chargement de la page, sans aucune action utilisateur : le gating du bouton Appliquer ne depend plus en pratique que de la sauvegarde.

- **`web/dashboard/views/aide.js:110`** — [NOUVEAU RE-RUN]
  _GITHUB_REPO_URL/_GITHUB_ISSUES_URL pointent vers github.com/Thomas05000005/CineSort alors que le depot n'est pas public : l'audit 2026-06-07 a neutralise ces memes liens dans about.js (l.38-42 'URL placeholder -> 404 GitHub a chaque clic') mais a oublie aide.js -> les boutons Repo GitHub/Changelog/Contributeurs (l.268-270) et le template d'issue ouvrent des 404.

### CSS / themes

- **`web/shared/components.css:9943`**
  Bloc de commentaire ouvert SANS `/*` (lignes 9943-9948, seulement ferme par `*/` a L9948) : le texte est parse comme un selecteur invalide qui avale la premiere regle suivante → `.historique-films-history { min-height:240px }` (L9949-9951) n'est jamais appliquee (regle silencieusement droppee par le parseur CSS).

- **`web/shared/components.css:4011`**
  --accent-rgb n'est jamais defini et le fallback est lui-meme malforme : `rgba(var(--accent-rgb, 96 165 250), 0.06)` se substitue en `rgba(96 165 250, 0.06)` (melange syntaxe espace + virgule, rejete par les navigateurs) → le hover des .clickable-row (qij.js:225, quality.js:234, lib-verification.js:119) et du resize-handle du panneau droit (L4069) n'a strictement aucun effet, contrairement au commentaire L4003 qui pretend fournir un fallback.

- **`web/dashboard/styles.css:1202`**
  #toast-container est a z-index var(--z-toast, 800) alors que toutes les modales v5 recentes sont hardcodees a 9000-10002 (perceptual, duplicate, danger-modal 10000, film-detail 10001, tmdb 10002) : tout toast emis pendant qu'une de ces modales est ouverte (ex. confirmation d'action dangereuse) est peint SOUS l'overlay assombri et illisible — inversion du contrat de l'echelle tokens ou toast(800) > modal(600).

- **`web/shared/components.css:7613`** — [NON RE-CONFIRME]
  Le composant .omdb-status (rendu par omdb-status.js:39-109 dans Parametres) utilise une palette light-theme en hex durs : statut erreur #b91c1c sur le fond sombre #06090F du dashboard ≈ 2.9:1, warning #b45309 ≈ 3.6:1, info #2563eb ≈ 3:1 — tous sous le seuil WCAG AA 4.5:1 ; le message critique "cle API invalide" est a peine lisible (les fallbacks #f1f5f9/#475569 confirment un copier-coller light-theme).

- **`web/dashboard/styles.css:374`**
  .btn-primary force color:#fff sur le degrade var(--accent)→var(--accent-hover) (idem .v5-btn--primary via --text-primary, components.css:59-62) : avec le theme PAR DEFAUT Luxe (index.html:2 data-theme="luxe", accent #FBBF24→#F59E0B) le contraste texte/fond tombe a ≈1.7:1 (blanc sur or), libelles quasi illisibles ; en theme AAA (accent #FFFF00→#FFFFFF) le texte blanc devient totalement invisible — il faudrait --text-inverse.

### Migrations SQL

- **`cinesort/infra/db/migrations/021_fk_cascade.sql:48`**
  Incoherence de filtrage des orphelins : l'INSERT INTO errors_new copie TOUTES les rows de errors sans le filtre WHERE EXISTS(runs) pourtant applique aux 3 autres tables (quality_reports L87, anomalies L125, apply_operations L167) avec le commentaire explicite 'une DB historique pourrait en contenir'. La migration n'a pas de marker @manager: disable_fk et connection.py:79 force PRAGMA foreign_keys=ON avant BEGIN, donc une row errors orpheline sur une DB pre-existante leve sqlite3.IntegrityError — non toleree par le manager qui ne capte que OperationalError (migration_manager.py:254) — rollback complet, et l'upgrade reste bloque a user_version=20 avec echec a chaque demarrage.

- **`cinesort/infra/db/migrations/025_run_pause_status.sql:64`**
  Le SELECT de recopie code en dur paused_at=NULL ('..., error_message, NULL FROM runs'). Correct a la premiere application, mais toute re-execution ecrase les paused_at existants : cas user_version regresse (DB restauree) ET surtout le bootstrap self-healing _ensure_required_schema de sqlite_store.py qui rejoue TOUTES les migrations concatenees sur une DB peuplee des qu'une table critique manque — chemin reellement exerce lors de l'incident des tables 023 (cf migrations 026/027). Resultat : perte des timestamps de pause de tous les runs PAUSED/SAVED. Utiliser une sous-requete preservant la colonne si elle existe, ou exclure 025 du replay.

### Transverse — contrats, concurrence, secrets

- **`web/dashboard/components/film-detail.js:873`**
  Contrat casse : _refreshPosterUnit appelle apiPost("integrations/get_tmdb_posters", {tmdb_ids, size, force_refresh:true}). La facade get_tmdb_posters(self, tmdb_ids, size="w92") n'accepte PAS force_refresh ; le dispatcher fait method(**params) -> TypeError -> HTTP 400 "Parametres invalides". Le bouton "Rafraichir le poster" du modal film echoue donc a chaque clic.

- **`web/dashboard/components/film-detail.js:1153`**
  Endpoint inaccessible : _openFolder appelle apiPost("open_path", {path}) via le client REST core/api.js (pas de fallback bridge). open_path est dans _EXCLUDED_METHODS et Pass1 legacy est desactive par defaut, donc le dispatcher renvoie 410 Gone ("Use /api/<facade>/<method> instead"). Le bouton "Ouvrir le dossier" du modal film echoue dans tous les modes (web ET natif pywebview, car l'appel ne passe jamais par window.pywebview.api).

- **`web/dashboard/views/accueil.js:299`**
  Mauvais nom de parametre pour Plex : le ping environnement de l'Accueil envoie {url, api_key: token, timeout_s} a integrations/test_plex_connection, mais la facade attend test_plex_connection(url, token, timeout_s). Le kwarg "api_key" est inconnu -> TypeError -> HTTP 400. La pastille Plex de la page d'Accueil affiche donc TOUJOURS "hors-ligne" meme quand Plex fonctionne (Jellyfin/Radarr utilisent bien api_key, seul Plex est errone).

- **`cinesort/ui/api/settings_support.py:1911`**
  Ordre du dispatcher _save_settings_payload_locked : _save_section_advanced (L1911) mappe le champ UI perceptual_workers_count vers perceptual_workers (fix audit 2026-05-24), mais _save_section_perceptual (L1918) re-emet ensuite TOUJOURS la cle perceptual_workers calculee depuis payload["perceptual_workers"] (0=auto si absente, clamp [0,16] vs [1,32] cote advanced) : la valeur saisie via perceptual_workers_count est systematiquement ecrasee au save — le fix de mismatch de nom est rendu inoperant par l'ordre des sections.

- **`cinesort/ui/api/run_flow_support.py:242`**
  _validate_and_init_plan_context logge le dict settings complet (f"start_plan called settings={safe_settings}") en ne masquant QUE tmdb_api_key (ligne 239-240). safe_settings provient de _hydrate_settings_from_store → read_settings (secrets DPAPI dechiffres) et contient plex_token, jellyfin_api_key, radarr_api_key, omdb_api_key, email_smtp_password EN CLAIR. scrub_secrets (log_scrubber.py:57) ne matche que des cles JSON entre guillemets DOUBLES, pas le repr Python d'un dict (apostrophes simples) → ces secrets sont ecrits en clair dans debug_api.log et debug.log quand le debug est actif. Lacune du scrubber.

---

## LOW (35)

### Domaine — scoring, parsing, perceptuel

- **`cinesort/domain/quality_score.py:533`**
  _normalize_video_bitrate_kbps : le commentaire (L533-534) affirme que n<1.0 "retourne int(0) -> None apres clamp" mais aucune conversion 0->None n'existe : int(round(0.4))=0 est retourne tel quel. 0 n'etant pas None, _score_video le traite comme un debit mesure (ratio 0.0 -> penalite maximale -14/-18 "Debit trop faible" au lieu de -8 "Debit non detecte") et _compute_confidence_helper (L1580-1585) accorde +4 de confiance ("debit present") au lieu de -12.

- **`cinesort/domain/quality_score.py:1263`**
  _build_invalid_profile_result : str(normalized_probe.get("probe_quality")) produit la chaine litterale "None" quand le champ est absent, au lieu d'une valeur du contrat FULL/PARTIAL/FAILED/UNKNOWN utilise partout ailleurs dans metrics.probe_quality — un consommateur downstream qui compare la valeur recoit "None" inattendu.

- **`cinesort/domain/title_helpers.py:418`**
  Corruption mojibake verifiee au niveau octets (od) : le tuple de separateurs de _expand_tmdb_queries contient deux entrees identiques ' ? ' (ASCII 0x3F) a la place des tirets typographiques ' – ' / ' — ' d'origine. La 2e entree est du code mort et l'expansion de query TMDb (prefixe avant sous-titre) est perdue pour les titres a tiret typographique ; un split litteral sur ' ? ' (typographie francaise) reste anodin car additif.

- **`cinesort/domain/duplicate_multi_signal.py:129`** — [NON RE-CONFIRME]
  L'indexation Phase B par premier token apres tri alphabetique (_index_token) ne tolere aucune variation dans ce token : "Amelie Poulain" vs "Amelia Poulain" (typo dans le token alphabetiquement premier) tombent dans des buckets differents et ne sont JAMAIS compares, alors que token_sort_ratio depasserait le seuil 88. La docstring ne garantit que l'invariance a l'ordre des mots ; la phase fuzzy, censee attraper les typos, est muette pour toute typo touchant le premier token trie — perte de rappel silencieuse.

- **`cinesort/domain/perceptual/video_analysis.py:108`**
  `if rc != 0 and not stderr:` — ffmpeg ecrit toujours sa banniere sur stderr en `-v info`, donc cette garde est du code mort : un code retour non nul n'est jamais traite et la sortie d'erreur part en parsing au lieu d'un retour [] explicite avec log. Pattern « return code ignore » sans impact supplementaire ici car le parsing d'un texte d'erreur produit [].

- **`cinesort/domain/perceptual/composite_score_v2.py:627`**
  collect_warnings (runtime mismatch) lit `video_data.get("duration_s")` alors que duration_s est une cle RACINE du probe normalise, absente du sous-dict video (_normalize_ffprobe.py:183) → le message afficherait toujours « Duree fichier 0 min ». Latent aujourd'hui car l'unique appelant (perceptual_support.py:467) passe runtime_vs_tmdb_flag=None, mais piege des que le flag sera branche.

- **`cinesort/domain/librarian.py:249`** — [NON RE-CONFIRME]
  health_score = round(100*(total_rows - len(problem_ids))/total_rows) sans intersection de problem_ids avec les row_ids reellement presents dans rows : un quality_report avec row_id vide ajoute '' comme probleme (L83-87, L206-209) et des reports orphelins (row absente du plan) degonflent le score, jusqu'a une valeur negative possible affichee dans la jauge sante.

### App — apply, plan, jobs

- **`cinesort/app/apply_core.py:1716`**
  Dans apply_single, le dossier saga `coll_dir` est cree (mkdir reel, hors dry_run) AVANT le kill-switch MAX_PATH (L1738), le check de conformite NOOP (L1748) et le guard src==dst (L1777), et sans passer par mkdir_counted ni record_op. Si la row est ensuite skippee, un dossier saga vide reste sur disque ; et n'etant pas journalise, il survit a tout rollback/undo (residu non reversible, res.mkdirs non incremente).

- **`plan_support_replan.py:621`**
  _resolve_file_runtime_min est appele sans store ni settings (seul call-site), donc toute la branche probe-cache (plan_support_dedup.py:622-646) est du code mort : le runtime HARD filter VN-D.3 ne s'applique qu'aux films dont le NFO porte un runtime et reste silencieusement inactif pour tous les fichiers sans NFO, contrairement au design documente ('NFO en priorite, puis probe cache').

- **`cinesort/app/cleanup.py:359`**
  _move_empty_top_level_dirs : skip_names contient le bucket vide, _review et collection mais PAS cfg.cleanup_residual_folders_folder_name, alors que _residual_cleanup_skip_names (lignes 112-121) exclut symetriquement le bucket vide. Avec un nom de bucket residuel personnalise sans prefixe "_" (defaut "_Dossier Nettoyage" protege par le filtre startswith("_")), le bucket de nettoyage residuel vide est deplace dans le bucket _Vide par l'etape dossiers-vides.

### Infra — REST, clients API, SQLite, probe

- **`cinesort/infra/rest_server.py:457`**
  Le bypass d'auth loopback emet `logger.info('Auth bypass localhost ...')` a CHAQUE requete autorisee (lignes 457-461). En usage desktop normal (pywebview, des centaines d'appels API), cela genere un spam INFO continu dans les logs sans valeur diagnostique ; devrait etre en debug ou logge une seule fois.

- **`cinesort/infra/tmdb_client.py:433`**
  search_movie reconstruit TmdbResult(**x) depuis le cache disque sans aucun guard (ni isinstance(x, dict) ni except TypeError — guard pourtant present dans find_by_tmdb_id:669-673) : une entree de cache polluee ou ecrite par une version au schema different fait crasher la recherche a CHAQUE cache hit jusqu'a expiration du TTL (jusqu'a 365 jours) ; meme probleme sur le fallback stale l.469.

- **`cinesort/infra/db/backup.py:234`**
  restore_backup ecrit le backup garde-fou 'before_restore' dans target.parent (le dossier db/ a cote de cinesort.sqlite) et non dans le dossier backups/ : il n'est donc jamais liste par list_db_backups (invisible dans l'UI de restauration) ni jamais rote -> accumulation indefinie de fichiers .bak dans le dossier db/ a chaque restore.

- **`cinesort/infra/log_scrubber.py:106`**
  scrub_secrets est applique a record.msg (la chaine de FORMAT) avant la substitution % des args. Le pattern api[_-]?key|apikey|token= matche aussi un placeholder litteral (ex. "token=%s") car %s appartient a [^&\s"'>]+ : il devient "token=[REDACTED]", supprimant le %s. A getMessage(), msg % args leve alors TypeError 'not all arguments converted' et le record est PERDU (sortie vide, erreur sur stderr). Verifie empiriquement. Latent : aucun site n'utilise actuellement 'key=%s' en chaine de format, mais la logique de scrub est defectueuse (muter la chaine de format desynchronise le nombre d'args).

- **`cinesort/infra/log_context.py:198`**
  RepeatedExceptionDedupFilter._counts grossit sans borne : la cle (logger, type_exc, msg[:80]) est creee via setdefault et ses timestamps sont purges (>60s) mais la CLE elle-meme n'est jamais supprimee, meme quand sa liste devient vide. Sur un process desktop tournant des jours/semaines avec beaucoup de signatures d'exception distinctes, le dict fuit en memoire indefiniment.

- **`cinesort/infra/log_context.py:210`**
  install_repeated_exception_dedup attache le filtre dedup aux .filters du root logger et aux handlers existants au moment de l'appel. Dans main_api il est appele (app.py:413) AVANT basicConfig et install_rotating_log : aucun handler n'existe encore, donc le filtre finit uniquement sur root.filters et reste inefficace pour les records des loggers enfants (meme gotcha de propagation que le scrubber), n'agissant donc pas sur le fichier rotatif. De plus, le mode GUI main() (app.py:648-649) n'appelle jamais cette fonction : l'anti-spam d'exceptions est totalement absent en mode fenetre.

### Backend UI/API — facades + modules support

- **`cinesort/ui/api/cinesort_api.py:1235`**
  Conversions de timeout non protegees : float(data.get("jellyfin_timeout_s")) L1235, et idem L1322, L1461 (plex), L1538/L1569 (radarr) sans clamp_timeout ni catch de ValueError (les try environnants n'attrapent que JellyfinError/PlexError/RadarrError). Un settings.json edite a la main avec une valeur non numerique fait remonter une exception brute, contrairement aux endpoints test_* qui clampent systematiquement (audit C7 P1 applique partiellement).

- **`cinesort/ui/api/settings_support.py:233`**
  _persist_protected_secret strip() le secret avant chiffrement et _unmask_secrets_for_save (L1219) strip aussi la valeur restauree, alors que _save_section_email (L1501) preserve volontairement email_smtp_password sans strip : un mot de passe SMTP avec espace(s) en debut/fin est silencieusement altere a la persistence DPAPI -> echec d'authentification SMTP difficile a diagnostiquer.

- **`cinesort/ui/api/perceptual_support.py:1236`**
  get_perceptual_compare_audio : le commentaire affirme garantir `duration_s` de marge avant la fin mais le code ne fait que `ts = max(0.0, ts)` sans clamp a duree-duration_s ; un timestamp_s utilisateur au-dela de la duree produit waveform/clip vides retournes silencieusement avec ok:True. De plus le fallback en cas de timestamp invalide (L1232) utilise max(dur_a,dur_b)/2 au lieu de min comme le defaut (L1234), pouvant depasser la duree du fichier le plus court.

- **`cinesort/ui/api/notifications_support.py:150`**
  _get_or_create_store fait un check-then-set non atomique sans lock (getattr puis api._notification_store = store) : deux threads concurrents au premier acces (ex. NotifyService scan_done + polling REST get_notifications_unread_count) peuvent creer deux NotificationStore, le perdant est ecrase et la/les notification(s) ajoutees dedans sont perdues silencieusement.

- **`cinesort/ui/api/run_flow_support.py:1374`** — [NOUVEAU RE-RUN]
  _enrich_one_group ne compare que les 2 PREMIERES rows d'un groupe (probes[0]/probes[1], payload construit sur rows[0]/rows[1]) meme pour les groupes de 3+ doublons. Consequence : le compteur 'Y Go recuperables' (size_savings_total, spec 01-doublons section 1) sous-estime l'espace recuperable et les doublons au-dela du 2e n'ont aucun verdict dans la comparaison.

### Boot — app.py, packaging, CSP

- **`web/dashboard/index.html:143`**
  bootstrap-bisect.js (outillage de bisect debug, 'test chaque module individuellement') est charge inconditionnellement a CHAQUE boot en production : 40 imports dynamiques paralleles + 40 timers de 5s, sans aucun gating mode dev (contrairement aux DevTools gates dans app.py L1096-1101). Surcout de boot et import force de toutes les vues hors routeur a chaque demarrage.

- **`cinesort.spec:226-228`**
  Presets TRaSH : le package cinesort.data.presets n'est JAMAIS importe statiquement (seule reference = chaine dans resources.files(), profiles_support_import_export.py:563) et est absent des hiddenimports, contrairement a la politique du spec qui liste explicitement tous les lazy imports (L104-153) ; preset_datas n'embarque que les *.json sans les __init__.py existants (cinesort/data/__init__.py + presets/__init__.py). En EXE frozen, le chargement repose sur le fallback namespace-package du frozen importer PyInstaller ; en cas d'echec, ModuleNotFoundError est avale (profiles_support_import_export.py:566-568) -> liste de presets embarques silencieusement vide dans le binaire alors qu'elle fonctionne en dev.

### Dashboard JS

- **`web/dashboard/app.js:128`**
  Si l'ntoken natif est pollue (BOM/non-ASCII), setToken (state.js:50-64) le rejette SANS appeler markTokenReady, et la branche else de _detectNativeBoot qui debloque le gate n'est pas executee (ntoken truthy) : le token-ready gate reste non resolu, toutes les requetes initiales sont bloquees 2s par le deadline de secours puis partent sans Authorization. Le commentaire ligne 129 "setToken appelle markTokenReady en interne" est faux dans ce cas de rejet.

- **`web/dashboard/app.js:701`**
  Le handler visibilitychange de _initSidebarFeatures recree _sidebarCountersInterval des que l'onglet redevient visible, y compris APRES logout (onClearToken vient de le nettoyer) : interval orphelin de 30s qui tourne indefiniment cote deconnecte (no-op grace au check hasToken dans _loadSidebarCounters, mais leak + tick inutile permanent, et il echappe au cleanup tant qu'aucun nouveau clearToken n'est declenche).

- **`web/dashboard/components/duplicate-comparator-modal.js:736`**
  Contrat API « rows seul » mort : la garde exige o.rowA ET o.rowB truthy AVANT le fallback rows[0]/rows[1] (l.753-754), rendant ce fallback inatteignable. Un appel documenté avec uniquement rows:[...] (cas 3+ fichiers) est rejeté avec un console.warn au lieu d'ouvrir le comparateur. Sans impact actuel (doublons.js:672 et perceptual-modal.js:702 passent rowA/rowB), mais le contrat d'en-tête (l.22-28) est faux.

- **`web/dashboard/components/table.js:94`**
  attachSort : au 3e clic (cycle asc→desc→none), les indicateurs passent à aria-sort='none' et les classes sort-* sont retirées, mais rows a été trié EN PLACE au clic précédent et l'ordre initial n'est jamais restauré : l'en-tête annonce « non trié » alors que les données restent triées desc — état visuel mensonger.

- **`web/dashboard/views/parametres.js:1021`**
  Feedback d'erreur probe tools avale : dans _loadProbeToolsStatus, le message d'erreur est ecrit (L1021, L1025) PUIS le finally re-rend le panneau placeholder (L1028 _refreshProbeToolsPanel) qui ne contient pas d'element [data-probe-tools-message] -> l'erreur n'est jamais visible. De plus, les handlers recheck (L2856-2857) et test par outil (L2936-2937) affichent inconditionnellement '✓ Statut rafraîchi.' / '✓ verifie' meme quand le recheck a echoue (status null).

- **`web/dashboard/views/processing.js:502`**
  Skeleton V2-08 de l'etape Review mort-ne : _renderSkeletonForStep(panel, "review") est ecrase de maniere synchrone par le panel.innerHTML suivant (l.504) avant tout repaint -> le skeleton n'est jamais visible, contrairement au commentaire qui l'annonce.

- **`web/dashboard/views/library/lib-analyse.js:240`**
  Contrat API casse : lecture de statsRes.data.recent_runs alors que run/get_global_stats ne renvoie jamais cette cle (dashboard_support.py retourne runs_summary/activity, L1651-1652) — le refresh post-scan du run_id via global stats est du code mort. Impact limite car finishedRunId est deja applique a _state.runId juste avant (L236-238), mais la meme reponse est lue avec la bonne cle runs_summary dans library.js:121, preuve de l'incoherence.

- **`web/dashboard/views/library/lib-apply.js:149`**
  Raccourci Ctrl+Z (cinesort:undo-shortcut) jamais branche si libState.runId est null au premier mount de la section Apply : le bind exige runId au moment du hook et le flag one-shot libState._undoShortcutBound n'est jamais reevalue apres demarrage d'un run dans la meme session. Par ailleurs le commentaire affirme qu'un AbortController detache le listener au demontage : il n'existe pas, le listener window persiste (sans effet nuisible grace au garde-fou, mais le contrat decrit est faux).

- **`web/dashboard/app.js:296`**
  La redirection retrocompat /logs cible '#/aide#diagnostic' mais aucun element id='diagnostic' ni [data-lib-section=diagnostic] n'existe dans aide.js (la section utilise id='aide-diagnostic-title') -> le scroll fragment du router (router.js:248-255) ne trouve rien et l'utilisateur atterrit en haut de la vue Aide au lieu de la section Diagnostic.

- **`web/dashboard/views/parametres.js:1786`** — [NOUVEAU RE-RUN]
  Predicat de succes du save trop laxiste : (res.data.ok || res.data === true || !res.data.message) traite toute reponse {ok:false} SANS champ message comme un succes ('✓ Sauvegardé' + invalidateSettingsCache). Latent aujourd'hui (les chemins d'erreur backend/api.js fournissent tous un message), mais le contrat repose uniquement sur cette convention non garantie.

### CSS / themes

- **`web/shared/tokens.css:30`**
  Le :root de tokens.css est volontairement HORS @layer alors que themes.css est wrappe dans @layer tokens : sur l'element <html>, les redefinitions AAA de --text-primary/secondary/muted (themes.css:250-259) perdent systematiquement contre tokens.css (unlayered > layered a specificite egale) ; le theme AAA ne fonctionne aujourd'hui que parce que data-theme est AUSSI pose sur <body> (index.html:36, top-bar-v5.js:245-246) — tout futur code posant l'attribut uniquement sur html casserait silencieusement les couleurs de texte du theme accessibilite.

### Migrations SQL

- **`cinesort/infra/db/migrations/030_field_locks.sql:58`**
  Index redondants reintroduits : idx_film_field_locks_film_id(film_id, field_name) duplique exactement l'index implicite cree par la contrainte UNIQUE(film_id, field_name) de la table (le commentaire 'Fix #5 index obligatoire pour la perf' est errone, l'index UNIQUE couvre deja le UPDATE WHERE film_id=?), et idx_film_field_locks_film(film_id) en est le prefixe leftmost donc redondant aussi. C'est exactement la classe de defaut corrigee par la migration 022 (issue #77 : double cout disque + double cout d'ecriture par INSERT/UPDATE sans benefice de lecture).

---

## INFO (1)

### Backend UI/API — facades + modules support

- **`cinesort/ui/__init__.py:1`**
  Aucun defaut. Le perimetre (tous les .py sous cinesort/ui/ hors cinesort/ui/api/) ne contient qu'un seul fichier de 1 ligne ('from __future__ import annotations'), lu integralement. Le glue pywebview / bridge JS / boot natif reside en realite dans cinesort/ui/api/ (cinesort_api.py, runtime_support.py, _run_state.py), cinesort/infra/rest_server.py et cinesort/app/ — hors perimetre de cet audit.

---

## Findings refutes par la verification de 1er niveau — ecartes

- dom-quality :: REFUTED|cinesort/domain/quality_score.py:640|LOW|_best_audio_track : le tiebreak entre pistes au meme nombre de canaux compare le bitrate brut (_to_int(t.get("bitrate"), 0)) sans passer par _normalize_audio_bitrate_kbps, contrairement au reste du module (hotfix C5, L971). Si les pistes remontent des unites mixtes (bps vs kbps, plausible avec la fusion mediainfo+ffprobe du champ sources), une piste lossy a 256000 bps bat une lossless a 3500 kbps -> mauvais best_audio, mauvais bonus codec (AAC au lieu de DTS-HD MA) et mauvais token hierarchie.|Unites mixtes inatteignables : _normalize_merge.py L172-191 prend audio_tracks en bloc depuis UN SEUL backend (ffprobe sinon mediainfo, jamais de mix par piste) et les deux backends normalisent chaque bitrate via to_optional_bitrate en bps uniforme (conversions.py L109-136). Au sein d'une meme liste les unites sont homogenes, donc la comparaison brute preserve l'ordre et le tiebreak reste correct ; le hotfix C5 ne concerne que les seuils absolus per-channel, pas un classement relatif.
- dom-naming :: REFUTED|cinesort/domain/tiers_helpers.py:596|MEDIUM|normalize_hierarchy_config rend impossible la SUPPRESSION d'un floor/ceiling par defaut : out part de default_hierarchy_config() puis merge user>default entree par entree. Une section fournie vide (ex resolution_ceilings:{} apres suppression par l'utilisateur) ou une entree a tier invalide (droppee ligne 603-604) retombe silencieusement sur le defaut — des que enabled=True, le ceiling "720p"→Silver et les floors TRaSH (2160p_probe→Gold, dolby_vision→Gold, truehd_atmos→Gold) s'appliquent toujours, sans aucun moyen de les desactiver individuellement via la config.|La desactivation individuelle EST possible via la config : surcharger l'entree avec le tier extreme (ceiling "720p"→Platinum = cap_tier no-op, floor "2160p_probe"→Reject = _floor_tier no-op), verifie par execution (tier inchange, applied=[]). Le merge user>default des sections partielles est le comportement intentionnel documente (docstring lignes 547-552, y compris le drop silencieux des tiers invalides).
- dom-naming :: REFUTED|cinesort/domain/naming.py:547|MEDIUM|folder_matches_template construit le contexte avec UNIQUEMENT title+year (build_naming_context(title=title, year=year)) : pour tout template contenant des variables probe ({resolution}, {video_codec}, {tmdb_tag}...) — presets jellyfin/quality/plex inclus — le nom attendu est ampute de ces parties et ne matchera JAMAIS un dossier reellement conforme (ex dossier "Inception (2010) [1080p]" vs attendu "Inception (2010)"). Faux negatifs systematiques de conformance pour duplicate_support.single_folder_is_conform (Check 1, duplicate_support.py:72).|Pas de faux negatif en pratique : TOUS les chemins de renommage reels construisent eux aussi le contexte sans probe_data (apply_core.py:1702-1708 et 1911-1917, library_support.py:1189-1199), donc le pipeline ne produit jamais de dossier "Inception (2010) [1080p]" — le nom attendu par folder_matches_template est exactement celui que l'app cree (seul le preview cinesort_api.py:1642 remplit les variables probe). Le check de conformance est coherent avec le comportement de renommage ; le contexte minimal est documente dans la docstring.
- dom-naming :: REFUTED|cinesort/domain/probe_models.py:431|LOW|RenameProposal.from_dict promet dans sa docstring de tolerer les champs manquants via les defauts du dataclass, mais src_path/target_path/op_type n'ont PAS de defaut : un dict partiel (ex desserialise d'un journal tronque) leve TypeError sur cls(**filtered) ligne 449 au lieu du comportement tolerant documente — contrat API non tenu sur le chemin de reconstruction roundtrip.|Lecture inexacte du contrat : la docstring promet la tolerance "en utilisant les defauts du dataclass", ce qui ne couvre par definition que les champs AYANT un defaut (no_op, reason, confidence...), pas les 3 champs requis. Le test dedie test_from_dict_uses_defaults_for_missing_fields (test_probe_models_extensions_v77.py:149) fixe cette semantique en fournissant toujours src_path/target_path/op_type. Le roundtrip to_dict→from_dict inclut toujours tous les champs, et aucun appelant prod ne passe de dict partiel (from_dict utilise uniquement dans les tests).
- dom-rest :: REFUTED|cinesort/domain/release_name_parser.py:55|MEDIUM|L'alternative `/bHD/b(?!R)` du pattern resolution 720p matche le 'HD' de 'DTS-HD'/'DTS-HD MA' : un nom de release sans token resolution mais avec piste DTS-HD recoit resolution_hint='720p' (height_hint=720). Quand le probe est PARTIAL/FAILED, ce hint comble le trou (_merge_probe_with_name_hints) et un REMUX/BluRay est score comme du 720p — sous-evaluation utilisateur-visible sur le chemin fallback.|Le hint ne comble rien : _merge_probe_with_name_hints (quality_score.py:1763-1770) ne remplit volontairement PAS video.width/height (commentaire explicite), il n'ajoute qu'un label informatif delta 0. Le fallback resolution reel (_resolution_label L599-606) utilise _RELEASE_720_RE = /b720p/b uniquement, que 'DTS-HD' ne matche pas. Aucun impact sur le score.
- dom-rest :: REFUTED|cinesort/domain/duplicate_support.py:285|LOW|find_duplicate_targets mute les rows passees en parametre pendant une operation de VERIFICATION (row.warning_flags.append('year_missing')) : effet de bord persistant sur le plan declenche aussi lors du check pre-apply (ui/api/apply_support.py:1193), et crash AttributeError si warning_flags est serialise en str (format 'a|b' tolere par librarian.py:105) plutot qu'en list.|Mutation intentionnelle et documentee (commentaire L280-287 'Fix bug TV series / BONUS Star Wars : ne plus skip silencieusement... on flag year_missing'), idempotente via le check 'not in'. Et le crash est inatteignable : PlanRow.warning_flags est type List[str] avec default_factory=list (core.py:409) et les deux loaders normalisent toujours en list (plan_support_core.py:103, run_data_support.py:64-68) ; la tolerance str de librarian.py:105 est du defensif generique, pas une preuve que des rows str atteignent find_duplicate_targets.
- app-apply :: REFUTED|cinesort/app/apply_core.py:687|LOW|Pour un doublon identique DEPLACE vers _review/_duplicates_identical, le code incremente a la fois `duplicates_identical_moved_count` ET `duplicates_identical_deleted_count` (L687-688) pour le meme fichier alors que rien n'est supprime — l'un des deux compteurs remonte a l'UI est faux par construction. De plus, move_duplicate_losers_to_user_decided (L1030, L1044, L1065) incremente `duplicates_identical_moved_count` pour les losers decides par l'utilisateur (bucket _duplicates_user_decided distinct, spec 01-doublons §3.7) : conflation des deux categories dans les totaux du resume d'apply.|Comportement intentionnel et documente : `duplicates_identical_deleted_count` est declare 'Backward-compatible alias kept for older consumers' (domain/core.py:1394-1395), l'UI l'affiche comme 'supprimes logiquement' (soft delete = retire de la bibliotheque, apply_support.py:1600) et le test test_apply_duplicate_identical_file_is_soft_deleted asserte explicitement les DEUX compteurs a 1 pour un move. Pour les losers, le code n'incremente volontairement QUE moved_count (pas deleted_count) et la spec 01-doublons §3.7 n'exige aucun compteur separe pour le bucket _duplicates_user_decided.
- app-rest :: REFUTED|cinesort/app/export_support.py:254|LOW|export_nfo_for_run : `int(row.get("decision_year") or row.get("proposed_year") or 0)` n'est pas protege — une valeur non numerique (chaine corrompue ou annotation libre) leve ValueError hors de tout try et fait crasher l'export NFO ENTIER au lieu de compter la row en skipped_no_data/errors comme le reste de la boucle le fait pour les erreurs IO (lignes 275-281).|Chemin inatteignable : l'unique caller (cinesort_api.py:2155-2167) construit les rows via dashboard_support._build_row_payload qui coerce deja decision_year en int via to_int (dashboard_support.py:908, fallback sur defaut en cas de TypeError/ValueError) et proposed_year via int() (ligne 902). Les rows passees a export_nfo_for_run contiennent toujours des ints — aucune valeur non numerique ne peut atteindre la ligne 254.
- infra-rest :: REFUTED|cinesort/infra/rest_server.py:1148|MEDIUM|La protection MIN_LAN_TOKEN_LENGTH (token >= 32 pour exposition LAN) n'est testee qu'avec `requested_host == '0.0.0.0'` au __init__ (ligne 1148) et `self._host == '0.0.0.0'` dans update_auth_token (ligne 1397). Si l'utilisateur configure un bind sur une IP LAN specifique (ex: '192.168.1.5'), aucune des deux gardes ne s'applique : le serveur s'expose sur le LAN avec un token arbitrairement court, et le hot-swap vers un token faible est accepte. La validation devrait couvrir tout host non-loopback, pas seulement la chaine litterale '0.0.0.0'.|Scenario inatteignable : les 2 seuls appelants prod (app.py:351 et app.py:453) calculent host = '0.0.0.0' if is_public else '127.0.0.1', et le schema settings _save_section_rest_api (settings_support.py:1470-1478) n'expose AUCUN champ host/bind. Aucun chemin ne permet de binder une IP LAN specifique type 192.168.1.5 ; le host ne peut valoir que les litteraux '0.0.0.0' ou '127.0.0.1', donc la comparaison litterale couvre tout cas non-loopback reellement atteignable.
- infra-clients :: REFUTED|cinesort/infra/omdb_client.py:299|MEDIUM|_save_cache_atomic serialise self._cache via json.dumps SANS prendre self._lock : un _cache_set concurrent leverait RuntimeError 'dictionary changed size during iteration' non rattrape.|OmdbClient n'est JAMAIS partage entre threads : il est instancie localement par run (run_flow_support.py:466) et par appel de test (cinesort_api.py:1593), sans cache module-level a la difference de TmdbClient (poster_proxy). La boucle cross-check (omdb_cross_check.py:132) est strictement sequentielle (aucun ThreadPool/threading). Aucun _cache_set ne peut donc s'executer pendant le json.dumps d'un meme instance : le RuntimeError est inatteignable. L'asymetrie de lock vs TmdbClient est reelle mais sans impact runtime.
- infra-clients :: REFUTED|cinesort/infra/integrations/ollama_client.py:230-236|MEDIUM|_invoke viole le contrat fail-soft : data.get('response') et parsed.get(key) levent AttributeError sur JSON non-dict, non couverts par except (ValueError, JSONDecodeError) l.233 ; idem generate_tags:186 ou metadata={'title': None} crashe sur .strip().|Chemin inatteignable en runtime : tous les callers de _invoke/generate_synopsis/generate_tags sont derriere AI_ENRICHMENT_FEATURE_FLAG (enrichment_facade.py:44 = False hardcode, aucun override). enrichment_for_film retourne tot si le flag est False (l.111), get_enrichment_status n'appelle que is_available. Le module est explicitement documente comme scaffold non-executant ('Aucune execution effective de prompts dans cette PR', docstring l.13-15). Les AttributeError existent dans le code mais ne sont pas atteints ; comportement intentionnel/documente pour un scaffold gate.
- infra-db :: REFUTED|cinesort/infra/db/repositories/decisions.py:294|MEDIUM|upgrade_deferred_to_accepted ne verifie JAMAIS que la decision precedente est 'deferred' : appele sur un film 'rejected' (ou sans decision), il le bascule silencieusement en 'accepted' (donc to_legacy_ok_bool=True -> film approuve pour apply). De plus get_decision puis set_decision s'executent sur deux connexions separees sans transaction commune (TOCTOU) : une decision concurrente posee entre les deux est ecrasee et previous_decision retourne est faux.|Methode jamais appelee en production (grep : seulement docstrings de run_facade/run_flow_support + tests) ; le flux reel save_validation passe par set_decision via _mirror_decisions_to_sql (run_flow_support.py:1248), protege par un verrou par-run_id (BUG-011). L'upsert sans verification d'etat prealable est un comportement assume et teste (test_upgrade_without_previous_decision documente 'accepted sans decision prealable = idempotent voulu').
- infra-probe :: REFUTED|cinesort/infra/probe/service.py:127-145|LOW|_get_tools_cached() lit/ecrit self._tools_status_cache et self._tools_status_cache_sig sans verrou alors que probe_files() appelle probe_file() depuis un ThreadPoolExecutor. Race check-then-return : invalidate_tools_status_cache() (declenche par une sauvegarde de settings concurrente) peut mettre le cache a None entre le test `is not None` et le `return`, renvoyant None ; les appelants hors-pool (quality_report_support, perceptual_support) feraient alors `tools['mediainfo']` (service.py:422) => TypeError remontant en HTTP 500. En batch parallele, la course provoque surtout des relances redondantes de subprocess `--version` (annule l'optim PERF-1).|Chaque appelant cree sa PROPRE instance ProbeService(store) par appel (quality_report_support.py:88, perceptual_support.py:1466, probe_support.py:451) : leur _tools_status_cache n'est jamais partage avec un pool, donc aucun invalidate concurrent ne touche leur cache et le chemin HTTP 500 decrit est inatteignable. La seule concurrence intra-instance est dans probe_files, ou le TypeError d'un cache None est capture en service.py:680 (tuple except OSError,RuntimeError,TypeError,ValueError) -> resultat d'erreur, pas de 500. invalidate n'a d'ailleurs aucun appelant externe (seul service.py:233).
- ui-god :: REFUTED|cinesort/ui/api/cinesort_api.py:1007|CRITICAL|_restart_api_server_impl lit rest_api_token via _get_settings_impl() qui MASQUE les secrets (settings_support._mask_secrets, rest_api_token est dans _SECRET_FIELDS). Le serveur REST relance donc avec token="••••••••" : tous les clients legitimes recoivent 401, et la constante masque (publique, 8 bullets) devient le Bearer token accepte — n'importe qui sur le LAN peut s'authentifier. Trou de securite + rupture du contrat API. Il faut lire le token brut via _read_settings() comme le fait _unmask_or_stored.|Le token masque est reel mais l'impact CRITICAL est faux : le restart ne passe pas `host` a RestApiServer dont le defaut est "127.0.0.1" (rest_server.py:1126) -> le serveur relance n'est JAMAIS bind sur le LAN (personne sur le LAN ne peut atteindre le socket, a fortiori s'authentifier avec le masque) ; meme en 0.0.0.0 le masque 8 chars < MIN_LAN_TOKEN_LENGTH=32 forcerait lan_demoted -> 127.0.0.1 (L1148-1150) ; et _check_auth bypasse l'auth pour tout client loopback quand bind=127.0.0.1 (rest_server.py:452-462, desktop trusted mode) -> pas de 401 pour les clients legitimes locaux.
- ui-settings :: REFUTED|cinesort/ui/api/settings_support.py:2063|HIGH|restore_settings_backup execute _backup_settings_before_write puis _rotate_settings_backups AVANT shutil.copy2 : avec 5 backups existants (etat nominal apres rotation), la creation du 6e backup fait supprimer le plus ancien par la rotation ; si c'est precisement le backup que l'utilisateur restaure (il est liste par list_settings_backups), il est unlink avant la copie -> copy2 leve FileNotFoundError (catche, return False) : la restauration echoue ET le backup choisi est definitivement detruit (perte de donnees).|Chemin inatteignable en production : restore_settings_backup et list_settings_backups ne sont cables a aucune facade, aucun endpoint REST ni aucune vue UI (seuls tests/test_settings_backup.py les appellent) ; le defaut logique existe mais le scenario utilisateur decrit ne peut pas se produire.
- ui-run :: REFUTED|cinesort/ui/api/run_flow_support.py:1374|LOW|_enrich_one_group ne compare que les 2 PREMIERES rows d'un groupe (probes[0]/probes[1], payload construit sur rows[0]/rows[1]) meme pour les groupes de 3+ doublons. Consequence : le compteur 'Y Go recuperables' (size_savings_total, spec 01-doublons section 1) sous-estime l'espace recuperable et les doublons au-dela du 2e n'ont aucun verdict dans la comparaison.|Comportement conforme a la spec : 01-doublons.md definit explicitement size_savings_total = sum(groups.comparison.size_savings) (section 1 L45 et section 4 L306-310, pseudo-code repris tel quel) et tout le design du comparateur est A/B a 2 fichiers (cartes A/B, modal Fichier A/B). Limitation de design documentee, pas un bug d'implementation.
- ui-lib :: REFUTED|cinesort/ui/api/library_support.py:768|MEDIUM|save_smart_playlist/delete_smart_playlist font un read-modify-write non verrouille sur settings.json complet : _get_playlists_from_settings lit, puis _write_playlists_to_settings relit get_settings, mute et reecrit TOUT le dict settings. Deux operations concurrentes (2 sauvegardes de playlist, ou playlist + save_settings depuis l'ecran Reglages) se font perdre mutuellement leurs ecritures (lost update du fichier de config entier).|Faux : l'ecriture est serialisee par api._settings_write_lock (cinesort_api.py:735) ET par un verrou par state_dir dans save_settings_payload (settings_support.py:1816-1817), et _save_settings_payload_locked repart de l'existant DISQUE relu sous verrou (merge L1871, commentaire anti-lost-update L44-49) en n'appliquant que des sections normalisees : un save_settings concurrent ne peut pas ecraser le fichier de config entier.
- ui-lib :: REFUTED|cinesort/ui/api/perceptual_support.py:1221|MEDIUM|Bug de precedence : `na = probe_a.get("normalized") if isinstance(probe_a, dict) else {} or {}` se parse `else ({} or {})` — si le probe reussit mais ne contient pas "normalized" (probe FAILED), na vaut None et `na.get("duration_s")` (L1223) leve AttributeError, ABSENT du tuple catche (OSError, KeyError, TypeError, ValueError) L1260 -> exception non geree/500. Les 2 endpoints freres utilisent la forme correcte `.get("normalized") or {}` (L698, L833).|Le parse `else ({} or {})` est exact mais la branche dangereuse est inatteignable : _load_probe passe par ProbeService.probe_file qui garantit "normalized" comme dict dans TOUTES ses branches de retour, y compris probe FAILED/degraded (service.py:365 to_dict(), 393 et 591 gardes isinstance else {}, 560 normalized_obj.to_dict()). na n'est donc jamais None ; bug latent de style, pas de 500 possible.
- ui-sup-a :: REFUTED|cinesort/ui/api/history_support.py:371|MEDIUM|La retention (cleanup_old_runs, appelee par le cron app/retention_cleanup.py) ne supprime que les rows DB via store.run.delete_run ; aucun code ne supprime les run_dirs runs/tri_films_* (plan.jsonl, ui_log.txt, validation.json, deletion_marks.json), alors que la docstring de delete_run (L339-341) promet qu'ils 'seront elimines a la rotation par retention' - fuite disque illimitee et dossiers orphelins qui ne correspondent plus a aucun run en DB.|Faux : state.clean_old_runs(state_dir, keep_last=20) (cinesort/infra/state.py L105-114) est appele a chaque start_plan (run_flow_support.py L293) et rmtree tous les run_dirs au-dela des 20 plus recents. Les dossiers SONT elimines a la rotation, la fuite disque est bornee a ~20 runs, pas illimitee.
- js-core :: REFUTED|web/dashboard/app.js:272|MEDIUM|Router leak : meme pattern pour /film/:id — unmountFilmDetail (film-detail.js:600 : abort de _state.eventsController, liberation containerRef/data) n'est jamais cable au router : initFilmDetail est async et retourne Promise<undefined>, et aucune cle `unmount` n'est declaree dans registerRoute. Les listeners DOM poses via AbortController et les references (_state.containerRef, _state.data) sont retenus apres navigation hors de la fiche film, accumulation a chaque visite.|Pas d'accumulation : _loadAndRender (film-detail.js:554-557) abort l'ancien eventsController et remplace innerHTML a CHAQUE mount, donc l'ancienne instance (listeners + nodes) devient collectable. La retention est bornee a la derniere fiche visitee (equivalent d'une vue cachee), liberee au prochain mount — le prejudice central du finding (accumulation par visite) est faux.
- js-core :: REFUTED|web/dashboard/core/api.js:629|MEDIUM|testConnection traite tout statut hors {401, 429, >=500} comme un succes : un 404 (endpoint deplace/renomme — exactement le bug #233 documente 5 lignes au-dessus) ou un 400/403 retourne {ok:true} et le flow de login accepte un token jamais valide. Aucun garde-fou n'a ete ajoute contre la recidive du #233 alors que le commentaire decrit precisement ce mode de panne ("404 cassait silencieusement le flow de login").|Chemin inatteignable avec le code actuel : l'endpoint /api/settings/get_settings existe (facade settings, runtime_facade/settings_support verifies), le body "{}" est valide, et le serveur ne renvoie que 200/401/429/5xx sur cette route. C'est une suggestion de durcissement contre une regression future, pas un bug actuel.
- js-core :: REFUTED|web/dashboard/app.js:891|MEDIUM|_loadDashTheme (et le fetch theme de _mountV5Shell ligne 423) traite une reponse d'erreur comme des settings : sur 429/5xx transitoire, res.data={ok:false} est truthy -> s.theme undefined -> theme force a "luxe", animation/effects reinitialises, ecrasant visuellement le theme configure de l'utilisateur jusqu'au prochain fetch reussi. Meme probleme dans _applyTheme ligne 919 : le merge {...{ok:false,message}, theme} part en save_settings sans roots -> echec backend silencieux (root_required), le changement de theme semble applique mais n'est jamais persiste.|La moitie load-bearing du finding est fausse : save_settings sans roots ne fail PAS — resolve_roots_from_payload (settings_support.py:1159-1169, priorite 3) retombe sur les roots sauvegardes sur disque, donc le theme EST persiste pour tout utilisateur configure (root_required seulement si aucun root n'a jamais existe). Le reste (fallback "luxe" au boot sur erreur transitoire) est le comportement de repli intentionnel documente ("Theme initial depuis settings (fallback luxe)"), impact cosmetique transitoire.
- js-comp :: REFUTED|web/dashboard/components/badge.js:78|HIGH|Violation de l'invariant couleurs tier : _TIER_PILL_COLORS hardcode platinum #A78BFA (violet !), gold #FBBF24, silver #9CA3AF, bronze #FB923C en styles inline, au lieu des hex fixes de l'invariant (tokens.css : platinum #E5E4E2, gold #FFD700, silver #C0C0C0, bronze #CD7F32) ou des variables --tier-X-solid. tierPill/scoreTierPill sont des exports publics (aucun consommateur dashboard actuel, violation latente) : tout futur appel rendra des couleurs tier fausses.|Palette intentionnelle « parité exacte avec desktop » : exigée verbatim par tests/test_tier_pill.py::test_dashboard_tier_colors_match_desktop, whitelistée comme « invariantes par design (cf badge.js) » dans tests/test_no_hardcoded_colors.py:29-40 et documentée dans JOURNAL_V7_3_0.md:470 ; aucun consommateur actuel donc zéro impact.
- js-comp :: REFUTED|web/dashboard/components/film-detail.js:1336|MEDIUM|Fuite de listener document : _ensureModeCOverlay supprime un overlay existant via existing.remove() SANS retirer son _escHandler de document (seul closeFilmDetail le retire, pour _state.overlayEl uniquement). Combiné au fait que closeFilmDetail() (l.1432-1452) ne remet jamais _state.onClose à null, chaque handler fuité ré-invoque closeFilmDetail() + le callback onClose périmé à chaque pression d'Échap, indéfiniment.|Chemin inatteignable : tous les déclencheurs de mode C sont des clics/dblclics dans les vues, bloqués par l'overlay plein écran (inset:0 + focus trap Tab + clic backdrop = fermeture propre) ; aucun raccourci clavier n'ouvre de fiche (doublons _onKeydown = flèches seulement) ; et aucun appelant ne passe onClose, donc la chaîne « callback périmé » est sans effet.
- js-comp :: REFUTED|web/dashboard/components/film-detail.js:873|MEDIUM|Refresh poster unitaire incohérent avec l'affichage : la fiche affiche le proxy /api/poster?size=w342 (l.191) mais le bouton 🔄 appelle integrations/get_tmdb_posters avec size:'w185' + force_refresh — seul le cache w185 est rafraîchi. L'img est remplacée par l'URL directe TMDb w185 (qualité dégradée vs w342) et au prochain _renderAll (clic score, expand candidats, _reload) le proxy w342 non rafraîchi reprend la priorité : l'ancien poster réapparaît.|Le scénario décrit ne s'exécute jamais : la facade get_tmdb_posters(tmdb_ids, size) n'accepte pas force_refresh et rest_server.py fait method(**params) → TypeError → 400 « Parametres invalides » (rest_server.py:1051+1068) ; l'appel échoue systématiquement (toast erreur), l'img n'est jamais remplacée et aucun cache n'est rafraîchi — le bug réel est différent (bouton toujours en échec).
- js-param :: REFUTED|web/dashboard/views/parametres.js:1786|LOW|Predicat de succes du save trop laxiste : (res.data.ok || res.data === true || !res.data.message) traite toute reponse {ok:false} SANS champ message comme un succes ('✓ Sauvegardé' + invalidateSettingsCache). Latent aujourd'hui (les chemins d'erreur backend/api.js fournissent tous un message), mais le contrat repose uniquement sur cette convention non garantie.|Chemin inatteignable : tous les chemins d'erreur de settings/save_settings portent 'message' (settings_support.py L1808 et L1858, err() defaut key='message', api.js 401/429/5xx/JSON invalide L274-290) ; le finding admet lui-meme que c'est latent — fragilite de style, pas un bug reel.
- js-trait :: REFUTED|web/dashboard/views/traitement.js:191|HIGH|_writeStep reecrit le hash en "#/traitement#step-X" sans preserver le fragment #run-XXX ; _onHashChange (l.2594-2606) voit alors nextRun=null != _targetRunId, reset le run cible et recharge get_dashboard sur "latest" SANS recharger _validationPlan/_decisionsState : les decisions du run cible peuvent ensuite etre postees via save_validation sous le run_id d'un autre run (ecriture croisee inter-runs).|core/router.js resolve() demonte puis remonte la vue a CHAQUE hashchange (cleanup l.148-152 + init l.224-242, sans early-return si meme route) : _onHashChange est retire par unmountTraitement pendant le dispatch et ne s'execute jamais, et le remount (initTraitement) recharge _runInfo, _validationPlan et _decisionsState ensemble sur le meme run "latest" — etat coherent, pas d'ecriture croisee inter-runs (la perte du fragment #run-XXX est reelle mais sans le mecanisme decrit).
- js-biblio :: REFUTED|web/dashboard/views/library/lib-duplicates.js:34|MEDIUM|Race au mount + gating affaibli : initDuplicates lance _loadDuplicates() immediatement alors que initValidation._loadRows (async, non awaite par library.js) n'a pas encore peuple _allRows -> buildDecisionsPayload() retourne {} (ou l'etat residuel d'une session precedente) et check_duplicates est evalue sans les vraies decisions. De plus markDuplicatesChecked() (L83) coche automatiquement 'Doublons verifies' dans la checklist Securite des le chargement de la page, sans aucune action utilisateur : le gating du bouton Appliquer ne depend plus en pratique que de la sauvegarde.|Le backend check_duplicates (run_flow_support.py:1605-1607/1633-1635) merge le payload entrant avec validation.json du disque en fallback : un payload {} au mount est donc evalue avec les vraies decisions sauvegardees, et check_duplicates ne persiste rien. Quant au flag dupsChecked apres _loadDuplicates reussi (auto-load inclus), c'est le comportement documente du fix v1.5.2 (commentaires lib-apply.js L20-26 et lib-duplicates.js L80-83 : 'peu importe qu'il y ait 0 ou N groupes : le check a ete fait').
- js-qual :: REFUTED|web/dashboard/views/qij.js:97|HIGH|Appels REST non prefixes par facade -> 410 Gone systematique|Code mort inatteignable : qij.js n'est monte par aucune route (app.js:291 redirige /qij) et initQij/initQuality/initJournal/initIntegrations ne sont appeles par aucun code — seul bootstrap-bisect.js importe le module pour un test de chargement sans rien executer. Aucune consequence utilisateur possible.
- js-qual :: REFUTED|web/dashboard/views/quality.js:49|HIGH|`_globalData.total_films` n'existe pas au top-level du payload run/get_global_stats|Code mort inatteignable : web/dashboard/views/quality.js n'est importe nulle part (ni app.js, ni bootstrap-bisect, ni HTML), initQuality jamais appele, /quality redirige vers /qualite (app.js:293) — la vue ne se rend jamais.
- js-qual :: REFUTED|web/dashboard/views/status.js:206|HIGH|Mapping casse de tier_distribution : lecture avec cles capitalisees|Code mort inatteignable : status.js n'est importe nulle part, initStatus jamais appele, /status redirige vers /accueil (app.js:290) — la carte Distribution qualite ne se rend jamais.
- js-qual :: REFUTED|web/dashboard/views/status.js:118|HIGH|KPIs lus au top-level de get_global_stats alors qu'ils sont sous `summary`|Code mort inatteignable : status.js n'est importe nulle part, initStatus jamais appele, /status redirige vers /accueil (app.js:290) — ni le ring hero ni la banniere premier lancement ne s'affichent jamais.
- js-qual :: REFUTED|web/dashboard/views/qij.js:114|HIGH|Meme contrat casse dans le tab Quality : stats.* lus top-level alors que le backend les nest sous `summary`|Code mort inatteignable : qij.js n'est jamais monte (initQij/initQuality jamais appeles, /qij redirige vers /accueil app.js:291) — les KPI du tab Quality ne se rendent jamais.
- js-qual :: REFUTED|web/dashboard/views/qij.js:897|HIGH|runs_summary expose `score_avg` et non `avg_score` : cards Journal score "—" et tri par score inoperant ; meme champ errone dans quality.js:149/:251|Code mort inatteignable : qij.js n'est jamais monte (initJournal jamais appele) et quality.js n'est importe nulle part — aucune des deux vues ne se rend jamais.
- js-qual :: REFUTED|web/dashboard/views/status.js:223|MEDIUM|Activite recente : `r.avg_score` et `r.started_at` n'existent pas dans runs_summary|Code mort inatteignable : status.js n'est importe nulle part, initStatus jamais appele — le feed Activite recente ne se rend jamais.
- js-qual :: REFUTED|web/dashboard/views/qij.js:131|HIGH|stats.technical_distribution et stats.all_scored_rows n'existent dans AUCUN endpoint backend -> Distribution technique et Outliers 2-sigma jamais affiches|Code mort inatteignable : qij.js n'est jamais monte et quality.js n'est importe nulle part — le finding decrit lui-meme du code mort, sans impact utilisateur atteignable.
- js-qual :: REFUTED|web/dashboard/views/quality.js:164|HIGH|Violation invariant couleurs tiers : barres de distribution colorees avec des vars semantiques themables au lieu des --tier-*-solid|Code mort inatteignable : quality.js n'est importe nulle part (ni app.js ni bisect), initQuality jamais appele — ces barres ne sont jamais rendues, l'invariant runtime n'est pas viole.
- js-qual :: REFUTED|web/dashboard/views/status.js:590|HIGH|Violation invariant couleurs tiers dans "Espace disque par qualite" : mapping vars semantiques au lieu des --tier-*-solid|Code mort inatteignable : status.js n'est importe nulle part, initStatus jamais appele — la carte Espace disque ne se rend jamais, l'invariant runtime n'est pas viole.
- js-qual :: REFUTED|web/dashboard/views/quality.js:183|HIGH|Filtres Etat/Tier/Score rendus mais jamais bindes, "Analyser les filtres" echoue systematiquement|Code mort inatteignable : quality.js n'est importe nulle part, initQuality jamais appele — ces filtres et ce bouton ne sont jamais rendus ni cliquables.
- js-qual :: REFUTED|web/dashboard/views/quality.js:139|MEDIUM|Toggle "Bibliotheque / Run courant" factice : _render ignore _mode et _runData jamais alimente|Code mort inatteignable : quality.js n'est importe nulle part, initQuality jamais appele — le toggle n'est jamais rendu.
- js-qual :: REFUTED|web/dashboard/views/qij.js:294|MEDIUM|Switch de dimension rollup sans abort ni garde-generation|Code mort inatteignable : qij.js n'est jamais monte (initQuality jamais appele, module seulement importe par bootstrap-bisect sans execution) — le switch rollup n'existe jamais dans le DOM.
- js-qual :: REFUTED|web/dashboard/views/status.js:349|MEDIUM|Etat perime en mode patch : _shellSignatureOf ne capture que des compteurs et _patchDynamic ne met pas a jour feed/health-score/espace disque/V2|Code mort inatteignable : status.js n'est importe nulle part, initStatus jamais appele — ni le rendu ni le polling de cette vue ne s'executent jamais.
- js-rest :: REFUTED|web/dashboard/views/aide.js:110|MEDIUM|_GITHUB_REPO_URL/_GITHUB_ISSUES_URL pointent vers github.com/Thomas05000005/CineSort alors que le depot n'est pas public : l'audit 2026-06-07 a neutralise ces memes liens dans about.js (l.38-42 'URL placeholder -> 404 GitHub a chaque clic') mais a oublie aide.js -> les boutons Repo GitHub/Changelog/Contributeurs (l.268-270) et le template d'issue ouvrent des 404.|La premisse est fausse : le depot Thomas05000005/CineSort EST public aujourd'hui (API GitHub private:false, HTTP 200 sur le repo, CHANGELOG.md et /graphs/contributors). Les liens d'aide.js fonctionnent, aucun 404 ; c'est le commentaire d'about.js qui est obsolete.
- css :: REFUTED|web/shared/components.css:920|MEDIUM|Le menu deroulant theme a z-index:9100 alors que le commentaire L916-919 affirme rester "Sous --z-modal (600) pour ne pas couvrir une modale" : contradiction directe, le menu couvre toutes les modales tokenisees (legacy .modal-overlay 600 et .v5-modal 600) ainsi que la modale perceptuelle (9000) ; l'echelle z-index documentee dans tokens.css (max 1000) est rompue par les modales hardcodees 9000-10002 (L7473, L8496, L7724, L8257, L9648).|Le commentaire est perime mais l'effet annonce n'existe pas : le menu est confine dans le stacking context de .v5-top-bar (components.css:787-801, position:relative + z-index:var(--z-top-bar)=300 + backdrop-filter), or toutes les modales sont montees sur document.body (modal.js:102/277, perceptual-modal.js:741) a z 600/9000+ > 300 — elles peignent donc TOUJOURS au-dessus du menu, le 9100 n'agit qu'a l'interieur de la top-bar.
- css :: REFUTED|web/dashboard/styles.css:1510|MEDIUM|.lib-bulk-bar (barre de selection groupee, position:fixed bottom) a z-index:9995 hardcode, superieur a .modal-overlay var(--z-modal,600) (L614) et a la modale perceptuelle (9000) : quand une modale est ouverte depuis la bibliotheque avec une selection active, la barre flottante reste peinte au-dessus de la modale.|Chemin inatteignable : .lib-bulk-bar est du CSS mort — aucun JS/HTML du projet ne cree cet element (grep : la classe n'existe que dans styles.css), les bulk actions de la grille bibliotheque sont explicitement reportees a des PRs futures (app.js:225). Le conflit z-index ne peut pas se produire en runtime.
- dom-quality :: REFUTED|cinesort/domain/quality_score.py:1888|MEDIUM|probe_quality present avec valeur None est classe UNKNOWN (permissif, aucun cap) au lieu de FAILED : probe.get("probe_quality") retourne None aussi bien pour cle-presente-a-null que pour cle-absente, contrairement au commentaire L1883-1887 ("present mais vide -> warning + fallback FAILED"). Un probe echoue serialise en JSON avec null contourne silencieusement le cap tier Silver (L2191-2201) et peut afficher Gold/Platinum non verifie.|Scenario inatteignable : NormalizedProbe.probe_quality est une str avec defaut "FAILED" (probe_models.py L261), to_dict/asdict et le cache JSON preservent la chaine, aucun chemin de prod ne produit null ; et router present-a-None (= absence d'information) vers UNKNOWN est conforme a l'intention permissive du hotfix BUG-019, "vide" designant la chaine vide qui retombe bien sur FAILED+warning (L1894-1904).
- dom-naming :: REFUTED|cinesort/domain/duplicate_multi_signal.py:129|LOW|L'indexation Phase B par premier token apres tri alphabetique ne tolere aucune variation dans ce token [...]|Bucketing intentionnel et documente : la docstring du module (lignes 16-17) et celle de _index_token justifient explicitement l'indexation pour eviter le NxN sur 1000+ films et ne promettent que l'invariance a l'ordre des mots. La limitation typo-sur-premier-token est inherente a tout blocking par cle exacte — compromis de conception assume, pas un defaut de code.
- dom-perc-b :: REFUTED|cinesort/domain/perceptual/constants.py:604|MEDIUM|TIER_V2_COLORS contredit l'invariant projet des couleurs tiers : platinum='#FFD700' (qui est l'hex reserve a Gold), gold='#22C55E', silver='#3B82F6', bronze='#F59E0B' au lieu de Gold #FFD700 / Silver #C0C0C0 / Bronze #CD7F32 / Platinum #E5E4E2. Le dict n'est actuellement reference nulle part dans le code (seulement dans docs/internal/plans/PLAN_CODE_v7_5_0.md), donc pas de violation runtime, mais c'est une mine : tout futur consommateur UI casserait l'invariante des couleurs de tiers.|Constante morte : aucun usage runtime (grep code = 0 reference, le finding l'admet lui-meme), donc chemin inatteignable. Et les valeurs reproduisent exactement la spec documentee PLAN_CODE_v7_5_0.md L4089 ('coherent avec la maquette') : choix intentionnel et documente pour les tiers perceptuels v2 (Platinum/Gold/Silver/Bronze/Reject), pas un bug.
- dom-rest :: REFUTED|cinesort/domain/mkv_title_check.py:53|MEDIUM|check_container_title retourne 'mkv_title_mismatch' pour TOUT ecart case-insensitive entre titre conteneur et titre propose (ex. 'Inception (2010)' vs 'Inception', titre + tags release), alors que _is_scene_title/_SCENE_PATTERNS/_SCENE_THRESHOLD (L17-32) ne sont JAMAIS appeles : la detection 'titre scene' documentee (docstring, CHANGELOG 9.23) n'est pas cablee. Resultat : warning encode_warnings (quality_report_support.py:313) bruyant sur quasi tout fichier ayant un tag title.|Comportement intentionnel et documente par les tests : test_mkv_title.py:63-65 ('Titre different mais propre → warning mkv_title_mismatch') exige explicitement le warning pour tout ecart, pas seulement les titres scene. Le flag s'appelle 'mismatch' et la ROADMAP 9.23 prevoit le nettoyage mkvpropedit Phase 2 de tout titre divergent. Les titres scene SONT detectes (sous-ensemble des mismatches) ; _is_scene_title est un helper non cable (dead code, smell mineur) mais aucun comportement documente n'est casse.
- dom-rest :: REFUTED|cinesort/domain/librarian.py:249|LOW|health_score = round(100*(total_rows - len(problem_ids))/total_rows) sans intersection de problem_ids avec les row_ids reellement presents dans rows : un quality_report avec row_id vide ajoute '' comme probleme (L83-87, L206-209) et des reports orphelins (row absente du plan) degonflent le score, jusqu'a une valeur negative possible affichee dans la jauge sante.|Scenario non atteignable de facon demontree : les quality_reports sont scopes au MEME run que les rows (dashboard_support.py:1209-1211 charge plan.jsonl et list_quality_reports du meme run_id), row_id n'est jamais vide a l'upsert (toujours issu d'une row reelle du plan, quality_report_support.py:126-128), et aucun chemin ne supprime des rows du plan.jsonl en laissant des reports orphelins (le re-match library_actions_support.py:376 REMPLACE la row en conservant row_id). L'absence d'intersection est un manque de defense, pas un bug observable.
- app-rest :: REFUTED|cinesort/app/merge_metadata.py:110|MEDIUM|_is_empty(False) retourne True car bool est une sous-classe d'int (`isinstance(False, (int, float)) and False == 0`). En mode "completer les manques uniquement" (replace_data=False), un champ booleen volontairement a False dans target est ecrase par la valeur source, en contradiction avec la garantie anti-ecrasement de la barriere (seuls None/""/collections vides/0 numerique sont documentes comme "manquants"). Ajouter un garde `isinstance(value, bool)` avant le test numerique.|Le comportement Python est exact mais le chemin est inatteignable en production : l'unique appelant reel de merge_metadata (library_actions_support.py:366) passe replace_data=True, branche qui ecrase tout sans jamais appeler _is_empty (merge_metadata.py:78-80). Le mode fill-missing n'est exerce que par les tests — bug latent sans consequence actuelle.
- infra-rest :: REFUTED|cinesort/infra/rest_server.py:874|MEDIUM|L'endpoint GET /api/poster n'appelle JAMAIS _check_auth (commentaire lignes 868-873 le justifie par le bypass loopback). Mais le bypass de _check_auth est conditionne a `bind_host == '127.0.0.1'` : quand le serveur est expose en 0.0.0.0 (LAN, autorise si token >= 32 chars), /api/poster reste totalement non authentifie alors que tout le reste exige le Bearer. Un client LAN non authentifie peut donc invoquer le proxy poster (relai sortant vers TMDb) ; risque limite par la validation anti-SSRF de poster_proxy mais c'est une surface d'auth manquante incoherente avec le bind LAN.|Misread: TOUS les endpoints GET (health, spec, dashboard static, shared, locales, poster) sont non-authentifies par design, seuls les POST exigent le Bearer (_check_auth uniquement dans _handle_post). Le poster doit etre public car un <img> ne peut pas porter d'header Authorization (dashboard LAN). poster_proxy contraint la sortie aux posters TMDb par regex id + whitelist size et ignore url/path. Intentionnel, documente et mitige, pas une incoherence.
- infra-clients :: REFUTED|cinesort/infra/tmdb_client.py:860|MEDIUM|find_by_imdb_id appelle data.get('movie_results') HORS du try et sans guard isinstance(data, dict) : un payload JSON non-dict provoque une AttributeError non rattrapee qui remonte dans le pipeline plan (cross-check Phase 6.2).|L'AttributeError ne remonte PAS dans le pipeline : les deux seuls call sites de tmdb.find_by_imdb_id sont enveloppes dans des try qui catchent explicitement AttributeError (plan_support_dedup.py:267 'except (KeyError, TypeError, ValueError, AttributeError)' et plan_support_dedup.py:433 idem) ; de plus la Phase 6.2 citee est le cross-check OMDb qui passe par OmdbClient._http_get, lequel a deja le guard isinstance(data, dict) (omdb_client.py:389-390). Le defaut interne existe mais l'impact revendique est deja garde a chaque chemin d'appel.
- infra-probe :: REFUTED|cinesort/infra/probe/auto_install.py:53-54,104-110|HIGH|EXPECTED_SHA256 a None et warning 'integrity UNVERIFIED'|Comportement intentionnel et explicitement documente dans le code (lignes 37-52) : les URLs rolling-release (gyan.dev, MediaInfo republiable) rendent le pin du hash impossible a embarquer en dur ; la regle adoptee est warning + log du hash reel pour audit, avec override `expected_sha256=` (kwarg) pour pinner cote packager/config. La defense fail-closed reste active des qu'un hash est fourni.
- infra-probe :: REFUTED|cinesort/infra/probe/service.py:127-145|LOW|Race _get_tools_cached() sans verrou => retour None => TypeError HTTP 500 + relances --version redondantes|Le scenario nuisible est inatteignable : invalidate_tools_status_cache() n'est PAS appele par la sauvegarde de settings (seul appelant : _maybe_purge_obsolete_tool_paths, service.py:233) ; les appelants hors-pool (quality_report_support.py:88, perceptual_support.py:1466, probe_support.py:451) creent chacun une instance ProbeService FRAICHE par appel, donc aucun invalidate concurrent possible sur leur cache => pas de TypeError/500 ; dans probe_files, le pool attrape TypeError (service.py:680) et degrade en ok=False par fichier. Reste seulement quelques relances --version redondantes bornees a la premiere vague de workers, qui n'annulent pas PERF-1.
- ui-run :: REFUTED|cinesort/ui/api/run_flow_support.py:969|MEDIUM|Incoherence d'unites dans get_status apres le recount Vague J : une fois le run done, total est recompte depuis plan.jsonl (ex. 853 PlanRow) mais idx reste le compteur de dossiers explores (ex. 855, discover). Le frontend affiche 'idx / total' (traitement.js:597) -> peut montrer '855 / 853' soit idx > total et une barre >100%. Meme probleme dans la branche DB-only (idx DB en dossiers vs total recompte L890).|Le recount n'a lieu que si done ET not running (L969 et L884 : done seulement pour DONE/FAILED/CANCELLED, running alors false). Or traitement.js n'affiche 'idx / total' (L595-597) et la barre de progression (L694-703) que si _runStatus.running est true, flag issu du MEME payload. Les unites ne divergent que quand done=true, jamais affichees ensemble : '855/853' et barre >100% inatteignables.
- js-core :: REFUTED|web/dashboard/app.js:281|MEDIUM|Router leak : la route /processing est enregistree avec `init: initProcessing` (fonction async -> Promise<undefined>) sans jamais cabler unmountProcessing (export jamais appele nulle part dans le code, verifie par grep). La boucle _pollStatus (processing.js:460-487, setTimeout auto-replanifie 2s) continue apres navigation hors de la vue tant que le run est "running" : fetchs run/get_status + re-render dans un container cache. stopAllPolling() du router ne la voit pas (timer brut, pas startPolling de state.js). Un retour sur /processing pendant qu'un tick est en await cree une 2e chaine de polling parallele dont la reference timer est perdue (non annulable).|Chemin inatteignable : _pollStatus n'est appele QUE par _startScan (processing.js:437), or _startScan early-return systematiquement ligne 427 car `settingsRes.value?.ok` est toujours undefined (apiPost retourne {status,data} sans champ `ok` top-level) ; meme garde morte ligne 435 (`res.ok`) et 846 (_fetchLastRunId). La boucle leakee ne peut jamais demarrer en l'etat.
- js-param :: REFUTED|web/dashboard/views/parametres.js:2018|MEDIUM|Race autosave vs reset : le handler Confirmer de la modale reset (L2018-2052) ne fait ni clearTimeout(_state.saveTimer) ni attente du save debounce en vol. Un autosave parti ~500ms avant peut atteindre le backend APRES settings/reset_settings et reecrire les anciens parametres, annulant silencieusement le reset (le _loadSettings qui suit L2034 peut aussi relire un etat intermediaire).|Fenetre pratiquement inatteignable : confirmer exige de taper CONFIRMER (+ countdown 3s pour all/__database__) soit plusieurs secondes apres la derniere edition, donc le debounce 500ms part et aboutit bien avant le POST reset sur une API localhost ; la modale bloque toute nouvelle edition et le backend serialise les writes sous _settings_write_lock (save_settings_payload L1816-1817), il faudrait un stall multi-secondes d'une requete deja emise pour inverser l'ordre.
- js-param :: REFUTED|web/dashboard/views/parametres.js:633|MEDIUM|Message OMDb faux quand la cle est configuree mais non testee : _computeOmdbState retourne 'config-pending' des que lastTest est null (L635), meme si une cle existe (le masque 8 bullets est truthy), et _renderOmdbStatus affiche alors 'Cle OMDb requise. Renseignez votre cle OMDb' (L668-672) a un utilisateur dont la cle est deja enregistree et fonctionnelle — l'etat 'cle presente non testee' est conflate avec 'cle absente'.|Comportement intentionnel et documente : le docblock L618-629 (Spec 11 §OMDb) specifie explicitement 'Sans test effectue (settings + cle presente), on retourne config-pending (l'utilisateur doit cliquer Tester pour basculer vers ok/ko-*)' — choix de design assume, pas un bug de code.
- js-qual :: REFUTED|web/dashboard/views/qij.js:97|HIGH|Appels REST non prefixes par facade -> 410 Gone systematique (rest_server.py:91-113, Pass 1 legacy desactivee par defaut depuis 2026-05) : "get_global_stats" (:97, :505, :719), "export_run_nfo" (:998), "get_jellyfin_sync_report"/"get_plex_sync_report"/"get_radarr_status"/"get_jellyfin_libraries" (INTEGRATIONS :398-404). Consequence : tab Quality en empty state permanent, Journal vide, rapports de sync et export NFO toujours en erreur. (Module actuellement non route : app.js:291 redirige /qij vers /accueil.)|Code mort inatteignable : qij.js n'est importe par aucun module (seul bootstrap-bisect.js le charge en test d'import sans appeler initQuality/initJournal), /qij redirige vers /accueil et /historique monte historique.js — les appels 410 ne sont jamais emis, aucun impact utilisateur.
- js-qual :: REFUTED|web/dashboard/views/qij.js:114|HIGH|Meme contrat casse dans le tab Quality : stats.total_films (:114/:167), stats.avg_score (:127), stats.premium_pct (:128), stats.trend (:129-130) lus top-level alors que le backend les nest sous `summary` -> KPI films=0, score moyen 0/100, Platinum 0%, tendance figee "→" (defaut independant du 410 : corriger le prefixe facade ne suffira pas).|Code mort inatteignable : qij.js n'est importe par aucun module actif, /qij redirige vers /accueil (app.js:291) — le tab Quality de qij.js n'est jamais monte, ces KPIs ne sont jamais affiches.
- js-qual :: REFUTED|web/dashboard/views/qij.js:897|HIGH|runs_summary expose `score_avg` (enrichissement dashboard_support.py:1605) et non `avg_score` : les cards Journal affichent toujours score "—" (:897) et le tri "par score" est inoperant (tous a -Infinity, _sortRuns :695-696). Meme champ errone dans quality.js:149/:251 (colonne Score des derniers runs toujours "—").|Code mort inatteignable : qij.js et dashboard/views/quality.js ne sont importes nulle part (routes /qij et /quality = redirections, le Journal reel est historique.js) — ces cards et ce tri ne sont jamais rendus.
- js-qual :: REFUTED|web/dashboard/views/status.js:223|MEDIUM|Activite recente : `r.avg_score` (:223) et `r.started_at` (:225) n'existent pas dans runs_summary (backend : score_avg + start_ts, run.py:359-368) et aucun fallback -> chaque entree affiche "pas de score", icone toujours "info" et date "—" (qij.js:689 a le fallback start_ts, status.js non).|Code mort inatteignable : status.js n'est importe nulle part, initStatus jamais appele, /status et /home redirigent vers /accueil — le feed Activite recente de cette vue n'est jamais affiche.
- js-qual :: REFUTED|web/dashboard/views/qij.js:131|HIGH|`stats.top_anomalies` mis a part, les sections "Distribution technique" (:132, :185-194) et "Outliers 2-sigma" (:135-145, :210-234) dependent de stats.technical_distribution et stats.all_scored_rows qui n'existent dans AUCUN endpoint backend (grep cinesort/ = 0 occurrence) -> ces deux fonctionnalites ne peuvent jamais s'afficher ; meme code mort cote quality.js:173-179 et :223-242.|Code mort inatteignable : qij.js et dashboard/views/quality.js ne sont montes par aucune route (redirections /qij->/accueil, /quality->/qualite) — sections jamais rendues, defaut sans impact ; le finding lui-meme qualifie deja ce code de mort.
- js-qual :: REFUTED|web/dashboard/views/quality.js:164|HIGH|Violation invariant couleurs tiers : les barres de distribution colorent les tiers avec des vars semantiques themables — Platinum=var(--success) (vert), Gold=var(--accent), Silver=var(--info), Bronze=var(--warning) (:165-169) — au lieu des couleurs tiers fixes (Gold #FFD700, Silver #C0C0C0, Bronze #CD7F32, Platinum #E5E4E2 via --tier-*-solid, cf. usage correct dans home-charts.js:7-14). Les couleurs de tier varient donc avec le theme.|Code mort inatteignable : dashboard/views/quality.js n'est importe nulle part et /quality redirige vers /qualite — ces barres ne sont jamais rendues, l'invariant ne peut pas etre viole a l'ecran.
- js-qual :: REFUTED|web/dashboard/views/quality.js:183|HIGH|Filtres Etat/Tier/Score rendus (:183-190) mais jamais bindes : aucun listener, `_filters` (:16) jamais lu ni ecrit, et "Analyser les filtres" envoie scope:"filtered" + row_ids:[] que le backend rejette (quality_support.py:214-221 : seuls scope all/validated resolvent les ids -> erreur "no_rows_for_quality") -> le bouton echoue systematiquement ; de plus les options tier sont les anciens noms (premium/bon/moyen/mauvais) incompatibles avec les 5 tiers actuels.|Code mort inatteignable : dashboard/views/quality.js n'est monte par aucune route (/quality = redirection vers /qualite qui a son propre drawer de filtres) — ces filtres et ce bouton n'existent jamais dans le DOM.
- js-qual :: REFUTED|web/dashboard/views/quality.js:139|MEDIUM|Toggle "Bibliotheque / Run courant" factice : les clics changent `_mode` (:267-268) mais _render ignore totalement _mode et `_runData` (:15) n'est jamais alimente -> "Run courant" reaffiche les memes stats globales sous une etiquette trompeuse.|Code mort inatteignable : dashboard/views/quality.js n'est importe nulle part, la vue (et son toggle) n'est jamais rendue — aucun utilisateur ne peut cliquer ce bouton.
- js-qual :: REFUTED|web/dashboard/views/qij.js:294|MEDIUM|Switch de dimension rollup sans abort ni garde-generation (:294-302) : si le fetch echoue, `_qState.rollupBy` est deja avance mais `rollupData` garde les groupes de l'ancienne dimension -> tableau affiche les donnees d'une dimension sous l'en-tete d'une autre ; deux clics rapides -> la reponse la plus lente peut ecraser la plus recente (etat affiche != bouton actif).|Code mort inatteignable : qij.js n'est monte par aucune route (/qij redirige vers /accueil), le tab Quality et son rollup ne sont jamais affiches — la race condition ne peut pas se produire.
- js-qual :: REFUTED|web/dashboard/views/status.js:349|MEDIUM|Etat perime en mode patch : _shellSignatureOf (:349-371) ne capture que des compteurs/booleens et _patchDynamic (:670-720) ne met a jour ni le feed Activite recente, ni le health-score (data-stat="health-score" :615 jamais patche), ni les chiffres Espace disque, ni le donut/line/KPIs V2 (_mountV2Overview appele uniquement au full render :795-799) -> pendant le polling 3-15s, ces sections affichent des donnees obsoletes tant qu'aucun changement structurel ne force un re-render complet (ex: nouveau run alors que 4 entrees d'activite existent deja).|Code mort inatteignable : status.js n'est importe nulle part, initStatus (:925) jamais appele, /status et /home redirigent vers /accueil — ce polling et ce mode patch ne s'executent jamais.
- css :: REFUTED|web/shared/components.css:7613|MEDIUM|Le composant .omdb-status (rendu par omdb-status.js:39-109 dans Parametres) utilise une palette light-theme en hex durs : statut erreur #b91c1c sur le fond sombre #06090F du dashboard ≈ 2.9:1, warning #b45309 ≈ 3.6:1, info #2563eb ≈ 3:1 — tous sous le seuil WCAG AA 4.5:1 ; le message critique "cle API invalide" est a peine lisible (les fallbacks #f1f5f9/#475569 confirment un copier-coller light-theme).|Composant jamais rendu : omdb-status.js est un module orphelin — buildOmdbStatusHtml/renderOmdbStatusInto ne sont importes nulle part. La vue Parametres rend son propre markup .parametres-omdb-status (parametres.js:652-703), pas .omdb-status. Chemin inatteignable, aucun impact utilisateur.

## Stats par lot (bruts -> confirmes, 1re passe)

- dom-quality: 9 bruts, 8 confirmes
- dom-core: 11 bruts, 11 confirmes
- dom-naming: 9 bruts, 6 confirmes
- dom-perc-a: 11 bruts, 11 confirmes
- dom-perc-b: 12 bruts, 12 confirmes
- dom-rest: 10 bruts, 8 confirmes
- app-apply: 7 bruts, 6 confirmes
- app-plan: 12 bruts, 12 confirmes
- app-rest: 10 bruts, 9 confirmes
- infra-rest: 5 bruts, 4 confirmes
- infra-clients: 12 bruts, 10 confirmes
- infra-db: 9 bruts, 8 confirmes
- infra-probe: 6 bruts, 5 confirmes
- infra-misc: 6 bruts, 6 confirmes
- ui-god: 11 bruts, 10 confirmes
- ui-apply: 8 bruts, 8 confirmes
- ui-settings: 10 bruts, 9 confirmes
- ui-run: 11 bruts, 10 confirmes
- ui-lib: 13 bruts, 11 confirmes
- ui-sup-a: 13 bruts, 12 confirmes
- ui-sup-b: 12 bruts, 12 confirmes
- ui-shell: 1 bruts, 1 confirmes
- boot: 8 bruts, 8 confirmes
- js-core: 11 bruts, 8 confirmes
- js-comp: 12 bruts, 9 confirmes
- js-param: 12 bruts, 11 confirmes
- js-trait: 15 bruts, 14 confirmes
- js-biblio: 14 bruts, 13 confirmes
- js-hist: 12 bruts, 12 confirmes
- js-qual: 15 bruts, 1 confirmes
- js-rest: 15 bruts, 14 confirmes
- css: 14 bruts, 12 confirmes
- sql: 5 bruts, 5 confirmes
- xcut-contract: 5 bruts, 5 confirmes
- xcut-conc: 11 bruts, 11 confirmes
- xcut-secrets: 5 bruts, 5 confirmes
