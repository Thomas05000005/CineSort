==========================================================================================
KEY: R8-034-035
SITES: R8-034: cinesort/domain/perceptual/audio_perceptual.py:141-142 (le "-v","quiet" dans cmd L131-144), declencheur du None a L146-148. R8-035: audio_perceptual.py:239-240 (crest/dynrange via _search_float sur overall_text) + helper _extract_overall_block L744-784 ; regex L62-63 ; defauts figes _compute_audio_score L625 (s_dyn=50), L626->en realite L621-626 s_crest=50/s_dyn=50.
FIX: R8-034: remplacer "-v","quiet" par "-v","info" (comme astats) afin que le JSON loudnorm atteigne stderr. Le JSON est ensuite extrait par _RE_JSON_BLOCK (L86, filtre 'input_i', findall[-1]) et json.loads (L160). Variante: garder verbosite >= info specifiquement, ne jamais descendre sous "info"/"verbose". R8-035: parser crest/dynrange depuis les blocs PAR CANAL, pas Overall. Soit agreger (min Dynamic range / min Crest sur tous les "Channel: N"), soit lire le canal 1. Concretement: appliquer _RE_CREST/_RE_DYNRANGE sur le texte AVANT _extract_overall_block (ex: stderr complet ou portion pre-Overall) puis reduire (min). Les cles astats sont "Crest factor:" et "Dynamic range:" (sans 'dB', cf L62-63 deja correctes).
INSTR: yes pour les deux, ffmpeg 8.1.1 reel present (Gyan build). R8-034: `ffmpeg -f lavfi -i sine=...:duration=2 -af loudnorm=print_format=json -f null -v quiet -` => stderr vide (None); avec "-v info" le bloc JSON loudnorm apparait. R8-035: VERIFIE — `ffmpeg -f lavfi -i sine -af astats=metadata=1:reset=0 -f null -` montre "Crest factor: 1.41" et "Dynamic range: 51.8" SOUS "Channel: 1" mais le bloc "Overall" liste seulement Peak/RMS/Flat/Noise/Bit depth (pas de Crest ni Dynamic). Confirme en mono ET en stereo (amerge).
NOTES: run_ffmpeg_text (ffmpeg_runner.py:89-106) capture bien stderr via capture_output=True, donc le seul blocage R8-034 est le niveau "quiet". R8-035 secondaire: meme si on corrige le parsing, noise_floor EST dans Overall (OK), mais crest/dynrange exigent les blocs canal. Attention: corriger R8-035 sans corriger R8-034 laisse quand meme la LRA absente. Les deux sont independants et tous deux reels.
==========================================================================================
KEY: R8-036
SITES: cinesort/domain/perceptual/video_analysis.py:90 (construction du filtre `vf`), :92-105 (argv, `-v info`), :115-159 (`_parse_filter_output`), :52-59 (regex), :375-390 (`_score_blockiness`), :393-407 (`_score_blur`), :531-545 (`_aggregate_filter_metrics` -> blockiness_mean/blur_mean), :585-586 + :574-598 (`_compute_visual_score`)
FIX: Ajouter `,metadata=mode=print` a la fin de `vf` (L90) ET aligner les regex (L52-59) sur les cles ffmpeg reelles imprimees: `lavfi.signalstats.YAVG=`, `lavfi.signalstats.YMIN/YMAX/SATAVG/TOUT/VREP`, `lavfi.block=`, `lavfi.blur=`. La sortie `metadata=print` met une cle=valeur par ligne (pas tout sur la ligne YAVG), donc revoir aussi le decoupage par frame dans `_parse_filter_output` (regrouper par `frame:`/`pts_time`). Defense secondaire: dans `_score_blockiness`/`_score_blur`, ne pas mapper l'absence de donnee (0.0) sur 95 — distinguer "0 frame analysee" (-> score neutre/None) de "mesure 0".
INSTR: yes — reproductible avec un vrai ffmpeg. (1) Lancer exactement l'argv L92-105 sur un fichier: stderr ne contient AUCUNE ligne YAVG/block/blur (filtres muets sur stderr). (2) Re-lancer avec `-vf "...,blockdetect,blurdetect,metadata=mode=print"` : stderr expose `lavfi.block=...`, `lavfi.blur=...`, `lavfi.signalstats.YAVG=...` — montrant que les cles ne sont PAS `block:`/`blur:`/`YAVG=` brut attendues par les regex. (3) Test unitaire: feed `filter_results=[]` a `analyze_video_frames` -> blockiness_mean=blur_mean=0.0, `_score_blockiness(0.0)=95`, `_score_blur(0.0)=95`.
NOTES: Bug plus large que R8-036: signalstats (YAVG/SATAVG/TOUT/VREP) est aussi muet sur stderr sans metadata=print, donc y_avg_mean/saturation_avg/tout_mean/vrep_mean ainsi que detection N&B et scenes sombres tombent aussi sur leurs defauts. `run_filter_graph` retourne `[]`; `analyze_video_frames` continue via la branche pixel (frames_data) si elle existe, mais blockiness/blur/temporal restent fabriques. La regex `_RE_BLUR` matcherait par erreur le mot "blur" dans des logs, mais le garde-fou L150 `'blurdetect' in line` empeche tout match accidentel -> liste vide confirmee.
==========================================================================================
KEY: R8-037
SITES: analyze_perceptual_batch signature: cinesort/ui/api/perceptual_support.py:525-531 (params: api, run_id, row_ids, options=None, progress_cb=None — AUCUN should_cancel/cancel_event). Derivation cancel: perceptual_support.py:561 `cancel_event = _resolve_cancel_event(api)` -> passe a run_batch_parallel L593. Helper: perceptual_support.py:1638-1647 `_resolve_cancel_event` lit `getattr(api,"_perceptual_cancel_event",None)`, retourne None si pas un threading.Event. Autres lecteurs: L419, L667, L1014. request_cancel pose le flag: cinesort/app/job_runner.py:684 `rt.cancel_event.set()` (event PAR-RUN, different). Appel post-scan: run_flow_support.py:564-566. Aucune assignation `_perceptual_cancel_event=` dans cinesort/** (grep vide).
FIX: Cabler les deux events. Minimal: dans le job_fn (run_flow_support.py, juste avant L564), recuperer l'Event du run et l'exposer sur l'api avant l'appel: `api._perceptual_cancel_event = rs.runner._runs[run_id].cancel_event` (ou via un accessor public ex. runner.get_cancel_event(run_id)), puis le nettoyer en finally. Mieux: ajouter param `cancel_event` a analyze_perceptual_batch et le passer explicitement (api._perceptual_cancel_event reste fallback pour les chemins UI directs). Idealement exposer un accessor sur JobRunner plutot que toucher `_runs` (prive). Sans cablage, request_cancel n'arretera jamais l'analyse perceptuelle post-scan.
INSTR: yes
NOTES: Instrumentable avec un vrai ffmpeg: scan d'un lot reel avec perceptual_enabled+perceptual_auto_on_scan ON, puis request_cancel pendant le batch -> observer que les ffmpeg continuent jusqu'au bout (cancel inerte). run_batch_parallel honore bien cancel_event quand il est non-None (verifie via tests existants L192/279 qui le forcent a None justement pour neutraliser); le bug est purement le cablage manquant, pas la logique du pool. Les checks H12 (perceptual_support.py:1003,1014) lisent le meme attribut et sont egalement inertes en prod.
==========================================================================================
KEY: R8-038-039
SITES: R8-038: cinesort/domain/quality_score.py:538-565 (function _normalize_audio_bitrate_kbps), threshold test at L563 `if n > 10000.0`. Consumed at L971-982 (per_channel bonus/malus). R8-039: cinesort/domain/quality_score.py:637-646 (_best_audio_track), called L954. Correct reference impl: cinesort/domain/duplicate_compare.py:425-438 (_best_audio). Rank table: cinesort/domain/codec_ranks.py:47-81.
FIX: R8-038: bps cannot be robustly distinguished from kbps by magnitude alone in the 1-10k band. Best fix: stop guessing units from value; trust ffprobe (always bps for stream/format bit_rate) and divide unconditionally /1000, OR read the unit from the probe source. Minimal pragmatic fix: lower/remove the special-case and divide any n>=1000 (>=1 kbps in bps terms) — but cleanest is to normalize bps->kbps at probe-ingest time once, not via heuristic here. R8-039: change _best_audio_track sort key to put codec_rank first, e.g. key=lambda t:(_audio_codec_rank_value(t) or 0, channels, bitrate), reusing codec_ranks; or simply delegate to duplicate_compare._best_audio. Correct key: codec_rank desc, then channels, then bitrate.
INSTR: R8-038: yes — encode a real low-bitrate mono AAC: `ffmpeg -i in.mp4 -c:a aac -b:a 8k -ac 1 out.mp4` then ffprobe shows bit_rate~8000; feed to _normalize_audio_bitrate_kbps and observe it stays 8000 (not 8). R8-039: yes — mux two real audio streams: TrueHD/DTS-HD via `ffmpeg ... -map ... -c:a:0 truehd -c:a:1 ac3 -ac:a:1 6 out.mkv`, probe, and check _best_audio_track picks the AC3 vs duplicate_compare._best_audio picks the lossless.
NOTES: R8-038 nuance vs registry: typical streams (>=10k bps) normalize correctly; the inversion bites only sub-10kbps streams, plus any source already reporting kbps would be misread if >10000. The real defect is unit-guessing by magnitude. R8-039: _best_audio_track also feeds _hierarchy_audio_codec_token (L649) and a_codec_bonus (L962), so the wrong-track pick contaminates codec bonus, channel bonus AND the hierarchy token simultaneously. A shared helper between the two modules would prevent divergence.
==========================================================================================
KEY: R8-040
SITES: cinesort/domain/scene_parser.py:378 (name.replace('.',' ').replace('_',' ')) ; :121-138 _NOISE_RE (tokens dd5\.?1|dd7\.?1|dd2\.?0 a L131) ; :398 _NOISE_RE.sub ; :200-203 _AUDIO_RESIDUE_RE ; :401 _AUDIO_RESIDUE_RE.sub
FIX: Fix minimal le plus sur : etendre _AUDIO_RESIDUE_RE (L200-203) pour absorber le prefixe DD/DDP colle au chiffre de canal post-replace, ex remplacer '[257][\s.][01]' par 'dd[ p]?[257][\s.][01]|[257][\s.][01]' (insensible casse, \b ouvrant deplace devant 'dd'). Ne touche NI a L378 (replace) NI au strip release-group (had_tech_marker L386, _RELEASE_GROUP_RE L172) -> aucun risque pour la correction R1/R4 du release-group. Le garde 'separateur OBLIGATOIRE entre canal et .1' (anti 21/50/71 de l'audit 2026-06-10) reste intact car on conserve '[\s.][01]'. Alternative rejetee : matcher les tags audio AVANT le replace L378 (plus invasif, reordonne tout le pipeline).
INSTR: no — pur traitement de chaine sur le nom de fichier; aucun appel ffmpeg/ffprobe n'intervient dans scene_parser.py. Testable a 100% par doctest/unit (parse_scene_title) sans binaire. Un vrai ffmpeg ne peut ni declencher ni observer ce bug.
NOTES: _NOISE_RE garde aussi la branche 'ddp' (capte DDP nu) et 'ac3/eac3' — inchanges. Apres correction de _AUDIO_RESIDUE_RE, verifier la non-regression sur les titres pieges de l'audit ('21 Jump Street','50 First Dates','71','20 000 Leagues','Ma Vie de Courgette') : la branche 'dd[ p]?' exige le prefixe litteral dd, donc ces titres restent intacts. Pattern de query TMDb pollue confirme : 'DD5 1'/'DDP5 1'/'DD7 1' subsistent aujourd'hui dans le titre nettoye.
==========================================================================================
KEY: R8-041
SITES: Ecriture: cinesort/infra/tmdb_client.py:515 (self._cache_set(cache_key, [r.__dict__ for r in results]) — results==[] quand 0 match). Lecture: tmdb_client.py:445-447 (cached = self._cache_get(cache_key); if cached is not None: return [TmdbResult(**x) for x in cached]). TTL: _SEARCH_CACHE_TTL_S = 7*24*3600 (L45), applique via _ttl_for_key (search| n'est pas dans _DETERMINISTIC_PREFIXES L61). _cache_set L289-301, _cache_get L227-262.
FIX: Ne pas cacher un resultat vide comme valide. Option minimale: en L515, ne pas ecrire si results est vide (if results: self._cache_set(...)). Mieux (evite de re-hammer TMDb a chaque scan tout en re-essayant vite): cacher les listes vides avec un TTL court/negatif distinct — sentinel ou cle prefixee detectee par _ttl_for_key (ex. quelques heures) pour [], 7j pour les non-vides. Eviter de changer la garde 'cached is not None' globalement (elle protege le cache positif legitime); cibler uniquement le cas value==[].
INSTR: n-a — ffmpeg ne touche pas TMDb. Le bug est dans le client HTTP TMDb (search_movie), aucun media/ffmpeg n'intervient. Reproductible via stub HTTP renvoyant 200 {results:[]} puis un 2e appel renvoyant des resultats: verifier que le 2e scan re-fetch au lieu de servir [] depuis le cache.
NOTES: Meme schema potentiel ailleurs: search_tv L979 (cache_items, vide-able) lu L917-918 avec garde isinstance(cached, list) — un [] y serait aussi fige. Les lookups deterministes (movie|, find_tmdb|, find_imdb|) cachent volontairement "" comme sentinel "introuvable" (L712, L876) avec TTL long, ce qui est plus discutable mais hors scope R8-041. Le registre disait ~L515 : exact, ecriture confirmee a L515, lecture a L445-447.
==========================================================================================
KEY: R8-042
SITES: Producteur: cinesort/domain/duplicate_compare.py:142-143 (calcul) + :78-79/149-150 (champs ComparisonResult.total_score_a / total_score_b). Rendu: web/dashboard/views/doublons.js:297-298 (lecture scoreA/scoreB) + :365 et :376 (affichage `${scoreA}/100` et `${scoreB}/100`).
FIX: Deux options. (1) Minimal/honnête: changer le libellé de rendu en 'points' au lieu de '/100' (doublons.js:365 et :376 -> `${scoreA} pts` / `${scoreB} pts`), éventuellement avec un denominateur réel = somme des poids des critères évalués. (2) Vraie échelle absolue 0..100: exposer un score normalisé indépendant côté producteur — réutiliser _compute_single_score(probe) (duplicate_compare.py:310) qui est déjà un score absolu par fichier (mais plafonné à ~90), le diviser par son max théorique *100, l'ajouter comme champ ex. quality_score_a/b dans ComparisonResult, et le lire dans doublons.js. Recommandé: (1) immédiat (1 ligne x2, zéro risque), (2) si on veut vraiment une note /100.
INSTR: yes — un vrai ffprobe sur 2 fichiers distincts (ex. 1080p x264 AAC vs 2160p HEVC DV TrueHD 7.1) produit des probes normalisés; compare_duplicates renverra typiquement total_score_a=0/total_score_b≈90 (le perdant à 0), ce qui matérialise '0/100' faux. Le critère perceptuel exige réellement les scores perceptuels (poids 10) mais n'est pas nécessaire pour reproduire le bug d'échelle.
NOTES: Le commentaire R8-042 disait 'le perdant ~toujours 0' : confirmé, car le perdant n'accumule que ses propres critères gagnés. Le '30/100' '0/100' de l'exemple est cohérent avec le code (A gagne résolution=30, B rien). Aussi: déterminer winner utilise sum(points_delta) brut (determine_winner:277-284) tandis que total_score_a/b sont les sommes positives séparées — deux logiques distinctes, ne pas confondre. Le rendu fait Math.round (inutile, les scores sont déjà entiers). Le même fichier existe en copies sous dist/ et dist_backup_*/ — patcher la source web/dashboard/views/doublons.js uniquement.
==========================================================================================
KEY: R8-043-044
SITES: R8-043: dataclass VideoPerceptual.to_dict cinesort/domain/perceptual/models.py:92-168 (aucune cle hdr_analysis); calcul HDR reel cinesort/domain/perceptual/hdr_analysis.py:270-335 (analyze_hdr_from_frame_data -> HdrInfo, classe SANS to_dict L56-71); sortie probe cinesort/infra/probe/_normalize_ffprobe.py:99-110 (cles PLATES hdr_type/hdr_is_valid... PAS d'objet hdr_analysis); consommateur UI web/dashboard/components/perceptual-modal.js:292. | R8-044: cinesort/domain/mkv_title_check.py:50 (ct.lower()==pt.lower()) + retour L53; appel cinesort/ui/api/quality_report_support.py:313.
FIX: R8-043: cablage minimal cote backend (pas la modale): dans _normalize_ffprobe.py:86 ajouter dans le dict video une cle 'hdr_analysis': {'hdr_format': hdr_info.hdr_type, 'is_hdr': hdr_info.hdr_type != 'sdr', ...} (ou exposer hdr_info.to_dict() apres avoir ajoute une methode to_dict a HdrInfo). Verifier que ce dict video alimente bien 'd' de la modale; sinon merger aussi dans le payload perceptuel consomme. R8-044: dans mkv_title_check.py, normaliser avant comparaison: lower + remplacer ./_/- par espace + strip annee/tags + collapser espaces, puis match par tokens (set overlap) ou ratio fuzzy (difflib.SequenceMatcher) avec seuil; ne flaguer que si non-recouvrement reel ; brancher _is_scene_title (deja ecrit) pour exclure les vrais release-names plutot que les flaguer.
INSTR: R8-043: yes — ffmpeg peut generer une fixture HDR: ffmpeg -f lavfi -i testsrc2 -vf format=yuv420p10le -color_primaries bt2020 -color_trc smpte2084 -colorspace bt2020nc ... (transfer smpte2084 + primaries bt2020 => detect_hdr_type retourne 'hdr10'). Pour mastering display/MaxCLL utiliser -metadata ou mux x265 (master-display/max-cll); HDR10+/DV difficiles a synthetiser via ffmpeg seul. R8-044: n-a — bug de logique pure (comparaison de chaines), aucun media requis; instrumentable par tests unitaires sur corpus container_title.
NOTES: Tests existants a mettre a jour: test_mkv_title.py:63-65 et :73-74 ASSERTENT explicitement que 'Mon Film' vs 'Inception' et 'rip.by.XxX' vs 'The Matrix' renvoient ['mkv_title_mismatch'] -> le fix de normalisation devra distinguer ces vrais conflits des faux (release-names matchant le titre). create_test_data.py:113/131 seedent mkv_title_mismatch. R8-043: la modale a une double faute de contrat (hdr_analysis + sous-cles hdr_format/is_hdr) absente partout; un simple ajout de hdr_type ne suffit pas, il faut la cle objet hdr_analysis attendue par le JS.